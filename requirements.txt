# Core agent: the graph, memory, retries, cost accounting.
#
# This is everything `research_agent.py`, `chat.py`, and `python -m evals`
# need. It deliberately does NOT include the web server -- a worker image or
# a batch job that imports the graph shouldn't ship FastAPI and uvicorn.
# For the HTTP service, install requirements-service.txt instead.
#
# Pinned to the versions this project was built and tested against.
# Loosen to >= if you'd rather track upstream.
langgraph==1.2.9
anthropic==0.120.0
voyageai==0.5.0
numpy==2.5.1
python-dotenv==1.2.2
