---
phase: 20-note-count-bound
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - docs/OPERATIONS.md
  - docs/DESIGN.md
  - README.md
  - .planning/phases/20-note-count-bound/20-VALIDATION.md
autonomous: true
requirements: [REQ-note-count-bound]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/OPERATIONS.md's configuration table documents NOTE_CAP_PER_OWNER beside NOTE_TTL_DAYS, including the clamp direction and that a non-positive or unparseable value reads as the default"
    - "No doc surface outside README's Limitations section still claims expiry is the only bound on notes — checked by measurement, with absences recorded as results"
    - "README's test counts match a measured post-phase keyless run, and the notes Limitations bullet is byte-identical to before the phase (grep count 1 before and after)"
    - "20-VALIDATION.md carries measured Status evidence for every automated row, and the Manual-Only row's disposition is stated in the SUMMARY"
  artifacts:
    - "docs/OPERATIONS.md — the NOTE_CAP_PER_OWNER row in the configuration table"
    - "README.md — measured test counts, Limitations bullet untouched"
    - ".planning/phases/20-note-count-bound/20-VALIDATION.md — Status column reconciled with measured evidence"
  key_links:
    - "the README bullet's falsification is recorded in the SUMMARY as a deliberate transient owned by Phase 22 — the same knowingly-false interval Phases 18 and 19 left at their own bullets"
    - "the OPERATIONS row's clamp wording mirrors COST_DISCOUNT_FACTOR's row, because P-01 adopted that clamp for the same reason"
---

<objective>
Re-derive the doc surfaces Phase 20 falsified — OPERATIONS gains the knob, DESIGN is read
and judged rather than assumed, README gets its whole-file pass with measured counts —
and reconcile the phase's validation contract with the evidence Wave 1 produced.

Purpose: the standing every-PR rule (README is a per-phase deliverable), plus the
milestone's honesty bar: the one bullet this phase makes false stays in place for
Phase 22, and the record says so out loud instead of letting a stranger catch it.

Output: one new OPERATIONS configuration row, a measured README pass, a reconciled
20-VALIDATION.md, and the phase gate run end to end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-note-count-bound/20-CONTEXT.md
@.planning/phases/20-note-count-bound/20-RESEARCH.md
@.planning/phases/20-note-count-bound/20-VALIDATION.md
@.planning/phases/20-note-count-bound/20-01-SUMMARY.md

@docs/OPERATIONS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OPERATIONS gains the knob; DESIGN and the codebase maps are judged, not assumed</name>
  <files>docs/OPERATIONS.md, docs/DESIGN.md</files>
  <reversibility rating="reversible">Prose. Revert restores the previous wording.</reversibility>
  <read_first>
    - `docs/OPERATIONS.md:650-678` — the configuration table: `NOTE_TTL_DAYS`'s row at :674 (the new row's neighbour and voice model) and `COST_DISCOUNT_FACTOR`'s row at :663, whose clamp phrasing ("<= 0 or unparseable falls back... so a typo cannot...") is the register P-01's row should rhyme with.
    - `docs/DESIGN.md` — the memory paragraphs (retrieval with a relevance floor around :30, pgvector around :34, one-variable backend selection around :56). Planning-time reading found NO claim about note lifecycle bounds in this file — the task verifies that finding rather than trusting it.
    - `.planning/phases/20-note-count-bound/20-01-SUMMARY.md` — what actually shipped, including any deviation from the plan's shape that the docs must describe truthfully.
  </read_first>
  <action>
Fix what this phase made untrue, and nothing else. Every claim below is a measurement to
take, not a sentence to trust.

1. **OPERATIONS, the configuration table.** Add a `NOTE_CAP_PER_OWNER` row immediately
   after `NOTE_TTL_DAYS` (:674), default `100`. The description, in the table's own
   operator voice, must carry: one owner's live notes are capped, and adding past the cap
   evicts that owner's oldest on the same `add()` that already sweeps expiry — identical
   on all four vector backends; and the clamp — `<= 0` or unparseable falls back to the
   default, mirroring `COST_DISCOUNT_FACTOR`'s row phrasing, so a plausible typo cannot
   silently disable recall by making every write evict itself. Close with the one
   sentence 20-RESEARCH Open Question 2 asked for: the cap applies to every `add()`,
   including notes the eval harness seeds — a future large-corpus eval case would be
   truncated to the cap. Keep it one row; do not invent a prose section.
2. **OPERATIONS, everything else it says about notes.** Grep the file for the note
   lifecycle vocabulary (the TTL row's neighbourhood, any prose about what bounds the
   store) and re-derive anything the cap falsifies. Planning-time reading found the
   `NOTE_TTL_DAYS` row itself still true (it describes expiry, not exclusivity) — verify,
   and record the greps and their outcomes in the SUMMARY either way; a documented
   absence is a result.
3. **DESIGN, read and decide.** Read `docs/DESIGN.md` end to end. 20-CONTEXT's rule: it
   gets the whole-file treatment IF it describes note lifecycle. Planning-time reading
   says it describes retrieval (floor, HNSW, backend selection) and never claims what
   bounds the store — in which case nothing here is falsified and the file is untouched;
   record that judgement with the reason in the SUMMARY. If reading finds otherwise, fix
   exactly what is false. Do not edit it just to touch it.
4. **The codebase maps, same discipline.** Grep `.planning/codebase/` for note-lifecycle
   claims. Planning-time grep found none; their other staleness (three maps naming an
   old judge model) is Phase 18's recorded deferral, not this phase's to fix. Record the
   grep result.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && grep -c "NOTE_CAP_PER_OWNER" docs/OPERATIONS.md</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && git diff --stat HEAD -- src tests | wc -l</automated>
  </verify>
  <done>
The OPERATIONS table carries the knob row (grep count at least 1) beside NOTE_TTL_DAYS in
the table's voice with the clamp and the eval-seeding sentence; every other note-bound
claim in OPERATIONS, DESIGN and the codebase maps was measured and either re-derived or
recorded as not falsified; no source or test file changed in this task (the second verify
prints 0 lines).
  </done>
</task>

<task type="auto">
  <name>Task 2: README whole-file pass, validation reconciliation, and the phase gate</name>
  <files>README.md, .planning/phases/20-note-count-bound/20-VALIDATION.md</files>
  <reversibility rating="reversible">Prose and planning metadata. Revert restores the previous wording.</reversibility>
  <read_first>
    - `README.md:15, 199` — the two test-count sites, both reading 773 entering the phase, both stale the moment Wave 1 landed.
    - `README.md:277-291` — the Limitations section. The notes bullet at :291 is the one this phase falsifies; Phase 22 owns the section and deletes the bullet there. The judge bullet at :285 is the Phase 18 precedent for leaving a knowingly-false bullet in place, recorded.
    - `.planning/phases/20-note-count-bound/20-VALIDATION.md` — the Per-Task Verification Map whose Status column this task fills, and the Manual-Only row this task disposes of in the SUMMARY.
    - `.planning/phases/20-note-count-bound/20-01-SUMMARY.md` — the measured evidence (commands, counts, mutation observations) the Status column is filled FROM; this task transcribes measurements, it does not re-derive claims.
  </read_first>
  <action>
Close the phase's record honestly.

1. **README, whole file.** Read it end to end. Replace both test counts with the number
   measured by THIS task's full-suite run below — not the count carried from 20-01's
   SUMMARY, and not the plan's projection. Check every claim the cap could falsify:
   the memory/notes description sections, the endpoint or configuration mentions if any
   name note behaviour. Fix what is false. **The Limitations bullet at :291 does not
   move**: before and after this task, `grep -c "Notes are bounded by expiry alone"
   README.md` must print 1. Its claim is now knowingly false — state that in the
   SUMMARY, in so many words, as a deliberate transient owned by Phase 22, exactly the
   interval Phases 18 and 19 left at their own bullets. Also state the production
   framing 20-CONTEXT asks for: the live database holds 8 notes across 7 sessions, so
   at today's volumes the bound is a guarantee, not an observable behaviour change —
   say so rather than overselling.
2. **20-VALIDATION.md reconciliation.** Fill the Status column for every automated row
   with measured evidence — the command run, the observed result, and what its recorded
   mutation did (red where the plan demanded, or the honestly-recorded alternative the
   row itself allows). The Task ID / Plan / Wave columns were assigned at planning; correct
   them only if execution actually landed a gate in a different task, and say so. The
   Manual-Only row (live pgvector eviction) is disposed of per its own Instructions
   column: the SUMMARY states the live bound is a guarantee at today's volumes and the
   armed arm is the proof — no live check exists to run. Leave the sign-off checkboxes
   and `nyquist_compliant` to phase verification.
3. **The phase gate**, measured in this task: full keyless suite (quote the counts
   against the 773 passed / 67 skipped baseline, with the phase's cumulative delta
   reconciled against Wave 1's accounting); the armed contract file against :54329;
   offline evals with the real exit status; ruff both forms.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && grep -c "Notes are bounded by expiry alone" README.md</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/pytest</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && DATABASE_URL=postgresql://postgres@localhost:54329/postgres .venv/bin/pytest tests/test_store_contract.py -q</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && .venv/bin/python -m evals --min-pass-rate 0.9; echo "offline evals exit: $?"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && .venv/bin/ruff check . && .venv/bin/ruff check src tests evals</automated>
  </verify>
  <done>
README's test counts match the run this task measured and quoted; the Limitations bullet
grep prints 1 before and after; the SUMMARY records the bullet as knowingly false and
owned by Phase 22, and records the guarantee-not-behaviour-change framing; every
automated 20-VALIDATION row carries measured Status evidence and the Manual-Only row is
disposed of as its Instructions specify; full keyless suite green with reconciled counts;
armed contract file green; evals 41/41 with a real exit 0; ruff clean both forms.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → published docs | README and OPERATIONS are what a stranger reads to decide whether the claims hold; a doc asserting a posture the code no longer has is the failure this milestone exists to end. |
| planning record → verification | 20-VALIDATION's Status column is what phase verification audits; unmeasured or transcription-drifted evidence corrupts the audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-11 | Repudiation | docs claiming expiry is the only bound after the cap ships | medium | mitigate | Task 1 re-derives each surface against a measured grep, recording absences as results; Task 2's README pass re-measures counts rather than carrying them. |
| T-20-12 | Repudiation | README's Limitations bullet knowingly false until Phase 22 | low | accept | Deliberate transient per 20-CONTEXT, matching the Phase 18 (:285) and Phase 19 precedents; the grep-count-1 gate keeps it byte-identical and the SUMMARY records the falseness out loud. |
| T-20-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; the phase adds no dependency. |
</threat_model>

<verification>
- `ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/pytest` green, counts quoted against
  773 passed / 67 skipped with the phase's cumulative delta reconciled.
- Armed contract file green against :54329.
- Offline evals 41/41 with a real `$?` of 0; ruff clean both forms.
- `grep -c "NOTE_CAP_PER_OWNER" docs/OPERATIONS.md` at least 1;
  `grep -c "Notes are bounded by expiry alone" README.md` exactly 1, before and after.
- Every automated 20-VALIDATION row carries measured Status evidence.
</verification>

<success_criteria>
1. OPERATIONS documents the knob beside the TTL it composes with, clamp included
   (ROADMAP criterion 3's doc half: the limitation is falsified by a passing test, and
   the surviving docs say only true things around it).
2. README's counts are measured, its Limitations bullet untouched, and the transient
   contradiction recorded rather than discovered.
3. DESIGN and the codebase maps judged by reading, edited only where falsified.
4. 20-VALIDATION reconciled: every automated row measured, the Manual-Only row disposed
   of as written.
5. The full phase gate green: keyless suite, armed contract file, evals, ruff.
</success_criteria>

<output>
Create `.planning/phases/20-note-count-bound/20-02-SUMMARY.md` when done
</output>
