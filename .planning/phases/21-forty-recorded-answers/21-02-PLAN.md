---
phase: 21-forty-recorded-answers
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - evals/fixtures/*.json (all 40 — 39 new, technical-figures.json re-recorded)
  - evals/__main__.py (the ASSUMED_* calibration constants and their comment, nothing else)
  - .planning/phases/21-forty-recorded-answers/record-stage1-report.json
  - .planning/phases/21-forty-recorded-answers/record-quote-rebased.txt
  - .planning/phases/21-forty-recorded-answers/record-stage2-a-report.json
  - .planning/phases/21-forty-recorded-answers/record-stage2-b-report.json
  - .planning/phases/21-forty-recorded-answers/record-stage2-c-report.json
  - .planning/phases/21-forty-recorded-answers/record-stage2-d-report.json
autonomous: false
requirements: [REQ-forty-recorded-answers]
user_setup:
  - service: anthropic
    why: "The record run drives the real pipeline and the real judge — this is the phase's paid operator act"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> API keys, or the operator's local .env"
  - service: voyage
    why: "Each recorded case embeds through a live per-case memory store"
    env_vars:
      - name: VOYAGE_API_KEY
        source: "dash.voyageai.com -> API keys, or the operator's local .env"

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Nothing passes --yes except immediately downstream of a checkpoint the user approved in that prompt; the recorder's own exit-2 refusal is observed fresh at each stage before approval is even requested"
    - "Stage 1 records exactly one case — technical-figures — and its new fixture carries the settled judge (claude-opus-4-8), an explicit critic equal to the pipeline model, and no forced stamp; a keyless replay of it exits 0 BEFORE stage 2 is proposed"
    - "The stage-2 quote is re-based: the calibration's measurement corrected the assumed constants by the file's own stated rule before the remaining 39 were re-quoted"
    - "The bulk 39 record in four --case-scoped batches committed per batch, so a mid-run death loses at most one batch and a resume re-runs only unrecorded ids — never a blind full re-run that re-spends"
    - "A refused case is surfaced verbatim as a finding, never auto-retried, never --forced; any re-run happens only as its own user-approved micro-checkpoint with the incremental cost stated"
    - "Per-stage actual-vs-quote numbers are captured from the recorder's own output at the time, not reconstructed later"
  artifacts:
    - "evals/fixtures/ — 40 fixture files, one per golden case id, each carrying real answers and the settled judge's verdicts as metadata"
    - "evals/__main__.py — ASSUMED_* constants corrected against the calibration measurement, with the CALIBRATED comment re-derived (new date, figures, sha, and what remains unmeasured)"
    - ".planning/phases/21-forty-recorded-answers/record-stage1-report.json and record-stage2-{a,b,c,d}-report.json — structured recordings[] evidence per stage"
    - ".planning/phases/21-forty-recorded-answers/record-quote-rebased.txt — the tighter stage-2 quote the second checkpoint presented"
  key_links:
    - "the env assertion runs AFTER any .env sourcing and BEFORE every --yes — the only ordering that catches a .env that itself sets CRITIC_MODEL or EVAL_JUDGE_MODEL"
    - "the post-stage keyless replay is what proves the recording shell was clean before more money rides on the same shell"
    - "record_preview prices technical-figures from its NEW fixture on the next quote, and the corrected constants tighten the other 39 — the two mechanisms compose into the re-based number checkpoint 2 presents"
    - "exit 1 with SKIP lines is the machinery working (the refused check in the exit rule), not a failure to fix — the executor stops and surfaces, never retries"
---

<objective>
The phase's paid operator act, exactly as 21-CONTEXT ratified it: two checkpoints, staged
calibrate-first, refusals surfaced as findings. Stage 1 records one case (~$0.36 quoted at
planning) which is simultaneously the calibration, the retirement of the stale-judge
fixture, and the first real Opus 4.8 judge verdict ever round-tripped (Phase 18's deferred
probe). Stage 2, separately approved against a re-based quote, records the remaining 39 in
four resumable batches.

Purpose: REQ-forty-recorded-answers criteria 1, 3 and 4 — all 40 cases gain fixtures under
the settled judge, refusals surface, and the spend is quoted, approved, and reported
against the quote per stage.

Output: 40 committed fixtures; corrected calibration constants; five report JSONs and the
re-based quote capture; the per-stage actual-vs-quote numbers 21-03 assembles into the
final table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-forty-recorded-answers/21-CONTEXT.md
@.planning/phases/21-forty-recorded-answers/21-RESEARCH.md
@.planning/phases/21-forty-recorded-answers/21-RECORD-RUNBOOK.md

@evals/__main__.py
@evals/harness.py
@evals/fixtures.py
</context>

<decisions_this_plan_locks>
**P-05 — The batch decomposition: four batches, by case family, follow-ups last.**
21-RESEARCH Open Question 1 left chunking open; settled here so a mid-run death loses at
most one batch and a re-run is expressible by family name. Planning-time quoted sums from
the 2026-08-15 preview (they move with the constants correction; checkpoint 2 presents the
re-derived numbers):

- **Batch A — routing/guardrail core, 10 cases, $3.8900:** contested-viewpoints,
  sparse-coverage, general-summary, unknown-label-falls-back, revision-then-approval,
  revision-cap-is-labelled, budget-cap-is-labelled, notes-are-persisted,
  empty-label-falls-back, chatty-label-falls-back.
- **Batch B — technical + contested strata, 11 cases, $4.2790:** technical-version-numbers,
  technical-benchmark-figures, technical-cache-pricing, technical-release-dates,
  technical-default-limits, technical-percentage-figures, contested-monorepo-vs-polyrepo,
  contested-rag-versus-finetuning, contested-service-boundaries,
  contested-open-weight-models, contested-static-typing-payoff.
- **Batch C — sparse + general + injection, 10 cases, $3.8900:** sparse-regional-adoption,
  sparse-niche-ecosystem, sparse-unpublished-internals, sparse-preannounced-product,
  sparse-vendor-incident-history, general-explains-a-concept, general-how-a-mechanism-works,
  general-defines-a-term, injection-in-a-recalled-note, injection-tries-to-force-approval.
- **Batch D — the eight follow-up-bearing cases (11 follow-up turns), $5.0645:**
  followup-uses-prior-notes, followup-admits-a-gap, followups-chain,
  followups-chain-of-three, followup-stays-inside-thin-notes,
  followup-refuses-an-uncovered-figure, followup-refuses-a-forecast,
  followup-with-no-prior-research.

D runs last deliberately: it is the most expensive batch, the one whose turn shapes are
still uncalibrated (the follow-up constants are unmeasured), and the one where refusals
are most plausible (three cases are judged on the honesty of a refusal). 21-01 Task 2
validated that A∪B∪C∪D plus technical-figures partitions GOLDEN exactly.

**P-06 — The recording shell must have `CRITIC_MODEL` and `EVAL_JUDGE_MODEL` unset, and
that is asserted, not assumed.** A fixture recorded with `CRITIC_MODEL` set replays
STALE-red in keyless CI forever (`grade_fixture_current` compares the recorded critic
against `graph.critic_model()`, and CI never sets the variable — `harness.py:374-381`); a
fixture recorded with `EVAL_JUDGE_MODEL` set carries verdicts from a judge that is not the
settled default, which 21-03's judge pin reds. The assertion runs after any `.env` sourcing
and before every `--yes`. The post-stage keyless replay is the backstop that catches a slip
after one case instead of after forty.

**P-07 — Refusal handling, made operational.** The recorder's loop continues past a refusal
and the run exits 1 with the refused case named (`evals/__main__.py:644-649,675-690`).
The executor treats that exit as a FINDING: complete the batch's commit, quote the refusal
verbatim, and pause for the operator. No retry inside the same invocation, no `--force`
ever (a forced fixture is a known-bad recording — nothing in this phase's scope wants one),
no silent dropping. A re-run of a refused case is its own micro-checkpoint: present the
case's per-case quote line as the incremental cost and run only on explicit approval. If a
refusal stands unresolved, the phase ends with fewer than 40 fixtures and 21-03's
completeness pin reds — which is the honest state, surfaced by the gate built for exactly
this.
</decisions_this_plan_locks>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Checkpoint 1 — approve the calibration spend (stage 1)</name>
  <decision>Spend real money on the one-case calibration recording (technical-figures)?</decision>
  <context>
21-CONTEXT ratified staged spend: one case first, then re-quote. Before presenting this
checkpoint, the executor runs the FRESH quote keylessly (no approval needed, spends
nothing, exits 2 by design):

    ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals --record --case technical-figures

and pastes its output here. At planning time (2026-08-15) that line read **$0.3577**
(measured pipeline $0.2427 from the 2026-08-10 fixture + assumed judge; 21-CONTEXT's
"~$0.39" was the same number quoted two days earlier). This single case:
re-records the ONLY existing fixture, whose `models.judge: "claude-opus-5"` is the
superseded judge (21-RESEARCH Finding 2 — keeping it would ship 40 fixtures with one
carrying verdicts from a judge Phase 18 replaced); calibrates the quote for the other 39;
and round-trips the first real `claude-opus-4-8` judge verdict, closing Phase 18's
deferred ~$0.06 probe.

What runs on approval: the operator's keys enter the shell (never a file, never a commit),
the env assertion from the runbook must pass, then
`--record --case technical-figures --yes` with a `--report` into the phase dir. Money is
spent at that moment and cannot be recalled. Nothing else runs `--yes` under this approval
— the remaining 39 have their own checkpoint after the re-quote.
  </context>
  <options>
    <option id="approve">
      <name>Approve stage 1 (~$0.36 at the fresh quote)</name>
      <pros>Retires the stale fixture, calibrates the quote, closes the deferred judge probe — all for one case's cost, exactly as ratified.</pros>
      <cons>Real spend on the operator's key; no runtime circuit breaker once the case starts (21-RESEARCH Pitfall 4) — the bound is the case's own shape.</cons>
    </option>
    <option id="decline">
      <name>Decline / hold</name>
      <pros>No spend; the keyless machinery from wave 1 stands on its own.</pros>
      <cons>The phase cannot proceed — every later stage depends on this calibration; the requirement stays open.</cons>
    </option>
  </options>
  <resume-signal>Reply "approve" (with the fresh quote visible in this prompt) or "decline". Any budget instruction given here binds the executor.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Stage 1 — record the calibration case, verify it cold, correct the constants, re-quote the 39</name>
  <files>evals/fixtures/technical-figures.json, evals/__main__.py, .planning/phases/21-forty-recorded-answers/record-stage1-report.json, .planning/phases/21-forty-recorded-answers/record-quote-rebased.txt</files>
  <precondition>The operator's ANTHROPIC_API_KEY and VOYAGE_API_KEY are reachable — exported in this shell or present in a repo-root .env to source. Nothing in the evals import chain loads a file (21-RESEARCH Finding 3), so if neither is available, halt and ask rather than running to an SDK auth error after the money approval.</precondition>
  <reversibility rating="costly">~$0.36 of real spend that cannot be recalled once run; the fixture file itself is an ordinary revertible commit. Checkpoint 1 immediately precedes this task by construction, and the recorder's own exit-2 gate was re-proven on this tree in wave 1.</reversibility>
  <read_first>
    - `.planning/phases/21-forty-recorded-answers/21-RECORD-RUNBOOK.md` — the env preparation, the assertion line, and the stage-1 commands. This task follows it verbatim; the runbook was written to be the single source of the invocations.
    - `evals/__main__.py:210-271` — the ASSUMED_* constants, their calibration comment, and the file's own correction rule ("raise what the measurement exceeded, keep what it did not"). The comment block's history paragraphs are the model for the re-derivation this task performs.
    - `evals/harness.py:566-588` — `record_case_to_fixture` writes all three model roles at record time; `evals/__main__.py:662-674` — the recorded/previewed/measured closing line this task captures verbatim.
  </read_first>
  <action>
Run stage 1 exactly per the runbook, then convert its measurement into the tighter stage-2
quote. Every sub-step's output is SUMMARY evidence — capture as you go, not at the end.

1. **Env prep + assertion.** Bring the keys into the shell (export, or source the .env),
   then run the runbook's assertion line. It must confirm: both keys non-empty,
   `CRITIC_MODEL` unset/empty, `EVAL_JUDGE_MODEL` unset/empty (P-06). If it fails, fix the
   shell and re-assert — never proceed hoping.
2. **Record.** `python -m evals --record --case technical-figures --yes --report
   .planning/phases/21-forty-recorded-answers/record-stage1-report.json`, full console
   captured. Expect one `REC` line and `recorded 1/1 case(s) · previewed $X · measured
   pipeline $Y + 2 judge call(s)`. If the case is REFUSED (SKIP): stop entirely, quote the
   refusal verbatim, and return to the user — P-07; a refused calibration is a finding
   about the pipeline, and the decision to re-run is not the executor's.
3. **Verify the fixture cold, before any more money.** Three checks:
   (a) a `python -c` read of the new `evals/fixtures/technical-figures.json` asserting
   `models == {"pipeline": graph.MODEL, "judge": G.DEFAULT_JUDGE_MODEL, "critic":
   graph.MODEL}`-equivalent values, no `forced` key, `recorded_at` today, and
   `pipeline_cost_usd` quoted in the SUMMARY beside the previous $0.2427 (a delta is
   calibration information, not a failure);
   (b) the keyless replay: `ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" python -m evals --case
   technical-figures` must exit 0 — this is what proves the recording shell was clean
   (P-06's backstop) while only one case's money is at risk;
   (c) quote one judge verdict's `detail` text from the fixture in the SUMMARY — the
   evidence closing Phase 18's deferred probe (first real Opus 4.8 verdict round trip).
4. **Commit** the fixture and the stage-1 report:
   `feat(21-02): stage 1 — technical-figures re-recorded under the settled judge`.
5. **Correct the constants.** Read the measured evidence (the report JSON's cost figures,
   the fixture's `pipeline_cost_usd`, and whatever token-level detail the run's trace
   carries) and apply the file's own rule to the RESEARCH constants only: raise what the
   measurement exceeded, keep what it did not. The follow-up and judge constants remain
   unmeasured — say so. Re-derive the CALIBRATED comment block: new date, the measured
   figures actually recovered (state which level — tokens or dollars — the evidence
   supported), the fixture's git sha, and what batch D will be the first to measure. If
   the measurement exceeded nothing, the honest edit is the comment alone — record that
   outcome rather than inventing a correction. Do not touch anything else in the file:
   no import changes, no new loaders of any kind (Pitfall 3 — the leak PR #28 closed).
   Run the preview tests (`pytest tests/test_evals.py -k "preview or record" -q`) — they
   compute from the constants relatively and must stay green.
6. **Re-quote the remaining 39**, keyless, no `--yes`: the runbook's four batch id lists
   concatenated into one `--record --case ...` invocation, output captured into
   `record-quote-rebased.txt` (exit 2). Append to the same file the FULL 40-case quote
   (`--record` bare), whose technical-figures line must now read measured-from-fixture
   with today's date — the basis conversion observed. Derive the four per-batch sums from
   the per-case lines for checkpoint 2.
7. **Commit** the constants edit and the quote capture:
   `feat(21-02): calibration measured, constants corrected, stage 2 re-quoted`.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && .venv/bin/python -c "import json; from evals import graders as G; from research_agent import graph; f = json.load(open('evals/fixtures/technical-figures.json')); assert f['models']['judge'] == G.DEFAULT_JUDGE_MODEL, f['models']; assert f['models']['critic'] == graph.MODEL and f['models']['pipeline'] == graph.MODEL; assert 'forced' not in f; print('stage 1 fixture:', f['models'], f['recorded_at'], f['pipeline_cost_usd'])"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals --case technical-figures --quiet; test $? -eq 0 && echo "keyless replay green"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && test -s .planning/phases/21-forty-recorded-answers/record-stage1-report.json && .venv/bin/python -c "import json; r = json.load(open('.planning/phases/21-forty-recorded-answers/record-stage1-report.json')); recs = r['recordings']; assert len(recs) == 1 and recs[0]['written'], recs; print('recorded:', recs[0]['case_id'], recs[0]['cost_usd'])"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ! grep -qi "dotenv" evals/__main__.py && .venv/bin/pytest tests/test_evals.py -q && .venv/bin/ruff check evals tests</automated>
  </verify>
  <done>
The re-recorded fixture carries the settled judge and an explicit critic; keyless replay of
it exits 0; the stage-1 report and console line are committed and quoted in the SUMMARY
(actual vs the checkpoint's quote, stated at the time); the constants are corrected (or the
comment honestly says the measurement exceeded nothing) with the preview tests green; the
re-based 39-case quote is captured with per-batch sums derived; both commits landed via -F.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Checkpoint 2 — approve the bulk spend (stage 2, four batches)</name>
  <decision>Spend the re-based quote to record the remaining 39 cases?</decision>
  <context>
The executor pastes here, from `record-quote-rebased.txt`: the re-based 39-case total, the
four per-batch sums re-derived from its per-case lines, and the basis line. For reference,
the planning-time numbers (assumed-constants basis, superseded by what is pasted above):
A $3.8900 · B $4.2790 · C $3.8900 · D $5.0645, totalling $17.1235 of the original
$17.4812. Stage 1's actual is also stated here, beside its quote — the calibration's
accuracy is the best evidence for how far to trust this number.

What runs on approval: the four batch commands from the runbook, in order A → B → C → D,
each `--case`-scoped (a blind unscoped run would RE-SPEND on every fixture already on
disk — 21-RESEARCH Finding 4), each with its own report JSON, each committed before the
next starts. A mid-run death loses at most the in-flight batch, and the resume re-runs
only unrecorded ids. Refusal policy P-07 is in force: a SKIP surfaces and pauses; nothing
is retried or forced under this approval.
  </context>
  <options>
    <option id="approve">
      <name>Approve all four batches at the re-based quote</name>
      <pros>One approval, four bounded commits, resumable at batch granularity; the phase's deliverable lands.</pros>
      <cons>The full remaining spend rides on one decision; per-case there is still no runtime circuit breaker once a case starts.</cons>
    </option>
    <option id="approve-batchwise">
      <name>Approve batch A only, re-confirm before each subsequent batch</name>
      <pros>Four smaller decisions; each batch's actual informs the next approval.</pros>
      <cons>Three additional interruptions; the quote does not get more accurate between batches of the same shape.</cons>
    </option>
    <option id="decline">
      <name>Decline / hold</name>
      <pros>Stage 1's calibration and the stale-fixture retirement stand on their own.</pros>
      <cons>39 cases stay unrecorded; the completeness pin cannot land; the requirement stays open.</cons>
    </option>
  </options>
  <resume-signal>Reply "approve", "approve-batchwise", or "decline". Any per-batch budget instruction given here binds the executor.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Stage 2 — record the 39 in four batches, committed per batch, refusals surfaced</name>
  <files>evals/fixtures/ (39 new files), .planning/phases/21-forty-recorded-answers/record-stage2-a-report.json, .planning/phases/21-forty-recorded-answers/record-stage2-b-report.json, .planning/phases/21-forty-recorded-answers/record-stage2-c-report.json, .planning/phases/21-forty-recorded-answers/record-stage2-d-report.json</files>
  <precondition>The same shell (or a re-prepared one) still passes the runbook's env assertion — keys present, the two poisoning variables unset. Re-run the assertion before EVERY batch's --yes; a laptop that slept or a new terminal is a new environment.</precondition>
  <reversibility rating="one-way">The remaining ~$17 of real spend, unrecoverable once run — the reason checkpoint 2 (Task 3) sits immediately before this task, per the ratified plan. The fixture files themselves are ordinary revertible commits; the money is not.</reversibility>
  <read_first>
    - `.planning/phases/21-forty-recorded-answers/21-RECORD-RUNBOOK.md` — the four batch commands and the resume recipe. Follow verbatim.
    - `evals/__main__.py:675-690` — the refused-cases closing block whose text this task must quote verbatim if it ever prints.
    - `evals/harness.py:711-724` — each fixture is written to disk inside the per-case loop, immediately; that is the fact the resume recipe rests on.
  </read_first>
  <action>
For each batch, in the checkpoint-approved order A → B → C → D:

1. Re-run the runbook env assertion (P-06). Then the batch's command:
   `python -m evals --record --case <the batch's ids> --yes --report
   .planning/phases/21-forty-recorded-answers/record-stage2-<letter>-report.json`,
   console captured. The closing `recorded N/M · previewed $X · measured pipeline $Y`
   line is this batch's actual-vs-quote evidence — record it immediately.
2. After the batch: keyless
   `ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" python -m evals --quiet` must exit 0 — every
   fixture recorded so far replays green, so a poisoned or malformed recording is caught
   one batch deep, not at the end.
3. Commit the batch's fixtures plus its report:
   `feat(21-02): stage 2 batch <letter> — <N> cases recorded ($actual vs $quoted)`.
   One commit per batch — the commit points ARE the resume points, and each batch's
   fixtures carry that commit's parent sha in their metadata (several distinct shas
   across the set is honest provenance, not drift).
4. **If the process dies mid-batch:** the fixtures already written are safe. Resume per
   the runbook — recompute the batch's unrecorded remainder (the 21-01
   `fixture_coverage()` missing-set intersected with the batch's ids) and re-run ONLY
   those ids with a fresh `--report` path. Never re-run the full batch command over
   already-written fixtures.
5. **If any case is REFUSED (SKIP line, exit 1):** P-07. The loop has already continued
   past it — finish capturing the batch, commit what WAS written, quote the refusal block
   verbatim, and PAUSE: present the refusal to the user as a micro-checkpoint with that
   case's per-case quote line as the incremental re-run cost. Re-run the single case only
   on explicit approval (`--record --case <id> --yes`). If the user declines, the case
   stays unrecorded, the refusal goes in the SUMMARY as a finding, and 21-03's
   completeness pin will red — the honest terminal state, not a failure to hide.
6. Never pass `--force` in this phase. A forced fixture is a known-bad recording stamped
   into the repo; no requirement here wants one.

After batch D: run the 21-01 coverage measurement expecting missing 0 / orphans 0 /
stale 0, and assemble the per-stage table (stage 1 quoted/actual; per-batch
quoted/actual; running total vs both the re-based quote and the original $17.4812) into
the SUMMARY from the report JSONs and captured console lines — numbers from files, not
memory (21-CONTEXT Specific Ideas: criterion 4 is the report, not the run).
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && .venv/bin/python -c "import sys; sys.path[:0]=['.','tests']; from test_evals import fixture_coverage, stale_judges; missing, orphans = fixture_coverage(); stale = stale_judges(); print(f'missing={sorted(missing)} orphans={sorted(orphans)} stale={stale}'); assert not missing and not orphans and not stale, 'the fixture set is not the full golden set under the settled judge'"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals --quiet; test $? -eq 0 && echo "all fixtures replay green keylessly"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && for f in a b c d; do test -s ".planning/phases/21-forty-recorded-answers/record-stage2-$f-report.json" || exit 1; done && .venv/bin/python -c "import json; total = sum(rec['cost_usd'] for l in 'abcd' for rec in json.load(open(f'.planning/phases/21-forty-recorded-answers/record-stage2-{l}-report.json'))['recordings']); print(f'stage 2 measured pipeline total: ${total:.4f}')"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/pytest -q 2>&1 | tail -2</automated>
  </verify>
  <done>
40 fixture files on disk covering exactly the golden ids, all under the settled judge, all
replaying green keylessly; four batch reports committed with four batch commits; the
per-stage actual-vs-quote table drafted in the SUMMARY from file evidence; the refusal
list stated — as its contents, or as explicitly empty; no --force ever passed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell env → committed fixtures | Keys, CRITIC_MODEL and EVAL_JUDGE_MODEL cross from the shell into what gets recorded and committed; a dirty shell poisons repo data that outlives the phase. |
| paid API calls → repo artifacts | Real answers and judge verdicts become committed files; spend happens with no runtime circuit breaker per case. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Information Disclosure | Real keys reaching committed files or import-time env mutation (the PR #28 leak reopened) | high | mitigate | Keys enter only the shell; no loader is added anywhere in evals/ (negative grep in Task 2's verify); reports and fixtures carry no env values by construction — spot-checked on the stage-1 report before commit. |
| T-21-04 | Tampering | Recording under a dirty shell — fixtures that replay stale-red in CI (CRITIC_MODEL) or carry non-settled-judge verdicts (EVAL_JUDGE_MODEL) | high | mitigate | P-06: assertion after sourcing, before every --yes; the post-stage keyless replay catches a slip at one case's cost (stage 1) or one batch's (stage 2); 21-03's judge pin is the permanent backstop. |
| T-21-05 | Denial of Service (of the budget) | No runtime circuit breaker once a case starts; the service's spend cap is structurally not in this path (21-RESEARCH Pitfall 4) | medium | accept | The controls are the quote-then---yes gate, per-case granularity, and batch scoping — worst case is one case's own shape overrunning its assumption, bounded by the graph's own revision/budget topology. Stated at both checkpoints rather than discovered. |
| T-21-06 | Information Disclosure | The operator's persistent notes leaking into a committed fixture | medium | mitigate | Existing control, verified in scope: `_per_case_memory` raises if the recorder is handed the process store or reuses one (`harness.py:591-629`); record mode uses per-case `live_memory_factory` stores. |
| T-21-SC | Tampering | npm/pip/cargo installs | high | accept | No install task in this phase; 21-RESEARCH Package Legitimacy Audit records N/A. |
</threat_model>

<verification>
- Both checkpoints show a fresh quote in the prompt and an explicit user approval before
  any `--yes` — the transcript is the authorization record (21-VALIDATION Manual-Only).
- The coverage/judge/replay trio all clean after batch D: missing 0, orphans 0, stale 0,
  keyless evals exit 0.
- Five report JSONs committed; per-stage actual-vs-quote captured at the time.
- Full keyless pytest suite green (this plan adds no tests; its one code edit is the
  constants block, covered by the existing preview tests).
- Refusal list stated in the SUMMARY — contents or explicit emptiness.
</verification>

<success_criteria>
1. All 40 golden cases carry fixtures recorded under the settled judge, committed in five
   bounded, resumable commits (ROADMAP criterion 1; criterion 4's quote/actual per stage).
2. NOTHING passed `--yes` outside the two approved checkpoints (plus any user-approved
   micro-checkpoint re-runs), and the recorder's own refusal gate was observed at each
   stage before approval.
3. Any refusal is a quoted finding with the operator's decision recorded — never a retry,
   never a `--force`, never a silence (ROADMAP criterion 3).
</success_criteria>

<output>
Create `.planning/phases/21-forty-recorded-answers/21-02-SUMMARY.md` when done
</output>
