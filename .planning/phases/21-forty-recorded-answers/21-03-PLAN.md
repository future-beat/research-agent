---
phase: 21-forty-recorded-answers
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - tests/test_evals.py
  - README.md
  - docs/OPERATIONS.md (only if its measured counts prove stale)
  - .planning/phases/21-forty-recorded-answers/21-VALIDATION.md
autonomous: true
requirements: [REQ-forty-recorded-answers]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The completeness pin lives in the suite and bites the REAL directory in both directions: a deleted fixture reds naming the missing case id; an orphan fixture reds naming the stem — both observed as mutations against the real tree, then restored"
    - "The judge pin proves all 40 fixtures carry the settled judge's verdicts; flipping one fixture's judge field reds it naming the file"
    - "A keyless run grades 80 checks — 40 behavioural + 40 @recorded, additive by design — with exit 0, and corrupting one recorded answer turns that into a red replay and exit 1 at a pass rate that would otherwise pass"
    - "Every count the docs now state was measured in this wave, not carried: README's test count, the eval denominator paragraph, and any OPERATIONS hit — while the recorded-answers Limitations bullet is byte-identical on the git axis (Phase 22 owns it)"
    - "The actual-vs-quote table is assembled from the committed report JSONs and stands in the SUMMARY per stage, delta stated even if embarrassing in either direction"
    - "21-VALIDATION's Status column carries measured evidence for every row, and its sign-off flips only on what was actually observed"
  artifacts:
    - "tests/test_evals.py — the two real-directory pins, green on commit, calling the wave-1 helpers"
    - "README.md — the Tests-and-evals section re-derived to the measured denominators; the :286 bullet untouched"
    - ".planning/phases/21-forty-recorded-answers/21-VALIDATION.md — Task/Plan/Wave and Status columns reconciled, nyquist_compliant flipped on evidence"
  key_links:
    - "the pins call fixture_coverage()/stale_judges() — the same functions proven red-both-ways in wave 1 — so the pins inherit their teeth"
    - "the README denominator paragraph quotes the same count line a fresh keyless run prints, so prose and machinery cannot disagree on the day they ship"
    - "the money table's every number traces to a committed report JSON or a captured console line — none to memory"
---

<objective>
The closure: the gates go live against the real 40-fixture directory, the new denominator
is measured and documented honestly, and the phase's money story is reconciled against its
quote.

Purpose: REQ-forty-recorded-answers criterion 2 needs "all 40, keylessly, on every push"
to be ENFORCED, not merely true today — the pins land here because only now is the tree
they assert green (21-01 P-04). DEC-13 needs the README's "41 cases" language to become
the measured truth. Criterion 4 needs the actual-vs-quote report in the record.

Output: two real-directory pins with their reds observed against real files; the measured
80-check denominator and before/after runtime; the doc pass; the reconciled VALIDATION
contract and the money table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-forty-recorded-answers/21-CONTEXT.md
@.planning/phases/21-forty-recorded-answers/21-RESEARCH.md
@.planning/phases/21-forty-recorded-answers/21-VALIDATION.md
@.planning/phases/21-forty-recorded-answers/21-01-SUMMARY.md
@.planning/phases/21-forty-recorded-answers/21-02-SUMMARY.md

@tests/test_evals.py
@README.md
</context>

<decisions_this_plan_locks>
This plan implements decisions locked in 21-01 (P-01 through P-04: pytest pins over a CLI
exit-rule change; both directions on stems; the DEFAULT_JUDGE_MODEL constant; pins land
only once the tree makes them green). One remaining call is settled here:

**P-08 — The pins are two tests, not one.** Coverage and judge-provenance are different
claims with different failure stories (a lost file vs a wrong-judge recording), and the
house rule is one claim per test so a red names its defect. Each pin carries its own
non-vacuity guard (the directory must hold exactly `len(GOLDEN)` files / the scan must
have iterated 40 fixtures) so neither can pass against an empty or misdirected directory.
</decisions_this_plan_locks>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The real-directory pins land, and their reds are observed against real files</name>
  <files>tests/test_evals.py</files>
  <precondition>All 40 fixtures are on disk: `ls evals/fixtures/*.json | wc -l` reads 40. If it reads less, a 21-02 refusal stands unresolved — HALT and report the gap to the user instead of pinning around it; a completeness pin committed red, skipped, or weakened is worse than the phase honestly staying open.</precondition>
  <reversibility rating="reversible">Two tests calling helpers that already exist. The mutations touch real fixture files but every one is restored from git before the task ends.</reversibility>
  <read_first>
    - `tests/test_evals.py` — the wave-1 `fixture_coverage()`/`stale_judges()` helpers and their unit tests (21-01 Task 1); the new pins are one assertion each on top of them.
    - `.planning/phases/21-forty-recorded-answers/21-RESEARCH.md` Finding 6b — the sketch and the reading of "on every push" these pins satisfy; Finding 7 — why no existing test pins the real directory's count (they all run against tmp_path sets, which is exactly the gap).
  </read_first>
  <behavior>
    - `test_every_golden_case_has_a_committed_fixture`: `fixture_coverage()` (no argument — the real tree) returns empty missing AND empty orphans, failure messages naming the offending ids; non-vacuity: the real directory holds exactly `len(GOLDEN)` fixture files.
    - `test_every_fixture_carries_the_settled_judge_verdict`: `stale_judges()` returns empty; non-vacuity: the scan iterated exactly `len(GOLDEN)` fixtures (a scan over zero files must not pass).
  </behavior>
  <action>
Add the two pins beside the wave-1 helpers, docstrings naming REQ-forty-recorded-answers
and what each defends: the first turns "someone once ran the recorder 40 times" into a
CI-enforced property of every future checkout (a bad rebase that drops one fixture, or a
dataset id rename that orphans one, reds the suite naming the id — 21-RESEARCH Finding
6b's exact scenarios); the second is the permanent record that these verdicts were filed
under the settled judge, and it reds if any future recording slips in under a moved one
(P-03: it deliberately checks what `grade_fixture_current` deliberately does not).

Both pins read the REAL directory — no monkeypatching of `FIXTURES_DIR` — which breaks the
test file's own stated hygiene convention (`tests/test_evals.py:2192-2196`) on purpose:
that convention exists so CODE tests don't move with the fixture set, and these two tests
are ABOUT the fixture set. Say exactly that in the docstrings.

Then the mutations against the real directory — the 21-VALIDATION rows 1-2 reds, run in
this order, each restored before the next:

1. `mv evals/fixtures/contested-viewpoints.json /tmp/` (or the scratchpad) → the
   completeness pin reds naming `contested-viewpoints`; the judge pin stays green (the
   directions are independent). Restore the file.
2. `cp evals/fixtures/technical-figures.json evals/fixtures/no-such-case.json` → the
   completeness pin reds naming `no-such-case` as an orphan. ALSO run the keyless CLI once
   with this orphan in place and record its behaviour (the existing replay leg reds it
   independently — two gates, different layers, both observed). `rm` the copy.
3. Flip one fixture's `models.judge` to the superseded value with a scripted edit → the
   judge pin reds naming the file. Restore via `git checkout -- evals/fixtures/`.

After restoration: the full test file green, `git status` clean in `evals/fixtures/`, and
`git diff --stat -- evals/fixtures/` empty — state the clean restoration in the SUMMARY.
Commit: `feat(21-03): the completeness and settled-judge pins go live`.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && .venv/bin/pytest tests/test_evals.py -k "committed_fixture or settled_judge" -q</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && .venv/bin/pytest tests/test_evals.py -q</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && git diff --stat -- evals/fixtures/ | wc -l | xargs test 0 -eq && echo "fixtures untouched after mutations"</automated>
    <mutation>The three real-directory mutations ARE this task's mutations, named in the action with their expected reds. All three observed, all three restored, all six observations (red + clean restore each) recorded in the SUMMARY.</mutation>
  </verify>
  <done>
Both pins green on the real tree; all three mutations observed red naming their exact
defect and restored to a clean git state; the whole test file green; commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 2: The 80-check denominator, the corrupt-fixture red, runtime, and the suite arithmetic</name>
  <files>(measurements only — no files change in this task)</files>
  <reversibility rating="reversible">Measurements plus one deliberately-broken-then-restored fixture. Nothing ships from this task but evidence.</reversibility>
  <read_first>
    - `.planning/phases/21-forty-recorded-answers/21-RESEARCH.md` Finding 6 — the additive denominator (40 behavioural + 40 @recorded = 80, deliberate design, `<id>` vs `<id>@recorded`), the all-must-pass-vs-rate split, and why 40 dated recordings cannot make the gate flakier over time (replay never re-calls a model and never reads the calendar into a verdict).
    - `evals/__main__.py:624-658` — the exit rule and the headline count line this task captures.
    - `.planning/phases/21-forty-recorded-answers/21-01-SUMMARY.md` — the before-runtime and the 41/41 baseline measured in wave 1.
  </read_first>
  <action>
Four measurements, each captured verbatim for the SUMMARY and 21-VALIDATION:

1. **The denominator.** `ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals`
   (unquieted): capture the headline — expected `PASS 80/80 cases (100% vs 90% required)`
   — the `replaying 40 recorded case(s)` line, and the caveat line, which now aggregates
   the recordings' dates/models/shas (several distinct shas, one per batch commit, is
   honest provenance — quote it as printed). Exit must be a real 0, not read through a
   pipe. 80 is a claim to check: if the true count differs, the fixture set and dataset
   moved under the plan — reconcile and report, don't force.
2. **The corrupt-fixture red (21-VALIDATION row 3's mutation).** Scripted edit of ONE
   fixture's research-turn `state["draft"]` to a short garbage string → keyless run: the
   `@recorded` case reds on recorded quality graders, the run exits 1, and the printed
   pass rate is ABOVE the 90% floor (79/80 = 98.75%) — quote the rate line as the proof
   the all-must-pass overlay, not the rate, is what fired. Restore via
   `git checkout -- evals/fixtures/`, re-run, exit 0. Record both observations.
3. **Runtime, before/after (21-VALIDATION row 7).** `time` the keyless run and state it
   beside wave 1's measured before-figure; state the `--min-pass-rate 0.9` semantics
   against the grown denominator in one sentence in the SUMMARY (the replay leg is
   all-must-pass regardless of rate — the threshold governs only the behavioural leg,
   unchanged by growth from 1 to 40 replays).
4. **The suite arithmetic.** Full keyless `pytest`: reconcile against 799 passed /
   72 skipped plus every test this phase added BY NAME (wave 1's unit tests, this wave's
   two pins — the plan projects +5 passed / +0 skipped, landing at 804 / 72; that is a
   claim to check, and the reconciliation names each new test or corrects the claim).
   Then `ruff check .` and `ruff check src tests evals`, and the CI-shaped eval
   invocation from 21-VALIDATION's Test Infrastructure table with its real `$?` echoed.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals --quiet; echo "evals exit: $?"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals --report /private/tmp/claude-501/-Users-hesamabbaszadi-Documents-Projects-Research-agent/77ee0aac-1050-4549-b834-197d8fc337b3/scratchpad/evals-report-21.json --quiet >/dev/null; .venv/bin/python -c "import json; r = json.load(open('/private/tmp/claude-501/-Users-hesamabbaszadi-Documents-Projects-Research-agent/77ee0aac-1050-4549-b834-197d8fc337b3/scratchpad/evals-report-21.json')); n = len(r['cases']); rec = sum(1 for c in r['cases'] if c['case_id'].endswith('@recorded')); print(f'{n} cases, {rec} recorded'); assert rec == 40 and r['fixtures']['count'] == 40, (n, rec, r['fixtures'])"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/pytest -q 2>&1 | tail -1 && .venv/bin/ruff check . && .venv/bin/ruff check src tests evals</automated>
    <mutation>The corrupt-fixture edit in step 2 is this task's mutation: one recorded answer broken → replay red + exit 1 at a passing rate (rate line quoted), restored to a clean git state and re-run green. Both observations recorded.</mutation>
  </verify>
  <done>
The 80-check denominator measured with a real exit 0 and the caveat quoted; the corrupt
mutation observed red at a passing rate and restored clean; runtime stated before/after;
the suite arithmetic reconciled test-by-test against 799/72; ruff clean both forms.
  </done>
</task>

<task type="auto">
  <name>Task 3: The doc pass, the money table, and the reconciled validation contract</name>
  <files>README.md, docs/OPERATIONS.md (only on measured staleness), .planning/phases/21-forty-recorded-answers/21-VALIDATION.md</files>
  <reversibility rating="reversible">Documentation and planning-record edits, all measured before written. Revert loses prose, never data.</reversibility>
  <read_first>
    - `README.md:196-234` — the Tests and evals section: the `pytest` comment line (its test count), the `python -m evals` comment line, and the recorded-answers paragraph (currently "One case of forty is recorded … a run now grades 41 cases") — ALL editable. `README.md:277-292` — the Limitations section: the recorded-answers bullet is NOT editable (Phase 22 owns the section; 21-CONTEXT locks this).
    - `.planning/phases/21-forty-recorded-answers/21-VALIDATION.md` — the eight rows this task reconciles and the sign-off checklist.
    - Phase 20's precedent (`.planning/phases/20-note-count-bound/20-02-*`): a doc pass measures every count it touches and greps beyond the sites the plan names — the family has produced a surprise stale count in each of the last six phases.
  </read_first>
  <action>
**README.** Re-derive the Tests and evals section against this wave's measurements:

1. The `pytest` comment's test count and the `python -m evals` comment — set to what
   Task 2 measured, wording judged by reading (the "40 golden cases + every recording"
   phrasing may survive on its merits; the numbers may not).
2. The recorded-answers paragraph: rewrite to the true state — all forty recorded
   (a deliberate, paid, staged operator act), a run now grades 80 checks (40 behavioural
   + 40 replayed, additive by design, the replay half all-must-pass), and the caveat
   aggregates the recordings' dates, models and commits. Keep the paragraph's existing
   honesty about WHAT replay claims: it grades what the pipeline said when recorded,
   never what the current model would say. Do not oversell — the sentence about `--live`
   being the only measure of current quality stays true and stays.
3. The Limitations bullet at README:286 ("Only one of forty answers …") stays
   byte-identical — it is now the fourth knowingly-false bullet Phase 22 deletes, joining
   :285/:289/:291. Gate it on the git axis, not just file state: the README diff for this
   phase contains no changed line inside the Limitations section, and the bullet's grep
   count is 1 before and after.
4. Whole-file pass: grep README beyond the named sites for anything this phase falsified
   — the stale candidates are the old denominator, the one-of-forty phrasing outside the
   bullet, and the test count — and re-measure any hit before editing it. Record the grep
   and its hits (or their absence) in the SUMMARY.

**OPERATIONS.** Grep `docs/OPERATIONS.md` for eval/test-count claims (the "41", the test
total, "one of forty"/"one case of forty" phrasings, golden-case counts). Phase 20 found a
stale count at OPERATIONS:609 the plan had not named; assume nothing. Edit only measured
staleness; record absences as results.

**The money table (ROADMAP criterion 4; 21-VALIDATION row 6).** Assemble into the SUMMARY,
from the five committed report JSONs and the captured console lines — never memory:
per stage, quoted vs actual (stage 1 calibration; the re-based stage-2 quote; batches
A–D), then the totals: measured pipeline spend + judge calls (billed separately, per the
recorder's own closing-line wording) against both the re-based quote and the original
$17.4812. State the delta plainly in either direction, plus the refusal list (contents or
explicit emptiness) carried forward from 21-02. Note this table is what the phase PR's
body quotes.

**The validation contract.** Fill 21-VALIDATION's Status column for all eight rows with
the measured evidence from all three plans (commands, observed results, mutation
observations, report-JSON paths); check the sign-off items that were actually observed;
set `nyquist_compliant: true` and `wave_0_complete: true` and `status: complete` in its
frontmatter only if every row's evidence is real. Anything unobserved stays unchecked
with a stated reason — an honest hole beats a decorated one.

Commit everything in this task as the phase's doc commit:
`docs(21): the denominator is 80 and the record run is reported against its quote`.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && test "$(grep -cF 'Only one of forty answers is recorded' README.md)" = "1" && git diff HEAD~1 -- README.md | grep -c "^[+-].*Only one of forty" | xargs test 0 -eq && echo "limitations bullet untouched on the git axis"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ! grep -qF "grades 41 cases" README.md && ! grep -qF "One case of forty is recorded" README.md && grep -q "80" README.md && echo "denominator language re-derived"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && grep -q "nyquist_compliant: true" .planning/phases/21-forty-recorded-answers/21-VALIDATION.md && ! grep -q "| TBD |" .planning/phases/21-forty-recorded-answers/21-VALIDATION.md</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/pytest -q 2>&1 | tail -1 && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals --quiet; echo "final evals exit: $?"</automated>
    <mutation>Restore the old "grades 41 cases" phrasing in README while keeping the new 80 elsewhere — the denominator-language gate above must red, proving it pins the SENTENCE that was false, not merely the presence of the new number (the 18-04 lesson: hold the citation constant, break the facts). Revert, re-run green, record both observations.</mutation>
  </verify>
  <done>
README's Tests-and-evals section states only measured numbers; the Limitations bullet is
proven untouched on the git axis; OPERATIONS hits (or their measured absence) recorded;
the money table stands in the SUMMARY with per-stage quoted-vs-actual and the stated
delta; 21-VALIDATION carries measured Status evidence on all eight rows with its
frontmatter flipped only on observed facts; the doc commit landed via -F.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixtures → every future CI verdict | The pins make the fixture SET a checked claim; a hand-edited or lost fixture must red loudly forever after. |
| measured facts → published docs | DEC-13: every count the README states must trace to a run performed this wave. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-07 | Tampering | A hand-edited or forced fixture replaying as if judge-approved | medium | mitigate | Existing controls verified in scope: recorded verdicts replay as gates (`_recorded_judge_grades`), `forced: true` is stamped and reds, and Task 2's corrupt-fixture mutation observes the red path against a REAL fixture rather than trusting the tmp_path tests. |
| T-21-08 | Repudiation | Doc counts drifting from the machinery they describe (the recurring stale-count family, six phases running) | medium | mitigate | Every number written in Task 3 is measured in Task 2 of this same wave; the whole-file grep pass hunts beyond the named sites; absences recorded as results. |
| T-21-09 | Tampering | The real-directory pins weakened to pass an incomplete set (e.g. `>= 39`, or skipped on refusal) | high | mitigate | The precondition HALTS on fewer than 40 rather than pinning around it; both pins carry non-vacuity guards; the three real-directory mutations prove the pins bite before they are trusted. |
| T-21-SC | Tampering | npm/pip/cargo installs | high | accept | No install task in this phase; 21-RESEARCH Package Legitimacy Audit records N/A. |
</threat_model>

<verification>
- Both real-directory pins green on the shipped tree; their three mutations observed red
  naming exact defects, all restored to a clean `git diff -- evals/fixtures/`.
- Keyless evals: 80 checks, real exit 0, caveat quoted; corrupt-fixture mutation red at a
  passing rate; runtime stated against wave 1's before-figure.
- Full keyless suite green, arithmetic reconciled by name against 799/72; ruff clean both
  forms.
- README language gates green including the byte-untouched Limitations bullet on the git
  axis; the money table complete with the delta stated.
- 21-VALIDATION reconciled: no TBD cells, Status evidence on all eight rows, sign-off
  flipped only on observations.
</verification>

<success_criteria>
1. "All 40 replayed and graded keylessly on every push" is enforced by committed gates
   that were observed to bite, not by convention (ROADMAP criteria 1-2, the phase's
   researched gap closed).
2. The docs state the measured denominator honestly (DEC-13) while Phase 22's Limitations
   surface stays byte-identical.
3. The spend is reported against the quote, per stage and in total, from file evidence
   (ROADMAP criterion 4), with the refusal list stated either way.
</success_criteria>

<output>
Create `.planning/phases/21-forty-recorded-answers/21-03-SUMMARY.md` when done
</output>