---
phase: 10.5
slug: close-the-live-endpoint-exposure-hotfix
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-04
---

# Phase 10.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (config in `pyproject.toml`, `[tool.pytest.ini_options]`) |
| **Config file** | `pyproject.toml` — note `pythonpath = [".", "src", "tests"]`; there is **no `conftest.py`** in this repo |
| **Quick run command** | `pytest tests/test_service.py -q` |
| **Full suite command** | `pytest -q` |
| **Estimated runtime** | ~18 seconds full suite (364 passed / 28 skipped locally) |

The 28 local skips are all `tests/test_store_contract.py` (27 need `DATABASE_URL`, 1 needs
`REQUIRE_POSTGRES`). CI runs Postgres for real: 392 passed, 0 skipped. This phase must not
change either number except by the tests it adds.

---

## Sampling Rate

- **After every task commit:** Run `pytest tests/test_service.py -q`
- **After every plan wave:** Run `pytest -q` plus `ruff check .`
- **Before `/gsd:verify-work`:** Full suite green **and** live verification (SC-6) performed
  against the deployed host
- **Max feedback latency:** 20 seconds

---

## Per-Task Verification Map

Task IDs are assigned by the planner; this map is the contract each task must satisfy. The
planner MUST fill the Task ID column and may split rows across plans, but may not drop a row.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | 1 | REQ-live-endpoint-exposure | T-10.5-01 | Token-only dependency returns 403 when `SESSIONS_TOKEN` is unset (fail closed) | unit | `pytest tests/test_service.py -q -k sessions_token` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | REQ-live-endpoint-exposure | T-10.5-01 | Each of the four routes returns 401/403 without a token | unit | `pytest tests/test_service.py -q -k unauthenticated_sessions` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | REQ-live-endpoint-exposure | T-10.5-01 | Each of the four routes returns its normal 200/204 with a valid token | unit | `pytest tests/test_service.py -q -k authorised_sessions` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | REQ-live-endpoint-exposure | T-10.5-02 | `DEMO_TOKEN` is accepted as a fallback value for `SESSIONS_TOKEN` | unit | `pytest tests/test_service.py -q -k token_fallback` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | REQ-live-endpoint-exposure | T-10.5-03 | Reads do **not** consume the research rate-limit quota and are **not** 429'd by the daily cap | unit | `pytest tests/test_service.py -q -k reads_not_metered` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | REQ-live-endpoint-exposure | T-10.5-04 | `DELETE /sessions/{id}` is rate-limited | unit | `pytest tests/test_service.py -q -k delete_rate_limited` | ❌ W0 | ⬜ pending |
| TBD | TBD | 1 | REQ-live-endpoint-exposure | T-10.5-05 | `POST /sessions/{id}/ask*` stays **unguarded** — the demo's second turn still works anonymously | unit | `pytest tests/test_service.py -q -k ask_still_anonymous` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | REQ-live-endpoint-exposure | T-10.5-06 | Structural: a future route added to the sessions group cannot be unguarded. **Recursive** walker + non-vacuity assertion | unit | `pytest tests/test_service.py -q -k route_guard_invariant` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | REQ-live-endpoint-exposure | T-10.5-07 | SSE error handler redacts exception text (existing helper wired to `service.py:263`) | unit | `pytest tests/test_service.py -q -k sse_error_redacted` | ❌ W0 | ⬜ pending |
| TBD | TBD | 2 | REQ-live-endpoint-exposure | — | `/demo`, `/`, `/health`, `/metrics` unaffected; `POST /research/stream` still anonymous | unit | `pytest -q` | ✅ | ⬜ pending |
| TBD | TBD | 3 | REQ-live-endpoint-exposure | T-10.5-08 | Live: unauthenticated `GET /sessions` on the deployed host returns 401/403 | manual | see Manual-Only Verifications | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_service.py` — new cases for every ❌ row above. Extend the existing module;
      do **not** add a `conftest.py` (repo convention — shared fakes live in the owning test
      module and are imported via `pythonpath`).
- [ ] Update the ~14 existing call sites that hit the four routes without a token. The
      smallest diff is a default header on the fixture's `TestClient` plus `SESSIONS_TOKEN`
      in `make_client`. `tests/test_service.py:807-815` needs an **explicit** fix — it breaks
      under every candidate design and a fixture-level sweep will not catch it.
- [ ] No framework install needed — pytest is already configured.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| SC-6: the fix is live | REQ-live-endpoint-exposure | Requires a Fly secret and a real deploy; cannot be asserted from the test suite | 1. `fly secrets set --stage SESSIONS_TOKEN=... -a research-agent` 2. `fly deploy -a research-agent` (single cutover, also carrying Phase 10's pending commits) 3. `curl -s -o /dev/null -w "%{http_code}" https://research-agent.fly.dev/sessions` → expect 401/403 4. Repeat with the token header → expect 200 5. Confirm `/demo` still loads and a research run still completes **anonymously** |

Every intermediate state of that sequence must fail closed. Staging the secret before the
deploy means the new code never runs without its token configured.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 20s
- [ ] Local suite still 364 passed / 28 skipped **plus** the new cases — skip count unchanged
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
