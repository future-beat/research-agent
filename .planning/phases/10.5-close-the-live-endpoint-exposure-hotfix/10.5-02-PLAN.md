---
phase: 10.5-close-the-live-endpoint-exposure-hotfix
plan: 02
type: execute
wave: 2
depends_on: ["10.5-01"]
files_modified:
  - src/research_agent/service.py
  - tests/test_service.py
autonomous: true
requirements: [REQ-live-endpoint-exposure]

must_haves:
  truths:
    - "The four session read/delete routes refuse an anonymous caller"
    - "DELETE /sessions/{id} consumes a rate-limit slot, and only after the token check passes"
    - "POST /sessions/{id}/ask and /ask/stream still work for an anonymous demo visitor"
    - "An SSE error event carries redacted, truncated exception text"
    - "The existing suite is green again"
  artifacts:
    - path: "src/research_agent/service.py"
      provides: "sessions APIRouter carrying require_sessions_token; SSE error redaction"
      contains: "APIRouter"
    - path: "tests/test_service.py"
      provides: "make_client fixture that configures SESSIONS_TOKEN and sends the header"
      contains: "SESSIONS_TOKEN"
  key_links:
    - from: "src/research_agent/service.py"
      to: "limits.require_sessions_token"
      via: "APIRouter(dependencies=[Depends(...)])"
      pattern: "Depends\\(limits\\.require_sessions_token\\)"
    - from: "src/research_agent/service.py DELETE route"
      to: "limits.check_rate_limit"
      via: "decorator-level dependencies list"
      pattern: "Depends\\(limits\\.check_rate_limit\\)"
    - from: "src/research_agent/service.py SSE error handler"
      to: "_redact"
      via: "detail field of the error event"
      pattern: "_redact\\("
---

<objective>
Wire the credential to the routes. Move `GET /sessions`, `GET /sessions/{id}`,
`GET /sessions/{id}/trace` and `DELETE /sessions/{id}` onto an `APIRouter` whose
router-level dependency is `require_sessions_token`, add `check_rate_limit` to the `DELETE`
only, wire the existing `_redact` helper into the SSE error handler, and repair the fourteen
existing test call sites that hit those routes with no token.

Purpose: this is the plan that actually closes the hole in code. Grouping is by **dependency,
not path prefix** — `POST /sessions/{id}/ask*` shares the prefix but is the demo's second turn
and must stay anonymous (CONTEXT decision 4).

Output: a route table where the four session routes carry an auth dependency, an SSE error
path that cannot leak a DSN, and a green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-CONTEXT.md
@.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-RESEARCH.md
@.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-01-SUMMARY.md
@.planning/codebase/TESTING.md
@src/research_agent/service.py
@tests/test_service.py

<interfaces>
<!-- Contracts the executor needs. Extracted from the tree; no exploration needed. -->

From src/research_agent/limits.py (Plan 01 output plus existing):
- `require_sessions_token(request: Request) -> None` — 403 unconfigured, 401 wrong/absent
- `check_rate_limit(request: Request) -> None` — per-IP sliding window, raises 429 + `Retry-After`
- `limiter() -> RateLimiter`, `reset_limiter() -> None`, `RateLimiter.tracked_keys() -> int`

From src/research_agent/service.py (current shape):
- `SESSION_LIST_LIMIT = 50` (47)
- `get_sessions(request) -> SessionStore` (124), `get_metrics(request) -> MetricsStore` (130)
- `guard(request, metrics=Depends(get_metrics)) -> None` (134)
- `_sse(event: str, payload: dict) -> str` (228)
- `_stream(state, metrics, on_complete) -> Iterator[str]` (232); its `except Exception as exc`
  handler at ~263 currently yields `{"error": type(exc).__name__, "detail": str(exc)}`
- `_index_json() -> dict` (302) — advertises `sessions` / `session` / `trace`
- `_redact(text: str) -> str` (337) — substitutes URL credentials with `***@`
- `_probe(fn) -> dict` (347) — the correct treatment to mirror:
  `message = str(exc).splitlines()[0] if str(exc) else ""` then `_redact(message)[:160]`
- `_require(store, session_id) -> Session` (587) — raises 404
- Routes to move, all currently `@app.*` with only `Depends(get_sessions)`:
  `list_sessions` (514), `get_session` (519, `response_model=SessionDetail`),
  `get_trace` (533), `delete_session` (539, `status_code=204`)
- Routes that MUST NOT move: `ask` (473) and `ask_stream` (496), both
  `@app.post("/sessions/{session_id}/ask...", dependencies=[Depends(guard)])`

From tests/test_service.py:
- `make_client` fixture (24) — sets `SESSION_DB_PATH`, `METRICS_DB_PATH`,
  `DEMO_RATE_LIMIT_PER_HOUR=0`, `DEMO_DAILY_USD_CAP=0`, delenvs `DEMO_TOKEN`, calls
  `limits.reset_limiter()`, pins `SESSION_BACKEND`/`METRICS_BACKEND`, installs
  `dependency_overrides` for `get_sessions`/`get_metrics`, builds `TestClient(service.app)`
- Call sites that hit the four routes with no token (verified by grep): lines
  144, 208, 243, 252, 262, 263, 264, 267, 312, 333, 372, 423, 499, 814
- `test_unknown_sessions_are_404` (267) — parametrised over `/sessions/nope`, `/sessions/nope/trace`
- `test_a_probe_failure_does_not_leak_the_dsn` (675) — the shape to mirror for the SSE test
- `test_the_guard_runs_before_anything_is_spent` (807-815) — sets `DEMO_TOKEN="s3cret"` at 810
  then reads `GET /sessions` at 814; breaks under every candidate design
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regroup the four session routes behind require_sessions_token</name>
  <files>src/research_agent/service.py</files>
  <read_first>
    - `src/research_agent/service.py` in full — the import block (32-40), `guard` (134-141),
      the `ask`/`ask_stream` routes (473-512), the four target routes (514-543), `_index_json`
      (302-334)
    - `.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-RESEARCH.md`
      § "Pattern 2" and § "Anti-Patterns to Avoid"
    - `src/research_agent/limits.py` as written by Plan 01
  </read_first>
  <action>
    Import `APIRouter` from `fastapi` alongside the existing `Depends, FastAPI, HTTPException,
    Request, Response`.

    Create a module-level router immediately above the four routes:
    `APIRouter(prefix="/sessions", tags=["sessions"], dependencies=[Depends(limits.require_sessions_token)])`.
    Name it `sessions_router`. Add a comment recording why the group exists: four routes each
    individually forgot a decorator, so membership must be structural rather than per-route —
    a future `@sessions_router.get("/{id}/notes")` inherits the guard by construction.

    Re-decorate the four routes onto it, keeping bodies, signatures, response models and status
    codes byte-identical:
      - `@sessions_router.get("")` for `list_sessions` — with `prefix="/sessions"` this produces
        the path `/sessions` exactly, no trailing slash and no double prefix (verified against
        fastapi 0.141.1 in RESEARCH § Pattern 2).
      - `@sessions_router.get("/{session_id}", response_model=SessionDetail)` for `get_session`.
      - `@sessions_router.get("/{session_id}/trace")` for `get_trace`.
      - `@sessions_router.delete("/{session_id}", status_code=204,
        dependencies=[Depends(limits.check_rate_limit)])` for `delete_session`. Only the DELETE
        gets the limiter. Add a comment stating: router dependencies run before decorator
        dependencies, so an unauthorised caller is refused before it can consume a rate-limit
        slot — the same cheapest-first ordering already argued in `limits.enforce`; and that the
        bucket is deliberately shared with research runs rather than adding a second module
        global and a second entry in `reset_limiter()`.
      - Drop `tags=["sessions"]` from the individual decorators; the router carries it.

    Call `app.include_router(sessions_router)` after the `@app.post("/sessions/{session_id}/ask")`
    and `.../ask/stream` route definitions. Those two routes stay on `app` with their existing
    `dependencies=[Depends(guard)]` — do NOT move them (CONTEXT decision 4; moving them breaks
    follow-ups for anonymous demo visitors, which is the demo's second turn).

    Do NOT apply `guard` to any of the four (CONTEXT decision 3): `guard` is `check_token` +
    `check_rate_limit` + `check_daily_cap` indivisibly, so it would make listing sessions consume
    the visitor's research quota and 429 all reads once the $5 daily cap is hit — contradicting
    the cap's own message "Read-only endpoints still work".

    Update `guard`'s docstring (134-141): its "Read-only endpoints stay open on purpose" claim is
    now imprecise. Distinguish ops reads (`/health`, `/metrics`, `/pricing`, `/memory`, `/demo`),
    which stay open because they are how you diagnose a refusing service, from session reads,
    which return other people's research and are closed by `require_sessions_token` — a separate,
    unmetered dependency, deliberately not `guard`.

    In `_index_json`, annotate the `sessions`, `session` and `trace` entries so the front door
    does not advertise them as open — suffix each value with ` (X-Demo-Token required)`.

    Do not add a CORS middleware, an `APIKeyHeader` security scheme, or any identity model.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
from research_agent import service
from fastapi.routing import APIRoute
try:
    from fastapi.routing import _IncludedRouter
except ImportError:
    _IncludedRouter = ()
found = []
def walk(routes, prefix=''):
    for r in routes:
        if isinstance(r, APIRoute):
            found.append((prefix + r.path, sorted(r.methods), {getattr(d.call, '__name__', '') for d in r.dependant.dependencies}))
        elif _IncludedRouter and isinstance(r, _IncludedRouter):
            walk(r.original_router.routes, prefix + r.include_context.prefix)
walk(service.app.routes)
s = [f for f in found if f[0].startswith('/sessions')]
assert len(s) >= 6, s
for path, methods, deps in s:
    if path.endswith('/ask') or path.endswith('/ask/stream'):
        assert 'guard' in deps, (path, deps)
        assert 'require_sessions_token' not in deps, (path, deps)
    else:
        assert 'require_sessions_token' in deps, (path, deps)
        if methods == ['DELETE']:
            assert 'check_rate_limit' in deps, (path, deps)
print('route table OK', len(s))
" && .venv/bin/ruff check src/research_agent/service.py</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `route table OK 6` (or higher) and exits 0.
    - The recursive walker finds exactly these six `/sessions` paths: `/sessions`,
      `/sessions/{session_id}`, `/sessions/{session_id}/trace` (GET), `/sessions/{session_id}`
      (DELETE), `/sessions/{session_id}/ask`, `/sessions/{session_id}/ask/stream`.
    - `/sessions` resolves without a trailing slash — the walker's path list contains the exact
      string `/sessions`, not `/sessions/`.
    - `grep -c 'Depends(limits.check_rate_limit)' src/research_agent/service.py` returns 1.
    - `grep -n 'sessions_router' src/research_agent/service.py` shows the router defined once and
      `app.include_router(sessions_router)` appearing after the `ask/stream` route definition
      (compare line numbers).
    - `.venv/bin/python -c "s=open('src/research_agent/service.py').read().split('def guard')[1].split('\"\"\"')[1]; assert 'Read-only endpoints stay open on purpose' not in s"` exits 0.
    - `grep -c 'X-Demo-Token required' src/research_agent/service.py` returns 3.
    - `grep -c 'CORSMiddleware\|APIKeyHeader' src/research_agent/service.py` returns 0 when
      filtered with `grep -v '^#'`.
    - `.venv/bin/ruff check src/research_agent/service.py` reports no findings.
  </acceptance_criteria>
  <done>
    The four session routes carry `require_sessions_token`; `DELETE` additionally carries
    `check_rate_limit`; `POST /sessions/{id}/ask*` still carries only `guard`. The test suite is
    expected to be RED at this point — Task 2 repairs it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Repair the fourteen existing call sites via the fixture, plus the one that needs surgery</name>
  <files>tests/test_service.py</files>
  <read_first>
    - `tests/test_service.py` lines 1-70 — the `make_client` fixture in full
    - `tests/test_service.py` lines 258-270 — `test_sessions_can_be_deleted` and
      `test_unknown_sessions_are_404`
    - `tests/test_service.py` lines 800-816 — `test_streaming_endpoints_are_guarded` and
      `test_the_guard_runs_before_anything_is_spent`
    - `.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-RESEARCH.md`
      § "Pitfall 2" (the enumerated fourteen call sites) and § "Pitfall 3"
    - `.planning/codebase/TESTING.md`
  </read_first>
  <action>
    Make the existing suite green again with the smallest diff, keeping the fixture the single
    place that knows about credentials — the way it already owns `DEMO_RATE_LIMIT_PER_HOUR`,
    `DEMO_DAILY_USD_CAP` and `SESSION_BACKEND`.

    In `make_client`, alongside the existing guardrail env setup: define a module-level constant
    `SESSIONS_TOKEN = "sessions-s3cret"`, `monkeypatch.setenv("SESSIONS_TOKEN", SESSIONS_TOKEN)`,
    and build the client as `TestClient(service.app, headers={"x-demo-token": SESSIONS_TOKEN})`.
    A default header on the client covers every existing call site at lines 144, 208, 243, 252,
    262, 263, 264, 267, 312, 333, 372, 423 and 499 with no edit to any of them. Keep the existing
    `monkeypatch.delenv("DEMO_TOKEN", raising=False)` — `DEMO_TOKEN` must stay unset so the
    guarded write path stays open by default, which is what the rest of the suite assumes.

    Then fix `test_the_guard_runs_before_anything_is_spent` (807-815) explicitly — a fixture
    sweep does not save it (CONTEXT decision 8). That test sets `DEMO_TOKEN="s3cret"` at line 810
    to prove a refused `POST /research` opens no session, then asserts emptiness by reading
    `GET /sessions`. With `SESSIONS_TOKEN` set the read succeeds through the fixture's default
    header — but if the fallback ordering were ever wrong, setting `DEMO_TOKEN` mid-test would
    change which value the session routes expect. Make the test's intent explicit: it is about
    spend, not auth. Keep `monkeypatch.setenv("DEMO_TOKEN", "s3cret")`, keep the `POST /research`
    refusal, and assert emptiness through a client that carries the sessions token — add a
    comment naming why the `DEMO_TOKEN` set at that line does not affect the read (the fixture's
    `SESSIONS_TOKEN` takes precedence in `sessions_token()`). If the assertion still fails, do
    not weaken it: read the failure and fix the precedence in `limits.sessions_token()`.

    Fix `test_unknown_sessions_are_404` (267) per Pitfall 3: auth now runs before the handler, so
    an unauthenticated caller gets 401 rather than 404 — which is correct, because 404-before-401
    leaks whether a session ID exists. Keep the existing parametrised test asserting 404 *with*
    the fixture's token, and add a sibling `test_unknown_sessions_are_401_without_a_token`
    asserting 401 for the same two paths from a bare `TestClient(service.app)`. Both facts are
    worth pinning.

    Do not add a `conftest.py` (CONTEXT decision 7). Do not sprinkle `headers={...}` across
    unrelated tests — a large diff of that shape means the fixture change was done wrong.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_service.py -q` passes with zero failures.
    - `.venv/bin/pytest -q` reports the local baseline `364 passed, 28 skipped` plus only the
      cases added by Plan 01 and this plan. The skip count must be exactly 28 — a change there
      means a test module failed to import.
    - `grep -c 'x-demo-token' tests/test_service.py` returns at most 3 (the fixture default plus
      any explicit negative-case header), proving the diff was fixture-shaped not sprinkled.
    - `grep -n 'SESSIONS_TOKEN' tests/test_service.py` matches inside `make_client`.
    - `grep -n 'def test_unknown_sessions_are_401_without_a_token' tests/test_service.py` returns
      one match, and `-k unknown_sessions` collects at least 4 tests.
    - `test -f tests/conftest.py || test -f conftest.py` exits non-zero.
    - `.venv/bin/ruff check tests/test_service.py` reports no findings.
  </acceptance_criteria>
  <done>
    The suite is green with the routes guarded, the fixture is the only place that knows the
    credential, and the 404-vs-401 ordering is pinned in both directions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire _redact into the SSE error handler</name>
  <files>src/research_agent/service.py, tests/test_service.py</files>
  <read_first>
    - `src/research_agent/service.py` lines 232-265 — `_stream` and its `except Exception as exc`
      handler, the site to change
    - `src/research_agent/service.py` lines 337-362 — `_redact` and `_probe`; `_probe` is the
      exact treatment to mirror (first line only, then `_redact(...)[:160]`)
    - `tests/test_service.py` lines 675-692 — `test_a_probe_failure_does_not_leak_the_dsn`, the
      assertion shape to mirror
    - `.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-RESEARCH.md` § "Pitfall 5"
  </read_first>
  <action>
    In `_stream`'s exception handler (currently
    `yield _sse("error", {"error": type(exc).__name__, "detail": str(exc)})` at ~line 263),
    mirror `_probe`'s treatment exactly: take the first line of `str(exc)` with
    `str(exc).splitlines()[0] if str(exc) else ""`, then emit
    `"detail": _redact(message)[:160]`. All three parts matter — first line, because a psycopg
    error is multi-line and the second line is often where the DSN sits; `_redact`, because that
    is the helper `/health` already uses correctly; and `[:160]`, because redaction alone still
    lets an arbitrarily long body reach the browser, which renders it. Leave `metrics.record`,
    the `# noqa: BLE001` comment and its reason, and the event name untouched. This is a missing
    call site, not new code — do not write a new regex (CONTEXT decision 6).

    Add `test_sse_error_redacted_and_truncated` to `tests/test_service.py`, sibling to
    `test_a_probe_failure_does_not_leak_the_dsn`. Drive a failure through `POST /research/stream`
    (make the faked graph client raise a `ConnectionError` whose message is
    `"connection failed: postgresql://user:sup3rsecret@host/db"` followed by a newline and a long
    second line), parse the `error` SSE event's `detail`, and assert: `"sup3rsecret" not in
    detail`, `"***@host/db" in detail` (redacted, not merely truncated), the second line's text is
    absent, and `len(detail) <= 160`. The name must contain `sse_error_redacted` so the validation
    contract's `-k sse_error_redacted` selector matches.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_service.py -q -k sse_error_redacted && .venv/bin/pytest tests/test_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_service.py -q -k sse_error_redacted` collects at least 1 test
      and passes; it must not report "no tests ran".
    - `.venv/bin/python -c "s=open('src/research_agent/service.py').read(); h=s.split('def _stream')[1].split('def _node_detail')[0]; assert '\"detail\": str(exc)' not in h; assert '_redact(' in h and '[:160]' in h"` exits 0.
    - `grep -c '_URL_CREDENTIALS' src/research_agent/service.py` returns 2 — the existing pattern
      definition and its single use in `_redact`; no second regex was introduced.
    - `.venv/bin/pytest tests/test_service.py -q` passes.
    - `.venv/bin/ruff check src/research_agent/service.py tests/test_service.py` reports no findings.
  </acceptance_criteria>
  <done>
    An SSE `error` event carries only the first line of the exception, with URL credentials
    substituted and the body capped at 160 characters — matching what `/health` already does.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → `/sessions*` route table | Anonymous HTTP crosses here; the router dependency is the only control |
| graph/store exception text → SSE response body | Untrusted-to-emit internal text crosses to a browser the caller controls |
| route decorator → route table | A future contributor's new route crosses here; per-route opt-in is what failed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.5-01 | Information Disclosure | `list_sessions`, `get_session`, `get_trace` | mitigate | Router-level `dependencies=[Depends(limits.require_sessions_token)]`; deny-by-default membership rather than four independent decorators |
| T-10.5-03 | Denial of Service | session reads if `guard` were applied | mitigate | Token-only dependency; `check_daily_cap` and the research quota deliberately excluded so reads stay unmetered and diagnosable |
| T-10.5-04 | Tampering / DoS | `delete_session` | mitigate | `dependencies=[Depends(limits.check_rate_limit)]` on the DELETE decorator only; router deps run first so an unauthorised caller cannot consume a slot |
| T-10.5-05 | Denial of Service | `POST /sessions/{id}/ask*` | mitigate | Grouped by dependency, not path prefix — the ask routes stay on `app` with `guard`; verified structurally by Task 1's route-table assertion |
| T-10.5-07 | Information Disclosure | `_stream` exception handler | mitigate | `_redact(first_line)[:160]`, mirroring `_probe`; asserted for absence of the password, presence of `***@`, and length |
| — | Information Disclosure | `_index_json` front door | accept | Endpoint paths are already public in `/openapi.json`; annotating them as token-required is courtesy, not a control |
| T-10.5-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies (RESEARCH § Package Legitimacy Audit: not applicable). If a task here would add a package, stop and run the legitimacy gate first. |
</threat_model>

<verification>
- The recursive route-table assertion in Task 1's verify passes.
- `.venv/bin/pytest -q` returns to the baseline (364 passed / 28 skipped) plus new cases.
- `.venv/bin/ruff check .` reports no findings.
- `POST /research/stream` and `POST /sessions/{id}/ask/stream` still succeed with no auth header
  in the existing streaming tests — the demo's two calls are untouched.
</verification>

<success_criteria>
- SC-1 (code half): the four routes are unreachable without a credential in-process.
- SC-3 (wiring half): `DELETE /sessions/{id}` carries `check_rate_limit`.
- SC-4: the SSE error handler redacts and truncates.
- The suite is green and the skip count is unchanged at 28.
</success_criteria>

<output>
Create `.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-02-SUMMARY.md` when done
</output>
