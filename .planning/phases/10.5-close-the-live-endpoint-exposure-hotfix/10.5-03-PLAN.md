---
phase: 10.5-close-the-live-endpoint-exposure-hotfix
plan: 03
type: execute
wave: 3
depends_on: ["10.5-02"]
files_modified:
  - tests/test_service.py
autonomous: true
requirements: [REQ-live-endpoint-exposure]

must_haves:
  truths:
    - "Each of the four routes returns 401 to a caller with no token and its normal 200/204 with one"
    - "With neither SESSIONS_TOKEN nor DEMO_TOKEN configured, the four routes return 403 rather than data"
    - "Setting only DEMO_TOKEN protects the four routes"
    - "Session reads neither consume the research rate-limit quota nor 429 under an exhausted daily cap"
    - "The eleventh DELETE from one IP is refused with 429 and a Retry-After header"
    - "An anonymous caller can still run POST /research/stream and POST /sessions/{id}/ask/stream"
  artifacts:
    - path: "tests/test_service.py"
      provides: "Behavioural auth and composition coverage for the guarded sessions group"
      contains: "unauthenticated_sessions"
  key_links:
    - from: "tests/test_service.py"
      to: "limits.limiter().tracked_keys()"
      via: "assertion that a refused caller consumed no rate-limit slot"
      pattern: "tracked_keys\\(\\)"
    - from: "tests/test_service.py"
      to: "TestClient(service.app)"
      via: "a token-less client for the anonymous cases"
      pattern: "TestClient\\(service\\.app\\)"
---

<objective>
Prove the behaviour, not just the wiring. Cover every row of the phase's verification contract
that is about what a caller actually gets back: refusal without a token, normal service with
one, fail-closed with nothing configured, the `DEMO_TOKEN` fallback, reads staying unmetered,
`DELETE` being rate-limited, and — the most valuable test in the phase — the demo surviving.

Purpose: the original defect passed a green suite for months because no test ever asked an
unauthenticated question. These tests ask it.

Output: new behavioural cases in `tests/test_service.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-CONTEXT.md
@.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-RESEARCH.md
@.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-VALIDATION.md
@.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-02-SUMMARY.md
@.planning/codebase/TESTING.md
@tests/test_service.py

<interfaces>
<!-- Contracts the executor needs. Extracted from the tree; no exploration needed. -->

From tests/test_service.py after Plan 02:
- `SESSIONS_TOKEN = "sessions-s3cret"` module constant
- `make_client` fixture — yields `build(critic_verdicts=("APPROVED",) * 8) -> (client, fake)`;
  sets `DEMO_RATE_LIMIT_PER_HOUR=0`, `DEMO_DAILY_USD_CAP=0`, `SESSIONS_TOKEN`, delenvs
  `DEMO_TOKEN`, installs `dependency_overrides`, returns a `TestClient` with a default
  `x-demo-token` header
- Existing pattern for a token-less client: build `TestClient(service.app)` directly — the
  `dependency_overrides` installed by `make_client` still apply through `include_router`
  (verified in RESEARCH § Code Examples 1)

From src/research_agent/limits.py:
- `limiter() -> RateLimiter`; `RateLimiter.tracked_keys() -> int`; `reset_limiter() -> None`
- `check_rate_limit` raises `HTTPException(429, ..., headers={"Retry-After": str(retry_after)})`
- `check_daily_cap` raises 429 when `metrics.spend_since(...) >= daily_cap_usd()`
- `sessions_token()` reads `SESSIONS_TOKEN` then falls back to `demo_token()`

Route surface after Plan 02:
- guarded by `require_sessions_token`: `GET /sessions`, `GET /sessions/{id}`,
  `GET /sessions/{id}/trace`, `DELETE /sessions/{id}` (DELETE also `check_rate_limit`)
- guarded by `guard` only: `POST /research`, `POST /research/stream`,
  `POST /sessions/{id}/ask`, `POST /sessions/{id}/ask/stream`
- open: `GET /`, `/health`, `/ready`, `/demo`, `/metrics`, `/pricing`, `/memory`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refusal, service, fail-closed, and the DEMO_TOKEN fallback</name>
  <files>tests/test_service.py</files>
  <read_first>
    - `tests/test_service.py` lines 1-70 (the `make_client` fixture as amended by Plan 02) and
      lines 230-270 (the existing session tests whose shape these mirror)
    - `.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-VALIDATION.md`
      § Per-Task Verification Map, rows for T-10.5-01 and T-10.5-02
    - `src/research_agent/limits.py` — `sessions_token`, `require_sessions_token`
  </read_first>
  <action>
    Add a new banner-commented section to `tests/test_service.py` titled for the guarded sessions
    group. Every test name below is load-bearing: the phase's verification contract selects on
    these exact substrings, so the names must contain them verbatim.

    - `test_unauthenticated_sessions_routes_are_refused` — parametrise over the four
      `(method, path)` pairs `("GET", "/sessions")`, `("GET", "/sessions/{sid}")`,
      `("GET", "/sessions/{sid}/trace")`, `("DELETE", "/sessions/{sid}")`, using a real session
      id created through the fixture's authorised client. Issue each request from a bare
      `TestClient(service.app)` that sends no `x-demo-token`, and assert `status_code == 401`.
      Parametrise rather than writing four near-identical tests (repo convention).

    - `test_authorised_sessions_routes_still_work` — through the fixture's client, assert
      `GET /sessions` returns 200 with the created session listed, `GET /sessions/{sid}` returns
      200, `GET /sessions/{sid}/trace` returns 200, and `DELETE /sessions/{sid}` returns 204.
      This is the "guarding did not break the endpoints" half of SC-1 and complements the
      pre-existing tests that now flow through the fixture's default header.

    - `test_sessions_token_unset_fails_closed` — `monkeypatch.delenv("SESSIONS_TOKEN")` and
      `delenv("DEMO_TOKEN")` after the fixture builds, then assert `GET /sessions` returns
      **403** — not 200, and not 401. Assert on 403 specifically: 401 would mean the caller could
      succeed by guessing a token, and 200 would mean the hotfix regressed to the original bug.
      Include a comment naming the reason: the defect this phase fixes was a control that was
      present in the code and inert in production.

    - `test_demo_token_fallback_protects_the_session_routes` — `delenv("SESSIONS_TOKEN")`, then
      `setenv("DEMO_TOKEN", "demo-only")`; assert a token-less `TestClient(service.app)` gets 401
      on `GET /sessions` (so the routes are still closed), and that a client sending
      `x-demo-token: demo-only` gets 200. This is SC-2 read literally — setting `DEMO_TOKEN`
      does protect them.

    Use `monkeypatch` for every env change so nothing leaks between tests, and call
    `limits.reset_limiter()` if a test changes a rate-limit env var. Do not add a `conftest.py`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_service.py -q -k "unauthenticated_sessions or authorised_sessions or sessions_token or token_fallback" && .venv/bin/pytest tests/test_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_service.py -q -k unauthenticated_sessions` collects exactly 4
      tests (one per parametrised route) and all pass.
    - `.venv/bin/pytest tests/test_service.py -q -k authorised_sessions` collects at least 1 test
      and passes.
    - `.venv/bin/pytest tests/test_service.py -q -k sessions_token` collects at least 1 test and
      passes; it must not report "no tests ran".
    - `.venv/bin/pytest tests/test_service.py -q -k token_fallback` collects at least 1 test and
      passes.
    - The fail-closed test asserts the literal integer `403`; confirm with
      `grep -n -A6 'def test_sessions_token_unset_fails_closed' tests/test_service.py | grep -c '403'` returning at least 1.
    - Deliberately breaking the guard (temporarily removing `dependencies=` from
      `sessions_router`) makes `-k unauthenticated_sessions` fail — verify once, then restore.
    - `.venv/bin/pytest tests/test_service.py -q` passes in full.
    - `.venv/bin/ruff check tests/test_service.py` reports no findings.
  </acceptance_criteria>
  <done>
    An anonymous caller is provably refused on all four routes, an authorised one is provably
    served, an unconfigured deployment provably refuses everyone with 403, and `DEMO_TOKEN` alone
    provably protects the group.
  </done>
</task>

<task type="auto">
  <name>Task 2: Composition — unmetered reads, a rate-limited DELETE, and a demo that survives</name>
  <files>tests/test_service.py</files>
  <read_first>
    - `src/research_agent/limits.py` — `check_rate_limit` (183), `check_daily_cap` (194) and its
      "Read-only endpoints still work" message (203), `limiter()` (156), `tracked_keys()` (140),
      `reset_limiter()` (164)
    - `tests/test_service.py` — the existing demo-guardrail section (~line 695 onward) for how
      the suite already drives `DEMO_RATE_LIMIT_PER_HOUR` and `DEMO_DAILY_USD_CAP`
    - `src/research_agent/static/index.html` lines 262-290 and 346 — the demo's only two `fetch`
      calls, neither of which sends an auth header
    - `.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-RESEARCH.md`
      § "Pitfall 4" and § "Validation Architecture"
  </read_first>
  <action>
    Add the composition tests. These are the ones that prove CONTEXT decisions 3 and 4 hold in
    behaviour, not just in the route table. Names again must contain the contract's substrings.

    - `test_session_reads_not_metered_by_the_rate_limit_or_daily_cap` — set
      `DEMO_RATE_LIMIT_PER_HOUR` to 1 and `limits.reset_limiter()`, then issue several authorised
      `GET /sessions` requests and assert every one returns 200 — reads must not consume the
      research quota. Then set `DEMO_DAILY_USD_CAP` low enough that the fixture's metrics store
      reports the cap exhausted, and assert `GET /sessions` still returns 200 rather than 429.
      Comment the reason: the cap's own 429 body says "Read-only endpoints still work", and a
      demo that hides its state at exactly the moment it starts refusing work is a demo you
      cannot debug.

    - `test_delete_rate_limited_after_the_hourly_limit` — set `DEMO_RATE_LIMIT_PER_HOUR` to a
      small number (1 is enough; do not drive eleven real deletes), `limits.reset_limiter()`,
      create sessions through the authorised client, then issue authorised `DELETE` calls until
      one returns 429. Assert the refusal is 429 and that the response carries a `Retry-After`
      header.

    - `test_delete_rate_limited_check_runs_after_the_token_check` — with the same small limit,
      record `limits.limiter().tracked_keys()`, issue an **unauthenticated** `DELETE` from a bare
      `TestClient(service.app)`, assert 401, and assert `tracked_keys()` is unchanged. Router
      dependencies run before decorator dependencies, so an unauthorised caller must not be able
      to burn another client's quota — the same ordering `limits.enforce` already argues for.

    - `test_ask_still_anonymous_for_the_demos_second_turn` — create a session through the
      authorised client, then from a bare `TestClient(service.app)` (no `x-demo-token`) issue
      `POST /sessions/{sid}/ask` and assert it is **not** 401/403 — it must return its normal
      success status. Comment that this is the demo's second turn and that grouping by path
      prefix instead of by dependency would break it.

    - `test_demo_stays_open_when_sessions_token_is_set` — with `SESSIONS_TOKEN` set by the fixture
      and `DEMO_TOKEN` unset, assert from a bare `TestClient(service.app)` that
      `POST /research/stream` is not refused, `GET /demo` returns 200 with
      `token_required` false, and `GET /health` returns 200. This is the regression test for the
      one failure mode that would destroy the project's portfolio purpose: the obvious
      remediation — setting `DEMO_TOKEN` in production — 401s every anonymous visitor because
      the page sends no header.

    Reset any module-global limiter state the tests touch (`limits.reset_limiter()`), matching the
    repo's explicit-teardown convention.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_service.py -q -k "reads_not_metered or delete_rate_limited or ask_still_anonymous or demo_stays_open" && .venv/bin/pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_service.py -q -k reads_not_metered` collects at least 1 test
      and passes.
    - `.venv/bin/pytest tests/test_service.py -q -k delete_rate_limited` collects at least 2 tests
      (the 429 case and the ordering case) and both pass.
    - `.venv/bin/pytest tests/test_service.py -q -k ask_still_anonymous` collects at least 1 test
      and passes.
    - `.venv/bin/pytest tests/test_service.py -q -k demo_stays_open` collects at least 1 test and
      passes.
    - `grep -c 'tracked_keys()' tests/test_service.py` returns at least 2 (before/after the
      unauthenticated DELETE).
    - `.venv/bin/pytest -q` reports 28 skipped — unchanged — and a passed count equal to the
      previous baseline plus this phase's new cases.
    - `.venv/bin/ruff check .` reports no findings.
  </acceptance_criteria>
  <done>
    Reads are provably unmetered, `DELETE` is provably rate-limited and provably checks the token
    first, and the demo's two anonymous calls are provably still open.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → route table | The only place the deployed behaviour is asserted before it reaches production |
| anonymous caller → shared rate-limit bucket | An unauthorised request must not decrement another client's quota |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.5-01 | Information Disclosure | four session routes | mitigate | `test_unauthenticated_sessions_routes_are_refused` (401 on all four) and `test_sessions_token_unset_fails_closed` (403, not 200) |
| T-10.5-02 | Elevation of Privilege | `DEMO_TOKEN` fallback path | mitigate | `test_demo_token_fallback_protects_the_session_routes` — the fallback closes rather than opens |
| T-10.5-03 | Denial of Service | session reads under an exhausted budget | mitigate | `test_session_reads_not_metered_by_the_rate_limit_or_daily_cap` |
| T-10.5-04 | Tampering / DoS | `DELETE /sessions/{id}` | mitigate | `test_delete_rate_limited_after_the_hourly_limit` (429 + `Retry-After`) and `test_delete_rate_limited_check_runs_after_the_token_check` (no quota burn by an unauthorised caller) |
| T-10.5-05 | Denial of Service | demo availability | mitigate | `test_ask_still_anonymous_for_the_demos_second_turn` and `test_demo_stays_open_when_sessions_token_is_set` |
| — | Spoofing | forged client IP escaping the DELETE limiter | accept | `client_ip` already prefers unforgeable `Fly-Client-IP`; `X-Forwarded-For` only with explicit `TRUST_FORWARDED_FOR`, already tested at `tests/test_limits.py:47,55` |
| — | Denial of Service | per-process limiter across multiple machines | accept | One machine today (`min_machines_running = 1`); Phase 11 raises machine count and `limits.py:94-98` already documents the consequence |
| T-10.5-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies. If a task here would add a package, stop and run the legitimacy gate first. |
</threat_model>

<verification>
- Every `-k` selector named in `10.5-VALIDATION.md`'s Per-Task Verification Map collects at least
  one test and passes — no selector may report "no tests ran".
- `.venv/bin/pytest -q` green, skip count still exactly 28.
- `.venv/bin/ruff check .` clean.
</verification>

<success_criteria>
- SC-1, SC-2, SC-3 and the demo-survival invariant are each covered by a named, passing test.
- Removing the router dependency makes the suite red — the tests actually bite.
</success_criteria>

<output>
Create `.planning/phases/10.5-close-the-live-endpoint-exposure-hotfix/10.5-03-SUMMARY.md` when done
</output>
