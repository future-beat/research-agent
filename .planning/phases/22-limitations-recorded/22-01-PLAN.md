---
phase: 22-limitations-recorded
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/0013-cost-approximation-by-design.md
  - docs/adr/README.md
  - docs/OPERATIONS.md
  - README.md
  - tests/test_evals.py
autonomous: true
requirements: [REQ-limitations-recorded]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The execution baseline is re-measured AFTER Phase 21.5 and recorded in the SUMMARY before any edit — suite counts, eval denominator, fixtures/refusals split, per-kind refusal counts, current Limitations bullet locations, next free ADR number. No number from planning-time documents is carried into any surface this phase writes."
    - "ADR-0013 exists at docs/adr/0013-cost-approximation-by-design.md in the house Nygard shape — Accepted, Source provenance, Context/Decision/Consequences — and its Rejected-alternatives section states the four measured reasons invoice reconciliation via Anthropic's Admin Usage & Cost API was rejected (criterion 1): org-scoped admin key with a bigger blast radius than either existing secret, daily-aggregate buckets with no per-run dimension, ~5-minute lag that may run longer, and new reconciliation infrastructure for a demo-scale service"
    - "docs/adr/README.md's index carries a 0013 row and every counting sentence in the file is re-derived — the existing derived-counts test is green at zero edits to the test itself"
    - "docs/OPERATIONS.md carries a new posture subsection under Going stateless that argues the single-region, no-replica, free-tier database choice as a deliberate posture with an upgrade path (criterion 3's target) — its facts cross-checked against OPERATIONS' own existing passages (the Sydney-cutover step and the connection budget), never against the README bullet being replaced"
    - "README's eval-section prose states the recorded/refused split, the eval denominator, and the two defect counts as measured post-21.5 values, and a new derived-counts test in the house pattern reds if any of them drifts from evals/fixtures/ plus evals/REFUSALS.json"
    - "The two paid-run defects (judge truncation, record/replay divergence) are recorded where the researcher argued they live — the eval-section prose with REFUSALS.json as evidence — never as Limitations bullets and never in a new doc surface"
  artifacts:
    - "docs/adr/0013-cost-approximation-by-design.md — the cost-approximation-by-design record, ~100-160 lines in the 0012 register"
    - "docs/adr/README.md — the 0013 index row plus re-derived counting prose, including the Source-provenance count in the paragraph below the table"
    - "docs/OPERATIONS.md — '### The free-tier posture, and the upgrade path' under '## Going stateless'"
    - "README.md — the Tests-and-evals section's three count-bearing passages re-derived from measured commands"
    - "tests/test_evals.py — test_the_readme_eval_counts_are_derived_from_the_tree, beside the ADR index test"
  key_links:
    - "22-02 links the README cost bullet to the exact filename this plan locks (P-06), and the README database bullet to the exact anchor this plan's heading produces (P-04) — a rename in either place after this plan lands breaks 22-02's link gate"
    - "the new test derives every count through the helpers the file already has — F.fixture_paths(), documented_refusals(), GOLDEN, _SPELLED — never a parallel loader"
    - "ADR-0013's Context leans on the telemetry record already committed (the README cost bullet's Voyage measurement, docs/OPERATIONS.md's dated 25-vs-40 measurement, usage.py's record_embedding docstring) rather than re-deriving the argument"
    - "the ADR index's counting prose is enforced by the EXISTING test — forgetting the prose after adding the row is the test's job to catch, observed red once via mutation"
---

<objective>
The records Phase 22's README rewrite will point at, plus the phase's measured baseline.
Three tracks: (1) re-measure everything Phase 21.5 moved, before any edit; (2) write
ADR-0013 (cost-approximation-by-design, criterion 1) and the OPERATIONS database posture
note (criterion 3's target); (3) re-derive README's eval-section prose from the tree and
gate it with a derived-counts test so the recorded-answers amended truth and the two
paid-run defects are recorded in a form that cannot silently go stale.

Purpose: REQ-limitations-recorded. Criterion 4's deletions and criterion 2/3/5's link
rewrite (plan 22-02) need these records to exist first — a bullet cannot point at a record
that hasn't landed, and the close-out's numbers must be measured, not carried.

Deviation from tracer-first, stated: this is a prose close-out with no architecture to
prove end-to-end; the de-risking the tracer normally buys is bought here by Task 1, which
runs the phase's entire verification machinery (suite, evals, greps, link targets) against
the post-21.5 tree before anything is edited.

Output: ADR-0013 + its index row; the OPERATIONS posture subsection; re-derived eval
prose; one new derived-counts test; the measured baseline in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-limitations-recorded/22-CONTEXT.md
@.planning/phases/22-limitations-recorded/22-RESEARCH.md
@.planning/phases/22-limitations-recorded/22-VALIDATION.md

@README.md
@docs/OPERATIONS.md
@docs/adr/README.md
@docs/adr/0012-judge-independent-of-the-critic.md
@evals/REFUSALS.json
@tests/test_evals.py
</context>

<decisions_this_plan_locks>
22-CONTEXT's Claude's-Discretion items and 22-RESEARCH's one open design call (Assumption
A1) are settled here; the executor implements them as written.

**P-01 — The refusal residue gets NO new Limitations bullet.** Its durable record is the
eval-section prose (re-derived in Task 3), `evals/REFUSALS.json` itself, and the
derived-counts gate. The section's new intro commits to "chosen, recorded, and argued
for"; an honestly-reported partial result is not a chosen limitation, and REQUIREMENTS'
own "no new bullet is born" spirit argues the same way. Adopts 22-RESEARCH Assumption A1.

**P-02 — The two defects share one record.** Both live in the existing eval-section
paragraph (README already narrates them in one breath), expanded and re-derived — their
kinds kept as distinct in the prose as `judge_truncated` and `recorded_then_failed_replay`
are in the JSON. No new ADR (no decision is being made — fixing is deferred), no
KNOWN-DEFECTS.md (a fourth doc surface with no discoverability story). The voice to match
is OPERATIONS' "Do not fix the five rls_enabled_no_policy notices" section: found,
explicitly not fixed here, with the reason why (ADR-0012's fence; the pins must also
satisfy dataset.py's reference reports).

**P-03 — No additional README pointer to REFUSALS.json.** The eval section's existing
inline link is the pointer; the Limitations section gets none (consequence of P-01).

**P-04 — The OPERATIONS posture note is a new `###` subsection under `## Going
stateless`, heading exactly `### The free-tier posture, and the upgrade path`.** That
heading produces the GitHub anchor `#the-free-tier-posture-and-the-upgrade-path`, which
22-02's README link targets — the heading text is load-bearing and must not drift. It
sits beside "Why Supabase and not Neon" (the closest existing argued-position sibling),
NOT inside "Supabase specifics", which is a runbook, not an argument.

**P-06 — ADR-0013's filename is `docs/adr/0013-cost-approximation-by-design.md`**, its
status `Accepted` (non-superseding — it adds a row, reopens nothing), its provenance line
`**Source:** Phase 22 (2026-08-XX), REQ-limitations-recorded` (the ADR-0006-onward
pattern; no DESIGN.md passage argues this position, so no Promoted-from), and the
Admin-API rejection lives in `### Rejected alternatives` under Consequences — the 0012
shape. 22-02 links this exact filename from the README cost bullet.

(P-05, P-07, P-08 are 22-02's locks — bullet shape, no-residue-bullet restated, and the
CONCERNS.md disposition.)
</decisions_this_plan_locks>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the execution baseline — post-21.5, before any edit</name>
  <files>(none — measurement only; evidence lands in the SUMMARY)</files>
  <precondition>Phase 21.5 has executed and merged: a `.planning/phases/21.5-*/` directory exists with its SUMMARY (and VERIFICATION if written), and the fixtures/refusals split it landed is on this branch. If that directory is missing or its work is unmerged, HALT — this phase is sequenced last by user ratification, and a baseline measured before 21.5 lands would rebuild exactly the staleness this task exists to prevent.</precondition>
  <reversibility rating="reversible">Nothing is written to the tree. Zero risk.</reversibility>
  <read_first>
    - `.planning/phases/21.5-*/` — every artifact in it, at execution time (22-CONTEXT's canonical refs defer this read to now, deliberately). Extract: which refused cases moved to fixtures, what the re-record checkpoint spent and found, whether any NEW finding category appeared beyond the two known defects (22-RESEARCH Open Question 1), and whether REQ-classifier-model's REQUIREMENTS/ROADMAP rows were flipped.
    - `.planning/phases/22-limitations-recorded/22-VALIDATION.md` — the gate contract this baseline anchors; its planning-time numbers (806/72, 59/59, 19/21) are the drift reference to diff against, never values to carry.
    - `evals/REFUSALS.json` — the post-21.5 refusal list: total, per-kind counts, and whether any entry the eval-section prose cites by shape (the contested pins, the hedged half-answer) moved or changed.
  </read_first>
  <action>
Measure, record, edit nothing. Every later gate in this phase anchors to what this task
writes down, not to any planning document.

Run and record, in this order:

1. Full keyless suite: the passed/skipped/collected triple, quoted.
2. Offline evals with a real exit code: the pass line, the denominator, `$?`.
3. The split: fixture count from `evals/fixtures/`, refusal total and per-kind counts
   from `evals/REFUSALS.json` (a `python -c` over `json.load`, counting entries per
   `kind`). Diff both against the planning-time 19/21 and name which cases moved.
4. The Limitations section, located by heading (`grep -n '^## Limitations' README.md`)
   and read in full: confirm all seven bullets are present, confirm the four deletion
   candidates each grep exactly once in README (the exact phrases and expected counts
   live in this task's verify block, not here), and record the current line numbers as
   this execution's locations — the planning-time :295/:296/:299/:301 and :297/:298/:300
   are hints, not facts.
5. The next ADR number from `ls docs/adr/` — believed 0013 at planning. If Phase 21.5
   landed an ADR of its own (its roadmap entry commits to recording the classifier model
   choice as an ADR-shaped decision) and took 0013, this phase's record takes the next
   free number: state the renumbering in the SUMMARY, carry it into Task 2's filename and
   into 22-02's link task, and keep the slug `cost-approximation-by-design`.
6. `ruff check .` — clean or not, recorded.
7. REQ-classifier-model's state in `.planning/REQUIREMENTS.md` and ROADMAP's Phase 21.5
   entry — flipped or not, recorded. 22-02 Task 3 acts on this; this task only records.

If 21.5's artifacts reveal a new finding category that changes the eval-section prose's
SHAPE (not just its numbers), record it here and carry it into Task 3's prose rather than
discovering it mid-edit.

Write the whole baseline into the SUMMARY as a dated, command-quoted block — the same
register 20-VERIFICATION used. This task produces evidence, not diffs; it makes no commit.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/pytest -q 2>&1 | tail -2</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals --quiet; echo "evals exit: $?"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ls evals/fixtures/*.json | wc -l && .venv/bin/python -c "import json,collections; r=json.load(open('evals/REFUSALS.json'))['refusals']; print(len(r), dict(collections.Counter(e['kind'] for e in r.values())))"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && test "$(grep -cF "The eval judge shares the critic's model" README.md)" -eq 1 && test "$(grep -cF 'Only one of forty answers is recorded' README.md)" -eq 1 && test "$(grep -cF 'checks that the API keys are *present*' README.md)" -eq 1 && test "$(grep -cF 'Notes are bounded by expiry alone' README.md)" -eq 1 && echo "all four deletion candidates present exactly once"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ls docs/adr/ | sort | tail -3 && ls .planning/phases/21.5-*/*-SUMMARY.md && grep -c "\[x\] \*\*Phase 21.5" .planning/ROADMAP.md</automated>
    <!-- checker W1/W3: a bare `ls -d` on the directory passes once 21.5 is merely
         PLANNED (CONTEXT/PLAN files, no execution) — the state this very phase's
         directory is in today. The gate must demand an executed 21.5: its SUMMARY
         on disk AND its ROADMAP checkbox flipped. -->

  </verify>
  <done>
The SUMMARY carries the full measured baseline — suite triple, eval denominator and exit,
fixtures/refusals split with per-kind counts and a named diff against 19/21, the four
deletion candidates confirmed present exactly once with current line numbers, the next ADR
number verified, ruff state, and REQ-classifier-model's flip state — every value from a
command run this session, none carried from a planning document.
  </done>
</task>

<task type="auto">
  <name>Task 2: ADR-0013, its index row, and the OPERATIONS posture note</name>
  <files>docs/adr/0013-cost-approximation-by-design.md, docs/adr/README.md, docs/OPERATIONS.md</files>
  <reversibility rating="reversible">Three doc files, one new. `git revert` removes the record and restores both indexes exactly; nothing reads these files at runtime.</reversibility>
  <read_first>
    - `docs/adr/0012-judge-independent-of-the-critic.md` — the register to write in: measured claims separated from attributed ones, `### Rejected alternatives` naming each option with the reason, a title that claims no more than was bought.
    - `docs/adr/README.md` — Record shape (title line, Status, Source, three-heading contract) and EVERY counting sentence: the "of the ... records are Accepted" prose above the table, the supersession count, and the "all seven carry `**Source:**`" paragraph below the table — each one this task's edit falsifies must be re-derived.
    - `docs/adr/0007-anonymous-identity-fairness-global-cap.md` — the other house exemplar; also what the identities bullet already points at (this task does not touch it; 22-02 verifies it).
    - `22-RESEARCH.md` Architecture Patterns §2 — the ADR's raw material: the four Admin-API rejection reasons with their doc citations, the already-committed telemetry record (README cost bullet, OPERATIONS' dated 25-vs-40 Voyage measurement, `usage.py:490-503`'s record_embedding docstring, the no-multipliers argument), and the DESIGN.md negative finding (no passage argues this position — hence Source, not Promoted-from).
    - `docs/OPERATIONS.md` — the "Going stateless" section head, "Why Supabase and not Neon" (the argued-position sibling the new subsection sits after), the cutover step 1 (region `ap-southeast-2`, immutable after creation, chosen beside Fly's `syd`), the connection budget passage (`hard_limit = 16` × 2, `PG_POOL_MAX_SIZE` 5, at most 10 of Nano's 60), and the measured round-trip numbers — the facts the posture note consolidates, cross-checked HERE and never against the README bullet being replaced.
  </read_first>
  <action>
Two records, one commit each or one combined commit — executor's call, but the ADR index
row and its counting prose land in the SAME commit as the ADR file (Pitfall 2: a
forgotten-prose commit is a red the full suite finds late; the targeted test finds it now).

**ADR-0013** at the P-06 filename (renumbered per Task 1 if 21.5 took 0013), in the house
shape:

- Title: `# ADR-0013 — Reported cost is an approximation by design, and invoice
  reconciliation is rejected` (or tighter, in the 0012 register — the title must claim the
  position, not the mechanism).
- `**Status:** Accepted` and the P-06 Source line with the execution date.
- `## Context` — the epistemic position the tree already holds, cited rather than
  re-derived: cost figures are computed client-side from effective-dated rate tables and
  surfaced synchronously (`/pricing`, `cost_usd` in the SSE payload); provider token
  counts are telemetry, not billing truth (the dated 25-vs-40-and-0 Voyage measurement in
  OPERATIONS; `record_embedding`'s "Zero tokens is not an error" docstring); no
  multipliers are invented across vendors (`COST_DISCOUNT_FACTOR` and `inference_geo` are
  Anthropic dimensions, applying them to Voyage would invent a discount nobody
  negotiated). Name the provider dashboards as the only billing authority.
- `## Decision` — reported cost is an estimate by design, read against `/pricing`; the
  service never reads a bill and no reconciliation job exists. One paragraph, decisive.
- `## Consequences` → `### Accepted`: readers treat `cost_usd` as an estimate that says
  so; the honest backstop for billing truth is the vendor dashboard; the global spend cap
  bounds exposure regardless of estimate error. `### Rejected alternatives`: invoice
  reconciliation via Anthropic's Admin Usage & Cost API, with the four measured reasons —
  (1) it requires an org-scoped Admin API key that outlives its creator and reads every
  workspace, a materially bigger secret than either the service manages, against an
  OPERATIONS posture where credentials never reach an image layer; (2) the cost endpoint
  buckets by day and groups by workspace or description — no per-request or per-session
  dimension exists, so reconciling a per-run claim against a daily aggregate is a new
  estimation problem, not a solution; (3) data lands ~5 minutes after request completion
  and sometimes later, against figures this service reports synchronously; (4) a
  scheduled job, storage for the aggregate, and correlation logic against the runs ledger
  is real infrastructure for a demo-scale service whose README already states the honest
  backstop. State that the four facts were read from Anthropic's official Usage & Cost
  API docs on 2026-08-16 (22-RESEARCH's fetch) and re-glanced at execution; if the API's
  shape has changed, record what changed rather than asserting the 2026-08-16 shape.
  Reason 1 doubles as the security note 22-RESEARCH's threat table asks for — the unbuilt
  alternative's blast radius, documented, is the mitigation.

**The index**, in `docs/adr/README.md`, same commit: add the 0013 row (Accepted, no
successor), then re-derive every counting sentence the file carries — the
Accepted-of-total prose above the table (the existing test derives both numbers from the
table and demands the spelled prose match), the supersession count if its phrasing needs
touching (it should not — supersessions did not change), and the below-table paragraph
counting how many records carry `**Source:**` (0006 onward — this record joins them, the
count grows by one and its list gains "ADR-0013 in Phase 22's close-out" in the existing
sentence pattern).

**The posture note**, in `docs/OPERATIONS.md`, under `## Going stateless`, placed after
the "Why Supabase and not Neon" subsection, heading exactly per P-04. Content, in
OPERATIONS' own argued voice (the Neon section and the RLS do-not-fix section are the
register):

- The posture, stated as chosen: one Supabase Nano project, one region
  (`ap-southeast-2`, immutable after creation, chosen beside Fly's `syd` — cite the
  cutover step rather than restating it), no read replica, free tier.
- Why that is acceptable at this traffic — this is the genuinely NEW sentence the tree
  lacks (22-RESEARCH verified by grep that no read-replica or deliberate-posture framing
  exists in OPERATIONS today): the fleet holds at most 10 of Nano's 60 connections (cite
  the connection-budget passage), the measured round trips sit orders of magnitude inside
  the health budget, and the free tier's one idle hazard is prevented by the same probes
  that disqualified Neon. Close with the README bullet's own honest closing thought —
  fine at this traffic, and the first thing to look at if it isn't — migrated here nearly
  verbatim, where it now has the argument behind it.
- The upgrade path, concretely: a paid-tier toggle raises the ceiling and unlocks a read
  replica when traffic warrants; the region constraint is the one thing an upgrade does
  NOT fix (region is fixed at creation — a move is a new project plus a migration, which
  is what `migrate.py`'s embedding commands and the documented cutover order already
  rehearse). One caveat, stated honestly per 22-RESEARCH Assumption A2: the no-replica
  fact is carried from the free tier's published shape, not from a measurement this repo
  made — if Supabase's current pricing page is cheap to check at execution, check it and
  cite the date; if not, state the posture as posture.

The Pitfall-3 bar: if the new subsection, diffed against the README bullet it will
replace, adds no information a careful reader didn't already have, it is not done — the
argued "why that's fine" and the upgrade path ARE the deliverable.

Run the targeted ADR test immediately after the index edit, not at the end.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ADR=$(ls docs/adr/*-cost-approximation-by-design.md) && test -f "$ADR" && grep -c '^## ' "$ADR" && grep -cE '^\*\*Status:\*\* Accepted' "$ADR" && grep -c '### Rejected alternatives' "$ADR"</automated>
    <!-- checker W2: the gates are slug-anchored, not number-anchored, so the
         renumber-if-21.5-took-0013 contingency in step 5 cannot contradict them. -->

    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && .venv/bin/pytest tests/test_evals.py -k "adr" -q</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && test "$(grep -c '^### The free-tier posture, and the upgrade path$' docs/OPERATIONS.md)" -eq 1 && grep -ci 'read replica' docs/OPERATIONS.md</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && awk '/^### The free-tier posture, and the upgrade path$/,/^## /' docs/OPERATIONS.md | grep -ciE 'upgrade path|paid tier'</automated>
    <mutation>Delete the 0013 row from docs/adr/README.md's index table (leave the counting prose) — `pytest tests/test_evals.py -k adr` must go red on the derived-count mismatch, proving the counting prose is enforced against the table rather than trusted. Restore, re-run green, record both observations in the SUMMARY.</mutation>
  </verify>
  <done>
ADR-0013 is on disk in the three-heading Nygard shape with Status, Source, and a
Rejected-alternatives section carrying all four measured Admin-API reasons; the index has
its row and every counting sentence re-derived, with the targeted test green and its
mutation observed red; OPERATIONS has the posture subsection under Going stateless with
the exact P-04 heading, the argued acceptability, the upgrade path, and the A2 caveat
handled honestly; every fact in it traces to an existing OPERATIONS passage or a stated
source, none to the README bullet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The defect record and the derived-counts gate on the eval-section prose</name>
  <files>README.md, tests/test_evals.py</files>
  <reversibility rating="reversible">Prose plus one additive test. `git revert` restores the pre-phase eval section exactly; no fixture, refusal file, or grader is touched.</reversibility>
  <read_first>
    - `tests/test_evals.py` — `test_the_adr_index_counting_prose_is_derived_from_the_table` (the pattern to mirror: extract the prose region, derive counts from the source of truth, assert spelled prose), `_SPELLED` (reuse, never re-invent), `documented_refusals()` and `GOLDEN` (already imported/defined — the new test's inputs), and `F.fixture_paths()` (the fixture counter the union test already uses).
    - `README.md` "Tests and evals" section, located by heading — the three count-bearing passages 22-RESEARCH inventoried: the recorded-cases sentence with the denominator, the refusal-count sentence, and the defect paragraph narrating the truncation and the record/replay divergence. Also the section's fenced command block, whose test-count comment 22-02's whole-file pass owns (not this task).
    - `evals/graders.py:744-795` — `Judge.verdict()`'s docstring and call site: the truncation's code-level evidence the prose cites (`max_tokens=1500` shared with adaptive thinking, predicted at `graders.py:758` before the run that measured it).
    - Task 1's baseline in the SUMMARY — the measured post-21.5 counts this prose states.
  </read_first>
  <behavior>
    - The new test derives: fixture count from `F.fixture_paths()`, refusal total from `documented_refusals()`, per-kind counts (`judge_truncated`, `recorded_then_failed_replay`) from the same dict, and the replay denominator as `len(GOLDEN)` + fixtures. Nothing is hardcoded; add a fixture or drop a refusal and the test demands the prose move.
    - It extracts the README section between the Tests-and-evals heading and the next `## ` heading, lowercases it, and asserts each derived number appears in its agreed phrase template — spelled via `_SPELLED` where the prose spells, digits where the prose uses digits (the denominator).
    - Red first: before the prose edit, the test fails against the stale planning-era numbers (if 21.5 moved any count); if 21.5 happened to move nothing, the red is manufactured by the mutation instead — either way a red is OBSERVED before the green is trusted.
    - After the prose re-derivation, the test passes, and so do the untouched union and refusal-kind tests.
  </behavior>
  <action>
Write the test first, then make the prose true.

**The test**, `test_the_readme_eval_counts_are_derived_from_the_tree`, in
`tests/test_evals.py` directly beside the ADR index test, in its exact spirit — the
docstring names the lesson ("a gate that greps for the string you just typed is not a
gate", 17-04) and states that every number is derived from `evals/fixtures/` and
`evals/REFUSALS.json`, never typed. Derive: `fixtures = len(list(F.fixture_paths()))`,
`refusals = documented_refusals()`, `total = len(refusals)`, per-kind counts via a
Counter over `entry["kind"]`, `denominator = len(GOLDEN) + fixtures`. Reuse the module's
`_SPELLED` through a local `spelled()` helper matching the ADR test's (assert the number
is in the dict rather than KeyError-ing). Extract the eval-section prose by partitioning
on the Tests-and-evals heading and splitting at the next `## ` heading. Assert the
phrase templates — recommended, grammar-stable forms:

- the recorded-split sentence: spelled fixture count + "cases of" + spelled
  `len(GOLDEN)` + "are recorded"
- the denominator: "grades" + the digit denominator + "cases"
- the refusal count: "the other" + spelled total + "are in"
- the truncation count: spelled `judge_truncated` count + "are a real defect" (or the
  template the executor lands — whatever phrase the prose uses, the test asserts THAT
  phrase with the DERIVED number in it; test and prose are co-authored, the number is
  never literal in the test)
- the divergence count: spelled `recorded_then_failed_replay` count + "are recordings
  that passed at record time" (same co-authoring rule)

If any derived kind-count measures zero post-21.5, the corresponding prose sentence is
removed rather than stating a zero, and the test asserts the phrase's absence for that
branch — the gate must describe the tree that exists, not the one planning predicted.

Run it: it must RED against the current prose if 21.5 moved any count. Record the red.

**The prose**, then: re-derive the three passages from Task 1's measured baseline —
the recorded/refused split and denominator told as the amended-criterion truth (recorded
cases are a deliberate, paid, operator act; the refusals are documented in
`evals/REFUSALS.json` with the union enforced by test — the existing framing survives,
the numbers move), and the defect paragraph expanded per P-02 into the full known-defect
record in the OPERATIONS do-not-fix voice: the judge's verdict truncating at a token
budget shared with adaptive thinking, predicted by `graders.py`'s own docstring before
the run measured it, cost stated from the baseline; and record-time versus replay-time
grading disagreeing, with the reason the contested pins cannot simply be re-authored
(they must also hold against `dataset.py`'s hand-authored reference reports) and the
hedged half-answer kept as a finding. Both explicitly not fixed here — ADR-0012's fence
stands, repairs are successor-milestone work — and both pointing at `evals/REFUSALS.json`
as the per-case evidence. P-01: the residue stays in this section; no Limitations bullet
is created for it (that section is 22-02's diff, untouched by this task). P-03: the
existing REFUSALS.json link is the only pointer added or needed.

Keep this task's diff INSIDE the Tests-and-evals section — the Limitations section and
the fenced block's test-count comment belong to 22-02.

Run the new test green, then the file's full test module, then the keyless suite.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && .venv/bin/pytest tests/test_evals.py -k "readme_eval or adr" -q</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && .venv/bin/pytest tests/test_evals.py -q 2>&1 | tail -1</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/pytest -q 2>&1 | tail -2</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals --quiet; echo "evals exit: $?"</automated>
    <mutation>In the working tree, pop one entry out of evals/REFUSALS.json's refusals dict — the new derived-counts test must go red against the now-stale prose, AND the existing union test must go red on its own axis (a case accounted nowhere), proving the two gates guard different claims. Restore the entry byte-identical (git checkout -- evals/REFUSALS.json), re-run green, record all observations in the SUMMARY.</mutation>
  </verify>
  <done>
The derived-counts test exists beside the ADR index test, derives every number from the
tree through the existing helpers, and was observed red (against stale prose or via the
mutation) before its green was trusted; the eval-section prose states the measured
post-21.5 split, denominator, and both defect counts, records both defects with their
evidence and the reason they are not fixed here, and creates no Limitations bullet; the
full keyless suite and offline evals are green at the Task 1 baseline plus exactly the
tests this task added, accounted by name.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → documentation | The docs ARE the deliverable; a false or stale claim misleads an employer-reader. No runtime boundary is touched — no code, auth path, or data flow changes in this plan. |
| documented alternative → operational secrets | ADR-0013 documents an UNBUILT integration whose key would out-scope every secret the service manages; the record must not read as a how-to for wiring it casually. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Information Disclosure | ADR-0013's Admin-API discussion | low | mitigate | Rejection reason 1 states the admin key's org-wide blast radius as a reason NOT to build — documenting the unbuilt alternative's risk is the mitigation; no key, endpoint wiring, or secret-handling change ships. |
| T-22-02 | Tampering | Doc prose drifting from the tree it describes | medium | mitigate | The derived-counts test (Task 3) and the existing ADR-index test make the two count-bearing surfaces self-checking; both observed red via mutation before their green is trusted. |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; 22-RESEARCH's Package Legitimacy Audit records N/A — this phase adds no dependency. |
</threat_model>

<verification>
- Task 1's baseline recorded in full before any edit — every later number in this phase
  traces to it.
- `ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/pytest` green, counts reconciled
  against the Task 1 baseline plus exactly the tests Task 3 added, by name.
- `python -m evals --quiet` green at the measured denominator, real exit 0.
- `.venv/bin/ruff check .` clean.
- `pytest tests/test_evals.py -k "adr or readme_eval"` green; both named mutations (index
  row deletion, refusal-entry pop) observed red and reverted, recorded in the SUMMARY.
- The Limitations section is byte-identical to the phase's start — this plan does not
  touch it (22-02's diff).
</verification>

<success_criteria>
1. ADR-0013 in the house shape with the four measured Admin-API rejection reasons
   (criterion 1's record half), indexed with re-derived counting prose.
2. OPERATIONS carries the argued free-tier posture note with the P-04 heading (criterion
   3's target half), facts cross-checked against OPERATIONS' own sources.
3. README's eval-section prose is measured-current and gated by a derived-counts test
   that reds on drift; the two paid-run defects are recorded there with evidence, per
   P-01/P-02/P-03 — no new bullet, no new doc surface.
4. The execution baseline is measured post-21.5 and recorded; no planning-time number
   appears in any written surface.
5. Suite, evals, ruff green; every named mutation observed red.
</success_criteria>

<output>
Create `.planning/phases/22-limitations-recorded/22-01-SUMMARY.md` when done
</output>
