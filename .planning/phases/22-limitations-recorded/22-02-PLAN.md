---
phase: 22-limitations-recorded
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - README.md
  - docs/DESIGN.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/phases/20-note-count-bound/deferred-items.md
  - .planning/phases/22-limitations-recorded/22-VALIDATION.md
autonomous: true
requirements: [REQ-limitations-recorded]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The four closed bullets — judge independence, recorded answers, credential validity, the note count bound — are gone from README: each distinctive phrase greps zero, and the git axis shows deletion, never a move into release notes or any other surface (criterion 4)"
    - "No other doc surface still asserts a deleted claim: the four sweep patterns return zero hits across docs/ and .planning/codebase/ outside exemptions listed BY PATH, each exemption a historical record the ADR convention forbids editing"
    - "Every surviving bullet points at its record and every link resolves — cost to ADR-0013 (criterion 1), identities to ADR-0007 (criterion 2, verified not re-authored), database to the OPERATIONS posture anchor (criterion 3) — in the identities bullet's shape: one sentence of why, then the link"
    - "The section intro reads as an honest ledger — what v1.2 closed, what it recorded by design, what the paid run discovered — and carries the phrase 'chosen, recorded, and argued for' verbatim (criterion 5)"
    - "The whole README is read end-to-end and every count it states is the measured post-21.5 value — nothing the rewrite falsified is left standing"
    - "REQUIREMENTS, ROADMAP, and STATE reflect the close: REQ-limitations-recorded flipped with evidence, Phase 22 checked, PROJECT.md's stale counts settled, and the two Phase-20 deferred items settled or dispositioned in writing"
    - "22-VALIDATION.md is reconciled: every row carries measured Status evidence, every named mutation was observed red, and nyquist_compliant is true"
  artifacts:
    - "README.md — the rewritten Limitations section: new intro, three surviving bullets each with a record link, four bullets deleted; plus whatever the whole-file pass re-derived"
    - ".planning/REQUIREMENTS.md — REQ-limitations-recorded checked, traceability row Complete; REQ-classifier-model's row verified against 21.5's actual state"
    - ".planning/ROADMAP.md — Phase 22 checked with an execution record in the house style"
    - ".planning/STATE.md — position, progress, and stopped_at reflecting the milestone's last phase done"
    - ".planning/PROJECT.md — the test-count lines re-derived from the measured baseline"
    - ".planning/phases/20-note-count-bound/deferred-items.md — both items' fates recorded"
    - ".planning/phases/22-limitations-recorded/22-VALIDATION.md — reconciled, signed off"
  key_links:
    - "README Limitations → docs/adr/0013-cost-approximation-by-design.md — the filename 22-01 locked (P-06); if Task 1 of 22-01 renumbered it, this plan links the renumbered name recorded in 22-01's SUMMARY"
    - "README Limitations → docs/OPERATIONS.md#the-free-tier-posture-and-the-upgrade-path — the anchor derives from the heading 22-01 locked (P-04)"
    - "README Limitations → docs/adr/0007-anonymous-identity-fairness-global-cap.md — pre-existing since Phase 12; must survive the rewrite intact"
    - "the deletion gate and the no-orphan sweep are DIFFERENT gates with different jobs: README has its own zero-count greps; the sweep covers docs/ and .planning/codebase/ only — a restored README bullet reds the first and must not red the second"
---

<objective>
The Limitations rewrite and the milestone close-out. Seven bullets become three: four
deleted on the git axis (criterion 4), three survivors each pointing at a record
(criteria 1–3), under an intro that states what v1.2 closed, recorded, and discovered —
what remains is chosen, recorded, and argued for (criterion 5). Then the sweep that
proves no deleted claim survives anywhere else, the whole-README pass, and the
close-out flips this milestone's last phase owes: REQUIREMENTS, ROADMAP, STATE,
PROJECT.md's stale counts, the two Phase-20 deferred items, and 22-VALIDATION's
reconciliation.

Purpose: REQ-limitations-recorded — the milestone's acceptance bar is that no bullet
stands uncovered: closed things gone, open things owned.

Output: the rewritten Limitations section with every link resolving; a clean no-orphan
sweep with path-listed exemptions; the milestone bookkeeping flipped with evidence; the
validation contract reconciled.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-limitations-recorded/22-CONTEXT.md
@.planning/phases/22-limitations-recorded/22-RESEARCH.md
@.planning/phases/22-limitations-recorded/22-VALIDATION.md
@.planning/phases/22-limitations-recorded/22-01-SUMMARY.md
@.planning/phases/20-note-count-bound/deferred-items.md

@README.md
@docs/adr/0007-anonymous-identity-fairness-global-cap.md
</context>

<decisions_this_plan_locks>
Continues 22-01's P-01..P-06 (which the executor treats as settled — especially P-01: no
residue bullet; P-04: the OPERATIONS heading; P-06: the ADR filename).

**P-05 — Surviving bullets match the identities bullet's existing shape: one bolded
first sentence, one sentence of why, then the record link.** 22-RESEARCH Open Question 2's
recommendation adopted: since two of the three survivors are being rewritten anyway, they
converge on the shape the third already has rather than inventing a new voice. Inline
"why" is one sentence, not a paragraph — the argument lives in the record the link
reaches.

**P-07 — The refusal residue's absence from this section is deliberate and stays.**
Restates 22-01's P-01 from this plan's side: the Limitations rewrite adds NO bullet for
the partial recorded/refused state, whatever 21.5's final split measured. Its record is
the eval section and REFUSALS.json. A rewritten section with more than three bullets is
this plan failing (22-RESEARCH Pitfall 4's warning sign, adopted as a gate).

**P-08 — CONCERNS.md:242 is dispositioned, not edited.** The file is a dated audit
snapshot regenerated wholesale by /gsd-map-codebase; Phase 19's precedent (correct only
what your own phase falsified) is the boundary. Phase 22 falsified nothing in it. The
disposition — stays as-is, owner is a map re-run — is recorded in deferred-items.md and
this plan's SUMMARY. PROJECT.md's stale counts, by contrast, ARE settled here: Phase 20's
deferral named this phase's doc pass as the natural owner, and the fix is a two-line edit
from the measured baseline.
</decisions_this_plan_locks>

<tasks>

<task type="auto">
  <name>Task 1: The Limitations rewrite — four deletions, three survivors, one honest intro</name>
  <files>README.md</files>
  <reversibility rating="reversible">One section of one markdown file. `git revert` restores all seven bullets and the old intro byte-identically; nothing at runtime reads this file.</reversibility>
  <read_first>
    - `.planning/phases/22-limitations-recorded/22-01-SUMMARY.md` — the measured baseline (counts, current bullet line numbers, the ADR's final filename if renumbered, what 21.5 landed and whether its re-record moved cases) and any prose-shape finding Task 1 of 22-01 flagged.
    - `README.md` `## Limitations` — the section as it stands, located by heading; the seven bullets, their dispositions per 22-RESEARCH's inventory table, and the identities bullet whose shape P-05 copies.
    - `22-CONTEXT.md` Specific Ideas — the intro's target register: an engineer's honest ledger, not a changelog; the strongest close-out sentence available is true and measured.
    - `docs/adr/0007-anonymous-identity-fairness-global-cap.md` — confirm it still exists and its Status line still reads Accepted (criterion 2 is verify-not-author: the identities bullet has linked it since Phase 12).
  </read_first>
  <action>
One section, one diff, three moves. Locate everything by heading and phrase, never by
planning-time line number.

**Delete the four closed bullets** — the judge-independence bullet, the recorded-answers
bullet, the credential-validity bullet, and the note-bound bullet, identified by the four
distinctive phrases enumerated in this task's verify block. Delete each bullet whole
(the entire list item, including its links), replace with nothing. Phases 18, 19, 20 and
21 each left their bullet byte-identical precisely so this diff could be four clean
removals — honour that. Nothing deleted is rewritten into release notes, a changelog
line, or any other surface (the standing convention; Task 2's sweep enforces it).

**Rewrite the intro** as v1.2's ledger, giving it the same treatment v1.1 gave v1.0's
list. State, with numbers that are structural facts of this very diff or measured values
from the 22-01 baseline: v1.2 closed four limitations (an independent eval judge, probed
credential validity, a second bound on notes, and the recorded-answers claim rebuilt on a
real paid run under its amended criterion); it recorded three by design — each bullet
below points at its record; and its paid run discovered things free testing structurally
could not see — the classifier drift (fixed, Phase 21.5, stated as whatever 21.5's
artifacts actually measured) and two defects documented with their evidence in the eval
section above. Which were closed, which recorded, which discovered — never a claim that
nothing new exists (the acceptance bar is met honestly, not numerically). Close the intro
with the criterion-5 commitment verbatim: what remains is **chosen, recorded, and argued
for**.

**Re-point the three survivors**, each in the P-05 shape (bolded first sentence, one
sentence of why, the record link):

1. Cost: keep the bolded first sentence and the strongest single why-sentence (the
   measured Voyage telemetry discrepancy earns its place); end with a "Recorded as
   [ADR-0013](docs/adr/0013-cost-approximation-by-design.md)." sentence — the exact
   filename from 22-01's SUMMARY (P-06, renumbered only if 21.5 took 0013).
2. Identities: verify, don't author. The bullet already carries the ADR-0007 link;
   leave its prose as-is unless aligning voice with its two rewritten neighbours
   improves the section, and if wording moves, the link and the claim survive intact.
3. Database: criterion 3 says one honest README line — the bolded first sentence, one
   clause of why (fine at this traffic), and the pointer to the posture note:
   `[the database posture note](docs/OPERATIONS.md#the-free-tier-posture-and-the-upgrade-path)`
   — the anchor P-04 locked. The detail the old bullet carried (region, ceiling,
   replica) now lives behind that link; the README line does not restate it.

P-07: the finished section has exactly three bullets. No bullet uses a defect-shaped
verb; every first sentence states a position, not a bug.

Commit with a message that names the four deletions and the criterion each edit serves.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && test "$(grep -cF "The eval judge shares the critic's model" README.md)" -eq 0 && test "$(grep -cF 'Only one of forty answers is recorded' README.md)" -eq 0 && test "$(grep -cF 'checks that the API keys are *present*' README.md)" -eq 0 && test "$(grep -cF 'Notes are bounded by expiry alone' README.md)" -eq 0 && echo "all four deleted"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && test "$(awk '/^## Limitations$/{f=1;next} /^## /{f=0} f' README.md | grep -c '^- \*\*')" -eq 3 && echo "exactly three bullets survive"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && test "$(grep -cF 'chosen, recorded, and argued for' README.md)" -ge 1 && grep -c 'cost-approximation-by-design.md' README.md && grep -c '0007-anonymous-identity-fairness-global-cap.md' README.md && grep -c 'the-free-tier-posture-and-the-upgrade-path' README.md</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && .venv/bin/pytest tests/test_evals.py -k "readme_eval or adr" -q</automated>
    <mutation>In the working tree, restore ONE deleted bullet (paste it back from git show HEAD~1). The four-phrase deletion gate above must go red on that phrase. Then run Task 2's sweep command over docs/ and .planning/codebase/ and observe it stays CLEAN — the two gates have different jobs (README's own state versus orphaned claims elsewhere), and this mutation proves neither is doing the other's. Revert the paste, re-run the deletion gate green, record all observations in the SUMMARY.</mutation>
  </verify>
  <done>
The section has exactly three bullets, each in the P-05 shape with a resolving record
link; the four closed bullets are gone with their phrases grepping zero; the intro is the
v1.2 ledger ending in the criterion-5 phrase verbatim; the derived-counts and ADR tests
are still green (this diff stayed out of their sections); the mutation was observed red
on the deletion gate and clean on the sweep, then reverted.
  </done>
</task>

<task type="auto">
  <name>Task 2: The git axis, the no-orphan sweep, the link gate, and the whole-README pass</name>
  <files>README.md, docs/DESIGN.md</files>
  <reversibility rating="reversible">Greps, git archaeology, and at most small prose corrections where the sweep finds an orphan or the read-through finds a falsified count. All revertible; nothing at runtime reads these files.</reversibility>
  <read_first>
    - `.planning/phases/20-note-count-bound/20-VERIFICATION.md` — the deletion-gate style precedent this task's evidence matches (bare grep/diff quoted as measurement).
    - `README.md` — the WHOLE file, top to bottom, this session: the fenced test-command block's count comment, the eval-section prose (22-01's, now load-bearing), the API table, the deployment section — anything the Limitations rewrite or 21.5's landing falsified.
    - `22-RESEARCH.md` Don't Hand-Roll row 3 and Pitfall 1 — the sweep's job and its failure mode (a deleted README bullet whose claim survives in DESIGN.md or a map).
  </read_first>
  <action>
Three gates and a read-through; evidence quoted in the SUMMARY for every one.

**The git axis.** For each of the four deleted phrases (this task's verify block carries
them), run `git log --oneline -S"<phrase>" -- README.md` and record the output. The claim
being verified: the text entered the repo and left it by deletion — never migrated to a
release-notes surface. Expect the birth commit and Task 1's deletion commit; where a
bullet's history shows more (a phrase edited over its life before the byte-identical
freeze), explain the extra commits from the log honestly rather than forcing "exactly
two". The gate is that the LAST commit touching each phrase is a removal, and that no
other file gained the phrase in the same range — which the sweep below proves for the
present tree.

**The no-orphan sweep.** For each of the four patterns in the verify block, sweep
`docs/` and `.planning/codebase/` (NOT README — Task 1's gate owns README, and the two
must stay distinguishable, per Task 1's mutation). For every hit, decide per the
protocol: an ADR's historical text (a superseded record's frozen body, or a record
narrating the claim it closed) is EXEMPT — add its path to the exemption list with a
one-line justification per path; the exemption list is enumerated paths, never a glob or
pattern. Any other hit is an orphan: re-derive that prose to the current truth (DESIGN.md
is the likeliest surface) and record the fix. `.planning/codebase/` maps are dated audit
snapshots — hits there are recorded and dispositioned like P-08, not edited. The sweep's
final state: zero non-exempt hits, every exempt path listed and justified in the SUMMARY.

**The link gate.** Every relative link in the rewritten Limitations section resolves:
extract each `](...)` target; for files, `test -f`; for the OPERATIONS anchor, re-derive
the anchor from the actual heading text (lowercase, punctuation stripped, spaces to
hyphens) and compare with the fragment README uses. Record each link and its resolution.

**The whole-README pass.** Read the entire file and re-derive anything false against the
post-21.5, post-rewrite tree: the fenced block's test-count comment updated to the
measured keyless count from the 22-01 baseline (re-run if this phase added tests since);
the golden-case framing still true; no sentence anywhere still implying the four closed
limitations stand. The eval-section counts are already gated by 22-01's test — trust the
green, re-read the words. This phase IS the README phase; the standing README-every-PR
rule is satisfied by substance, but the pass is still owed in full.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && for p in "The eval judge shares the critic's model" "Only one of forty answers is recorded" "checks that the API keys are" "bounded by expiry alone"; do git log --oneline -S"$p" -- README.md | head -5; echo "---"; done</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && for p in "shares the critic's model" "one of forty" "checks that the API keys are" "bounded by expiry alone"; do grep -rnF "$p" docs/ .planning/codebase/ 2>/dev/null; done; echo "sweep done (hits above must each be on the justified exemption list)"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && awk '/^## Limitations$/{f=1;next} /^## /{f=0} f' README.md | grep -oE '\]\([^)]+\)' | tr -d ']()' | while read -r t; do f="${t%%#*}"; [ -z "$f" ] || test -f "$f" || echo "BROKEN: $t"; done; echo "link gate done (no BROKEN lines above)"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/pytest -q 2>&1 | tail -2 && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals --quiet; echo "evals exit: $?"</automated>
    <mutation>(a) Plant one deleted claim's distinctive phrase (any one from this task's sweep list) as a sentence in docs/DESIGN.md — the sweep must go red naming that file, proving it reads the surfaces and not just README. (b) Rename the OPERATIONS posture heading by one word — the link-gate anchor comparison must red against README's fragment. Restore both, re-run both gates clean, record all four observations in the SUMMARY.</mutation>
  </verify>
  <done>
The git-axis evidence for all four deletions is quoted with any history honestly
explained; the sweep is clean with every exemption listed by path and justified; every
Limitations link resolves including the anchor; the whole file was read and every
falsified count or claim re-derived (each named in the SUMMARY); both mutations observed
red and reverted; suite and evals green at the measured baseline.
  </done>
</task>

<task type="auto">
  <name>Task 3: The close-out flips and the validation reconciliation</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, .planning/phases/20-note-count-bound/deferred-items.md, .planning/phases/22-limitations-recorded/22-VALIDATION.md</files>
  <reversibility rating="reversible">Planning-meta hand edits, all revertible; no app code, no doc surface a reader lands on first.</reversibility>
  <read_first>
    - `.planning/REQUIREMENTS.md` — the REQ-limitations-recorded checkbox and traceability row; the completed rows' evidence style (REQ-note-count-bound's and REQ-forty-recorded-answers' rows are the register to match); REQ-classifier-model's state as 22-01's baseline recorded it.
    - `.planning/ROADMAP.md` — Phase 21's executed-record paragraph (the style an execution record follows) and Phase 22's entry with its Plans list.
    - `.planning/STATE.md` — front matter (progress counters), Current Position, and the stopped_at convention.
    - `.planning/phases/20-note-count-bound/deferred-items.md` — both items, their measured values, and their candidate owners (item 2 names this phase).
    - `.planning/phases/22-limitations-recorded/22-VALIDATION.md` — every row's Status cell to fill, the sign-off list, the front-matter flags.
  </read_first>
  <action>
The milestone's last phase closes its own books. Hand edits, each with evidence, no
wholesale rewrites — scoped edits only.

**REQUIREMENTS.md.** Flip REQ-limitations-recorded to `[x]` with a dated completion note
in the house style (what landed, the evidence: four deletions on the git axis, three
survivors linked, ADR-0013, the posture note, the derived-counts gate). Update its
traceability row to Complete with the same evidence shorthand the completed rows use.
Then REQ-classifier-model: if 21.5 flipped it, verify and leave it; if 21.5 left it
Pending (22-01's baseline recorded which), flip it now citing 21.5's own SUMMARY as the
evidence, and state in this plan's SUMMARY that the flip was done here on 21.5's behalf —
a milestone must not close with a completed requirement reading Pending. Update the
coverage line if its arithmetic changed.

**ROADMAP.md.** Check Phase 22's box in the phase list and both plan checkboxes in its
entry; append an executed-record paragraph to the Phase 22 entry in Phase 21's style —
dated, measured (final suite/eval numbers from this phase's runs, the bullet arithmetic
seven-to-three, the records created), naming what was discovered versus closed. Do NOT
touch milestone archival — `/gsd:complete-milestone` is its own step after this phase
merges (explicitly out of scope per 22-CONTEXT).

**STATE.md.** Update front matter (progress counters counting this phase and its plans
complete; last_updated), stopped_at (Phase 22 complete, milestone execution done, next
step is verification then /gsd:complete-milestone), and the Current Position section —
carrying forward the still-open Phase 19 Manual-Only items rather than silently dropping
them.

**PROJECT.md.** Settle deferred item 2: re-derive the stale test-count lines from the
measured baseline (final numbers after this phase's own additions — re-run the keyless
suite if any test landed since the 22-01 measurement, and quote the command's output in
the SUMMARY). Two-line edit, exactly as the deferral predicted.

**deferred-items.md.** Record both fates: item 2 (PROJECT counts) — settled by Phase 22,
with the measured values; item 1 (CONCERNS.md:242) — dispositioned per P-08: the map
stays as a dated audit snapshot, unedited, owner remains a /gsd-map-codebase re-run.
CONCERNS.md itself is not touched.

**22-VALIDATION.md.** Reconcile: fill every row's Status cell with the measured evidence
(command + observed result + mutation observation where the row names one), tick the
sign-off boxes that are true, and flip the front matter — `status: complete`,
`wave_0_complete: true`, `nyquist_compliant: true` — only if every named mutation was
genuinely observed red and every gate genuinely ran. Anything that did not happen stays
unticked with a sentence saying why: the contract records reality.
  </action>
  <verify>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && grep -n "REQ-limitations-recorded" .planning/REQUIREMENTS.md | head -3 && grep -c '\[x\] \*\*REQ-limitations-recorded\*\*' .planning/REQUIREMENTS.md</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && grep -n '\[x\] \*\*Phase 22' .planning/ROADMAP.md && grep -c '\[x\] 22-0[12]-PLAN.md' .planning/ROADMAP.md</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query state.load >/dev/null && echo "STATE parses"</automated>
    <automated>cd "/Users/hesamabbaszadi/Documents/Projects/Research agent" && grep -c 'nyquist_compliant: true' .planning/phases/22-limitations-recorded/22-VALIDATION.md && test "$(grep -c '749 tests\|816 collected' .planning/PROJECT.md)" -eq 0 && echo "stale PROJECT counts gone"</automated>
  </verify>
  <done>
Every flip carries evidence in the house style; REQ-classifier-model's state is verified
or corrected with attribution; ROADMAP has the execution record and no milestone-archive
edits; STATE parses and carries the open Phase-19 items forward; PROJECT.md's counts are
the measured values; both deferred items' fates are in writing; 22-VALIDATION reads
status complete and nyquist_compliant true with every Status cell measured — or honestly
records what did not happen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader → documentation | The rewritten section is the milestone's public claim; a bullet pointing at a record that doesn't argue what the bullet claims misleads exactly the reader the project is built for. |
| this phase → historical records | The sweep touches surfaces the ADR convention freezes; an over-eager orphan fix that edits a superseded record's body would corrupt the record system this phase exists to complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-03 | Tampering | ADR historical texts during the orphan sweep | medium | mitigate | Exemption BY LISTED PATH with per-path justification (never a pattern); the ADR convention (bodies of superseded records are never edited) is restated in the task; any sweep fix lands in DESIGN.md or current prose only. |
| T-22-04 | Repudiation | Close-out flips claiming completion without evidence | medium | mitigate | Every flip carries dated, command-quoted evidence in the house style; 22-VALIDATION's Status cells are filled from observed runs, and anything unobserved stays unticked with a reason. |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; this phase adds no dependency. |
</threat_model>

<verification>
- All four deletion phrases grep zero in README; exactly three bullets survive, each with
  a resolving record link; the intro carries the criterion-5 phrase verbatim.
- The no-orphan sweep is clean over docs/ and .planning/codebase/ with exemptions listed
  by path and justified; the git-axis evidence is quoted for all four deletions.
- `ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/pytest` green at the reconciled
  count; `python -m evals --quiet` green at the measured denominator with real exit 0;
  `.venv/bin/ruff check .` clean.
- Every named mutation across both plans observed red and reverted, recorded in the
  SUMMARYs.
- REQUIREMENTS/ROADMAP/STATE/PROJECT flipped with evidence; deferred items dispositioned;
  22-VALIDATION reconciled to nyquist_compliant: true.
</verification>

<success_criteria>
1. Criterion 4: four bullets deleted, verified on the git axis, orphan-free everywhere
   the sweep reaches.
2. Criteria 1–3: cost → ADR-0013, identities → ADR-0007 (verified), database → the
   OPERATIONS posture anchor — every link resolves.
3. Criterion 5: the intro is v1.2's honest ledger — closed, recorded, discovered — ending
   in "chosen, recorded, and argued for", and the section holds exactly three bullets
   (P-07).
4. The whole README is measured-current; the milestone books are closed with evidence;
   both Phase-20 deferred items have written fates.
5. 22-VALIDATION reconciled: every gate ran, every named mutation observed red,
   nyquist_compliant: true.
</success_criteria>

<output>
Create `.planning/phases/22-limitations-recorded/22-02-SUMMARY.md` when done
</output>
