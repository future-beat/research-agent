---
phase: 15-answer-quality-evals
plan: 06
type: execute
wave: 6
depends_on: [15-03, 15-04, 15-05]
files_modified: [docs/adr/0009-recorded-answer-quality-evals.md, docs/adr/README.md, docs/DESIGN.md, README.md, evals/fixtures/, evals/graders.py, tests/test_evals.py]
autonomous: false
requirements: [REQ-offline-eval-quality]

user_setup:
  - service: anthropic + voyage (operator laptop only)
    why: "The calibration recording and (optionally) the full 40-case record run are live, paid, operator-triggered acts. CI never needs these."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "already present on the operator laptop (used for the Phase 13 live demo 2026-08-09)"
      - name: VOYAGE_API_KEY
        source: "already present on the operator laptop"

must_haves:
  truths:
    - "ADR-0009 exists: Status Accepted + Source line (the 0008 precedent — DEC-20 was never promoted, so nothing is superseded); states what the suite may now claim AND the per-grader cannot-catch column AND the staleness gate's own cannot-catch line (fixture_current detects graph.MODEL changes only today; per-node models require extending the fixture models map and the gate, then re-recording); records the staleness mechanism and the exact caveat wording"
    - "The README limitation bullet ('Offline evals can't measure answer quality, and twelve live cases are a smoke test, not a benchmark') is rewritten to the honest new claim"
    - "One real recorded fixture exists, committed, and replays green keyless (the calibration case) — OR the SUMMARY states honestly that the operator declined even the calibration spend"
    - "The full 40-case record run either happened or its deferral is stated explicitly — the phase is honest either way"
  artifacts:
    - path: "docs/adr/0009-recorded-answer-quality-evals.md"
      provides: "The replacement guarantee for DEC-20's scope"
      contains: "**Status:** Accepted"
    - path: "docs/adr/README.md"
      provides: "index row for 0009 (baseline 8 records → 9)"
      contains: "0009"
    - path: "README.md"
      provides: "rewritten Limitations bullet"
    - path: "docs/DESIGN.md"
      provides: "Testing section forward-link to ADR-0009 (the Phase 10 convention)"
      contains: "0009"
  key_links:
    - from: "docs/adr/0009-recorded-answer-quality-evals.md"
      to: "evals/graders.py claim boundaries"
      via: "the per-grader can-claim / cannot-catch table reproduced from the docstrings, plus fixture_current's own cannot-catch line"
      pattern: "Cannot catch"
    - from: "evals/fixtures/*.json (calibration)"
      to: "offline replay leg"
      via: "committed fixture graded keyless on every push"
      pattern: "schema_version"
---

<objective>
Record the replacement guarantee (ADR-0009), rewrite the README limitation honestly, link
DESIGN's Testing section forward, then spend real money deliberately: a one-case
calibration recording (~$0.25) that tunes the quality graders against a real transcript,
and an explicit operator decision on the full 40-case run (~$10–16 estimated) — which MAY
be deferred past the phase without blocking it, as long as the deferral is stated.

Purpose: SC-4 and the DEC-20 tension — the caveat and the docs must match exactly what has
become true, and the recording spend must be a deliberate operator act, never an implied one.
Output: ADR-0009, updated ADR index, README + DESIGN edits, a committed calibration
fixture (or its honest absence), and a recorded decision on the full run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-answer-quality-evals/15-RESEARCH.md
@docs/adr/README.md
@docs/adr/0008-embedding-migration-two-commands.md
@docs/DESIGN.md
@README.md

<interfaces>
The 0008 precedent (the format contract for 0009): title `# ADR-0009 — <Title>`;
`**Status:** Accepted`; `**Source:** Phase 15 (2026-08-XX), REQ-offline-eval-quality;
supersedes the scope of DEC-20 in .planning/intel/decisions.md` — DEC-20 was never
promoted to a numbered record, so there is no status line to flip and no
"supersedes ADR-000M" form (exactly 0008's relationship to DEC-10); `## Context`,
`## Decision`, `## Consequences` in that order; what-survives / what-is-new recorded in
prose.

DEC-20 verbatim scope (what survives must be argued against this): offline runs check
routing, guardrails, follow-up isolation, the unapproved-draft invariant; they cannot
speak to answer quality; the caveat prints every run — "a green suite that quietly implies
'the model is good' is worse than no suite."

Do NOT pre-empt Phase 16 (DEC-22, the judge rationale): 0009 mentions only that judge
verdicts are recorded as fixture metadata.

From plans 02/03: the quality graders' "Cannot catch:" docstrings, the fixture_current
models.pipeline gate and ITS docstring's own "Cannot catch:" line, the caveat wording —
0009 reproduces these; the code is the source of truth where any plan prose disagrees.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-0009, index, DESIGN forward-link, README rewrite</name>
  <read_first>docs/adr/0008-embedding-migration-two-commands.md in full (the structural template); docs/adr/README.md (index format + the "odd ones out" paragraph); docs/DESIGN.md § Testing first paragraph (the DEC-20 prose); README.md § Limitations (the exact bullet); evals/graders.py quality-grader docstrings and evals/harness.py fixture_current docstring (the claim boundaries as shipped, not as planned)</read_first>
  <files>docs/adr/0009-recorded-answer-quality-evals.md, docs/adr/README.md, docs/DESIGN.md, README.md</files>
  <action>
    Write docs/adr/0009-recorded-answer-quality-evals.md following the 0008 precedent
    exactly (title, `**Status:** Accepted`, `**Source:** Phase 15 (2026-08-XX),
    REQ-offline-eval-quality; supersedes the scope of DEC-20 in
    .planning/intel/decisions.md`, then Context / Decision / Consequences). Content
    requirements (RESEARCH §ADR-0009, reproduced against the SHIPPED code):
    - What survives of DEC-20: the caveat prints on every offline run (upgraded, not
      removed); the suite never claims the current model is good; free/deterministic/
      keyless every-push untouched; the judge stays live-only.
    - What is new: the suite may now claim, deterministically, that RECORDED answers are
      grounded (token-level), on-question (term-overlap), well-formed, and honest about
      refusals — with a per-grader table carrying BOTH columns: what each grader checks
      and what it cannot catch (paraphrase, negation flips, misattribution,
      notes-wrongness, current-model behaviour), copied from the graders' actual
      docstrings.
    - The staleness mechanism: model-mismatch gates (fixture_current compares the
      fixture's models.pipeline against graph.MODEL and fails replay hard — non-zero
      exit), age prints and never gates (calendar determinism), the recorder refuses
      failing fixtures; quote the exact shipped caveat wording from evals/__main__.py.
      The gate gets its OWN claim boundary, stated exactly like the graders get
      "Cannot catch:" lines: fixture_current detects graph.MODEL (pipeline) changes only
      today; a per-node model change — e.g. Phase 16's independently configurable
      critic — does NOT fire it, and recordings would stay green with only the printed
      date hinting staleness, until the fixture models map and the gate are extended
      with the per-node entry and fixtures re-recorded. Copy this from the shipped
      fixture_current docstring; the graders get cannot-catch columns, the gate deserves
      one too.
    - Rejected alternatives with the CONTEXT rationale: scheduled live job (unattended
      spend, provider outages), judge-only (not deterministic), reference answers (rot as
      models change; recorded answers are what the pipeline actually said).
    - One line only on the judge: verdicts recorded as fixture metadata; Phase 16 owns
      the judge's rationale (do not pre-empt DEC-22).

    docs/adr/README.md: add the 0009 index row (Status Accepted, Superseded by —), and
    extend the "odd ones out" paragraph: 0009 carries `**Source:**` like 0006/0007/0008,
    and like 0008 it supersedes the scope of an unpromoted DEC (DEC-20) in prose.

    docs/DESIGN.md § Testing: append to the first paragraph (the offline-evals one) a
    sentence that Phase 15 extended what offline runs may claim — recorded answers are
    graded deterministically with the recording's date and model printed — "Recorded as
    [ADR-0009](adr/0009-recorded-answer-quality-evals.md)." Do not delete the original
    argument; it is what 0009's Context quotes.

    README.md § Limitations: replace the bullet "**Offline evals can't measure answer
    quality**, and twelve live cases are a smoke test, not a benchmark." with the honest
    new claim, in the README's voice: offline now also grades RECORDED answers
    deterministically (grounding/coverage/structure/refusal), stamped with recording
    date and model and hard-gated on model match; what it still cannot claim is that the
    CURRENT model produces those answers — that needs `--live`; and the live set is 40
    cases across a stated taxonomy (technical/contested/sparse/general/ambiguous/
    guardrails/follow-ups/adversarial), each stating what it exists to catch. If the
    full record run was deferred (Task 3), the bullet must say fixtures exist for N of
    40 cases — the README never claims recordings that don't exist.
  </action>
  <verify>
    <automated>test -f docs/adr/0009-recorded-answer-quality-evals.md && grep -c "\*\*Status:\*\* Accepted" docs/adr/0009-recorded-answer-quality-evals.md && grep -c "Source:" docs/adr/0009-recorded-answer-quality-evals.md && grep -ci "cannot catch" docs/adr/0009-recorded-answer-quality-evals.md && grep -c "0009" docs/adr/README.md && grep -c "0009" docs/DESIGN.md && grep -c "twelve live cases are a smoke test" README.md</automated>
  </verify>
  <acceptance_criteria>
    - ADR file exists; "**Status:** Accepted" count ≥ 1; "Source:" count ≥ 1; "cannot catch" (case-insensitive) count ≥ 2 in the ADR — the per-grader column AND the fixture_current gate's own line (baseline for all: file absent)
    - `grep -c "0009" docs/adr/README.md` ≥ 2 (index row + odd-ones-out note; measured baseline 0); ADR record count 8 → 9
    - `grep -c "0009" docs/DESIGN.md` ≥ 1 (measured baseline 0)
    - `grep -c "twelve live cases are a smoke test" README.md` = 0 (measured baseline 1 — honest-green grep gate: the old claim is gone because it stopped being the whole truth)
    - `grep -c "DEC-22" docs/adr/0009-recorded-answer-quality-evals.md` = 0 or mentions only non-pre-emptively (manual read: no judge-rationale re-derivation)
  </acceptance_criteria>
  <done>The replacement guarantee is a numbered, statused record; index, DESIGN and README all match what the code actually claims — including what the staleness gate itself cannot catch.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: CALIBRATION — record one real case (~$0.25, operator spend)</name>
  <action>Pause for the operator: run the calibration recording per how-to-verify below (real keys, ~$0.25), tune quality-grader normalization/thresholds against the real transcript if needed (Pitfall 2), commit the fixture and any tuning. Claude cannot do this — it is real spend from the operator's accounts, and the operator decides to spend it.</action>
  <what-built>The complete record pipeline: `--record` previews cost via price_for, refuses without --yes, drives the real graph with capture_state=True, judge-grades, and writes a fixture only if everything passed. Never yet run against a real API.</what-built>
  <how-to-verify>
    On the operator laptop (real ANTHROPIC_API_KEY + VOYAGE_API_KEY exported):
    1. `.venv/bin/python -m evals --record --case technical-figures` — confirm it prints
       the preview (assumed-tokens basis, ~$0.2–0.5 for one case + judge) and EXITS
       WITHOUT SPENDING (exit 2).
    2. `.venv/bin/python -m evals --record --case technical-figures --yes` — the real
       run (~$0.25). Confirm: fixture written to evals/fixtures/technical-figures.json;
       its models map records pipeline claude-sonnet-5 and the judge model, judge
       verdicts present and passed, git_sha matches `git rev-parse --short HEAD`,
       pipeline_cost_usd is a plausible measured figure (~$0.15–0.25); compare the
       preview's estimate to the measured cost and note the ratio (the estimate is
       supposed to be an upper bound).
    3. `ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" DATABASE_URL="" .venv/bin/python -m evals`
       — the fixture replays keyless: replay leg appears (technical-figures@recorded),
       verdict PASS, and the caveat prints the recording date, model, sha and age.
    4. CALIBRATION PROPER (Pitfall 2): if any quality grader false-positives on the real
       transcript (e.g. paraphrase like "one million" vs "1M", a legitimate figure
       flagged), tune the normalization table / COVERAGE_THRESHOLD in evals/graders.py,
       re-run the keyless replay until green, and record what was tuned. If graders
       needed no tuning, record that too. If the assumed-token preview constants were
       badly off the measured cost, correct the constants now.
    5. Commit the fixture (and any tuning) on this branch.
  </how-to-verify>
  <resume-signal>Type "calibrated" with a note of measured cost + any tuning, or "declined" if you choose not to spend even the calibration — the SUMMARY then records that the machinery is proven by fakes only and the replay leg is dormant until a recording exists.</resume-signal>
  <done>Either a committed fixture that replays green keyless with the new caveat printing its date/model/sha/age, or an explicit "declined" recorded for the SUMMARY.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: The full 40-case record run — now, or deferred (both honest)</name>
  <action>Present the decision below to the operator and wait. If record-now: run the full record command with the preview shown, verify fixtures, run keyless replay, commit. If defer: write the deferral into 15-06-SUMMARY.md and the 15-VALIDATION.md full-run row. Either outcome closes the phase honestly; silence does not.</action>
  <decision>Spend the full recording budget now, or defer the operator act past the phase?</decision>
  <context>
    The full run is ~$10–16 estimated at intro pricing (~$14–22 from 2026-09-01 — the
    Sonnet window flips then, which argues for recording before September if it happens
    at all). The preview will quote the real number at runtime, preferring the
    calibration case's measured cost as its basis. The settled decision: this run MAY be
    deferred past the phase without blocking it — the machinery is proven by the
    calibration case, the dataset is ready, and CI stays green either way (the replay leg
    grades whatever fixtures exist; zero-or-few fixtures is the honest pre-recording
    state). Phase 17's before-evidence needs the follow-up cases recorded eventually, but
    "eventually" is the operator's call.
  </context>
  <options>
    <option id="record-now">
      <name>Record all 40 now</name>
      <pros>Locks intro pricing (before 2026-09-01); the benchmark is fully live-graded and replayed on every push from today; Phase 16/17 before-measures captured immediately</pros>
      <cons>~$10–16 real spend in one sitting; any grader mistuning surfaces across 40 cases at once</cons>
    </option>
    <option id="defer">
      <name>Defer the full run</name>
      <pros>Zero further spend; the phase ships complete machinery + a proven calibration fixture; record whenever wanted (the command is one line)</pros>
      <cons>Replay leg covers 1 of 40 cases for now; recording after 2026-09-01 costs ~50% more on the Sonnet legs; Phase 17's before-evidence deferred with it</cons>
    </option>
  </options>
  <resume-signal>Select: record-now (then run `.venv/bin/python -m evals --record --yes`, verify all 40 fixtures written and refusals investigated, run the keyless replay, commit) or defer (the SUMMARY and 15-VALIDATION.md state the deferral explicitly — which of the two happened is a required, recorded fact of this phase).</resume-signal>
  <done>The chosen outcome executed and recorded: either 40 committed fixtures replaying green keyless, or an explicit deferral statement in the SUMMARY and VALIDATION.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → reader | the README/ADR claims must not exceed what the code proves |
| operator → paid APIs | the only real spend in the phase; both checkpoints gate it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-19 | Spoofing (overclaim) | README/ADR-0009 | mitigate | claims copied from shipped docstrings/caveat, incl. the staleness gate's own cannot-catch line; README states fixture coverage N of 40 when deferred |
| T-15-20 | DoS (spend) | calibration + full run | mitigate | both are blocking human checkpoints; preview-first; defer is a first-class outcome |
| T-15-21 | Elevation (miscalibrated graders pinned) | quality graders vs real transcripts | mitigate | calibration step tunes normalization/thresholds against a real recording BEFORE the full run (Pitfall 2 ordering baked into checkpoint order) |
</threat_model>

<verification>
- `.venv/bin/pytest` bare — green (plain baseline entering phase: 563/65; grown since)
- `ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" DATABASE_URL="" .venv/bin/python -m evals --report /tmp/evals-report.json --min-pass-rate 0.9` — exit 0; with the calibration fixture committed, the replay leg and the new caveat both visible in output
- `ruff check .` clean
- ADR count 9; README bullet rewritten; DESIGN forward-linked
</verification>

<success_criteria>
- SC-4 closed: caveat, README, DESIGN and ADR-0009 all state exactly what the suite can and cannot claim — the graders' boundaries AND the staleness gate's boundary
- VALIDATION rows: ADR grep gates, README/CI-invariant gates, CALIBRATION manual row, and the full-run row resolved with an explicit recorded outcome (done or deferred)
- One PR for the whole phase can now be opened from this branch
</success_criteria>

<output>
Create `.planning/phases/15-answer-quality-evals/15-06-SUMMARY.md` when done. The SUMMARY
MUST state which full-run outcome happened (record-now or defer) — the settled decision
requires the phase to say so.
</output>
