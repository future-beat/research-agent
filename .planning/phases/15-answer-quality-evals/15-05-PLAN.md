---
phase: 15-answer-quality-evals
plan: 05
type: execute
wave: 5
depends_on: [15-01, 15-03, 15-04]
files_modified: [evals/__main__.py, evals/harness.py, tests/test_evals.py]
autonomous: true
requirements: [REQ-offline-eval-quality]

must_haves:
  truths:
    - "`python -m evals --record` prints a cost preview computed from usage.price_for() at runtime — never a hardcoded dollar figure — and REFUSES to spend without --yes"
    - "The preview prefers measured pipeline_cost_usd from existing fixtures and says which basis it used; assumed-token fallbacks are named constants priced at today's rates"
    - "Record mode drives run_case(capture_state=True) with the real client, judge mandatory, per-case InMemoryStore (never the persistent store), and writes fixtures through the refusing writer"
    - "The preview path and the refusal path construct no API client (testable with fakes, no network, no spend)"
  artifacts:
    - path: "evals/__main__.py"
      provides: "--record, --yes, --force flags; preview-then-refuse-or-spend flow"
      contains: "--record"
    - path: "evals/harness.py"
      provides: "record_suite (or record entry point) composing run_case(capture_state=True) + build_fixture + write_fixture"
      contains: "capture_state=True"
  key_links:
    - from: "evals/__main__.py preview"
      to: "src/research_agent/usage.py price_for()"
      via: "runtime rate lookup (effective-dated; Sonnet flips 2026-09-01)"
      pattern: "price_for"
    - from: "record loop"
      to: "evals/fixtures.py write_fixture"
      via: "refusal-unless-force of failing recordings"
      pattern: "write_fixture"
---

<objective>
Give the operator the recording command: `--record` previews the cost from live rate
tables, refuses without `--yes`, then drives the real graph with state capture and judge
grading, and writes only known-good fixtures. All machinery proven with fakes; the actual
spend is plan 06's checkpoint.

Purpose: the full 40-case record run is real money (~$10–16 estimated at intro pricing,
~$14–22 after 2026-09-01 — an ESTIMATE, per RESEARCH, verified only by calibration). The
Phase 13/14 preview idiom makes the spend deliberate, priced at runtime, and refusable.
Output: --record/--yes/--force CLI, runtime cost preview, record loop, fake-driven tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-answer-quality-evals/15-RESEARCH.md
@evals/__main__.py
@evals/harness.py
@src/research_agent/usage.py

<interfaces>
From src/research_agent/usage.py (READ-ONLY — the eval CLI reads rates, never duplicates
them; DEC-12's whole point):
- `price_for(model, on=None) -> Price` — Price has per-MTok fields input/output/
  cache_write_5m/cache_read; effective-dated windows (Sonnet intro until 2026-08-31,
  standard from 2026-09-01); raises UnknownModelPricing for an absent model.
- `WEB_SEARCH_USD_PER_REQUEST` = 10.0/1000.

From evals/graders.py: `JUDGE_MODEL` (env EVAL_JUDGE_MODEL, default "claude-opus-5"),
`Judge(client)`.

From plan 15-01 (evals/fixtures.py): `build_fixture`, `write_fixture(fixture, result,
force=False, directory=None)` (refuses failing recordings), `fixture_paths`,
`load_fixture` (measured `pipeline_cost_usd` per fixture feeds the preview).

From evals/harness.py: `run_case(case, *, client_factory, memory_factory, judge, capture_state)`,
`live_memory_factory` (per-case InMemoryStore with real embeddings — record mode MUST use
this, never the persistent store: Pitfall 5).

From evals/__main__.py: `main()` — the --live branch lazily imports anthropic and builds
one client + Judge; --record composes on top of that branch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the runtime cost preview</name>
  <read_first>src/research_agent/usage.py PRICES/price_for/window_for and WEB_SEARCH_USD_PER_REQUEST; 15-RESEARCH.md §Cost of a 40-case live/record run (the estimate table and the preview idiom); evals/__main__.py main()</read_first>
  <files>evals/__main__.py, tests/test_evals.py</files>
  <action>
    Add to evals/__main__.py a pure function `record_preview(cases, fixtures_by_case_id) ->
    tuple[str, float]` returning (printable preview text, total estimate USD). Per case:
    - Basis 1 (preferred): an existing fixture for the case → use its measured
      `pipeline_cost_usd`, basis labelled "measured (fixture <recorded_at date>)".
    - Basis 2 (fallback): assumed token counts as MODULE-LEVEL NAMED CONSTANTS (e.g.
      ASSUMED_RESEARCH_INPUT_TOKENS / _OUTPUT_TOKENS, ASSUMED_FOLLOWUP_INPUT_TOKENS /
      _OUTPUT_TOKENS, ASSUMED_JUDGE_INPUT_TOKENS / _OUTPUT_TOKENS, WEB_SEARCHES_PER_RESEARCH_TURN = 2)
      priced via `price_for(graph.MODEL)` for pipeline turns and `price_for(G.JUDGE_MODEL)`
      for judge calls (2 per research turn + 1 per follow-up turn), plus
      WEB_SEARCH_USD_PER_REQUEST per search — all resolved AT RUNTIME so the 2026-09-01
      Sonnet flip changes the quote by itself; basis labelled "assumed tokens at today's
      rates". Pick assumption values consistent with RESEARCH's table (~research turn
      $0.15–0.25 at intro rates); the constants exist to be corrected by calibration, and
      a comment says so.
    - The preview text lists per-case line items with their basis, the grand total, which
      basis dominated, and the sentence "estimate — treat as an upper bound; run a
      one-case calibration first" (Phase 13's preview over-counted; honesty is the idiom).
    - Never a hardcoded dollar total anywhere: the only dollars in the source are the
      constants' token counts priced through price_for.

    Tests (append):
    - `test_record_preview_prices_through_price_for`: monkeypatch evals.__main__ price
      lookup (patch the imported price_for) to a known Price; assert the assumed-basis
      arithmetic matches hand-computed expected value from the named constants — the
      preview's math is the table's math, not a literal.
    - `test_record_preview_prefers_measured_fixture_costs`: give one case a fixture with
      pipeline_cost_usd 0.214; its line uses 0.214 and says "measured".
    - `test_record_preview_states_its_basis_and_uncertainty`: preview text contains
      "upper bound" and a per-line basis.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_evals.py --collect-only -q -k "record_preview" | grep -c "test" && .venv/bin/pytest tests/test_evals.py -k "record_preview"</automated>
  </verify>
  <acceptance_criteria>
    - Selector collects ≥ 3 tests; all green
    - `grep -n "price_for" evals/__main__.py` non-empty; `grep -En "\\$[0-9]+" evals/__main__.py` shows no hardcoded dollar estimate in the preview path (format strings interpolating computed values are fine; literal dollar quantities are not)
    - Preview computes with ANTHROPIC_API_KEY="" (no client construction on the preview path)
  </acceptance_criteria>
  <done>A preview whose numbers come from usage.py's effective-dated tables at runtime, preferring measured fixture costs, honest about being an upper-bound estimate.</done>
</task>

<task type="auto">
  <name>Task 2: --record / --yes / --force and the record loop</name>
  <read_first>evals/__main__.py main() --live branch; evals/harness.py run_case + live_memory_factory; evals/fixtures.py write_fixture refusal semantics (plan 01); 15-RESEARCH.md Pitfall 5 (the recorder inherits real environment)</read_first>
  <files>evals/__main__.py, evals/harness.py, tests/test_evals.py</files>
  <action>
    CLI: add `--record` (action=store_true, help: record live runs as committed fixtures —
    implies --live, costs money), `--yes` (spend without confirming), `--force` (write a
    fixture even when its grades failed; stamps forced: true — for deliberate
    known-bad pins only). `--record` sets live mode; `--record` without `--yes` prints the
    preview and exits 2 WITHOUT constructing the anthropic client (build the preview
    before the lazy import — order matters and is the testable property). `--force`
    without `--record` is an argparse error.

    Record flow in main() when --record and --yes: build client + Judge exactly as --live
    does (judge is MANDATORY in record mode — verdicts are fixture metadata and the
    refusal gate; there is no judgeless recording); memory_factory is live_memory_factory
    (per-case InMemoryStore — assert in code that record mode never uses the persistent
    store, Pitfall 5). For each selected case: `run_case(..., judge=judge,
    capture_state=True)`, then `build_fixture(case.id, result, model=graph.MODEL,
    judge_model=judge.model)` and `write_fixture(fixture, result, force=args.force)`;
    print per-case outcome (written path, measured cost, or the refusal message with the
    failing graders). A refused case does not stop the loop; the exit code is non-zero if
    any case was refused. After the loop print the total measured spend.

    Implementation note: put the per-case record composition in a small helper
    (harness.py `record_case_to_fixture` or equivalent) so tests drive it with the
    ScriptedClient + a fake judge and __main__ stays thin.

    Tests (append; ALL with fakes, zero network):
    - `test_record_refuses_without_yes`: main(["--record"]) returns 2, preview text
      printed, and a monkeypatched anthropic import (or client constructor) asserts it
      was NEVER called — no key needed, no client built, nothing spent.
    - `test_record_writes_a_fixture_per_case_with_fakes`: drive the record helper with
      offline_client_factory-style fakes and a fake judge object whose verdict() returns
      (True, "grounded") — fixture file appears in a tmp FIXTURES_DIR with judge entries,
      model, sha, measured cost fields.
    - `test_record_refuses_a_failing_case_and_continues`: fake judge returns (False,
      "ungrounded") for one of two cases → that fixture absent, the other written, exit
      code non-zero, refusal message names the judge grader.
    - `test_force_stamps_forced_true`: same failing case with --force → file written
      containing "forced": true.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_evals.py --collect-only -q -k "record" | grep -c "test" && .venv/bin/pytest tests/test_evals.py && ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" DATABASE_URL="" .venv/bin/python -m evals --min-pass-rate 0.9</automated>
  </verify>
  <acceptance_criteria>
    - Record selector collects ≥ 7 tests (Task 1's included); all green with no network
    - `ANTHROPIC_API_KEY="" .venv/bin/python -m evals --record` exits 2 with the preview printed and no traceback (refusal proven from the exact keyless env)
    - Offline CI command still exits 0, 40/40 — the new flags changed nothing on the default path
    - `git diff --name-only` does not contain .github/workflows/ci.yml
  </acceptance_criteria>
  <done>The operator has a priced, refusable, judge-gated record command whose whole flow is proven against fakes; only the checkpoint in plan 06 spends real money.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator laptop → paid APIs | the only place in the phase where real money moves |
| operator environment → fixtures | a record run must not inherit persistent stores or stale env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-15 | DoS (unbounded spend) | --record | mitigate | preview + exit 2 without --yes; per-case measured costs printed; full run remains an operator decision (plan 06 checkpoint) |
| T-15-16 | Repudiation (lying preview) | record_preview | mitigate | rates only via price_for at runtime; measured-fixture basis preferred; "upper bound" stated; arithmetic pinned by test against the constants |
| T-15-17 | Tampering (polluted recording) | record memory | mitigate | live_memory_factory per-case InMemoryStore asserted; seeded_notes still seed per-case (plan 04 seam) |
| T-15-18 | Elevation (judgeless fixture) | record flow | mitigate | judge mandatory in record mode; write_fixture refusal (plan 01) is the second lock |
</threat_model>

<verification>
- `.venv/bin/pytest` bare — green
- `ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" DATABASE_URL="" .venv/bin/python -m evals --min-pass-rate 0.9` — 40/40 keyless
- `ANTHROPIC_API_KEY="" .venv/bin/python -m evals --record` — exit 2, preview, no client
- `ruff check .` clean
</verification>

<success_criteria>
- VALIDATION row record_preview green (preview via price_for, never hardcoded, --yes idiom)
- Recording machinery complete and spend-free; the calibration and full-run decisions are plan 06's checkpoints
</success_criteria>

<output>
Create `.planning/phases/15-answer-quality-evals/15-05-SUMMARY.md` when done.
</output>
