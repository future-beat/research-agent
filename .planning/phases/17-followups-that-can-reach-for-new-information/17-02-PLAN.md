---
phase: 17-followups-that-can-reach-for-new-information
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/research_agent/graph.py
  - tests/test_supervisor_routing.py
  - tests/test_graph_smoke.py
  - evals/dataset.py
  - tests/test_evals.py
autonomous: true
requirements: [REQ-followup-live-search]
must_haves:
  truths:
    - "A follow-up with no prior notes routes to the researcher instead of ending with no_prior_research (SC-1, path 1) — and forced_stop_reason stays empty"
    - "A follow-up research pass ENLARGES the session's note set: pre-existing notes provably survive verbatim as a prefix (SC-2) — the pin failed RED against the graph.py:333 REPLACE before the fix"
    - "Guardrails outrank the new route: a capped or over-budget follow-up still ENDs with the honest cap reason, never researches (SC-5)"
    - "no_prior_research appears in the trace as a reach reason, never as forced_stop_reason (SC-4, code half)"
    - "Every changed routing row is pinned by SIDE EFFECTS with row-swap and row-delete mutations observed red — next_step alone proves nothing (destination-invisible flip)"
  artifacts:
    - path: "src/research_agent/graph.py"
      provides: "notes_insufficient + followup_research_done state keys; row 4 flipped in place; new row 5; researcher append-not-replace"
      contains: "followup_research_done"
    - path: "tests/test_supervisor_routing.py"
      provides: "eight precedence pairs pinned by side effects; reachability extension; per-turn default pins"
    - path: "tests/test_graph_smoke.py"
      provides: "append-not-replace pin (red-first); no-prior-notes follow-up researches end to end"
    - path: "evals/dataset.py"
      provides: "followup-with-no-prior-research flipped to the route-then-guardrail end-to-end pin"
  key_links:
    - from: "supervisor_node row 4/5"
      to: "state['trace']"
      via: "followup_research key on the supervisor's trace entry"
      pattern: "followup_research"
    - from: "researcher_node"
      to: "state['research_notes']"
      via: "append when existing, not replace"
      pattern: "research_notes"
---

<objective>
The routing reversal, path 1: flip row 4's destination from END to researcher IN PLACE, add
the flag-gated insufficiency row 5 (supervisor side only — the responder learns to set the
flag in plan 03), fix the researcher's REPLACE to an append, and land the full
side-effect-pinning precedence suite with mutations observed red. Flip every test and golden
case that pinned the old END behavior IN THIS PLAN, so the wave ends green.

The sharpest trap (settled decision): `state["research_notes"] = notes` at graph.py:333 is
a REPLACE that silently discards the session's notes on a follow-up pass while everything
stays green. The append pin must be observed FAILING against today's code before the fix.

The second trap: the row-4 flip is destination-invisible against the generic
`not research_notes → researcher` row below it. Precedence tests pin SIDE EFFECTS (trace
event, followup_research_done, empty forced_stop_reason, classifier-skip discriminator) —
never next_step alone.

Purpose: REQ-followup-live-search SC-1/SC-2/SC-4(code)/SC-5.
Output: flipped supervisor + appending researcher, precedence suite, flipped path-1 eval
case with its taxonomy pins in the same commit. Suite and evals green at plan end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-followups-that-can-reach-for-new-information/17-CONTEXT.md
@.planning/phases/17-followups-that-can-reach-for-new-information/17-RESEARCH.md
@src/research_agent/graph.py
@tests/test_supervisor_routing.py
</context>

<interfaces>
Read from the tree at plan time (executor: line numbers may drift; the tree wins, record deviations):

- Routing table BEFORE (graph.py:484–514, verbatim order): 1 iteration cap END, 2 revision
  cap END, 3 budget END, 4 `mode=="followup" and not research_notes` → END
  `no_prior_research`, 5 `not topic_type` → classifier, 6 `not research_notes` →
  researcher, 7 `not draft` → author, 8 `not reviewed` → critic, 9 `not approved` →
  author, 10 else END.
- The REPLACE: `state["research_notes"] = notes` (graph.py:333) inside
  `researcher_node`'s embedding-meter block; `store.add(f"[{state['task']}] {notes}", ...)`
  on the next line provides SC-2 attribution unchanged.
- Trace append: single site `state["trace"].append({"node": "supervisor", "routed_to": state["next_step"]})`
  (graph.py:516) — augment this entry, do not add a second append (RESEARCH Q2 rec).
- `followup_state` builds via `initial_state` (graph.py:227), so keys added to
  `initial_state` are fresh per turn automatically — the one-pass bound is per-turn by
  design (tests/test_supervisor_routing.py:365–373).
- Test helpers: `state(**overrides)`, `decide(**overrides)`, `followup(...)` in
  tests/test_supervisor_routing.py; smoke fixture `fake_client` in tests/test_graph_smoke.py.
- Precedence-test shape to copy: `test_caps_outrank_an_approved_draft` and
  `test_the_iteration_cap_still_outranks_the_budget` (tests/test_supervisor_routing.py:186–235).
- Flag names (Claude's discretion, adopted): `notes_insufficient`,
  `followup_research_done`; trace key `followup_research` with values
  `"no_prior_research" | "notes_insufficient"`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Red pins first, then the flip — state keys, row 4 → researcher, row 5, append-not-replace (17-02-T1)</name>
  <files>src/research_agent/graph.py, tests/test_supervisor_routing.py, tests/test_graph_smoke.py</files>
  <read_first>src/research_agent/graph.py:156–239 (AgentState/initial_state/followup_state), :292–347 (researcher_node), :472–534 (supervisor_node); tests/test_supervisor_routing.py:117–122 (the test being flipped); tests/test_graph_smoke.py:201–210 (the smoke test being flipped) and its fake_client fixture</read_first>
  <behavior>
    Write these tests FIRST and run them against today's code — both MUST fail red; paste
    the failure output into the SUMMARY before implementing (quality gate):
    - test_notes_append_not_replace_on_a_followup_pass (tests/test_graph_smoke.py): build a
      followup-mode state whose research_notes is a sentinel string like
      "PRIOR NOTES MUST SURVIVE", run the graph (or researcher_node directly with the
      fake_client fixture), assert the resulting research_notes STARTS WITH the sentinel
      verbatim AND contains the scripted new notes after it. RED today: the REPLACE at
      graph.py:333 discards the sentinel.
    - test_followup_no_notes_routes_to_researcher (tests/test_supervisor_routing.py —
      renames/replaces test_followup_without_prior_research_refuses_to_answer): a followup
      state with empty research_notes → next_step == "researcher", forced_stop_reason ==
      "", followup_research_done is True, and the supervisor's trace entry carries
      followup_research == "no_prior_research". RED today: the row ENDs with the stop.
  </behavior>
  <action>
    Implement, per the settled decisions (Path 1) and 17-RESEARCH.md Q1–Q3:

    1. `AgentState` gains `notes_insufficient: bool` and `followup_research_done: bool`;
       `initial_state` sets both False. `followup_state` needs no edit — it builds on
       `initial_state`, so both keys are fresh False per turn (per-turn one-pass allowance
       is CORRECT: each turn is a new run with its own budget/run_id/reservation). Never
       read these keys off `previous` (Pitfall 6 — pre-17 Postgres state blobs lack them).
    2. Row 4, SAME position (above the classifier row — that position is what keeps
       "follow-ups never classify" a table property): destination becomes "researcher";
       it no longer sets forced_stop_reason; it sets `followup_research_done = True`; the
       supervisor's single trace append gains the key `followup_research` with value
       `"no_prior_research"` for this branch (use a local variable folded into the one
       existing append at graph.py:516 — do not add a second trace entry). Rewrite the
       row's comment: stopping is no longer the honest move — reaching is, and the honest
       window is that no answer ships until the critic reviews one (D: replacement
       guarantee).
    3. NEW row 5 immediately below row 4, above the classifier row:
       `mode == "followup" and notes_insufficient and not followup_research_done` →
       "researcher"; sets `followup_research_done = True`, clears `notes_insufficient`,
       trace key `followup_research = "notes_insufficient"`. (Nothing sets the flag until
       plan 03 — the row is fully unit-testable via hand-built state, which is how this
       suite works.)
    4. Researcher append-not-replace: replace graph.py:333 with join-if-existing — when
       `state["research_notes"]` is non-empty, the new value is existing + blank line +
       notes; otherwise notes. Unconditional, no mode check (in research mode existing is
       always empty at researcher time — the generic row only routes on empty notes).
       `store.add` keeps its task-prefix line unchanged (SC-2 attribution: task prefix +
       owner + turn trace — defined as satisfied per RESEARCH Q3.3; no schema change).
    5. Add pin: after a followup-mode researcher pass, the store's newest note text starts
       with the bracketed follow-up question (`[<question>] ...`) — SC-2 attribution
       observable.
    6. Add per-turn default pins in tests/test_supervisor_routing.py:
       `initial_state` has both keys False; `followup_state` yields both False even when
       the previous turn's dict has them True.
    7. Flip tests/test_graph_smoke.py:201–210
       (test_followup_without_prior_research_makes_no_api_calls) to the new truth — name it
       test_a_followup_with_no_prior_notes_researches_instead_of_refusing: the follow-up
       now makes researcher, responder, and critic calls (assert those node names appear in
       client.calls in that order), forced_stop_reason == "", and the draft is non-empty.
       Also update the old smoke test's docstring claim ("Refusing costs nothing").
    Do NOT touch responder_node, critic_node, chat.py, or any docstring outside the
    supervisor comment — those are plan 03/04 surfaces (file-ownership discipline).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_supervisor_routing.py tests/test_graph_smoke.py -k "notes_append_not_replace or followup_no_notes_routes_to_researcher or researches_instead_of_refusing" --collect-only -q (state count, >= 3) && .venv/bin/pytest tests/test_supervisor_routing.py tests/test_graph_smoke.py</automated>
  </verify>
  <acceptance_criteria>
    - RED evidence recorded in SUMMARY for both behavior pins before the implementation commit
    - `.venv/bin/pytest tests/ -k notes_append_not_replace` green (VALIDATION row 17-02-T1a)
    - `.venv/bin/pytest tests/ -k followup_no_notes_routes_to_researcher` green (VALIDATION row 17-02-T1b)
    - `grep -c "no_prior_research" src/research_agent/graph.py` counts ONLY trace-event
      usages (state the count and each line's role; zero forced_stop_reason assignments of it)
    - Routing + smoke suites green; full-suite/evals gate deferred to Task 3 (the golden
      case and e2e eval test still pin the old behavior until then — named, expected, honest)
  </acceptance_criteria>
  <done>Every acceptance criterion above holds, with its evidence (counts, probes observed red, diffs) recorded in the plan SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: The precedence suite — eight pairs, side effects, mutations observed red (17-02-T2)</name>
  <files>tests/test_supervisor_routing.py</files>
  <read_first>17-RESEARCH.md § Q1 (the AFTER table and the eight-pair pinning table — implement it row for row), tests/test_supervisor_routing.py:186–235 (the shapes to copy), :266–291 (reachability test)</read_first>
  <action>
    Implement every row of RESEARCH Q1's precedence-pair table. Naming discipline: caps
    pairs are named test_guardrails_outrank_followup_research_* (they satisfy BOTH
    VALIDATION selectors `-k precedence` is not needed for them); all other pairs named
    test_precedence_*. The pairs:

    1. Caps vs row 4 (three tests, parametrize or write separately): followup, no notes,
       plus (a) iteration > MAX_ITERATIONS, (b) revision_count > MAX_REVISIONS,
       (c) monkeypatched budget with cost over it → each ENDs with its own stop reason,
       next_step "done" not "researcher", followup_research_done stays False, no
       followup_research trace key.
    2. Row 4 side effects (may extend Task 1's pin): followup, no notes → researcher,
       forced_stop_reason "", trace followup_research == "no_prior_research",
       followup_research_done True — set at the SUPERVISOR (assert before any researcher
       runs).
    3. Row 4 vs classifier row — THE row-delete discriminator: mode "followup",
       topic_type "", research_notes "" → "researcher", never "classifier". (With row 4
       deleted, this state hits the classifier row first. followup_state always sets a
       topic_type, so only a hand-built state can see this — that is the point.)
    4. Caps vs new row 5 (three tests): followup, notes present, notes_insufficient True,
       each cap tripped → the cap's stop reason, not researcher.
    5. Row 5 vs author row: followup, notes present, draft "", notes_insufficient True,
       followup_research_done False → "researcher" not "responder"; and side effects:
       notes_insufficient cleared, followup_research_done True, trace
       followup_research == "notes_insufficient".
    6. Row 5 one-pass bound (routing half): same state but followup_research_done True →
       "responder" (falls through to the author row). Name contains one_pass_bound.
    7. Row 5 mode guard: mode "research", notes_insufficient True (hand-built), notes
       present, draft "" → "writer" — normal research routing, the new row never fires
       outside followup mode.
    8. Reachability: extend test_every_reachable_next_step_has_an_edge's override list
       with the two new combos — followup + no notes (now reaches researcher) and
       followup + notes + notes_insufficient (reaches researcher via row 5).

    MUTATION PROTOCOL (Pitfall 2 — the destination-invisible rows; three consecutive
    phase-16 waves shipped gates with no probe): apply each mutation to graph.py
    temporarily, run the named discriminating test, observe RED, revert, and log
    mutation → red test name → failing assertion in the SUMMARY:
    - M1 row-delete row 4 → test 3 reds (routes classifier)
    - M2 row-swap row 4 below the generic researcher row → test 2 reds (no trace key, flag False)
    - M3 remove row 4's followup_research_done side effect → test 2 reds
    - M4 row-swap row 5 below the author row → test 5 reds (routes responder)
    - M5 move row 5 above the cap rows → one of the test-4 caps tests reds
    A green suite with no red-observed mutation is a vacuous gate — do not record it as done.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_supervisor_routing.py -k "precedence or guardrails_outrank_followup_research or one_pass_bound" --collect-only -q (state count, >= 10) && .venv/bin/pytest tests/test_supervisor_routing.py</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/pytest tests/test_supervisor_routing.py -k precedence` collects >= 5, green
    - `.venv/bin/pytest tests/ -k guardrails_outrank_followup_research` collects >= 6 (3 caps x 2 rows), green
    - `.venv/bin/pytest tests/ -k one_pass_bound` collects >= 1 here (routing half), green
    - All five mutations M1–M5 logged with their red assertion in the SUMMARY
    - Routing suite total stated against the 38-green baseline (delta explained by adds/renames)
  </acceptance_criteria>
  <done>Every acceptance criterion above holds, with its evidence (counts, probes observed red, diffs) recorded in the plan SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Path-1 eval flip — golden case + taxonomy pins in the SAME COMMIT (17-02-T3)</name>
  <files>evals/dataset.py, tests/test_evals.py</files>
  <read_first>evals/dataset.py:832–860 (followup-with-no-prior-research), tests/test_evals.py:160–175 (strata), :216–226 (flip-tag test), :625–670 (the e2e test being flipped); evals/graders.py grade_notes_stored (find it — its semantics decide the expect_notes_stored call below)</read_first>
  <action>
    Flip the path-1 golden case to the route-then-guardrail end-to-end pin (settled: this
    ONE case pins BOTH the row-4 flip and "guardrails outrank the new route" through the
    real graph). Dataset edit and its dependent test edits in ONE commit (Pitfall 4).

    1. `followup-with-no-prior-research`: the follow-up's expectations become
       expect_research=True, expect_forced_stop="budget_exceeded" (fresh usage at turn 1 →
       row 4 routes to researcher; the scripted researcher's folded usage trips the case's
       1e-7 budget at supervisor turn 2), expect_approved=False, answer text updated to
       state the reach happens and the budget stops it before any draft. Rewrite `why` to
       state the reversal (this satisfies the new after-tag pin below). RESEARCH A2 is
       LOW-risk but unverified end-to-end: run the single case first and let the tree
       decide the exact stop; record any deviation.
    2. expect_notes_stored: the follow-up's researcher pass now writes a note BEFORE the
       supervisor sees the cost (the memory write is inside researcher_node). Read
       grade_notes_stored's actual semantics and set the case field to what the tree
       grades (likely True now); record the call and the asymmetry (it is ADR-0011
       consequence material — new notes survive a budget stop for the next turn).
    3. tests/test_evals.py, same commit:
       - test_a_followup_with_no_prior_notes_stops_honestly → rename to
         test_a_followup_with_no_prior_notes_reaches_then_hits_the_guardrail: probe case
         gains the same flipped expectations; assert followup turn's forced_stop_reason ==
         "budget_exceeded", draft empty, trace contains a researcher entry AND a
         supervisor entry with followup_research == "no_prior_research",
         "followup_research_bounded" and "followup_reach_traced" in the grade names, all
         grades pass.
       - test_dataset_taxonomy_followup_strata: the no_prior clause (requires a
         no_prior_research stop case) becomes the route-then-guardrail clause: >= 1 case
         whose follow-up has expect_research=True and expect_forced_stop="budget_exceeded".
         The refusal clause (>= 3 not-answerable) still holds — the three path-2 cases are
         untouched until plan 03. Keep the >= 4 answerable and chain clauses verbatim.
       - test_dataset_taxonomy_phase17_flip_cases_are_tagged: its purpose expires with the
         flip; replace with the after-pin — every case containing a follow-up with
         expect_research=True must state the reversal in its `why` (assert "Phase 17" or
         "reach" appears; pick one token and assert it exactly).
    4. Full gate: whole suite + keyless evals now green again — the wave ends at baseline
       shape (41/41; suite count stated).
  </action>
  <verify>
    <automated>.venv/bin/pytest && ANTHROPIC_API_KEY="" .venv/bin/python -m evals && .venv/bin/ruff check . && git log --oneline -1 --stat | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `git show --stat HEAD` for the flip commit lists BOTH evals/dataset.py and
      tests/test_evals.py (same-commit contract)
    - `ANTHROPIC_API_KEY="" .venv/bin/python -m evals` exactly 41/41
    - Full suite plain green (state measured count vs 692/65 baseline)
    - `grep -c "no_prior_research" evals/dataset.py` == 0 as an expectation value
      (prose/history mentions allowed only in `why` text that names the reversal)
    - The flipped case's followup has expect_research=True and
      expect_forced_stop="budget_exceeded" (grep both from the case body)
  </acceptance_criteria>
  <done>Every acceptance criterion above holds, with its evidence (counts, probes observed red, diffs) recorded in the plan SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supervisor routing table | Row order is load-bearing; the Phase 10 mapping's warned accident is now the intent and must ship deliberately |
| researcher → note store | Session note set integrity across turns (SC-2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Tampering | graph.py:333 REPLACE shipping green | mitigate | append pin observed RED against today's code before the fix (Task 1 behavior block) |
| T-17-05 | Tampering | row moved without its precedence pin | mitigate | M1–M5 mutation protocol, each observed red on a side-effect assertion (Task 2) |
| T-17-06 | Elevation | new route outranking a guardrail | mitigate | six caps-vs-reach tests; M5 observed red |
| T-17-07 | Repudiation | old END behavior surviving in evals unnoticed | mitigate | path-1 case + e2e test flipped in the same plan; wave gate is full suite + 41/41 |
| T-17-SC | Tampering | package installs | accept | zero packages installed this phase |
</threat_model>

<verification>
Plan gate: full suite plain green (measured), evals 41/41 keyless, ruff clean, routing
suite delta vs 38 explained, mutation log M1–M5 complete, both red-first pins evidenced.
</verification>

<success_criteria>
- SC-1 path 1 live: no-notes follow-up researches; forced_stop_reason empty
- SC-2 mechanical half: notes append with prior notes surviving verbatim; store prefix pin
- SC-5: guardrails outrank both reach rows, tested six ways
- SC-4 code half: no_prior_research emitted only as a trace event in graph.py
- Eight precedence pairs pinned by side effects; five mutations observed red
- Suite + evals at baseline shape at plan end
</success_criteria>

<output>
Create `.planning/phases/17-followups-that-can-reach-for-new-information/17-02-SUMMARY.md`
including: both RED-first outputs, the M1–M5 mutation log, measured suite/eval counts, the
grade_notes_stored decision, and any line-number drift from RESEARCH (the tree wins).
</output>
