---
phase: 17-followups-that-can-reach-for-new-information
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evals/dataset.py
  - evals/harness.py
  - evals/graders.py
  - evals/__main__.py
  - tests/test_evals.py
autonomous: true
requirements: [REQ-followup-live-search]
must_haves:
  truths:
    - "All 41 offline evals still pass keylessly — every new field defaults to today's behavior; no golden case flips in this plan"
    - "The grader registry can express 'this follow-up is expected to research, exactly once, never classifying' — the machinery Phase 17's flips will route through"
    - "The record-cost preview prices a research-triggering follow-up like a research turn (the Phase 15 35% lesson, fixed before it repeats)"
  artifacts:
    - path: "evals/graders.py"
      provides: "grade_followup_research_bounded (expectation-keyed) and grade_followup_reach_traced (SC-4 trace-event grading), registered in FOLLOWUP_GRADERS"
    - path: "evals/dataset.py"
      provides: "Followup gains expect_research, research_notes, insufficiency fields with honest docstrings"
    - path: "evals/harness.py"
      provides: "ScriptedClient pop-list researcher outputs and interleaved responder outputs"
    - path: "evals/__main__.py"
      provides: "_assumed_pipeline_cost prices expect_research follow-up turns with the measured research constants"
  key_links:
    - from: "evals/harness.py ScriptedClient"
      to: "Followup.research_notes / Followup.insufficiency"
      via: "pop-lists built in __init__"
      pattern: "research_notes"
    - from: "evals/graders.py FOLLOWUP_GRADERS"
      to: "grade_followup_research_bounded"
      via: "registry tuple"
      pattern: "grade_followup_research_bounded"
---

<objective>
Build the eval-side machinery Phase 17's behavior flips will land on, WITHOUT flipping any
behavior. Every default preserves today's grading byte-for-byte, so this plan ends with the
measured baselines intact: suite plain 692/65, offline evals 41/41 keyless.

Why first: the offline evals drive the REAL graph. Plan 02 flips graph.py row 4; the moment
that lands, `grade_followup_did_not_research` (which reds ANY researcher visit on a
follow-up) and the `no_prior_research` stop expectations red the eval suite. The
expectation-keyed replacements must already exist so plan 02 can flip case + graph + pins in
one green wave. This is a deliberate deviation from the sketched wave shape (graph first),
forced by the per-wave-green gate — recorded here so the checker sees it was reasoned, not
drifted.

Purpose: REQ-followup-live-search (eval before/after per 17-RESEARCH.md Q5).
Output: new Followup fields, ScriptedClient pop-lists, two new graders, retired grader,
corrected cost-preview topology. Zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-followups-that-can-reach-for-new-information/17-CONTEXT.md
@.planning/phases/17-followups-that-can-reach-for-new-information/17-RESEARCH.md
@.planning/phases/17-followups-that-can-reach-for-new-information/17-VALIDATION.md
@evals/dataset.py
@evals/harness.py
@evals/graders.py
</context>

<interfaces>
Key contracts read from the tree at plan time (executor: verify line drift; the tree wins):

- `Followup` dataclass (evals/dataset.py:~50–66): frozen; fields `question`,
  `answerable=True`, `expect_approved=True`, `expect_forced_stop=None`, `answer=""`.
  The `expect_forced_stop` docstring claims "today only no_prior_research" — falsified by
  this phase, rewrite it here.
- `ScriptedClient` (evals/harness.py:99–131): dispatches on prompt substrings —
  `"Respond with exactly one word"` → classifier, `"Search the web"` → researcher (returns
  `self.case.notes` EVERY time — the Pitfall 5 flaw this plan fixes), `"follow-up question"`
  → responder (pops `self.answers`), `"Does the"` → critic (pops `self.verdicts`).
- `grade_followup_did_not_research` (evals/graders.py:186–194): grader name string
  `"followup_reuses_notes"`; reds any `classifier`/`researcher` node in the turn trace.
- `FOLLOWUP_GRADERS` registry (evals/graders.py:~248–253): 4 graders.
- Cost preview (evals/__main__.py:245–298): `ASSUMED_RESEARCH_INPUT_TOKENS = 72_000`,
  `ASSUMED_RESEARCH_OUTPUT_TOKENS = 8_000`, `WEB_SEARCHES_PER_RESEARCH_TURN = 5`,
  `ASSUMED_FOLLOWUP_INPUT_TOKENS = 6_000`, `ASSUMED_FOLLOWUP_OUTPUT_TOKENS = 2_000`;
  `_assumed_pipeline_cost` prices every follow-up turn at the follow-up constants with zero
  web searches.
- Synthetic-state grader tests (tests/test_evals.py:~355–412): `STOPPED` uses the literal
  `"no_prior_research"` as its stop name.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Followup script fields + ScriptedClient pop-lists (17-01-T1)</name>
  <files>evals/dataset.py, evals/harness.py, tests/test_evals.py</files>
  <read_first>evals/dataset.py:30–110 (Followup + Case dataclasses), evals/harness.py:99–131 (ScriptedClient), tests/test_evals.py (existing ScriptedClient/harness tests — grep "ScriptedClient")</read_first>
  <action>
    Per the settled evals decisions (17-CONTEXT.md § Post-research calls) and 17-RESEARCH.md
    Q5 "Offline scripting mechanics":

    1. `Followup` gains three fields, all defaulting to no-change:
       `expect_research: bool = False` (this turn is expected to trigger exactly one
       research pass), `research_notes: str = ""` (the scripted output of the triggered
       researcher pass), `insufficiency: str = ""` (the authored `INSUFFICIENT: ...` line
       the responder emits pre-research on path-2 cases; empty for path-1 and no-research
       cases). Docstrings state what each does AFTER the flip — and rewrite the
       `expect_forced_stop` docstring, which currently says "today only no_prior_research"
       (dataset.py:60–65): that claim is falsified by this phase; describe the field
       generically (any named guardrail stop, asserted per turn).
    2. `ScriptedClient.__init__` builds `self.researcher_notes` as a pop-list:
       `[case.notes]` followed by each follow-up's non-empty `research_notes`, in follow-up
       order. The `"Search the web"` branch pops from it (`pop(0)` when non-empty, else
       fall back to `case.notes` — exhaustion must not raise, matching the `verdicts`
       idiom). `self.answers` interleaves: for each follow-up, append `fu.insufficiency`
       when non-empty, then `fu.answer`. This fixes Pitfall 5 (every researcher call
       returning the same notes) while keeping "the script is authored in the dataset"
       true.
    3. New unit tests in tests/test_evals.py: build a synthetic `Case` with one follow-up
       carrying `research_notes` and `insufficiency`, drive `ScriptedClient.create` with
       hand-built prompts containing the dispatch substrings, and assert: first researcher
       call returns `case.notes`, second returns `fu.research_notes`; first responder call
       returns `fu.insufficiency`, second returns `fu.answer`; exhausted researcher list
       falls back to `case.notes`. Name tests `test_scripted_client_*` so they collect
       distinctly.
    4. Backward-compat proof: no existing case sets the new fields, so every existing
       case's script sequence is byte-identical. Run the full offline evals and state the
       count.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_evals.py -k scripted_client --collect-only -q (state count, must be >= 3) && .venv/bin/pytest tests/test_evals.py && env -u CRITIC_MODEL ANTHROPIC_API_KEY="" .venv/bin/python -m evals</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "expect_research" evals/dataset.py` >= 2 (field + docstring)
    - `grep -c "today only" evals/dataset.py` == 0 (the falsified docstring is gone)
    - `.venv/bin/pytest tests/test_evals.py -k scripted_client` collects >= 3, all green
    - `env -u CRITIC_MODEL ANTHROPIC_API_KEY="" .venv/bin/python -m evals` reports exactly 41/41 (baseline held)
    - Probe (mutation observed red): temporarily revert the `"Search the web"` branch to
      always return `case.notes`; the second-researcher-output test reds; revert. Record in SUMMARY.
  </acceptance_criteria>
  <done>Every acceptance criterion above holds, with its evidence (counts, probes observed red, diffs) recorded in the plan SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Grader redesign, green-preserving (17-01-T2)</name>
  <files>evals/graders.py, tests/test_evals.py</files>
  <read_first>evals/graders.py:180–260 (follow-up graders + registry), evals/graders.py:520–590 (REFUSAL_PATTERNS + grade_recorded_refusal — NOT weakened by this plan), tests/test_evals.py:350–415 (synthetic-state grader tests)</read_first>
  <action>
    Per 17-RESEARCH.md Q5 grader table:

    1. Replace `grade_followup_did_not_research` with `grade_followup_research_bounded`
       (grade name string `"followup_research_bounded"`). `expect_research=False` branch:
       today's check verbatim — any `classifier` or `researcher` node in the turn trace is
       a fail (the no-research property SURVIVES, scoped to cases whose notes suffice —
       this is not a softening). `expect_research=True` branch: EXACTLY one `researcher`
       trace entry (zero is a fail — the reach never happened; two is a fail — the one-pass
       bound broke) and ZERO `classifier` entries (follow-ups still never classify).
       Docstring states both branches and why the old unconditional form died BY DESIGN
       (Phase 17 gives follow-ups a bounded reach). Grep for the old grade name string
       `"followup_reuses_notes"` across tests/ and evals/ and update every reference.
    2. New grader `grade_followup_reach_traced` (grade name `"followup_reach_traced"`):
       `expect_research=False` → ok "not a reach case"; `expect_research=True` → the turn
       trace must contain a supervisor entry carrying a `followup_research` key whose value
       is `"no_prior_research"` or `"notes_insufficient"` (the redefined trace event, SC-4
       — graded rather than asserted; plan 02 makes the supervisor emit it). Register both
       graders in `FOLLOWUP_GRADERS`; remove the retired one.
    3. Rewrite the follow-up grader docstrings at graders.py:186–219 ("The whole point is
       that the notes are already on disk" is now false; the `no_prior_research` example
       citation at :201 goes).
    4. tests/test_evals.py synthetic-state updates: swap the `"no_prior_research"` stop
       literal in `STOPPED` and its three tests to `"budget_exceeded"` (graders are
       generic; the retired vocabulary leaves living tests — RESEARCH Q5). Add unit tests
       for both new graders, each with its discriminating red probed: bounded/False reds a
       researcher visit; bounded/True reds zero researcher entries, reds two researcher
       entries, reds one classifier entry; reach_traced/True reds a trace with no
       `followup_research` key. Name them `test_followup_research_bounded_*` and
       `test_followup_reach_traced_*`.
    5. Do NOT touch `grade_recorded_refusal`, `REFUSAL_PATTERNS`,
       `grade_followup_was_checked`, `grade_followup_approval`,
       `grade_followup_forced_stop` beyond the docstring sweep in step 3 — RESEARCH Q5
       says they survive and stay load-bearing.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_evals.py -k "followup_research_bounded or followup_reach_traced" --collect-only -q (state count, must be >= 6) && .venv/bin/pytest tests/test_evals.py && env -u CRITIC_MODEL ANTHROPIC_API_KEY="" .venv/bin/python -m evals && .venv/bin/pytest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def grade_followup_did_not_research" evals/graders.py` == 0
    - `grep -c "def grade_followup_research_bounded" evals/graders.py` == 1
    - `grep -c "def grade_followup_reach_traced" evals/graders.py` == 1
    - `grep -rn "no_prior_research" tests/test_evals.py | grep -v "^\s*#" | grep -vE ':(17[35]|222|6(26|49|64)):'` — zero hits as a
      stop-name literal in synthetic states (doc-prose mentions allowed only if they state history)
    - Full suite plain: >= 692 passed (state the measured count; new tests raise it)
    - `env -u CRITIC_MODEL ANTHROPIC_API_KEY="" .venv/bin/python -m evals` exactly 41/41 — no behavior flip in this plan
  </acceptance_criteria>
  <done>Every acceptance criterion above holds, with its evidence (counts, probes observed red, diffs) recorded in the plan SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Record-cost preview prices the new topology (17-01-T3)</name>
  <files>evals/__main__.py, tests/test_evals.py</files>
  <read_first>evals/__main__.py:210–304 (calibration comment, constants, _assumed_pipeline_cost, _judge_calls_for)</read_first>
  <action>
    The exact 35% lesson Phase 15 paid once (evals/__main__.py:214–221) — fix the
    assumptions BEFORE a record run repeats it (settled decision):

    1. In `_assumed_pipeline_cost`, price each follow-up turn whose `fu.expect_research`
       is True with the RESEARCH constants — `ASSUMED_RESEARCH_INPUT_TOKENS`,
       `ASSUMED_RESEARCH_OUTPUT_TOKENS`, `web_search_requests=WEB_SEARCHES_PER_RESEARCH_TURN`
       — instead of the follow-up constants. Turns with `expect_research=False` keep the
       follow-up constants. Judge-call counting (`_judge_calls_for`) is unchanged.
    2. Extend the calibration comment: a research-triggering follow-up is priced as a
       research turn as of Phase 17; the follow-up constants now cover only
       notes-sufficient turns; both remain unmeasured until the deferred record run.
    3. `grep -rn "16.51" . --exclude-dir=.planning --exclude-dir=.git` — verified absent
       from shipped surfaces at plan time. If the grep agrees, record "no prose quotes the
       old total" in the SUMMARY; if it finds a hit, update that prose with the new figure
       (the tree wins over this plan's verification).
    4. New test `test_record_preview_prices_research_triggering_followups` in
       tests/test_evals.py: build two synthetic cases identical except one follow-up has
       `expect_research=True`; assert `_assumed_pipeline_cost` for the reaching case
       exceeds the other (deterministic — same day, same price table). Probe: with the
       step-1 change reverted, the test reds.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_evals.py -k record_preview --collect-only -q (state count, must be >= 1) && .venv/bin/pytest tests/test_evals.py -k record_preview && env -u CRITIC_MODEL ANTHROPIC_API_KEY="" .venv/bin/python -m evals && .venv/bin/ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "expect_research" evals/__main__.py` >= 1
    - `.venv/bin/pytest tests/test_evals.py -k record_preview` collects >= 1, green; red
      observed with the pricing change reverted (recorded in SUMMARY)
    - Evals 41/41 keyless; ruff clean; `git diff main -- .github/workflows/ci.yml` empty
  </acceptance_criteria>
  <done>Every acceptance criterion above holds, with its evidence (counts, probes observed red, diffs) recorded in the plan SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evals → real graph | Offline evals drive the real supervisor; grading changes here gate what plan 02/03 may ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | grade_followup_research_bounded False branch | mitigate | False branch is the OLD check verbatim, probed red against a researcher visit — the surviving no-research property cannot be silently weakened |
| T-17-02 | Repudiation | preview under-quote | mitigate | expect_research turns priced at measured research constants; discriminating test probed red |
| T-17-03 | Tampering | vacuous grader (reach_traced passing on absent event) | mitigate | unit test reds a trace with no followup_research key before green is believed |
| T-17-SC | Tampering | package installs | accept | zero packages installed this phase (RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
Plan gate (all must hold before plan 02 starts):
- `.venv/bin/pytest` plain: >= 692 passed / 65 skipped (state measured count and delta)
- `env -u CRITIC_MODEL ANTHROPIC_API_KEY="" .venv/bin/python -m evals`: exactly 41/41
- `.venv/bin/ruff check .` clean
- Every `-k` selector shipped with its `--collect-only` count (SIXTEEN vacuous gates this
  milestone; three phase-16 waves had gates with no probe — every gate above names its probe)
</verification>

<success_criteria>
- Followup carries expect_research / research_notes / insufficiency; ScriptedClient
  dispatches per-call researcher outputs and interleaved responder outputs
- grade_followup_did_not_research retired BY DESIGN; bounded + reach-traced graders
  registered, each with discriminating probes observed red
- Preview prices expect_research turns as research turns
- Zero behavior flips: 41/41 keyless, suite green, baselines stated
</success_criteria>

<output>
Create `.planning/phases/17-followups-that-can-reach-for-new-information/17-01-SUMMARY.md`
including: measured counts (suite, evals, collect-only per selector), every mutation/probe
observed red with the assertion that caught it, and the 16.51-grep result.
</output>
