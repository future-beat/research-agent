---
phase: 17-followups-that-can-reach-for-new-information
plan: 03
type: execute
wave: 3
depends_on: [17-02]
files_modified:
  - src/research_agent/graph.py
  - src/research_agent/chat.py
  - src/research_agent/limits.py
  - docs/OPERATIONS.md
  - tests/test_graph_smoke.py
  - evals/dataset.py
  - tests/test_evals.py
autonomous: true
requirements: [REQ-followup-live-search]
must_haves:
  truths:
    - "Path 2 works: the responder's INSUFFICIENT: sentinel sets the flag, the draft stays EMPTY (the insufficiency window ships NO answer — it routes, never generates), and the supervisor routes to the researcher (SC-1)"
    - "ONE research pass per follow-up turn: post-research insufficiency ships the honest refusal as a critic-reviewed draft WITH the research attempt visible in the trace (the one-pass bound, both halves)"
    - "The critic still grades every shipped follow-up answer against notes as sole source — critic_node byte-identical to main (SC-3, ADR-0002 reaffirmed untouched)"
    - "no_prior_research is gone from the forced-stop vocabulary in every shipped code surface and lives on as a trace event (SC-4)"
    - "The three path-2 golden cases flip per the settled directions: admits-a-gap → research-then-grounded; refuses-an-uncovered-figure → honest-refusal-after-one-pass (A1 DECIDED); refuses-a-forecast → honest-refusal-after-one-pass"
  artifacts:
    - path: "src/research_agent/graph.py"
      provides: "responder pre/post-research prompt branch + sentinel parse gated on the identical condition; rewritten module/responder/supervisor docstrings; MAX_ITERATIONS comment gains the follow-up worst-case line"
      contains: "INSUFFICIENT:"
    - path: "src/research_agent/chat.py"
      provides: "no_prior_research special-case removed; HELP no longer promises no new web search"
    - path: "evals/dataset.py"
      provides: "three path-2 case flips with authored sentinel lines and second-pass notes; module docstring rewritten to the after-truth"
    - path: "src/research_agent/limits.py"
      provides: "reservation docstring notes research-triggering follow-ups join the research cost class (no resize — locked)"
  key_links:
    - from: "responder_node sentinel parse"
      to: "supervisor row 5"
      via: "notes_insufficient state flag (draft stays empty so the author row cannot fire first)"
      pattern: "notes_insufficient"
    - from: "responder prompt branch"
      to: "ScriptedClient dispatch"
      via: "the literal 'follow-up question' MUST survive the prompt rewrite (harness.py:102–105 dispatches on it — by design)"
      pattern: "follow-up question"
---

<objective>
The routing reversal, path 2: the responder signals insufficiency with the critic's own
sentinel idiom (`INSUFFICIENT: ...` parsed like `APPROVED`/`REVISE:`), the flag routes ONE
research pass, and the enlarged note set gets one authoring attempt — post-research, the
honest refusal ships critic-reviewed with the attempt in its trace. Then flip the three
path-2 golden cases and finish the no_prior_research vocabulary sweep in code surfaces.

Replacement guarantee (locked, ADR-0011's core): grounding = "no answer from parametric
knowledge", never "no new searches". The insufficiency window produces NO answer. The
responder's alternatives stay exactly two: answer from notes, or signal. The signal routes;
it never generates.

Purpose: REQ-followup-live-search SC-1/SC-3/SC-4.
Output: sentinel mechanism + smoke pins, three case flips with final taxonomy shape (same
commit), code-surface vocabulary sweep. Suite + evals green at plan end (41/41).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-followups-that-can-reach-for-new-information/17-CONTEXT.md
@.planning/phases/17-followups-that-can-reach-for-new-information/17-RESEARCH.md
@src/research_agent/graph.py
@evals/dataset.py
</context>

<interfaces>
Read from the tree at plan time (executor: verify drift; the tree wins):

- The idiom to copy: critic_node prompts for `'APPROVED'` or `'REVISE: ' + feedback` and
  parses `verdict.startswith("APPROVED")` (graph.py:458–466). The responder's sentinel is
  this, verbatim in spirit. ADR-0001 stays intact by the same argument that makes
  `approved` legal: the supervisor routes on plain state; only the flag's ORIGIN is model
  output parsed by a fixed prefix.
- responder_node (graph.py:394–438): prompt's closing instruction today is "If the notes do
  not cover what was asked, say plainly that the research didn't cover it rather than
  guessing." The prompt line "The user is now asking a follow-up question:" MUST survive —
  ScriptedClient dispatches on the substring "follow-up question".
- Supervisor rows 4/5 and flags landed in 17-02: `notes_insufficient`,
  `followup_research_done`, trace key `followup_research`.
- ScriptedClient after 17-01: researcher pop-list `[case.notes] + research_notes...`,
  responder answers interleave `fu.insufficiency` then `fu.answer`.
- Graders after 17-01: `grade_followup_research_bounded` (True → exactly one researcher,
  zero classifier), `grade_followup_reach_traced` (event present), plus the surviving
  refusal machinery — `REFUSAL_PATTERNS` must still match the honest-tail answers.
- The three path-2 cases (evals/dataset.py): `followup-admits-a-gap` (:303, Gartner 2027
  spending), `followup-refuses-an-uncovered-figure` (:778, lock_timeout default),
  `followup-refuses-a-forecast` (:806, 2028 MCP share). Each has
  critic_verdicts=(APPROVED, APPROVED) — verdict 1 is the research turn's, verdict 2 the
  follow-up's; exhaustion defaults to APPROVED.
- chat.py:126–134 (the no_prior_research REPL branch), :148–158 (HELP `/ask` line).
- limits.py:114–158 (`reserved_run_usd` docstring with the 2026-09-01 threshold).
- Caps walk (RESEARCH Q4, arithmetically checked): path-2 worst case is ten supervisor
  turns — identical to research mode — under MAX_ITERATIONS=12. NO cap constant changes;
  the derivation comment at graph.py:41–48 gains one line.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The insufficiency sentinel — prompt branch, gated parse, smoke pins (17-03-T1)</name>
  <files>src/research_agent/graph.py, tests/test_graph_smoke.py</files>
  <read_first>src/research_agent/graph.py:394–438 (responder_node), :441–469 (critic_node — the idiom, NOT edited), tests/test_graph_smoke.py fake_client fixture and its scripted-output mechanism</read_first>
  <behavior>
    Write these smoke tests first (fake_client scripted outputs), observe red, then implement:
    - test_insufficiency_signal_routes_to_researcher_and_ships_no_answer: follow-up with
      notes; scripted responder outputs ["INSUFFICIENT: the notes name no such figure",
      "a grounded answer from the enlarged notes"]; scripted critic APPROVED. Assert: final
      draft == the grounded answer (never the sentinel text); trace node order contains
      responder → supervisor → researcher → responder → critic; the sentinel pass left
      draft empty (assert via the trace entry {"node": "responder", "insufficient": True}
      and absence of an answer_length entry for that pass); research_notes contains BOTH
      the prior notes and the researcher's addition; exactly ONE researcher entry.
    - test_one_pass_bound_ships_the_honest_refusal_with_the_attempt: same start, but the
      post-research responder output is refusal prose ("The research didn't cover ...").
      Assert: draft == the refusal, approved True, forced_stop_reason "", exactly one
      researcher entry, supervisor trace has followup_research == "notes_insufficient" —
      the honest tail ships WITH the attempt.
    - test_one_pass_bound_post_research_sentinel_is_an_ordinary_draft (the leak gate):
      script the POST-research responder output to start with "INSUFFICIENT:". Assert it
      is stored as the draft, goes to the critic, notes_insufficient stays False, and no
      second researcher entry appears — the parse is gated on the same condition as the
      prompt, so a stray post-research sentinel cannot re-route (and row 5 is flag-gated
      anyway).
    - test_grounding_survives_followup_research: in the first test's trace, a critic entry
      follows the post-research responder entry — no path ships an unreviewed answer (SC-3).
  </behavior>
  <action>
    Implement in responder_node only (per RESEARCH Q2, adopted as settled):

    1. Compute one boolean, e.g. pre_research = not state["followup_research_done"], and
       use it for BOTH the prompt branch and the parse — identical gating is the Pitfall 3
       defense.
    2. Prompt: when pre_research, the closing instruction becomes — respond with exactly
       "INSUFFICIENT: " followed by one line naming what is missing, if the notes do not
       cover what was asked; never answer from your own knowledge. When not pre_research,
       keep today's wording VERBATIM (the honest refusal is the correct post-research
       answer). The "follow-up question" line survives unchanged in both branches.
    3. Parse: when pre_research and the reply startswith "INSUFFICIENT:" → set
       notes_insufficient True, leave draft UNTOUCHED (stays empty — the sentinel text is
       never a draft and never reaches critic or caller), reviewed stays False,
       revision_count untouched, append trace entry {"node": "responder",
       "insufficient": True}. Otherwise: today's behavior byte-for-byte.
    4. Rewrite responder_node's docstring ("it never searches" / "saying so is the correct
       answer" are now conditional truths — state the two alternatives: answer from notes,
       or signal; and that the signal routes).
    5. critic_node: ZERO edits. Verify with git diff main -- src/research_agent/graph.py
       and confirm no hunk falls between the "def critic_node" and "def supervisor_node"
       lines; record in SUMMARY (SC-3 / ADR-0002 zero-diff at the function level).
    Note: offline path-2 golden cases stay green through this task — their scripted
    responder outputs are the authored refusals, which do not start with the sentinel, so
    no flag is set and their old script sequences still play out. The flip is Task 2's.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_graph_smoke.py -k "insufficiency_signal_routes or one_pass_bound or grounding_survives" --collect-only -q (state count, >= 4) && .venv/bin/pytest tests/test_graph_smoke.py tests/test_supervisor_routing.py && ANTHROPIC_API_KEY="" .venv/bin/python -m evals</automated>
  </verify>
  <acceptance_criteria>
    - RED-first evidence for the four behavior pins in the SUMMARY
    - `.venv/bin/pytest tests/ -k insufficiency_signal_routes` green (VALIDATION 17-03-T1a)
    - `.venv/bin/pytest tests/ -k one_pass_bound` now collects >= 3 total (routing half from
      17-02 + two here), all green (VALIDATION 17-03-T1b)
    - `.venv/bin/pytest tests/ -k grounding_survives_followup_research` green (VALIDATION 17-03-T1c)
    - critic_node zero-diff vs main confirmed and recorded
    - Evals still 41/41 (no case flipped yet — stated, honest green)
  </acceptance_criteria>
  <done>Every acceptance criterion above holds, with its evidence (counts, probes observed red, diffs) recorded in the plan SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Path-2 golden flips + final taxonomy shape, SAME COMMIT (17-03-T2)</name>
  <files>evals/dataset.py, tests/test_evals.py</files>
  <read_first>evals/dataset.py:1–29 (module docstring — rewrite), :303–324 (admits-a-gap), :778–805 (uncovered-figure), :806–831 (refuses-a-forecast), tests/test_evals.py strata + after-tag tests as reshaped by 17-02-T3</read_first>
  <action>
    Flip the three path-2 cases per the SETTLED directions — note A1 was DECIDED against
    RESEARCH Q5's recommendation for uncovered-figure; the decision wins:

    1. `followup-admits-a-gap` → research-then-grounded (the one grounded-flip pin):
       followup gains expect_research=True, answerable=True, expect_approved=True,
       insufficiency="INSUFFICIENT: the notes contain no Gartner forecasts or spending
       figures.", research_notes authored WITH a concrete Gartner 2027 agent-memory
       spending figure (FACTS-style, sourced-and-dated like the case's existing notes),
       answer rewritten to a grounded answer citing that figure. Rewrite `why`: it now
       pins "a research-triggered follow-up produces a grounded answer" and states the
       reversal (keeps the after-tag pin green).
    2. `followup-refuses-an-uncovered-figure` → honest-refusal-after-ONE-pass (A1 DECIDED:
       research IS triggered, the scripted researcher STILL lacks the figure, the refusal
       ships WITH the attempt in trace — this pins the otherwise-uncovered one-pass-then-
       honest-gap behavior): expect_research=True, answerable stays False, insufficiency
       authored, research_notes authored WITHOUT any lock_timeout default (e.g. the pass
       surfaced more statement_timeout material and nothing on lock_timeout), answer stays
       refusal prose that matches REFUSAL_PATTERNS ("didn't cover"), expect_approved=True.
       Rewrite `why` accordingly.
    3. `followup-refuses-a-forecast` → honest-refusal-after-one-pass, same shape (a 2028
       market-share forecast is genuinely unanswerable — the case content decides it, as
       CONTEXT instructs): expect_research=True, answerable False, authored insufficiency +
       thin research_notes with no projections, refusal answer, expect_approved=True.
    4. Unchanged, verify not regressed: followup-uses-prior-notes, followups-chain,
       followups-chain-of-three, followup-stays-inside-thin-notes — notes cover their
       questions, no sentinel fires, expect_research stays False and
       grade_followup_research_bounded's False branch keeps meaning something.
    5. tests/test_evals.py, SAME COMMIT — final strata shape
       (test_dataset_taxonomy_followup_strata): >= 1 research-triggered grounded
       (expect_research and answerable, no stop); >= 2 research-then-still-refuses
       (expect_research and not answerable, no stop); >= 1 route-then-guardrail
       (expect_research and expect_forced_stop == "budget_exceeded"); >= 4 answerable
       no-research; chain clause verbatim. Assertions as specific as the before-pins were
       — a rewritten taxonomy test with assert-True-shaped conditions is the vacuous-gate
       failure mode this phase names.
    6. Module docstring (dataset.py:1–29): "a follow-up that re-searches" as a listed
       failure and the "Three of these are deliberately Phase 17's before-measure"
       paragraph are rewritten to the after-truth (the reach is bounded and graded; the
       before-behaviour lives in git history and ADR-0011).
    7. Sanity: critic_verdicts stay (APPROVED, APPROVED) — the follow-up's single critic
       call pops the second verdict; the sentinel pass never reaches the critic. If the
       tree disagrees anywhere (verdict counts, judge-call pricing via _judge_calls_for),
       the tree wins; record the deviation.
  </action>
  <verify>
    <automated>ANTHROPIC_API_KEY="" .venv/bin/python -m evals && .venv/bin/pytest tests/test_evals.py && git show --stat HEAD | head -15</automated>
  </verify>
  <acceptance_criteria>
    - The flip commit's stat lists BOTH evals/dataset.py and tests/test_evals.py
    - `ANTHROPIC_API_KEY="" .venv/bin/python -m evals` exactly 41/41 (count unchanged: the
      three CONTEXT pins land on flipped cases per the settled Post-research calls — zero
      net-new cases, stated here so the checker sees it was decided, not dropped)
    - `grep -c "expect_research=True" evals/dataset.py` == 4 (three path-2 + the path-1 flip from 17-02)
    - `grep -c "INSUFFICIENT:" evals/dataset.py` == 3 (the three authored sentinel lines)
    - Strata probe: temporarily set admits-a-gap's expect_research to False → the grounded
      clause of the strata test reds; revert. Recorded in SUMMARY.
  </acceptance_criteria>
  <done>Every acceptance criterion above holds, with its evidence (counts, probes observed red, diffs) recorded in the plan SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Vocabulary sweep, code surfaces — chat.py, graph docstrings, caps comment, reservation prose (17-03-T3)</name>
  <files>src/research_agent/graph.py, src/research_agent/chat.py, src/research_agent/limits.py, docs/OPERATIONS.md, tests/test_graph_smoke.py</files>
  <read_first>src/research_agent/graph.py:1–22 (module docstring), :41–48 (MAX_ITERATIONS derivation), :472–479 (supervisor docstring); src/research_agent/chat.py:118–160; src/research_agent/limits.py:114–158; docs/OPERATIONS.md (grep "reserv" for the mirror prose)</read_first>
  <action>
    SC-4's code half, using RESEARCH Q6's inventory (README/DESIGN/ADR/index.html/service.py
    are plan 04's — do not touch them here):

    1. graph.py module docstring (:5–19): rewrite the followup-mode diagram and the
       "Follow-ups skip classification and search entirely" sentence — follow-ups never
       classify, and they reach for new notes when the existing set cannot answer, one
       bounded pass per turn, critic unchanged.
    2. graph.py supervisor docstring (:472–479): "The only thing `mode` changes is which
       node produces the text" is now false — mode also changes reach; rewrite honestly.
    3. graph.py MAX_ITERATIONS comment (:41–48): add ONE line noting the path-2 follow-up
       worst case is the same ten supervisor turns (signal+researcher replaces
       classifier+researcher), so the revision cap remains the reachable backstop. No
       formula change (locked: caps unchanged).
    4. chat.py: delete the no_prior_research special-case branch (:126–134) — the generic
       "stopped by guardrail (reason)" note covers every reason now that the stop is
       retired; rewrite the HELP `/ask` line (:153) — the "no new web search" promise is
       the thing being reversed (e.g. "follow up on the last run — reaches for new
       information when its notes can't answer").
    5. limits.py `reserved_run_usd` docstring: add one sentence — research-triggering
       follow-ups now sit in the research cost class (~$0.21) rather than the pennies
       class; the flat $0.20 estimate stands with its documented 2026-09-01 threshold
       (LOCKED: no resize). Mirror one sentence in docs/OPERATIONS.md where its
       reservation prose lives (grep "reserv" — Phase 16 wave 2 added 6 mentions).
    6. New gate test test_no_prior_research_redefined_out_of_the_stop_vocabulary (in
       tests/test_graph_smoke.py): read the two sources with pathlib and assert —
       (a) "no_prior_research" does not appear in chat.py at all; (b) in graph.py, count
       occurrences on lines that are not comments (strip lines whose lstrip starts with
       "#" — the grep-gate hygiene rule) and assert the count equals the number of
       trace-event usages implemented in 17-02 (state the expected number IN the test with
       a comment saying what each occurrence is); (c) "no new web search" appears nowhere
       in chat.py. Source-reading tests are blunt — that bluntness is the point of a
       vocabulary gate.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/ -k no_prior_research_redefined --collect-only -q (state count, >= 1) && .venv/bin/pytest && ANTHROPIC_API_KEY="" .venv/bin/python -m evals && .venv/bin/ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "no_prior_research" src/research_agent/chat.py` == 0
    - `grep -c "no new web search" src/research_agent/chat.py` == 0
    - `grep -v "^\s*#" src/research_agent/graph.py | grep -c "skip classification and search"` == 0
    - `.venv/bin/pytest tests/ -k no_prior_research_redefined` green (VALIDATION 17-03-T3)
    - Full suite + evals 41/41 + ruff clean at plan end (measured counts stated)
  </acceptance_criteria>
  <done>Every acceptance criterion above holds, with its evidence (counts, probes observed red, diffs) recorded in the plan SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model output → routing flag | The sentinel is model text parsed by fixed prefix; the parse gate is the only thing keeping it out of shipped answers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Spoofing | sentinel leaking into a shipped answer | mitigate | draft stays untouched on signal; leak-gate smoke test; post-research sentinel treated as ordinary draft for the critic |
| T-17-09 | Elevation | insufficiency generating instead of routing | mitigate | the parse sets a flag and nothing else; test asserts no answer_length trace entry on the sentinel pass |
| T-17-10 | DoS | research→insufficient→research loop | mitigate | one-pass bound flag-gated (pinned in 17-02) + honest-tail e2e test here |
| T-17-11 | Tampering | critic touched under cover of the responder edit | mitigate | function-level zero-diff check vs main, recorded in SUMMARY |
| T-17-12 | Repudiation | dataset flipped without its taxonomy pins | mitigate | same-commit contract asserted via git show --stat; strata probe observed red |
| T-17-SC | Tampering | package installs | accept | zero packages installed this phase |
</threat_model>

<verification>
Plan gate: full suite plain green (measured vs baseline), keyless evals exactly 41/41, ruff
clean, `git diff main -- .github/workflows/ci.yml` empty, every `-k` selector shipped with
its collect-only count, every probe observed red recorded.
</verification>

<success_criteria>
- SC-1 path 2 live end to end offline; the window ships no answer
- One-pass bound proven in routing, in smoke, and in grading
- SC-3: critic zero-diff at function level; every shipped answer critic-reviewed
- SC-4 code surfaces swept; the redefinition gate test green
- Three path-2 flips per settled directions (A1 honored over RESEARCH's Q5 lean); final
  strata shape as specific as the before-pins
</success_criteria>

<output>
Create `.planning/phases/17-followups-that-can-reach-for-new-information/17-03-SUMMARY.md`
including: RED-first evidence, the critic zero-diff confirmation, the strata probe log,
measured counts, and any tree-vs-plan deviations.
</output>
