---
phase: 17-followups-that-can-reach-for-new-information
plan: 04
type: execute
wave: 4
depends_on: [17-03]
files_modified:
  - docs/adr/0011-followups-reach-for-new-information.md
  - docs/adr/0003-followups-reuse-critic-no-prior-research.md
  - docs/adr/README.md
  - docs/DESIGN.md
  - README.md
  - src/research_agent/static/index.html
  - src/research_agent/service.py
  - tests/test_service.py
autonomous: false
requirements: [REQ-followup-live-search]
user_setup:
  - service: anthropic + voyage (already held)
    why: "The closing live checkpoint spends real money (~$0.35–0.45, ceiling $0.60)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "operator-held; only the live checkpoint needs it"
      - name: VOYAGE_API_KEY
        source: "operator-held; only the live checkpoint needs it"
must_haves:
  truths:
    - "ADR-0011 records the reversal: Accepted, supersedes ADR-0003; ADR-0003 changes ONLY its status line; ADR-0002 is zero-diff and reaffirmed by citation"
    - "Every one of the 11 shipped 'no new search' locations is updated; the final grep gate over RESEARCH's inventory finds zero stale promises"
    - "README's follow-up limitation is DELETED, and the closure of the ORIGINAL nine-limitation list is stated where a reader will see it"
    - "service.py's diff against main is docstring-only — it still holds no routing logic (SC-6)"
    - "The reversal is demonstrated (or explicitly deferred) on the wire: one research-triggered follow-up, ceiling $0.60, record-or-defer"
  artifacts:
    - path: "docs/adr/0011-followups-reach-for-new-information.md"
      provides: "The reversal record — what dies, what survives, the one-pass bound as the new deliberate limit"
      contains: "supersedes"
    - path: "docs/adr/README.md"
      provides: "Index row 0011; row 0003 flipped; counting prose eight-of-eleven / three supersessions; odd-ones-out paragraph extended"
    - path: "README.md"
      provides: "API row + routing table + mode paragraph rewritten; limitation deleted; Status entry 17; the nine-list closure sentence"
    - path: "src/research_agent/static/index.html"
      provides: "FOLLOWUP_PLACEHOLDER + follow-up comment rewritten (demo needs ZERO JS — researcher node events already render)"
  key_links:
    - from: "docs/adr/0003-followups-reuse-critic-no-prior-research.md"
      to: "docs/adr/0011-followups-reach-for-new-information.md"
      via: "status line supersession (Nygard convention: only the status line changes)"
      pattern: "Superseded by"
    - from: "tests/test_service.py"
      to: "src/research_agent/static/index.html"
      via: "placeholder-copy pin moves with the copy in the SAME commit"
      pattern: "FOLLOWUP_PLACEHOLDER|Follow up on that"
---

<objective>
The record and the sweep: ADR-0011 supersedes ADR-0003 (the milestone's strongest reversal,
pre-named by the index), the 11-location "no new search" inventory goes to zero, the README
limitation dies — closing the ORIGINAL nine-limitation list, said where a reader will see
it — and the phase closes with wire evidence (or an explicit record-or-defer deferral).

Purpose: REQ-followup-live-search SC-4 (docs half) / SC-6, and the phase's demonstrable
artifact — this project exists to be shown to employers, and a live trace of a follow-up
that reaches is the cheapest possible proof the guarantee replacement holds.
Output: ADR-0011 + companion edits, README whole-file pass, demo copy, final grep gate,
live checkpoint. Full suite plain AND armed, evals 41/41, ruff, ci.yml zero-diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-followups-that-can-reach-for-new-information/17-CONTEXT.md
@.planning/phases/17-followups-that-can-reach-for-new-information/17-RESEARCH.md
@docs/adr/README.md
@docs/adr/0003-followups-reuse-critic-no-prior-research.md
@README.md
</context>

<interfaces>
Read from the tree at plan time (executor: verify drift; the tree wins):

- ADR-0011 skeleton: 17-RESEARCH.md Q7 has the full recommended body — use it as the base;
  it already carries the rejected alternatives (critic-as-detector, direct answering,
  multi-pass, turn/session column, regex router) and the budget-stop asymmetry consequence
  (notes stored before the supervisor sees cost survive for the next turn — 17-02's
  grade_notes_stored decision feeds this sentence). Filename pre-named:
  docs/adr/0011-followups-reach-for-new-information.md; carries **Source:** (Phase 17),
  not **Promoted from:**.
- ADR-0003: status line `**Status:** Accepted` → `**Status:** Superseded by
  [ADR-0011](0011-followups-reach-for-new-information.md)` — the Nygard convention the
  index itself states ("Only the status line changes"). Its § Expected reversal prose
  ("That reversal has not happened") stays UNTOUCHED — history, accurate at writing time.
- ADR index (docs/adr/README.md:45–63): counting prose "Eight of the ten records are
  Accepted today. Two supersessions..." → eight of ELEVEN, THREE supersessions (0003,
  0005, 0006) — the Phase 16 impossible-arithmetic lesson applies: make the counts add up.
  Row 0003 Status → Superseded, Superseded-by → ADR-0011 (Phase 17); the *expected:*
  forecast is now fact. New row 0011 `Accepted — supersedes ADR-0003`. The odd-ones-out
  paragraph ("ADR-0006 through ADR-0010...") extends to cover 0011 (Source: Phase 17's
  follow-up reach — no DESIGN.md passage behind it).
- DESIGN.md § the graph, the DEC-04 paragraph ("Follow-ups reuse the critic instead of
  bypassing it... the single failure mode this whole pipeline exists to prevent...
  ADR-0003"): rewrite to the new guarantee (grounding = notes-as-sole-source; a follow-up
  whose notes can't answer reaches, once, and the critic reviews what ships); forward-link
  ADR-0011. ADR-0002 stays zero-diff.
- README sites: :99 API row "Follow-up from that session's notes — no new search"; :150–166
  routing table row "follow-up with no prior notes | END *(no_prior_research)*" plus the
  "That substitution is the *only* thing mode changes; the caps, the critic hop and the
  revision loop are byte-identical in both" paragraph (now false — check the whole
  paragraph); :254 the limitation bullet (DELETE — verified at research time to contain no
  facts living only there); § Status v1.1 list (:38–48) gains the phase-17 entry in the
  established style.
- index.html:155–156 comment ("Follow-ups only use the last two — which is the point of
  follow-ups") and :165 FOLLOWUP_PLACEHOLDER. Demo needs ZERO JS: `_stream` emits node
  events with no mode filter (service.py:322–327) and LABELS.researcher ("searching the
  web") renders on follow-up turns already.
- tests/test_service.py:2541 pins `page.count("Follow up on that — answered from its
  notes, no new search") == 1` — moves with the copy, same commit.
- service.py:701 ask docstring "No new web search." — one line; NOTHING else in
  service.py moves (SC-6).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ADR-0011, the status-line supersession, the index arithmetic, DESIGN.md (17-04-T1)</name>
  <files>docs/adr/0011-followups-reach-for-new-information.md, docs/adr/0003-followups-reuse-critic-no-prior-research.md, docs/adr/README.md, docs/DESIGN.md</files>
  <read_first>17-RESEARCH.md § Q7 (the skeleton — the base text), docs/adr/README.md (whole file — conventions + counting prose), docs/adr/0003-followups-reuse-critic-no-prior-research.md (whole file), docs/DESIGN.md:15–25, docs/adr/0010-judge-rederived-for-an-independent-critic.md (the freshest supersession record — match its register)</read_first>
  <action>
    1. Write docs/adr/0011-followups-reach-for-new-information.md from the RESEARCH Q7
       skeleton, adjusted for what actually shipped: the A1-decided case direction
       (uncovered-figure is the honest-tail pin), the 17-02 grade_notes_stored/asymmetry
       decision, the adopted flag/event names (notes_insufficient, followup_research_done,
       trace key followup_research), and the ADR-0001 equivalence argument (the flag's
       origin is model output parsed by fixed prefix — exactly how approved always
       worked). Status: Accepted — supersedes ADR-0003. Source line, not Promoted-from.
       State explicitly: ADR-0002 (notes as sole source of truth) is reaffirmed untouched
       — the guarantee that survives; "no searches after session start" is the one that
       dies, and it was never the point, only the cheapest enforcement of the point. The
       one-pass bound is named as the NEW deliberate limit (the reversal's own honest
       constraint, with multi-pass in rejected alternatives).
    2. ADR-0003: edit ONLY the status line. Acceptance is exact:
       `git diff main --numstat -- docs/adr/0003-followups-reuse-critic-no-prior-research.md`
       prints `1	1` (one added, one deleted).
    3. ADR index: new 0011 row; 0003 row flipped (Status Superseded, Superseded-by
       ADR-0011 (Phase 17)); counting prose becomes eight of eleven Accepted, three
       supersessions — verify the arithmetic against the table before committing (11 rows,
       Superseded: 0003, 0005, 0006); extend the odd-ones-out Source-vs-Promoted paragraph
       to cover 0011.
    4. DESIGN.md: rewrite the DEC-04 paragraph to the new guarantee; forward-link
       ADR-0011; keep the ADR-0003 link as the historical record it points to. Do not
       touch the ADR-0001/0002 paragraphs.
    5. ADR-0002 zero-diff check:
       `git diff main -- docs/adr/0002-separate-critic-node.md` must be empty.
  </action>
  <verify>
    <automated>ls docs/adr/ | wc -l && git diff main --numstat -- docs/adr/0003-followups-reuse-critic-no-prior-research.md && git diff main --name-only -- docs/adr/0002-separate-critic-node.md | wc -l && grep -c "0011" docs/adr/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/adr/*.md | grep -cv README` == 11 (baseline 10 → 11)
    - numstat for ADR-0003 is exactly `1	1`; ADR-0002 diff empty (0 lines of output)
    - `grep -c "Superseded by" docs/adr/0003-followups-reuse-critic-no-prior-research.md` == 1
    - `grep -ci "eight of the eleven\|eight of eleven" docs/adr/README.md` == 1 and
      `grep -ci "three supersessions" docs/adr/README.md` == 1
    - `grep -c "0011" docs/DESIGN.md` >= 1; `grep -c "single failure mode" docs/DESIGN.md`
      == 0 OR the phrase survives only inside prose that states the guarantee's
      replacement (executor judgment recorded — README facts must not be lost with
      deleted prose; grep the paragraph for facts living only there before rewriting)
  </acceptance_criteria>
  <done>Every acceptance criterion above holds, with its evidence (counts, probes observed red, diffs) recorded in the plan SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: README whole-file pass, demo copy + its pin, service docstring, the final 11-location grep gate (17-04-T2)</name>
  <files>README.md, src/research_agent/static/index.html, src/research_agent/service.py, tests/test_service.py</files>
  <read_first>README.md (WHOLE FILE — the pass is whole-file by locked decision; grep first for facts living only in prose you delete), src/research_agent/static/index.html:150–170, tests/test_service.py:2530–2555, src/research_agent/service.py:695–710</read_first>
  <action>
    1. README whole-file pass:
       - :99 API row → the follow-up route reaches for new information when the notes
         can't answer (say it in the row's register — short).
       - Routing table (:150–161) → the AFTER table: the no-notes follow-up row routes to
         researcher with the trace event; add the insufficiency row; caps rows stay on
         top. Rewrite the "That substitution is the *only* thing mode changes /
         byte-identical in both" paragraph — mode now also changes reach; the caps, critic
         hop and revision loop remain shared.
       - :254 DELETE the limitation bullet "Follow-ups can't reach for new information."
         (verified at research time: no facts live only there).
       - THE CLOSURE, said where a reader will see it (locked): the original v1.0
         Limitations list had nine entries; this deletion closes the last of them. Put one
         sentence at the top of § Limitations (e.g. amending "Known, and deliberate for
         the scope.") mentioning the original nine and that v1.1 closed the list, and make
         the § Status v1.1 phase-17 entry say the same in its own words.
       - § Status v1.1 list: add `- [x] **17 — Follow-ups reach for new information.**`
         entry in the established style (one or two sentences: the reversal, the one-pass
         bound, grounding kept, ADR-0011 link).
       - Whole-file grep before finishing: `no new` · `didn't cover` · `no_prior_research`
         · `reach for new` · `never searches` — every hit either updated or justified in
         the SUMMARY.
    2. index.html: rewrite FOLLOWUP_PLACEHOLDER (:165) to the reach truth (e.g. "Follow up
       on that — reaches for new information if its notes can't answer") and the :155–156
       comment (follow-ups start at the responder and can add a researcher stage — ZERO JS
       changes; the researcher stage row renders on follow-up turns already, verified).
    3. tests/test_service.py:2541: update the placeholder pin to the NEW string, count
       still == 1 — SAME COMMIT as the index.html edit (the suite reds otherwise; that red
       is the pin working).
    4. service.py: the ask docstring's "No new web search." sentence → one honest line
       (reaches when the notes can't answer; the supervisor routes it). NOTHING else:
       SC-6's gate is `git diff main -- src/research_agent/service.py` showing only the
       docstring hunk.
    5. The final inventory gate (RESEARCH Q6, all 11 locations — most were closed in plans
       02/03; this is the phase-level zero check):
       `grep -rn "no new search\|no new web search" README.md src/ docs/` → 0 hits;
       `grep -rn "can't reach for new information" README.md docs/` → 0 hits;
       `grep -rn "no_prior_research" src/research_agent/` → only graph.py trace-event
       lines (state each hit and its role).
  </action>
  <verify>
    <automated>grep -rn "no new search\|no new web search" README.md src/ docs/ | wc -l && grep -c "can't reach for new information" README.md; .venv/bin/pytest tests/test_service.py && .venv/bin/pytest && env -u CRITIC_MODEL ANTHROPIC_API_KEY="" .venv/bin/python -m evals && .venv/bin/ruff check . && git diff main --stat -- src/research_agent/service.py .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - Inventory grep: 0 hits for the "no new (web )search" promise across README.md, src/, docs/
    - `grep -c "can't reach for new information" README.md` == 0 (limitation DELETED)
    - `grep -ci "nine" README.md` >= 1 within § Limitations or § Status context (the
      closure said visibly — quote the sentence in the SUMMARY)
    - README § Status contains a `**17 —` entry
    - index.html + tests/test_service.py placeholder change share one commit (git show --stat)
    - `git diff main -- src/research_agent/service.py` shows exactly one docstring hunk, no
      code lines (SC-6) — paste the diff into the SUMMARY
    - Phase-level gate: full suite plain green AND armed run against local PG :54329 green
      (baselines 692/65 plain, 756/1 armed — state measured counts and deltas), evals
      41/41 keyless, ruff clean, ci.yml zero-diff
  </acceptance_criteria>
  <done>Every acceptance criterion above holds, with its evidence (counts, probes observed red, diffs) recorded in the plan SUMMARY.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: The live closing checkpoint — one research-triggered follow-up on the wire (17-04-T3)</name>
  <action>Pause for the operator. Everything automatable is already done and green; this checkpoint exists because the demonstration spends real money on operator-held keys. Run the live sequence below, or take the defer path — both resolve the checkpoint.</action>
  <what-built>
    The milestone's strongest reversal, fully pinned offline: a follow-up whose notes can't
    answer now routes to the researcher (both paths), bounded to one pass, critic-reviewed,
    caps on top, recorded in ADR-0011. All 4 plans green: suite, evals 41/41 keyless,
    routing suite, grep gates at zero.
  </what-built>
  <how-to-verify>
    A3 decided: the milestone's last phase closes with wire evidence, record-or-defer style
    (13-05/15-06/16-04 precedent). Estimated spend ~$0.35–0.45; CEILING $0.60 — abort past it.

    1. With operator keys in the shell (local service or the deployed v10+ service,
       post-merge — no deploy config changes in this phase):
       `POST /research` with a NARROW question (cheap notes, e.g. a single Postgres
       setting).
    2. `POST /sessions/{id}/ask` (or /ask/stream) a follow-up the notes cannot cover
       (a neighbouring figure the research never touched).
    3. Confirm, and capture for the SUMMARY:
       - the SSE stream (or trace) shows a researcher stage on the FOLLOW-UP turn
         ("searching the web — recalled N note(s)" renders with zero JS changes)
       - the final answer is grounded in the NEW notes (or refuses honestly WITH the
         attempt visible in the trace — both are passes)
       - `/sessions/{id}/trace` carries the supervisor entry with the followup_research
         reason; forced_stop_reason is NOT no_prior_research
       - the run cost reflects a research-class pass (~$0.15–0.25 for the follow-up turn)
    4. DEFER path (equally valid, state it): all behaviour is fully pinned offline; record
       the deferral in the SUMMARY exactly like Phase 15's record run — what would be run,
       what it costs, why deferred.
  </how-to-verify>
  <resume-signal>Type "recorded" with the trace/cost evidence, or "deferred" with the deferral note</resume-signal>
  <done>The SUMMARY carries either the live trace + cost evidence or the explicit deferral note in the Phase 15 record-run style.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → reader | The record is the product for a portfolio project; wrong arithmetic or lost facts are shipped defects |
| operator shell → live APIs | Real spend, bounded by the stated ceiling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13 | Repudiation | README facts lost with deleted prose | mitigate | grep-before-delete rule on every deleted bullet/paragraph; findings recorded |
| T-17-14 | Tampering | ADR-0003 edited beyond its status line | mitigate | numstat `1 1` gate — exact, not approximate |
| T-17-15 | Tampering | index counting prose vs table drift | mitigate | arithmetic verified against the table (11 rows, 3 Superseded) before commit — the Phase 16 lesson |
| T-17-16 | Tampering | service.py code moving under a docstring edit | mitigate | full-file diff vs main pasted into SUMMARY; docstring-only hunk (SC-6) |
| T-17-17 | DoS | live checkpoint overspend | mitigate | $0.60 ceiling stated in the checkpoint; defer path first-class |
| T-17-SC | Tampering | package installs | accept | zero packages installed this phase |
</threat_model>

<verification>
Phase gate (this plan closes the phase):
- Full suite plain AND armed (local PG :54329, LC_ALL=C) — measured vs 692/65 and 756/1
- `env -u CRITIC_MODEL ANTHROPIC_API_KEY="" .venv/bin/python -m evals` exactly 41/41; ci.yml zero-diff
- `.venv/bin/ruff check .` clean
- All grep gates at zero; ADR count 11; VALIDATION sign-off boxes satisfiable
- One branch, one PR (locked) — no merges performed by this plan
</verification>

<success_criteria>
- ADR-0011 Accepted and superseding; 0003 status-line-only; 0002 zero-diff; index arithmetic true
- 11/11 inventory locations updated; final greps at zero
- README limitation deleted; the original nine-limitation list closed and SAID so; Status 17 entry present
- SC-6 proven by diff
- Wire evidence captured, or deferral recorded in the 15-06 style
</success_criteria>

<output>
Create `.planning/phases/17-followups-that-can-reach-for-new-information/17-04-SUMMARY.md`
including: the service.py diff, the closure sentence quoted, the index arithmetic check,
live-run trace/cost evidence or the deferral note, and final measured counts for every baseline.
</output>
