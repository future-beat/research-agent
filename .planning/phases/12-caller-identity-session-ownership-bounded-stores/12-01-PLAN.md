---
phase: 12-caller-identity-session-ownership-bounded-stores
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_store_contract.py
  - src/research_agent/db.py
  - tests/test_db_transaction.py
autonomous: true
requirements: [REQ-store-lifecycle-and-ownership, REQ-demo-authentication]
must_haves:
  truths:
    - "The shared behavioural contract suite parametrizes FOUR note backends (json, memory, chroma, pgvector), and the chroma arm COLLECTS (does not skip) in CI"
    - "A caller can run check-then-write inside one real Postgres transaction holding a transaction-scoped advisory lock, on a pool that is autocommit by default"
  artifacts:
    - path: "pyproject.toml"
      provides: "chromadb==1.4.1 reachable from the dev extra so CI installs it"
      contains: "chroma"
    - path: "src/research_agent/db.py"
      provides: "Database.transaction() context manager yielding a cursor inside conn.transaction()"
      contains: "def transaction"
    - path: "tests/test_store_contract.py"
      provides: "notes fixture parametrized over 4 arms"
      contains: "chroma"
    - path: "tests/test_db_transaction.py"
      provides: "transaction commit/rollback + advisory-lock-inside-transaction proof"
  key_links:
    - from: "tests/test_store_contract.py notes fixture"
      to: "research_agent.memory.ChromaMemoryStore"
      via: "params=['json','memory','chroma','pgvector']"
      pattern: "ChromaMemoryStore"
    - from: "src/research_agent/db.py Database.transaction"
      to: "psycopg conn.transaction()"
      via: "with self._pool.connection() as conn: with conn.transaction()"
      pattern: "conn\\.transaction"
---

<objective>
Lay the two foundations the rest of Phase 12 stands on, before any behaviour changes.

1. Make SC-5 provable: `chromadb==1.4.1` must be installed by the `dev` extra (it lives in the
   `chroma` extra today and CI installs only `dev`), and the shared contract suite's `notes`
   fixture must parametrize a fourth arm — chroma — that COLLECTS in CI rather than skips.
2. Add `Database.transaction()` so Wave 2's spend-cap reservation can hold
   `pg_advisory_xact_lock` inside a real transaction — the pool is `autocommit=True`, so there
   is no transaction for a transaction-scoped lock to hang on today.

Purpose: nothing downstream can honestly claim "four backends behave identically" until the
fourth arm collects, and the cap-race fix cannot be written until a transaction helper exists.
Output: an installed-and-collecting chroma arm, and a tested `Database.transaction()` helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-caller-identity-session-ownership-bounded-stores/12-RESEARCH.md
@.planning/phases/12-caller-identity-session-ownership-bounded-stores/12-VALIDATION.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Already in the tree; the executor uses these directly, no exploration needed. -->

pyproject.toml (extras today):
  dev = ["research-agent[service]", "pytest==9.1.1", "ruff==0.16.1"]
  chroma = ["chromadb==1.4.1"]      # NOT installed by CI, which installs dev

src/research_agent/memory.py:
  class ChromaMemoryStore(MemoryStore):
      def __init__(self, path=CHROMA_PATH, collection=CHROMA_COLLECTION, embedder: Embedder | None = None)
      def add(self, text: str) -> None
      def query(self, query_text, top_k=3, min_similarity=0.3) -> list[str]
  # imports chromadb lazily inside __init__ and raises ImportError with an install hint if absent

src/research_agent/db.py:
  SCHEMA_LOCK_KEY = 3895545195   # zlib.crc32(b"research_agent.schema"); SESSION-scoped lock, autocommit
  class Database:
      @contextmanager
      def cursor(self, row_factory=None)          # one pool checkout; PoolTimeout/PoolClosed re-raised first
      def execute(self, sql, params=None) -> None
      def fetchone(self, sql, params=None); def fetchall(self, sql, params=None) -> list
  # _connect_kwargs() sets autocommit=True on every pooled connection (see the docstring there)

tests/test_store_contract.py:
  HAS_POSTGRES = db.postgres_configured()
  FAKE_DIMENSIONS = len(FakeEmbedder.VOCAB)   # 5-dim fake embedder
  CONTRACT_NOTES_TABLE = "contract_test_notes"
  @pytest.fixture(params=["json", "memory", "pgvector"])   # <-- the 3 arms to become 4
  def notes(request, tmp_path): ...
  # FakeEmbedder lives in tests/test_memory_stores.py; imported by name (pythonpath includes tests/)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (T-01-1): Add chroma to the dev extra and grow the contract notes fixture to 4 collecting arms</name>
  <files>pyproject.toml, tests/test_store_contract.py</files>
  <read_first>
    - pyproject.toml (the `[project.optional-dependencies]` block: `dev` and `chroma`)
    - tests/test_store_contract.py (the `notes` fixture, lines ~93-108, and `_skip_without_postgres`)
    - tests/test_memory_stores.py (FakeEmbedder — the 5-dim fake used by contract note tests)
    - src/research_agent/memory.py (ChromaMemoryStore.__init__ / add / query)
    - .planning/codebase/TESTING.md (no conftest.py; fakes imported by name; skip-detector convention)
  </read_first>
  <action>
    In pyproject.toml, make CI install chromadb by adding `research-agent[chroma]` to the `dev`
    extra's list (mirroring how `dev` already pulls in `research-agent[service]`). Do NOT unpin or
    move the `chromadb==1.4.1` line out of the `chroma` extra — `dev` should compose it, so a
    SQLite/JSON deploy still never pulls chromadb. chromadb==1.4.1 is pre-existing and already
    pinned in this tree (see 12-RESEARCH.md Package Legitimacy Audit — no NEW package is added).

    In tests/test_store_contract.py, add `"chroma"` to the `notes` fixture's `params` list so it
    reads `["json", "memory", "chroma", "pgvector"]`. Add a chroma branch to the fixture: construct
    `ChromaMemoryStore(path=str(tmp_path / "chroma"), collection=CONTRACT_NOTES_TABLE, embedder=embedder)`
    using the same FakeEmbedder the other arms use, so no VOYAGE_API_KEY is needed. The chroma arm
    must COLLECT unconditionally in CI (chromadb is now in dev); do NOT gate it behind HAS_POSTGRES.
    If chromadb is genuinely absent (local dev without the extra), let the arm skip with the same
    reason-string convention the pgvector arm uses ("chromadb not installed") — but CI installs dev,
    so CI collects it. Ensure the fixture yields then closes the store (chroma stores hold a client).
  </action>
  <verify>
    <automated>.venv/bin/pip install -e '.[dev]' >/dev/null 2>&1; .venv/bin/pytest tests/test_store_contract.py -k "chroma and notes" --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -c "import chromadb"` succeeds after `pip install -e '.[dev]'` (baseline: import FAILS on the current tree — chromadb is not installed by dev today, so this gate is falsifiable by removal of that failure).
    - `pytest tests/test_store_contract.py -k notes --co -q` collects the note tests under FOUR distinct fixture params — json, memory, chroma, pgvector (baseline measured 2026-08-05: THREE arms; target FOUR). The chroma arm is present in the collection IDs, not skipped, when chromadb is installed.
    - The existing note contract tests (`test_notes_are_recalled`, `test_top_k_bounds_the_result_count`, `test_the_relevance_floor_excludes_unrelated_notes`, `test_describe_reports_the_count`) all PASS on the chroma arm: `.venv/bin/pytest tests/test_store_contract.py -k "chroma" -q` is green.
    - No new package is introduced: `git diff pyproject.toml` shows only `dev` composing the existing `chroma` extra; the `chromadb==1.4.1` pin is unchanged.
  </acceptance_criteria>
  <done>The contract suite parametrizes four note backends; the chroma arm collects in CI and passes the existing note contract; chromadb reaches CI via the dev extra with no new/altered pin.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave-0 helper): Add Database.transaction() and prove it holds an advisory lock inside a real transaction</name>
  <files>src/research_agent/db.py, tests/test_db_transaction.py</files>
  <read_first>
    - src/research_agent/db.py (`_connect_kwargs` autocommit=True; `cursor()` and its PoolTimeout/PoolClosed re-raise-first arm; `_apply_schema` docstring on session- vs transaction-scoped locks; `SCHEMA_LOCK_KEY`)
    - tests/test_store_contract.py (`HAS_POSTGRES`, `_dsn_tagged`, the `@pytest.mark.skipif(not HAS_POSTGRES)` convention for real-Postgres tests)
    - 12-RESEARCH.md "Database.transaction() addition (db.py)" and the anti-pattern note on transaction-scoped vs session-scoped locks
  </read_first>
  <action>
    Add a `transaction()` context manager to `Database` (db.py) that checks out one pooled
    connection, opens `conn.transaction()` on it, and yields a cursor bound to that same connection
    — so a `pg_advisory_xact_lock` acquired inside is scoped to the transaction and auto-releases at
    COMMIT, and cannot leak across pool checkouts (the exact failure `_apply_schema`'s docstring
    warns about for session-scoped locks). Mirror `cursor()`'s PoolTimeout/PoolClosed pass-through
    arm (re-raise first, before any OperationalError checkout retry) so an unreachable database does
    not double the wait. The block commits on clean exit and rolls back if the body raises (psycopg's
    `conn.transaction()` gives this for free). Add a module-level `CAP_LOCK_KEY = zlib.crc32(b"research_agent.spendcap")`
    constant next to `SCHEMA_LOCK_KEY`, DIFFERENT from the schema key, for Wave 2 to import — do not
    reuse `SCHEMA_LOCK_KEY`. Import `zlib` at module top.

    Create tests/test_db_transaction.py (Postgres-gated with `@pytest.mark.skipif(not HAS_POSTGRES)`
    following test_store_contract's pattern; import HAS_POSTGRES/_dsn_tagged idioms or replicate the
    guard locally — no conftest.py). Cover: (a) a transaction that inserts into a scratch temp table
    and exits cleanly COMMITs (row visible afterward); (b) a transaction whose body raises ROLLS BACK
    (row absent afterward); (c) `SELECT pg_advisory_xact_lock(CAP_LOCK_KEY)` inside the transaction
    succeeds and the lock is not held after the block — prove by acquiring it in a second, separate
    transaction without blocking. Use a scratch temp table or a dedicated table you TRUNCATE, not a
    production table.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_db_transaction.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def transaction" src/research_agent/db.py` == 1 (baseline: 0 today — the method does not exist, so this gate is falsifiable).
    - `grep -c "CAP_LOCK_KEY" src/research_agent/db.py` >= 1 and `CAP_LOCK_KEY != SCHEMA_LOCK_KEY` is asserted by the test (the two constants must differ; a shared key would serialise the cap against schema DDL).
    - With `DATABASE_URL` set and the local Postgres (:54329) container up, `.venv/bin/pytest tests/test_db_transaction.py -q` passes all three cases; commit-visible, rollback-absent, and advisory-lock-auto-release are each an assertion (not a print).
    - The helper re-raises `psycopg_pool.PoolTimeout`/`PoolClosed` before any OperationalError retry arm — asserted by reading, matching `cursor()`'s ordering (a comment cites the reason).
    - Without `DATABASE_URL`, the module skips with a `DATABASE_URL is not set` reason (a green run with the test skipped is NOT evidence — CI runs it with real Postgres).
  </acceptance_criteria>
  <done>Database.transaction() exists, commits/rolls-back correctly, holds a transaction-scoped advisory lock under CAP_LOCK_KEY that auto-releases, and is proven against real Postgres in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI dependency install → runtime image | a package added to `dev` runs in CI and (transitively) can reach the build |
| pooled connection → Postgres | a transaction/advisory-lock primitive that leaks would corrupt Wave 2's cap accounting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-SC | Tampering | `chromadb==1.4.1` promoted into the `dev` extra | mitigate | Not a new package — pre-existing, pinned in the `chroma` extra of this tree (12-RESEARCH.md Package Legitimacy Audit: no external package added, none flagged [SUS]/[SLOP]). `dev` composes the existing pinned extra; the pin is unchanged. No install of an unvetted name occurs, so no blocking legitimacy checkpoint is required. |
| T-12-01-02 | Denial of Service | `Database.transaction()` on the autocommit pool | mitigate | PoolTimeout/PoolClosed re-raised before any retry (no doubled wait on an unreachable DB); transaction-scoped lock auto-releases at COMMIT so a crashed caller cannot pin CAP_LOCK_KEY across checkouts. |
| T-12-01-03 | Elevation of Privilege | shared advisory-lock keyspace | accept→mitigate | CAP_LOCK_KEY is distinct from SCHEMA_LOCK_KEY; the test asserts inequality so a later edit collapsing them fails loudly. |
</threat_model>

<validation_map_coverage>
| VALIDATION row (Wave / Criterion) | Task | Automated Command |
|-----------------------------------|------|-------------------|
| Wave 0 — ChromaMemoryStore imports and runs; suite parametrizes 4 arms | T-01-1 | `pytest tests/test_store_contract.py -k chroma` collects ≥1 |
| Wave 0 requirement — `Database.transaction()` helper with a test | Task 2 | `pytest tests/test_db_transaction.py -q` |
</validation_map_coverage>

<verification>
- `.venv/bin/pip install -e '.[dev]'` then `.venv/bin/python -c "import chromadb; print(chromadb.__version__)"` prints 1.4.1.
- `.venv/bin/pytest tests/test_store_contract.py -k notes --co -q` shows four fixture params.
- With Postgres up: `.venv/bin/pytest tests/test_store_contract.py tests/test_db_transaction.py -q` green.
- Full-suite regression: `.venv/bin/pytest` — no reduction below the 12-VALIDATION.md baseline (436 passed / 34 skipped) other than the intended additions.
</verification>

<success_criteria>
- Contract suite has four note arms; chroma collects and passes in CI.
- `Database.transaction()` exists, is tested against real Postgres, and exposes `CAP_LOCK_KEY` for Wave 2.
- No new external package introduced; chromadb pin unchanged.
</success_criteria>

<output>
Create `.planning/phases/12-caller-identity-session-ownership-bounded-stores/12-01-SUMMARY.md` when done.
</output>
