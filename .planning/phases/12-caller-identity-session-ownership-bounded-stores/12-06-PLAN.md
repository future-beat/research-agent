---
phase: 12-caller-identity-session-ownership-bounded-stores
plan: 06
type: execute
wave: 5
depends_on: [12-02, 12-03, 12-04, 12-05]
files_modified:
  - docs/adr/0007-anonymous-identity-fairness-global-cap.md
  - docs/adr/0006-separate-sessions-token-fails-closed.md
  - docs/adr/README.md
  - README.md
  - src/research_agent/service.py
  - src/research_agent/static/index.html
  - .env.example
  - tests/test_service.py
autonomous: false
requirements: [REQ-demo-authentication, REQ-store-lifecycle-and-ownership]
user_setup:
  - service: fly
    why: "The identity signing secret must be app-wide so both machines verify tokens minted by either"
    env_vars:
      - name: IDENTITY_SIGNING_SECRET
        source: "Generate locally (python -c 'import secrets;print(secrets.token_hex(32))'), then set as a Fly secret"
    dashboard_config:
      - task: "fly secrets set IDENTITY_SIGNING_SECRET=... (staged restart) and deploy the phase"
        location: "fly secrets set / fly deploy from the repo"
must_haves:
  truths:
    - "ADR-0007 exists, status 'Accepted — supersedes ADR-0006', and explicitly carries forward the parts of 0006 that survive (SESSIONS_TOKEN as fail-closed operator credential; read/cap decomposition; router grouping; DEMO_TOKEN-never-in-prod)"
    - "ADR-0006's status line reads 'Superseded by ADR-0007 (Phase 12)' and NOTHING else in 0006 is edited; both README index cells are updated; the README 'rate-limited, not authenticated' limitation is corrected"
    - "Criterion 6: a stranger with cleared storage reaches a working demo with ZERO added friction — first-paint page differs from the pre-phase page by EXACTLY TWO text deltas (footer identity sentence + reworded #limits line), no banner/prompt/modal/spinner/consent, grep -c innerHTML stays 0, font-size/weight sets unchanged"
    - "The owned-session list (<details id=mine>) is not in the DOM until populated; a side-effect-free card renderer is extracted (result() is NOT called per stored turn); session resume posts follow-ups to /sessions/{id}/ask/stream"
    - "Live: IDENTITY_SIGNING_SECRET is set as a Fly secret and both machines verify a cookie minted by either; a real cleared-storage browser reaches the demo without a wall"
  artifacts:
    - path: "docs/adr/0007-anonymous-identity-fairness-global-cap.md"
      provides: "the reversal record with carry-forward"
      contains: "supersedes ADR-0006"
    - path: "src/research_agent/static/index.html"
      provides: "footer identity sentence, reworded #limits, refreshSessions(), <details id=mine>, side-effect-free card renderer, resume flow"
      contains: "mine"
    - path: "tests/test_service.py"
      provides: "criterion-6 static-file gates: innerHTML==0, font-size/weight sets, verbatim copy, details#mine, renderTurnCard, no external URLs"
      contains: "innerHTML"
  key_links:
    - from: "index.html refreshSessions()"
      to: "GET /sessions (caller-scoped)"
      via: "fetch, render only when non-empty, swallow non-2xx"
      pattern: "/sessions"
    - from: "index.html session row click"
      to: "GET /sessions/{id} then /sessions/{id}/ask/stream"
      via: "render stored turns via the side-effect-free renderer, set session, focus input"
      pattern: "ask/stream"
---

<objective>
Record the reversal, correct the docs, ship the identity-aware page without an auth wall, and cut
over live. ADR-0007 supersedes ADR-0006 with explicit carry-forward (per docs/adr/README.md's
status-line convention — 0006's Context/Decision/Consequences are NEVER edited, only its status line).
The README limitation ("rate-limited, not authenticated") is corrected. The demo page gains exactly
what 12-UI-SPEC.md specifies and nothing more: a muted footer identity sentence, a reworded `#limits`
line naming both scopes, an owned-session `<details id="mine">` that only enters the DOM when
populated, a side-effect-free card renderer (the existing `result()` fabricates `$0.0000` and carries
side effects — it must NOT be called per stored turn), and a session-resume flow. Criterion 6 is a
HARD gate: cleared-storage first paint differs from the pre-phase page by exactly two text deltas, no
interactive step, `innerHTML` count stays 0, font sets unchanged. Finally the live cutover sets
`IDENTITY_SIGNING_SECRET` as a Fly secret, deploys, and verifies two-machine identity continuity and
the résumé-link first visit in a real browser.

The page work and its criterion-6 test harness are split into two tasks (T-06-2 does the DOM/CSS
edits; T-06-3 writes the static-file assertions) so neither concern crowds the other's context — the
five index.html changes and the font/innerHTML gate machinery are distinct seams.

Output: ADR-0007 + doc corrections, the identity-aware page, the criterion-6 gates, and a verified
live deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-caller-identity-session-ownership-bounded-stores/12-RESEARCH.md
@.planning/phases/12-caller-identity-session-ownership-bounded-stores/12-CONTEXT.md
@.planning/phases/12-caller-identity-session-ownership-bounded-stores/12-UI-SPEC.md
@.planning/phases/12-caller-identity-session-ownership-bounded-stores/12-VALIDATION.md
@docs/adr/0006-separate-sessions-token-fails-closed.md
@docs/adr/README.md

<interfaces>
<!-- From the tree, read this session. Executor uses these directly. -->

docs/adr/README.md Supersession convention (verbatim): overturned record's STATUS LINE ONLY becomes
  "**Status:** Superseded by ADR-000N (Phase NN)"; new record's status is "**Status:** Accepted — supersedes ADR-000M";
  update both cells in the Index table. A superseded record's Context/Decision/Consequences are NEVER edited.
  ADR-0006 carries "**Source:**" (no docs/DESIGN.md passage) — ADR-0007 also uses **Source:** Phase 12, not **Promoted from:**.

src/research_agent/static/index.html (source of truth; self-contained, no build, strict-CSP):
  - el(tag, cls, text) builds DOM via textContent; renderReport() for markdown; NO innerHTML (baseline grep -c innerHTML = 0)
  - <p class="limits" id="limits"></p> between the form and <div id="out">   (the <details id=mine> goes between these)
  - <footer> holds a <span class="mono"> link row; add the identity sentence ABOVE it, inheriting footer styles (--muted, .82rem)
  - result(payload, isFollowup) at ~L225-254: fabricates "$"+(cost||0).toFixed(4), sets session/placeholder/clears input (side effects) — DO NOT reuse verbatim per stored turn
  - refreshLimits() at ~L344 reads d.rate_limit_per_hour / d.daily_cap_usd / d.spent_24h_usd / d.token_required; called on load + after each run
  - run() posts to /research/stream or /sessions/${session}/ask/stream; busy guard; showError(title, detail) error card
  - fetch call sites today = 2 (measured baseline); font-size set = {.78rem,.82rem,.85em,.88em,.9rem,.95rem,1.15rem,1.6rem}; font-weight set = {600,700}

12-UI-SPEC.md Copywriting Contract (use verbatim):
  Footer identity line: "Your sessions and notes are private to this browser and expire after 7 days of inactivity."
  Session list summary: "Your recent research (N)"; row ages: "just now"/"{n}m ago"/"{n}h ago"/"{n}d ago"
  Limits line, identity-aware: "Shared demo — {n} requests/hour for this browser · ${x} of ${y} spent by all visitors in the last 24h" (keep the token_required branch)
  Expired/refused resume (403/404): title "That session has expired" / detail "Sessions are kept for 7 days after their last activity. Ask a new question to start fresh."

src/research_agent/service.py:
  health() credentials block: {"anthropic": bool(...), "voyage": bool(...)}  -> add "identity_signing": bool(os.environ.get("IDENTITY_SIGNING_SECRET"))
  limits.status(metrics): additive per-identity fields were added in 12-03 for the reworded #limits line
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (T-06-1): ADR-0007 supersedes ADR-0006 with carry-forward; README + /health + .env.example corrected</name>
  <files>docs/adr/0007-anonymous-identity-fairness-global-cap.md, docs/adr/0006-separate-sessions-token-fails-closed.md, docs/adr/README.md, README.md, src/research_agent/service.py, .env.example</files>
  <read_first>
    - docs/adr/README.md (the Supersession convention, verbatim, and the Index table; the note that 0006 uses **Source:** not **Promoted from:**)
    - docs/adr/0006-separate-sessions-token-fails-closed.md (the four Decision parts and the "Expected reversal" section — parts 1 and 2 are the casualties; parts 3 and 4 survive)
    - README.md (the "rate-limited, not authenticated" limitation passage to correct)
    - src/research_agent/service.py (health() credentials block)
    - 12-RESEARCH.md Pattern 8 (the supersede-with-carry-forward verdict) and Runtime State Inventory (IDENTITY_SIGNING_SECRET; .env.example + /health presence)
  </read_first>
  <action>
    Create docs/adr/0007-anonymous-identity-fairness-global-cap.md as a Nygard record with title
    "# ADR-0007 — Fairness keys on an auto-issued anonymous identity; the global cap bounds the bill",
    "**Status:** Accepted — supersedes ADR-0006", a "**Source:** Phase 12" line (NOT Promoted from — there
    is no DESIGN.md passage), and ## Context / ## Decision / ## Consequences headings in that order.
    Content: the reversal of "rate-limited, not authenticated"; the replacement guarantee verbatim from
    CONTEXT (fairness keys on an auto-issued identity; spend remains bounded globally regardless of
    identity count); the documented fairness ceiling (identities are free to mint — clearing storage
    mints fresh limits); the signed-HttpOnly-cookie transport and the CSRF reasoning (SameSite=Lax +
    JSON-body-only + no CORS middleware); 404-not-403 for foreign sessions; TRUST_FORWARDED_FOR demoted
    from load-bearing to vestigial. The Decision section MUST explicitly restate what it carries forward
    from 0006: SESSIONS_TOKEN survives as the fail-closed OPERATOR credential (its meaning inverts —
    unset now closes only the operator view, not the visitor path); the read/cap decomposition and router
    grouping (0006 parts 3 and 4) are reaffirmed; DEMO_TOKEN-never-in-production still holds.

    Edit ONLY the status line of docs/adr/0006-...md to "**Status:** Superseded by ADR-0007 (Phase 12)".
    Do not touch its Context/Decision/Consequences. In docs/adr/README.md, add a row for 0007 and update
    the Status and "Superseded by" cells for both 0006 and 0007; adjust the "All six records are Accepted"
    sentence so it is no longer false.

    Correct README.md's limitation passage: "rate-limited, not authenticated" becomes the honest
    post-phase reading (callers authenticate to an anonymous auto-issued identity; the global cap remains
    the backstop; possession is not a verified person). Add IDENTITY_SIGNING_SECRET to .env.example with
    a comment that unset degrades to per-machine ephemeral identity. Add
    "identity_signing": bool(os.environ.get("IDENTITY_SIGNING_SECRET")) to health()'s credentials block
    (presence-not-value, same posture as anthropic/voyage — never log the value).
  </action>
  <verify>
    <automated>grep -c "supersedes ADR-0006" docs/adr/0007-anonymous-identity-fairness-global-cap.md; grep -c "Superseded by ADR-0007 (Phase 12)" docs/adr/0006-separate-sessions-token-fails-closed.md; .venv/bin/pytest tests/test_service.py -k health -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "supersedes ADR-0006" docs/adr/0007-anonymous-identity-fairness-global-cap.md` >= 1 and the status line reads exactly "**Status:** Accepted — supersedes ADR-0006".
    - `grep -c "Superseded by ADR-0007 (Phase 12)" docs/adr/0006-separate-sessions-token-fails-closed.md` == 1, and a `git diff docs/adr/0006-...md` shows ONLY the status line changed (Context/Decision/Consequences byte-identical — the convention forbids editing them).
    - `docs/adr/0007-...md` has the three Nygard headings (`## Context`, `## Decision`, `## Consequences`) and a `**Source:** Phase 12` line (not `**Promoted from:**`).
    - The Decision section names all four carry-forward items (SESSIONS_TOKEN operator credential, read/cap decomposition, router grouping, DEMO_TOKEN-never-in-prod) — `grep -c "SESSIONS_TOKEN\|DEMO_TOKEN" docs/adr/0007-...md` >= 2.
    - docs/adr/README.md Index has an 0007 row and both records' Status/Superseded-by cells updated; `grep -c "0007" docs/adr/README.md` >= 1.
    - README no longer asserts "rate-limited, not authenticated" as the current state (the passage is corrected, not merely appended to).
    - `grep -c "identity_signing" src/research_agent/service.py` == 1; the /health test still passes and reports identity_signing as a bool.
    - `.venv/bin/pytest tests/test_service.py -k health -q` green.
  </acceptance_criteria>
  <done>ADR-0007 records the reversal with explicit carry-forward, 0006's status line only is flipped, the README index and limitation are corrected, and /health reports the signing-secret presence.</done>
</task>

<task type="auto">
  <name>Task 2 (T-06-2): Identity-aware demo page — footer sentence, reworded limits, owned-session list + resume, side-effect-free renderer</name>
  <files>src/research_agent/static/index.html</files>
  <read_first>
    - src/research_agent/static/index.html (el(); renderReport(); the <p class=limits> and <footer> placement; result() at ~L225-254 and its side effects; refreshLimits() at ~L344; run()'s follow-up path; showError())
    - 12-UI-SPEC.md (the full spec — placement, the <details id=mine> "not in DOM until populated" rule, the session-row spec, the resume flow, the Copywriting Contract, AC1/AC7/AC8/AC10/AC11)
    - 12-RESEARCH.md Pattern 6 (owner) as background; Pitfall 8 (additive /demo shape — refreshLimits must tolerate the fields)
  </read_first>
  <action>
    Edit ONLY src/research_agent/static/index.html (self-contained, no framework, no external resource).
    (1) Footer: add one plain-text line inside <footer> ABOVE the .mono link row with the verbatim copy
    "Your sessions and notes are private to this browser and expire after 7 days of inactivity.",
    inheriting footer styles (no new CSS rule needed). (2) #limits: reword refreshLimits() to emit the
    identity-aware string from the Copywriting Contract naming BOTH scopes (per-browser rate + all-visitors
    spend), reading the additive fields /demo now returns (12-03); keep the token_required branch; keep it
    additive so the old page tolerates the API during rollout. (3) Owned-session list: add a
    `<details id="mine">` container that is NOT appended to the DOM until it has at least one row (empty →
    render nothing, no placeholder/skeleton/spinner); a `refreshSessions()` that `fetch("/sessions")`,
    renders up to 10 newest rows as `<button type="button" class="session-row">` (task text via textContent,
    ellipsised, flex:1; muted relative age), and on any non-2xx or `[]` renders nothing and swallows
    silently (same posture as refreshLimits' catch). Call refreshSessions() on load and after each completed
    run. Add the session-row and #mine CSS using ONLY existing custom properties and existing font sizes
    (.9rem for rows and summary — already in the set); accent only on hover/focus-visible. (4) Extract a
    SIDE-EFFECT-FREE card renderer `renderTurnCard(turn)` that builds the stage line ("Question:"/"Follow-up:")
    + a .card with the answer through renderReport(), rendering ONLY the badges the payload actually supports
    (approved, cost, revisions) and OMITTING absent fields — never fabricating $0.0000 or "undefined
    revision(s)". Refactor result() to reuse it for the live-run card but keep result()'s own side effects
    (set session, placeholder, clear input) OUTSIDE the shared renderer. (5) Resume flow: clicking a row
    (ignored while busy) `fetch GET /sessions/{id}`, appends each stored turn to #out via renderTurnCard
    (NOT result()), sets `session = id`, sets the follow-up placeholder, collapses <details> (open=false),
    focuses the input; a 403/404 shows the "That session has expired" error card (verbatim copy), removes
    the row, and does NOT set session; a network failure shows the existing "Could not reach the service"
    card. All new DOM via el()/textContent — no innerHTML, ever.
  </action>
  <verify>
    <automated>grep -c innerHTML src/research_agent/static/index.html; grep -c "renderTurnCard\|details id=\"mine\"" src/research_agent/static/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c innerHTML src/research_agent/static/index.html` == 0 (measured baseline 2026-08-05: 0 — no innerHTML introduced).
    - The file contains the verbatim footer sentence, the verbatim "Your recent research (N)"-style summary text, and a `<details id="mine">` element (grep each).
    - `renderTurnCard` (the side-effect-free renderer) exists and result() reuses it; a grep confirms result() no longer unconditionally toFixed(4)s a fabricated cost inside the shared renderer path (stored turns omit absent badges).
    - The file is self-contained: no `http://`/`https://` resource URLs beyond the API endpoints; `grep -c "src=\"http\|href=\"http\|cdn" src/research_agent/static/index.html` == 0.
    - refreshSessions() calls `fetch("/sessions")` and the resume path calls `GET /sessions/{id}` then posts to `/sessions/{id}/ask/stream`; `grep -c "ask/stream" src/research_agent/static/index.html` >= 1.
    - (The font-set and innerHTML guards are enforced as a test in T-06-3; this task's own gates confirm the DOM elements and copy are present.)
  </acceptance_criteria>
  <done>The page shows identity honestly in two muted text deltas, lists owned sessions only when they exist, resumes them via a side-effect-free renderer, and uses only el()/textContent — the first visit is friction-free.</done>
</task>

<task type="auto">
  <name>Task 3 (T-06-3): Criterion-6 static-file test harness — innerHTML gate, font-size/weight set guards, verbatim copy + DOM presence</name>
  <files>tests/test_service.py</files>
  <read_first>
    - src/research_agent/static/index.html (T-06-2 output — the footer sentence, the "Your recent research" summary, <details id=mine>, renderTurnCard, the font-size/font-weight literals)
    - tests/test_service.py (how the static file is located/read today, if at all; the existing test style)
    - 12-UI-SPEC.md (AC1/AC7/AC8/AC10/AC11 — the two-delta first-paint and the font-set baselines)
  </read_first>
  <action>
    Add tests to tests/test_service.py that read the static file (resolve the path to
    src/research_agent/static/index.html) and assert the criterion-6 gates as SETS, not counts:
    `page_keeps_innerhtml_at_zero` — `index.html` contains no `innerHTML` (count == 0, the measured
    baseline). `page_font_sets_unchanged` — extract the SET of `font-size:` literal values and the SET of
    `font-weight:` literal values from the file and assert they equal exactly the AC7 baselines
    ({.78rem, .82rem, .85em, .88em, .9rem, .95rem, 1.15rem, 1.6rem} and {600, 700}); adding ANY new value
    fails (a set-equality gate, not a `>=1` count — this is what makes the "exactly two text deltas"
    criterion falsifiable). `page_copy_and_dom_present` — the file contains the verbatim footer sentence,
    the verbatim "Your recent research" summary text, a `<details id="mine">` element, and a
    `renderTurnCard` definition (grep each); and `grep -c "src=\"http\|href=\"http\|cdn"` == 0 (still
    self-contained). These are static-file assertions and run without a live server.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_service.py -k "page_keeps_innerhtml_at_zero or page_font_sets_unchanged or page_copy_and_dom_present" -q</automated>
  </verify>
  <acceptance_criteria>
    - Test `page_keeps_innerhtml_at_zero`: asserts the static file's `innerHTML` occurrence count == 0 (measured baseline 2026-08-05: 0 — falsifies if any innerHTML is introduced).
    - Test `page_font_sets_unchanged`: the SET of `font-size:` literals equals exactly {.78rem, .82rem, .85em, .88em, .9rem, .95rem, 1.15rem, 1.6rem} and the SET of `font-weight:` literals equals exactly {600, 700} (AC7 baseline; adding any value changes a measured SET, not a `>=1` count) — asserted on set equality so a stray size fails.
    - Test `page_copy_and_dom_present`: the verbatim footer sentence, the "Your recent research (N)"-style summary, `<details id="mine">`, and `renderTurnCard` are all present, and there are no external resource URLs — asserted.
    - `.venv/bin/pytest tests/test_service.py -q` green.
  </acceptance_criteria>
  <done>The criterion-6 first-paint constraint is machine-checked: innerHTML stays 0, the font sets are frozen at their baselines, and the required copy/DOM elements are present — a stray size or a fabricated cost fails the suite.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 (T-06-4): Live cutover — set the Fly secret, deploy, verify two-machine continuity and the résumé-link first visit</name>
  <action>
    Claude prepares everything automatable first: confirm the full suite and CI are green (real Postgres
    AND chroma), generate a candidate secret value, and hand the operator the exact `fly secrets set` and
    `fly deploy` commands plus the curl/browser verification script below. The human then performs the
    manual deploy (project convention: deploys are manual) and the real-browser + two-machine checks,
    which cannot be exercised in the test suite. This task PAUSES for the operator per <resume-signal>.
  </action>
  <what-built>
    All of Phase 12 is merged and green in CI (identity middleware, Postgres-backed identity-keyed
    limits with cap reservation, session ownership + 7-day expiry, owner-scoped notes with 7-day TTL,
    ADR-0007, and the identity-aware page). The only remaining steps require a real Fly fleet and a
    real browser and cannot be exercised in the test suite (12-VALIDATION.md Manual-Only Verifications).
  </what-built>
  <how-to-verify>
    1. Generate a secret: `python -c "import secrets; print(secrets.token_hex(32))"`.
    2. Set it app-wide (both machines receive it; this stages a restart):
       `fly secrets set IDENTITY_SIGNING_SECRET=<value>`.
    3. Deploy the phase (per the project's manual-deploy convention): `fly deploy`.
    4. Confirm on BOTH machines that /health reports the credential present:
       `curl -H "fly-force-instance-id: <machine A id>" https://<app>/health` and again for machine B —
       each shows `"credentials": {... "identity_signing": true}` and its own `machine` id.
    5. Two-machine identity continuity: in a browser, run one question (cookie minted on machine A);
       force the follow-up onto machine B (fly-force-instance-id) and confirm the session resolves and
       the cookie verifies on the machine that did not mint it. Record both FLY_MACHINE_IDs.
    6. Criterion 6 (résumé-link first visit): open the live demo in a fresh/incognito profile with
       cleared storage; confirm the question form is usable with ZERO added step (no banner/prompt/
       modal/spinner/consent); run one question end-to-end; reload and confirm "Your recent research"
       now appears. Confirm in dev tools the `ra_id` cookie is HttpOnly, Secure, SameSite=Lax and is
       invisible to `document.cookie`, and rides both the fetch and the fetch-stream calls.
    7. Rollout tolerance: during the sequential machine deploy, confirm the old page against the new
       API still works (additive /demo shape — Pitfall 8).
  </how-to-verify>
  <resume-signal>Type "approved" with the two FLY_MACHINE_IDs recorded, or describe any failure (wall shown, cookie missing/insecure, cross-machine session 404, /health identity_signing false).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser first paint → stranger | criterion 6 forbids any friction before the first question |
| Fly secret → both machines | cross-machine token verification depends on one shared secret |
| untrusted session task text → DOM | rendered stored turns must use textContent, never innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | Denial of Service (adoption) | an auth wall or visible step kills the résumé-link demo | mitigate | criterion-6 two-delta guard; <details id=mine> absent until populated; mint-on-response (Wave 1) never blocks; verified live in a real browser |
| T-12-06-02 | Information disclosure | signing secret leaked via /health or logs | mitigate | presence-not-value in /health credentials; secret never logged |
| T-12-06-03 | Tampering (XSS) | untrusted session task text rendered via innerHTML | mitigate | all new DOM via el()/textContent; grep -c innerHTML == 0 gate (T-06-3) |
| T-12-06-04 | Spoofing (cross-machine) | per-machine ephemeral secret → a cookie minted on A fails on B | mitigate | IDENTITY_SIGNING_SECRET set as an app-wide Fly secret; live two-machine continuity check with both machine IDs recorded |
| T-12-06-05 | Repudiation | ADR left "Accepted" while its central claim is no longer true | mitigate | ADR-0007 supersedes 0006 with carry-forward; 0006 status line flipped per convention |
</threat_model>

<validation_map_coverage>
| VALIDATION row (Wave / Criterion) | Task | Automated / Manual |
|-----------------------------------|------|--------------------|
| Wave 5 — ADR-0007 supersedes 0006 w/ carry-forward; 0006 status edited; README limitation updated | T-06-1 | grep gates + `pytest -k health` |
| Wave 5 — UI page: footer sentence, reworded limits, owned-session list + resume, side-effect-free renderer | T-06-2 | grep gates on index.html |
| Wave 5 — UI (criterion 6): innerHTML 0, font sets unchanged, verbatim copy/DOM present | T-06-3 | `pytest -k "page_keeps_innerhtml_at_zero or page_font_sets_unchanged or page_copy_and_dom_present"` |
| Wave 5 — criterion 6 live: two-delta first paint in a real browser | T-06-4 | live browser diff (manual AC1) |
| Manual-only — two-machine identity continuity | T-06-4 | live, both FLY_MACHINE_IDs recorded |
| Manual-only — résumé-link first visit; cookie attributes | T-06-4 | live incognito browser |
</validation_map_coverage>

<verification>
- `.venv/bin/pytest` full suite green (at or above the 436/34 baseline, plus this phase's additions), CI green with real Postgres AND chroma.
- `grep -c innerHTML src/research_agent/static/index.html` == 0; font-size/weight sets equal the AC7 baselines (T-06-3).
- ADR grep gates pass; `git diff docs/adr/0006-...md` shows only the status line.
- Live: /health reports identity_signing true on both machines; two-machine continuity and criterion-6 first visit verified in a real browser (Task 4 checkpoint approved).
</verification>

<success_criteria>
- Criterion 6: a stranger following a résumé link reaches a working demo without an auth wall — proven in a real browser from cleared storage.
- The reversal is recorded honestly (ADR-0007 supersedes ADR-0006 with carry-forward) and the README is corrected.
- The demo runs live on two machines with shared identity, ready for one PR at the end of the phase.
</success_criteria>

<output>
Create `.planning/phases/12-caller-identity-session-ownership-bounded-stores/12-06-SUMMARY.md` when done.
</output>
