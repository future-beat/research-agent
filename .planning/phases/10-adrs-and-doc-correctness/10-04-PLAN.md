---
phase: 10-adrs-and-doc-correctness
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - docs/DESIGN.md
autonomous: true
requirements: [REQ-adr-promotion]

must_haves:
  truths:
    - "docs/DESIGN.md names four MemoryStore backends — json, memory, chroma, pgvector — not three"
    - "docs/DESIGN.md states the September rate change as 2026-09-01, so no reader or gate can read the introductory rate as permanent"
    - "A reader of any of the five promoted passages in docs/DESIGN.md is pointed at the ADR that records it, closing the loop the ADRs opened"
  artifacts:
    - path: "docs/DESIGN.md"
      provides: "Corrected backend count, ISO-dated pricing window, and forward-links to all five ADRs"
      contains: "docs/adr/0001-deterministic-python-routing.md"
  key_links:
    - from: "docs/DESIGN.md § The graph"
      to: "docs/adr/0001-deterministic-python-routing.md and 0002-separate-critic-node.md and 0003-followups-reuse-critic-no-prior-research.md"
      via: "inline see-also link on each promoted paragraph"
      pattern: "adr/000"
    - from: "docs/DESIGN.md § Data and backends"
      to: "docs/adr/0004-sessions-in-sqlite-not-langgraph-checkpointer.md"
      via: "inline see-also link"
      pattern: "adr/0004"
    - from: "docs/DESIGN.md § Testing"
      to: "docs/adr/0005-opus-5-eval-judge.md"
      via: "inline see-also link"
      pattern: "adr/0005"
---

<objective>
Correct two false or fragile statements in `docs/DESIGN.md` and wire its five promoted
passages forward to the ADRs that now record them.

Purpose: SC-2 (a reader can trace each ADR back to its DESIGN passage *and* the prose points
forward), SC-4 (four backends, not three), and the DESIGN half of SC-6 (the rate change
stated as `2026-09-01`, not "September 1", so the claim is unambiguous and machine-checkable).
Output: one edited document. No code, no test, no `src/` change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-adrs-and-doc-correctness/10-CONTEXT.md
@.planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md

Wave 2. Depends on plans 10-01 and 10-02 because Task 3 links to files those plans create.
Sole owner of `docs/DESIGN.md` — plan 10-03 owns `docs/OPERATIONS.md` and `README.md`, and
must not have touched this file.

**The five ADR paths, exactly as created in wave 1:**

| ADR | Path | DESIGN.md passage |
|-----|------|-------------------|
| 0001 | `docs/adr/0001-deterministic-python-routing.md` | § The graph — "Routing is a state machine, not a prompt" (line 11) |
| 0002 | `docs/adr/0002-separate-critic-node.md` | § The graph — "The critic is a separate node with its own rubric" (line 13) |
| 0003 | `docs/adr/0003-followups-reuse-critic-no-prior-research.md` | § The graph — "Follow-ups reuse the critic instead of bypassing it" (line 17) |
| 0004 | `docs/adr/0004-sessions-in-sqlite-not-langgraph-checkpointer.md` | § Data and backends — "Sessions store completed runs, not mid-run checkpoints" (line 48) |
| 0005 | `docs/adr/0005-opus-5-eval-judge.md` | § Testing — "The judge runs on a different, stronger model than the pipeline" (line 70) |

**Paragraphs in this file are single unwrapped lines.** An edit to a paragraph rewrites one
long line; line numbers do not shift unless a line is added. Locate targets by text.

**macOS grep caution:** BSD `grep -rn … .` emits paths without a `./` prefix, so an
exclusion written `grep -v '^./.planning'` never matches. Every gate below names a file
directly and counts with `wc -l` or `grep -c`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/DESIGN.md names four MemoryStore backends</name>
  <files>docs/DESIGN.md</files>
  <read_first>
    - `docs/DESIGN.md` line 28 — "**The store and the embedder are separate seams.**", the paragraph containing "with three implementations behind a `VECTOR_STORE` env var: JSON (default), in-memory, and Chroma"
    - `docs/DESIGN.md` line 30 — the pgvector paragraph that already exists directly below it
    - `src/research_agent/memory.py` lines 10–20 and 201–240 — the four concrete stores and the `VECTOR_STORE` values (`json`, `memory`, `chroma`, plus pgvector); read only, this phase changes no code
    - `docs/OPERATIONS.md` line 168 — already says "four backends", the wording to stay consistent with
  </read_first>
  <action>
    In `docs/DESIGN.md`, in the paragraph beginning "**The store and the embedder are separate
    seams.**", replace the clause "with three implementations behind a `VECTOR_STORE` env var:
    JSON (default), in-memory, and Chroma" so it reads four implementations and names all
    four: JSON (default), in-memory, Chroma, and pgvector. Use the literal phrase
    `four implementations` and the literal token `pgvector` inside this same paragraph — the
    gate asserts both appear on that one line.

    Leave the rest of the paragraph intact, and take care with the sentence that ends it:
    "A test asserts the graph never reaches past those four methods" refers to the four ABC
    methods `add()`, `query()`, `len()`, `describe()`, **not** to backends. Do not renumber,
    reword or merge it — after this edit the paragraph contains both "four implementations"
    and "those four methods", and both are correct.

    Do not restate what the pgvector paragraph on the next line already says about HNSW and
    the brute-force scan; this edit only fixes the count and the list.
  </action>
  <verify>
    <automated>[ "$(grep -c 'three implementations' docs/DESIGN.md | tr -d ' ')" -eq 0 ] && [ "$(grep -c 'four implementations' docs/DESIGN.md | tr -d ' ')" -eq 1 ] && [ "$(grep 'four implementations' docs/DESIGN.md | grep -c 'pgvector' | tr -d ' ')" -eq 1 ] && [ "$(grep -c 'those four methods' docs/DESIGN.md | tr -d ' ')" -eq 1 ] && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'three implementations' docs/DESIGN.md` returns 0
    - `grep -c 'four implementations' docs/DESIGN.md` returns 1
    - `grep 'four implementations' docs/DESIGN.md | grep -c 'pgvector'` returns 1 — same paragraph
    - `grep 'four implementations' docs/DESIGN.md | grep -c 'Chroma'` returns 1 — no backend dropped while adding one
    - `grep -c 'those four methods' docs/DESIGN.md` returns 1 — the ABC-methods sentence survives untouched
    - `git status --porcelain src/ tests/ evals/ | wc -l` returns 0
  </acceptance_criteria>
  <done>The seams paragraph in `docs/DESIGN.md` names four implementations including pgvector, no occurrence of "three implementations" remains, and the unrelated four-methods sentence is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: State the Sonnet 5 rate change as 2026-09-01</name>
  <files>docs/DESIGN.md</files>
  <read_first>
    - `docs/DESIGN.md` line 39 — "**Prices are effective-dated, because one of them expires this month.**", which currently ends the window sentence with "moves to $3/$15 on September 1"
    - `src/research_agent/usage.py` lines 55–80 — the two contiguous windows (`until=date(2026, 8, 31)`, `since=date(2026, 9, 1)`) the prose must match; read only, no code change
    - `.planning/ROADMAP.md` line 130 — SC-6 as written
  </read_first>
  <action>
    In `docs/DESIGN.md`, in the paragraph beginning "**Prices are effective-dated**", make two
    edits and no others.

    First, replace the prose date "September 1" with the ISO date `2026-09-01`, so the
    sentence states the introductory $2/$10 per MTok window through 2026-08-31 and the
    $3/$15 rate from 2026-09-01. Both figures and both ISO dates must end up on this one
    line — the phase gate asserts that every line mentioning `$2/$10` also mentions `$3/$15`.

    Second, replace the opening clause "because one of them expires this month", which is
    relative to a writing date that has already passed, with wording that dates itself:
    the introductory window is time-boxed and the standard rate takes over on 2026-09-01.
    Keep the rest of the argument as written — a single hardcoded rate reporting numbers a
    third too low, `price_for()` resolving the rate for a date, a test pinning both windows,
    `/pricing` exposing what accounting uses today, and the cache-rate assertions. Keep the
    literal token `/pricing` in this paragraph; it is the named live source.

    Change no other paragraph in § Cost. The unpriced-model and metrics-denominator
    paragraphs are accurate.
  </action>
  <verify>
    <automated>[ "$(grep -c 'September 1' docs/DESIGN.md | tr -d ' ')" -eq 0 ] && [ "$(grep -c 'expires this month' docs/DESIGN.md | tr -d ' ')" -eq 0 ] && [ "$(grep -rn '2/\$10' docs README.md | grep -v '3/\$15' | wc -l | tr -d ' ')" -eq 0 ] && [ "$(grep '2026-09-01' docs/DESIGN.md | grep -c '/pricing' | tr -d ' ')" -ge 1 ] && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'September 1' docs/DESIGN.md` returns 0
    - `grep -c 'expires this month' docs/DESIGN.md` returns 0
    - `grep -c '2026-08-31' docs/DESIGN.md` returns ≥ 1 and `grep -c '2026-09-01' docs/DESIGN.md` returns ≥ 1
    - Cross-document SC-6 gate: `grep -rn '2/\$10' docs README.md | grep -v '3/\$15' | wc -l` returns 0 — no line quotes the introductory rate alone
    - `grep '2026-09-01' docs/DESIGN.md | grep -c '/pricing'` returns ≥ 1 — the live source is named in the same paragraph
    - `grep -c 'pricing_unknown' docs/DESIGN.md` returns ≥ 1 — the neighbouring unpriced-model paragraph survives
    - `git status --porcelain src/ tests/ evals/ | wc -l` returns 0
  </acceptance_criteria>
  <done>The pricing paragraph states both windows with ISO dates on one line, names `/pricing` as the live source, carries no "this month" phrasing, and no document quotes the introductory rate without the standard one.</done>
</task>

<task type="auto">
  <name>Task 3: Forward-link the five promoted passages to their ADRs</name>
  <files>docs/DESIGN.md</files>
  <read_first>
    - `docs/DESIGN.md` lines 11, 13, 17, 48, 70 — the five promoted passages
    - `docs/adr/README.md` — the index created by plan 10-01, for link style and record titles
    - `docs/adr/0001-deterministic-python-routing.md` through `docs/adr/0005-opus-5-eval-judge.md` — confirm the exact filenames and titles before linking
  </read_first>
  <action>
    Add a forward-link to each of the five promoted paragraphs in `docs/DESIGN.md`, using the
    exact relative paths from the table in `<context>` (`adr/0001-deterministic-python-routing.md`
    and so on — this file lives in `docs/`, so the link target is `adr/…`, and the phase gate
    matches on the substring `adr/000`).

    Wording and placement are Claude's discretion, but be consistent: one short markdown link
    per paragraph, appended as a trailing sentence or parenthetical, e.g. a "recorded as
    ADR-0001" link. Each link must carry both the record number (`ADR-000N`) and the path.

    Also add one line near the top of the file — after the existing "For what the system *is*,
    see the [README]" line — saying that the load-bearing decisions are recorded as numbered
    ADRs and linking `adr/README.md`. That gives a reader arriving at DESIGN.md the directory,
    not just five scattered links.

    Do not add links to paragraphs that were not promoted. DEC-03 (bounded loops), DEC-05
    (per-node inference settings), DEC-10 (migration copies embeddings), DEC-15 (one
    `DATABASE_URL`) and DEC-20 (offline evals) stay as narrative with no ADR — linking them
    to records that do not exist is worse than no link.
  </action>
  <verify>
    <automated>[ "$(grep -c 'adr/000' docs/DESIGN.md | tr -d ' ')" -ge 5 ] && [ "$(grep -o 'adr/000[1-5]-[a-z0-9-]*\.md' docs/DESIGN.md | sort -u | wc -l | tr -d ' ')" -eq 5 ] && [ "$(grep -c 'adr/README.md' docs/DESIGN.md | tr -d ' ')" -ge 1 ] && for f in $(grep -o 'adr/000[1-5]-[a-z0-9-]*\.md' docs/DESIGN.md | sort -u); do test -f "docs/$f" || exit 1; done && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'adr/000' docs/DESIGN.md` returns ≥ 5
    - `grep -o 'adr/000[1-5]-[a-z0-9-]*\.md' docs/DESIGN.md | sort -u | wc -l` returns 5 — all five distinct records linked
    - Every linked path resolves: `for f in $(grep -o 'adr/000[1-5]-[a-z0-9-]*\.md' docs/DESIGN.md | sort -u); do test -f "docs/$f" || echo "DANGLING $f"; done` prints nothing
    - `grep -c 'adr/README.md' docs/DESIGN.md` returns ≥ 1 — the directory is linked from the top of the file
    - `grep -c 'ADR-000' docs/DESIGN.md` returns ≥ 5 — each link carries its record number
    - Reverse direction still holds: `grep -L 'DESIGN.md' docs/adr/000[1-5]-*.md | wc -l` returns 0
    - `git status --porcelain src/ tests/ evals/ | wc -l` returns 0
  </acceptance_criteria>
  <done>Each of the five promoted paragraphs links to its ADR by number and path, every link resolves to a file that exists, the ADR index is linked from the top of `docs/DESIGN.md`, and no un-promoted paragraph gained a link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DESIGN prose → running code | The backend count and the price windows are claims about `src/research_agent/memory.py` and `usage.py`; a wrong claim here is exactly the failure this phase exists to end |
| DESIGN link → ADR file | A forward-link to a filename that does not exist is worse than no link — it looks like the record exists |
| grep gate → macOS BSD grep | A vacuous gate certifies a correction that was never made |
| wave 1 output → wave 2 input | This plan links to files plans 10-01/10-02 created; a slug mismatch produces five dangling links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Spoofing | forward-links to ADR paths | mitigate | The gate resolves every extracted path with `test -f "docs/$f"`, so a slug mismatch fails loudly rather than shipping a dangling link |
| T-10-14 | Tampering | the "those four methods" sentence | mitigate | Task 1's action calls out the collision explicitly and the gate asserts `grep -c 'those four methods'` still returns 1 |
| T-10-15 | Information disclosure (stale truth) | "expires this month" / "September 1" | mitigate | Both phrases are gated to zero occurrences and replaced with ISO dates that match `usage.py`'s window boundaries |
| T-10-16 | Denial of service (vacuous gate) | the SC-6 cross-document gate | mitigate | Written as `grep -rn '2/\$10' docs README.md \| grep -v '3/\$15' \| wc -l` — names its search roots, needs no `^./` exclusion, and counts with `wc -l` so exit codes cannot fake a pass |
| T-10-17 | Elevation of privilege (scope creep) | un-promoted paragraphs | mitigate | Task 3 names the five decisions that stay narrative and forbids linking them |
| T-10-SC | Tampering | package installs | accept | This plan installs nothing — documentation only |
</threat_model>

<verification>
Run from the repo root after all three tasks:

1. `grep -c 'three implementations' docs/DESIGN.md` returns 0; `grep 'four implementations' docs/DESIGN.md | grep -c 'pgvector'` returns 1
2. `grep -rn '2/\$10' docs README.md | grep -v '3/\$15' | wc -l` returns 0
3. `grep -o 'adr/000[1-5]-[a-z0-9-]*\.md' docs/DESIGN.md | sort -u | wc -l` returns 5, and every path resolves under `docs/`
4. `grep -L 'DESIGN.md' docs/adr/000[1-5]-*.md | wc -l` returns 0 — the trace works in both directions
5. `git diff --name-only` lists only `docs/DESIGN.md` for this plan
6. `.venv/bin/pytest 2>&1 | tail -1` still reports `388 passed, 28 skipped`
</verification>

<success_criteria>
- `docs/DESIGN.md` names four `MemoryStore` backends including pgvector, with no "three implementations" left
- The pricing paragraph carries both windows with ISO dates on one line and names `/pricing`
- All five promoted paragraphs link forward to their ADR by number and path, with zero dangling links, plus a link to `adr/README.md` near the top
- No code changed; the suite is unchanged at 388/28
</success_criteria>

<output>
Create `.planning/phases/10-adrs-and-doc-correctness/10-04-SUMMARY.md` when done.
</output>
