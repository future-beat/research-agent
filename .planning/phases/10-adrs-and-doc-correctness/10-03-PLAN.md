---
phase: 10-adrs-and-doc-correctness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/OPERATIONS.md
  - README.md
autonomous: true
requirements: [REQ-adr-promotion]

must_haves:
  truths:
    - "No document in docs/ or README.md claims deploys run through Fly's GitHub integration"
    - "docs/OPERATIONS.md states that deploys are manual via fly deploy -a research-agent, matching what fly releases -a research-agent shows"
    - "README.md's cost limitation names /pricing as the live source and says rates are effective-dated, so no reader takes a single Sonnet 5 rate as permanent"
  artifacts:
    - path: "docs/OPERATIONS.md"
      provides: "A deploy section that matches reality"
      contains: "Deploys are manual"
    - path: "README.md"
      provides: "A cost limitation that points at the live rate source"
      contains: "/pricing"
  key_links:
    - from: "docs/OPERATIONS.md"
      to: "the actual release history"
      via: "fly releases -a research-agent"
      pattern: "fly releases -a research-agent"
    - from: "README.md § Limitations"
      to: "the /pricing endpoint"
      via: "list-prices bullet naming the live source"
      pattern: "/pricing"
---

<objective>
Stop `docs/OPERATIONS.md` asserting a deploy pipeline that does not exist, and make
`README.md` name `/pricing` as the live rate source rather than leaving list prices
sounding fixed.

Purpose: SC-3 and the README half of SC-6. The deploy claim is verified false — every
release in `fly releases -a research-agent` is attributed to the owner's personal account,
and Phase 10.5's cutover was run by hand. A reader who believes the current text will push
to `main` expecting a deploy that never happens.
Output: two edited documents. No code, no test, no `src/` change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-adrs-and-doc-correctness/10-CONTEXT.md
@.planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md

Runs in wave 1 alongside plans 10-01 and 10-02, which touch only `docs/adr/`. No file
overlap. `docs/DESIGN.md` belongs to plan 10-04 — do not edit it here.

**Line numbers have moved before.** Phase 10.5 edited `docs/OPERATIONS.md` to add the
`SESSIONS_TOKEN` row but did not touch the deploy claim. Locate the target by its text,
not by the line number quoted in the criterion.

**macOS grep caution:** BSD `grep -rn … .` emits paths without a `./` prefix, so an
exclusion written `grep -v '^./.planning'` never matches and the gate passes vacuously.
Every gate below names `docs README.md` explicitly rather than recursing from `.`, and
pipes to `wc -l` rather than relying on `grep -c` exit codes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the false deploy claim in docs/OPERATIONS.md</name>
  <files>docs/OPERATIONS.md</files>
  <read_first>
    - `docs/OPERATIONS.md` lines 25–52 — the deploy section, the "New files from Fly.io Launch" warning, and the paragraph being replaced
    - `docs/OPERATIONS.md` lines 83–106 — the CI section, so the replacement text agrees with it about what CI does gate
    - `.planning/STATE.md` § Blockers/Concerns and § Session Continuity — the Phase 10.5 outcome, including the observed protection bypass on the 2026-08-04 push to `main`
    - `.planning/phases/10-adrs-and-doc-correctness/10-CONTEXT.md` § Canonical References → "Facts to correct"
    - `.planning/ROADMAP.md` line 127 — SC-3 as written
  </read_first>
  <action>
    In `docs/OPERATIONS.md`, find the paragraph that currently begins "Deploys currently run
    through Fly's GitHub integration, which is **not** gated on CI:" and continues "a direct
    push that fails tests still deploys, because branch protection only gates pull requests."
    It presently sits at lines 49–51, immediately after the "New files from Fly.io Launch"
    blockquote — verify the current position by searching for the text.

    Replace that whole paragraph with a statement of what actually happens. It must say, on
    a single unwrapped line, `Deploys are manual` — there is no Fly GitHub integration and no
    deploy job in CI. Releases are cut by hand with `fly deploy -a research-agent` from a
    working tree the operator has tested; `fly releases -a research-agent` attributes every
    release to the owner's personal account, which is the evidence. State the consequence
    that replaces the old one: because nothing automates the deploy, nothing can deploy a
    failing tree either — but equally, merging to `main` ships nothing until someone runs the
    command, so `main` and the deployed release can drift. Point at the CI section for what
    *is* gated.

    **State the gating honestly — this is the half the old text got wrong in the other
    direction.** `main` is protected with two required checks (`lint · tests · evals` and
    `image build · container smoke test`, `strict: true`), but **`enforce_admins` is `false`**,
    so an admin pushing directly to `main` succeeds and GitHub records a "Bypassed rule
    violations" notice rather than blocking the push. Verified 2026-08-04 against
    `gh api repos/future-beat/research-agent/branches/main/protection`, and observed live:
    the Phase 10.5 push to `main` reported `Bypassed rule violations for refs/heads/main:
    2 of 2 required status checks are expected`. So the accurate statement is that the checks
    gate **pull requests**, not every path to `main` — a direct push bypasses them, and CI
    then runs *after* the fact. Say so plainly; a reader who believes "protected `main` means
    nothing lands unchecked" will be wrong in exactly the way the old paragraph was wrong.
    Do not recommend a settings change here — that is an operator decision, not a docs fix.

    Do not touch the "New files from Fly.io Launch" blockquote above it or the
    `min_machines_running` paragraph — both are still accurate. Do not edit the CI section,
    the configuration table, or the `SESSIONS_TOKEN` row Phase 10.5 added.

    Then sweep for any other occurrence of the same false claim: search `docs` and
    `README.md` for `GitHub integration` and for auto-deploy phrasing, and remove or correct
    anything that survives. The CI badge in `README.md:3` is not this claim — it links a
    workflow that really exists; leave it.
  </action>
  <verify>
    <automated>[ "$(grep -rn 'GitHub integration' docs README.md | wc -l | tr -d ' ')" -eq 0 ] && [ "$(grep -c 'Deploys are manual' docs/OPERATIONS.md | tr -d ' ')" -ge 1 ] && [ "$(grep -c 'fly releases -a research-agent' docs/OPERATIONS.md | tr -d ' ')" -ge 1 ] && [ "$(grep -c 'fly deploy -a research-agent' docs/OPERATIONS.md | tr -d ' ')" -ge 1 ] && [ "$(grep -c 'enforce_admins' docs/OPERATIONS.md | tr -d ' ')" -ge 1 ] && [ "$(grep -ci 'bypass' docs/OPERATIONS.md | tr -d ' ')" -ge 1 ] && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'GitHub integration' docs README.md | wc -l` returns 0
    - `grep -c 'Deploys are manual' docs/OPERATIONS.md` returns ≥ 1 (the phrase is on one line, not wrapped)
    - `grep -c 'fly deploy -a research-agent' docs/OPERATIONS.md` returns ≥ 1
    - `grep -c 'fly releases -a research-agent' docs/OPERATIONS.md` returns ≥ 1 — the evidence is named
    - `grep -c 'enforce_admins' docs/OPERATIONS.md` returns ≥ 1 AND `grep -ci 'bypass' docs/OPERATIONS.md` returns ≥ 1 — the doc names the setting and states that a direct push to `main` bypasses the required checks. **Do not weaken this to `grep -i 'direct push'`**: the false paragraph being replaced already contains "a direct push that fails tests still deploys", so that pattern passes vacuously against the unfixed text. Neither `enforce_admins` nor `bypass` occurs in the file today (verified 2026-08-04).
    - The two required check names appear verbatim: `grep -c 'lint · tests · evals' docs/OPERATIONS.md` returns ≥ 1
    - The still-accurate neighbours survive: `grep -c 'New files from Fly.io Launch' docs/OPERATIONS.md` returns ≥ 1 and `grep -c 'min_machines_running' docs/OPERATIONS.md` returns ≥ 1
    - The Phase 10.5 addition survives: `grep -c 'SESSIONS_TOKEN' docs/OPERATIONS.md` returns ≥ 1
    - `git diff --stat -- docs/OPERATIONS.md` shows changes only in the deploy paragraph region
  </acceptance_criteria>
  <done>`docs/OPERATIONS.md` states that deploys are manual via `fly deploy -a research-agent` with `fly releases -a research-agent` as the evidence, no document under `docs/` or `README.md` mentions a GitHub integration, and the neighbouring accurate warnings are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Name /pricing as the live rate source in README.md</name>
  <files>README.md</files>
  <read_first>
    - `README.md` line 203 — the "**Cost is computed from list prices**" limitation bullet being amended
    - `README.md` line 84 — the endpoint table row that already describes `/pricing` as "live rates"
    - `docs/DESIGN.md` line 39 — the effective-dated pricing paragraph, so the README agrees with it (plan 10-04 edits that line; do not edit it here)
    - `src/research_agent/usage.py` lines 55–80 — the two contiguous windows the docs must not contradict (read only; this phase changes no code)
  </read_first>
  <action>
    In `README.md` § Limitations, amend the bullet that begins "**Cost is computed from list
    prices**". Keep its existing point — no enterprise discounts, no `inference_geo`
    multiplier, so `/metrics` tracks the shape of the bill rather than the bill. Add that
    list prices are effective-dated rather than fixed: Claude Sonnet 5's introductory rate
    runs through 2026-08-31 and the standard rate applies from 2026-09-01, and `/pricing`
    reports whichever window accounting is using today. Name `/pricing` with the leading
    slash so it is greppable, and write the dates in ISO form (`2026-08-31`, `2026-09-01`),
    not as prose months.

    Do not quote dollar figures in `README.md`. The rates belong in `docs/DESIGN.md`, which
    plan 10-04 fixes; repeating them here creates a second copy to keep in sync. If the
    edited bullet mentions a rate at all it must be by window name, not by number.

    Change nothing else in `README.md`: not the CI badge, not the endpoint table, not the
    Deployment section, not the session-token limitation Phase 10.5 added.
  </action>
  <verify>
    <automated>[ "$(grep -c '/pricing' README.md | tr -d ' ')" -ge 2 ] && [ "$(grep 'list prices' README.md | grep -c '/pricing' | tr -d ' ')" -ge 1 ] && [ "$(grep -c '2026-09-01' README.md | tr -d ' ')" -ge 1 ] && [ "$(grep -nE '2/\$10|3/\$15' README.md | wc -l | tr -d ' ')" -eq 0 ] && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '/pricing' README.md` returns ≥ 2 (the endpoint table row plus the amended bullet)
    - `grep 'list prices' README.md | grep -c '/pricing'` returns ≥ 1 — the same bullet names the live source
    - `grep -c '2026-08-31' README.md` returns ≥ 1 and `grep -c '2026-09-01' README.md` returns ≥ 1
    - `grep -nE '2/\$10|3/\$15' README.md | wc -l` returns 0 — no duplicated dollar figures
    - Untouched neighbours survive: `grep -c 'The public demo is rate-limited, not authenticated' README.md` returns ≥ 1 and `grep -c 'actions/workflows/ci.yml' README.md` returns ≥ 1
    - `git status --porcelain src/ tests/ evals/ | wc -l` returns 0
  </acceptance_criteria>
  <done>`README.md`'s cost limitation says list prices are effective-dated with both ISO dates and names `/pricing` as the live source, without duplicating the rate figures, and nothing else in the README changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operations doc → operator action | A reader who believes "pushing deploys" will merge to `main` and assume the service updated; the deployed tree then silently diverges |
| README rate claim → `src/research_agent/usage.py` | A doc that fixes a rate contradicts the two-window price table and will read as false from 2026-09-01 |
| grep gate → macOS BSD grep | `grep -rn … .` with a `^./` exclusion passes vacuously, certifying a correction that was never made |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Repudiation | deploy provenance in `docs/OPERATIONS.md` | mitigate | The replacement names `fly releases -a research-agent` as the checkable evidence, and the gate requires that literal string in the file |
| T-10-10 | Tampering | collateral edits to accurate neighbours | mitigate | Gates assert the survival of `New files from Fly.io Launch`, `min_machines_running`, `SESSIONS_TOKEN`, the CI badge, and the Phase 10.5 session-token limitation |
| T-10-11 | Information disclosure (stale truth) | duplicated rate figures | mitigate | README is gated to contain zero `$2/$10` / `$3/$15` occurrences; rates stay in one place (`docs/DESIGN.md`, plan 10-04) |
| T-10-12 | Denial of service (vacuous gate) | `grep -rn 'GitHub integration' docs README.md` | mitigate | The search names `docs README.md` directly instead of recursing from `.`, and counts with `wc -l` so an empty result is a real zero |
| T-10-SC | Tampering | package installs | accept | This plan installs nothing — documentation only |
</threat_model>

<verification>
Run from the repo root after both tasks:

1. `grep -rn 'GitHub integration' docs README.md | wc -l` returns 0
2. `grep -c 'Deploys are manual' docs/OPERATIONS.md` returns ≥ 1
3. `grep 'list prices' README.md | grep -c '/pricing'` returns ≥ 1
4. `git diff --name-only` lists only `docs/OPERATIONS.md` and `README.md` for this plan
5. `git status --porcelain src/ tests/ evals/ | wc -l` returns 0
6. `.venv/bin/pytest 2>&1 | tail -1` still reports `388 passed, 28 skipped`
</verification>

<success_criteria>
- No file in `docs/` or `README.md` claims deploys run through Fly's GitHub integration
- `docs/OPERATIONS.md` states deploys are manual, gives the command, and names `fly releases -a research-agent` as the evidence
- `README.md` names `/pricing` as the live rate source in the same bullet that mentions list prices, with both window dates in ISO form and no duplicated figures
- Accurate neighbouring content is untouched; no code changed and the suite is unchanged at 388/28
</success_criteria>

<output>
Create `.planning/phases/10-adrs-and-doc-correctness/10-03-SUMMARY.md` when done.
</output>
