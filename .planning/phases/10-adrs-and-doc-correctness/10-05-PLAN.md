---
phase: 10-adrs-and-doc-correctness
plan: 05
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-03", "10-04"]
files_modified:
  - .planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md
autonomous: false
requirements: [REQ-adr-promotion]

must_haves:
  truths:
    - "Every grep gate in 10-VALIDATION.md has been run as written on macOS and recorded green or red — none passed vacuously"
    - "No file under src/ changed between the phase base commit 715e9aa and phase end, and the suite still reports exactly 388 passed, 28 skipped"
    - "SC-5 is re-verified against the live host — release v4 or later, four endpoints returning 200, main not ahead of origin/main — and the evidence is recorded, not a redeploy"
    - "10-VALIDATION.md carries a per-row status for all fourteen rows and a completed sign-off"
  artifacts:
    - path: ".planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md"
      provides: "The executed verification contract with per-row status and SC-5 evidence"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "10-VALIDATION.md per-task map"
      to: "the plans that satisfy each row"
      via: "Task ID column filled with real 10-NN-TN ids"
      pattern: "10-0[1-5]-T[1-3]"
    - from: "the phase base commit 715e9aa"
      to: "phase end"
      via: "git diff --quiet 715e9aa -- src/"
      pattern: "git diff --quiet"
---

<objective>
Close the phase: run every gate the phase promised, prove no production code moved, and
re-verify — not redeploy — that the live release still matches `main`.

Purpose: a documentation phase is only worth what its gates are worth. Phase 10.5 was bitten
by a gate that passed vacuously on BSD grep and by a structural test that scanned the wrong
tree; this plan runs each gate as written and records the actual output rather than a
judgement about it.
Output: an executed `10-VALIDATION.md` with per-row status and recorded SC-5 evidence. No
code, no doc rewrite, no deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-adrs-and-doc-correctness/10-CONTEXT.md
@.planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md

Wave 3. Every doc change has landed: plans 10-01 and 10-02 created `docs/adr/`, plan 10-03
corrected `docs/OPERATIONS.md` and `README.md`, plan 10-04 corrected and forward-linked
`docs/DESIGN.md`.

**Phase base commit: `715e9aa`** — the tip of `main` when Phase 10 planning began, after
Phase 10.5 shipped. All regression diffs are taken against it.

**Regression baseline: exactly `388 passed, 28 skipped`.** Confirmed by running the suite at
planning time. Any deviation means this phase touched code and is a red gate, not a new
baseline.

**Live host:** `https://research-agent.fly.dev`, Fly app `research-agent`, currently at
release **v4** shipped 2026-08-04.

**macOS grep caution:** BSD `grep -rn … .` emits paths without a `./` prefix, so an
exclusion written `grep -v '^./.planning'` never matches and the gate passes vacuously.
Every gate below names its search roots (`docs`, `README.md`, `docs/adr/`) and counts with
`wc -l`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate battery and the regression gates</name>
  <files>.planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md` § Per-Task Verification Map — the fourteen rows, each with its Task ID
    - `.planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md` § Wave 0 Requirements — the BSD grep caution
    - The four SUMMARY files `10-01-SUMMARY.md` … `10-04-SUMMARY.md` — what each plan claims it did
  </read_first>
  <action>
    Run every gate below from the repo root, capture the literal output of each, and hold the
    results for Task 3. Do not edit any document to make a gate pass — a red gate is a finding
    and gets reported, not fixed by rewording the gate.

    ADR set (SC-1, SC-2):
    count `docs/adr/000[1-5]-*.md` expecting 5; assert no file is missing a
    `**Status:** Accepted` line; assert each of the five carries `## Context`, `## Decision`
    and `## Consequences`; assert each contains `DESIGN.md`; assert the five subject tokens
    `deterministic`, `critic`, `no_prior_research`, `checkpointer`, `judge` each match at
    least one record; assert `Superseded by ADR-` appears in `docs/adr/README.md`; assert
    `docs/DESIGN.md` links five distinct `adr/000N-*.md` paths and every one resolves.

    Doc corrections (SC-3, SC-4, SC-6):
    `grep -rn 'GitHub integration' docs README.md | wc -l` is 0; `Deploys are manual` and
    both `fly deploy -a research-agent` and `fly releases -a research-agent` appear in
    `docs/OPERATIONS.md`; `three implementations` is gone from `docs/DESIGN.md` and
    `four implementations` shares its line with `pgvector`;
    `grep -rn '2/\$10' docs README.md | grep -v '3/\$15' | wc -l` is 0; `/pricing` appears in
    both `docs/DESIGN.md` and `README.md`.

    Regression:
    `git diff --quiet 715e9aa -- src/` exits 0; widen it to `git diff --stat 715e9aa -- src/
    tests/ evals/ pyproject.toml` and confirm the output is empty; `.venv/bin/pytest` (bare,
    no extra `-q`) reports exactly `388 passed, 28 skipped`; `ruff check .` is clean.

    Non-vacuity check, run once: deliberately confirm two gates can fail — run the
    `GitHub integration` search against a string that does exist (`fly deploy`) and confirm it
    returns a non-zero count, and run the ADR count against a glob that matches nothing and
    confirm it returns 0. A gate battery that has never been observed failing has not been
    tested.
  </action>
  <verify>
    <automated>set -e; [ "$(ls docs/adr/000[1-5]-*.md | wc -l | tr -d ' ')" -eq 5 ]; [ "$(grep -L '^\*\*Status:\*\* Accepted' docs/adr/000[1-5]-*.md | wc -l | tr -d ' ')" -eq 0 ]; [ "$(grep -L 'DESIGN.md' docs/adr/000[1-5]-*.md | wc -l | tr -d ' ')" -eq 0 ]; [ "$(grep -rn 'GitHub integration' docs README.md | wc -l | tr -d ' ')" -eq 0 ]; [ "$(grep -c 'three implementations' docs/DESIGN.md | tr -d ' ')" -eq 0 ]; [ "$(grep -rn '2/\$10' docs README.md | grep -v '3/\$15' | wc -l | tr -d ' ')" -eq 0 ]; [ "$(grep -o 'adr/000[1-5]-[a-z0-9-]*\.md' docs/DESIGN.md | sort -u | wc -l | tr -d ' ')" -eq 5 ]; git diff --quiet 715e9aa -- src/; [ "$(.venv/bin/pytest 2>&1 | tail -3 | grep -c '388 passed, 28 skipped' | tr -d ' ')" -eq 1 ]; ruff check . ; echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `ls docs/adr/000[1-5]-*.md | wc -l` returns 5
    - `grep -L '^\*\*Status:\*\* Accepted' docs/adr/000[1-5]-*.md | wc -l` returns 0
    - `for f in docs/adr/000[1-5]-*.md; do grep -q '^## Context' "$f" && grep -q '^## Decision' "$f" && grep -q '^## Consequences' "$f" || echo "MISSING $f"; done` prints nothing
    - `grep -L 'DESIGN.md' docs/adr/000[1-5]-*.md | wc -l` returns 0
    - `for t in deterministic critic no_prior_research checkpointer judge; do [ "$(grep -il "$t" docs/adr/000[1-5]-*.md | wc -l | tr -d ' ')" -ge 1 ] || echo "UNCOVERED $t"; done` prints nothing
    - `grep -c 'Superseded by ADR-' docs/adr/README.md` returns ≥ 1
    - `grep -o 'adr/000[1-5]-[a-z0-9-]*\.md' docs/DESIGN.md | sort -u | wc -l` returns 5, and each resolves under `docs/`
    - `grep -rn 'GitHub integration' docs README.md | wc -l` returns 0
    - `grep -c 'Deploys are manual' docs/OPERATIONS.md` returns ≥ 1
    - `grep -c 'three implementations' docs/DESIGN.md` returns 0; `grep 'four implementations' docs/DESIGN.md | grep -c 'pgvector'` returns 1
    - `grep -rn '2/\$10' docs README.md | grep -v '3/\$15' | wc -l` returns 0
    - `git diff --quiet 715e9aa -- src/` exits 0
    - `git diff --stat 715e9aa -- src/ tests/ evals/ pyproject.toml` produces no output
    - `.venv/bin/pytest 2>&1 | tail -3 | grep -c '388 passed, 28 skipped'` returns 1
    - `ruff check .` exits 0
    - The non-vacuity check is recorded: a control search that should match returns non-zero, proving the gate shape can fail
  </acceptance_criteria>
  <done>Every gate has been run as written on macOS with its literal output captured, the non-vacuity control is recorded, `src/` is byte-identical to `715e9aa`, the suite reports 388/28 and `ruff check .` is clean — or any red gate is captured verbatim as a finding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SC-5 — re-verify the live release matches main (do not redeploy)</name>
  <read_first>
    - `.planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md` § Manual-Only Verifications — the SC-5 row and its "re-verify only" instruction
    - `.planning/ROADMAP.md` § Phase 10.5 **Outcome** — the v4 cutover that already carried the README restructure, the `src/` reorganisation and its bugfix
    - `.planning/ROADMAP.md` line 129 — SC-5 as written
  </read_first>
  <what-built>
    Nothing was deployed. Phase 10.5's cutover shipped Fly release **v4** on 2026-08-04,
    carrying the README restructure, the `src/` reorganisation and its bugfix, so the deployed
    tree already equals `main`. This phase added documentation only. Before pausing, Claude
    runs and reports the evidence:

    - `fly releases -a research-agent` — expect v4 or later, status healthy
    - `curl -s -o /dev/null -w '%{http_code}\n' https://research-agent.fly.dev/` and the same
      for `/health`, `/demo`, `/metrics` — expect `200` from each
    - `git rev-list --count origin/main..main` — expect 0 (nothing unpushed)
    - `git diff --quiet 715e9aa -- src/` — expect exit 0 (this phase moved no code, so the
      deployed image is still current)

    If the Fly CLI is not authenticated, report that and fall back to the four HTTP probes
    plus the git checks.
  </what-built>
  <how-to-verify>
    1. Read the four HTTP status codes Claude reported. All four must be `200`.
    2. Read the `fly releases -a research-agent` output. The current release must be **v4 or
       later** and healthy. Confirm it is attributed to your personal account — this is also
       the evidence behind the `docs/OPERATIONS.md` correction in plan 10-03.
    3. Confirm nothing was deployed during this phase: no `fly deploy` was run.
    4. If anything has drifted since 2026-08-04 — a rolled-back release, a non-200, or commits
       on `main` that are not in the deployed image — say so rather than approving. Drift is a
       **finding for the phase**, not something to fix by redeploying inside a docs phase.
  </how-to-verify>
  <acceptance_criteria>
    - Four recorded HTTP status codes, all `200`, for `/`, `/health`, `/demo`, `/metrics` on `https://research-agent.fly.dev`
    - Recorded `fly releases -a research-agent` output showing v4 or later (or an explicit note that the CLI is unauthenticated, with the HTTP evidence standing in)
    - `git rev-list --count origin/main..main` returns 0
    - `git diff --quiet 715e9aa -- src/` exits 0
    - No `fly deploy` command appears anywhere in this plan's execution record
  </acceptance_criteria>
  <resume-signal>Type "approved" to record SC-5 as re-verified, or describe the drift you saw.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the results in 10-VALIDATION.md</name>
  <files>.planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md` — all fourteen rows, the sign-off checklist, and the frontmatter
    - The captured gate outputs from Task 1 and the SC-5 evidence approved in Task 2
  </read_first>
  <action>
    Update `.planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md` in place.

    Set every row's Status column in § Per-Task Verification Map from the Task 1 and Task 2
    results: `✅ green` or `❌ red`. Leave no row `⬜ pending`. Do not change the Task ID, Plan,
    Wave, Requirement, Criterion or Automated Command columns — those are the contract as
    planned, and rewriting a command after running it is how a gate becomes decorative.

    Append a short `## Execution Record` section below the map holding the literal evidence:
    the ADR file count, the suite line (`388 passed, 28 skipped`), the `git diff --quiet
    715e9aa -- src/` result, the `ruff check .` result, the non-vacuity control result, and
    the SC-5 evidence (release number, four HTTP codes, `origin/main..main` count) with the
    date it was checked. Mark SC-5 explicitly as **re-verified, not redeployed**.

    In frontmatter set `status: complete` and `nyquist_compliant: true`, and tick the
    § Validation Sign-Off checklist. Set `**Approval:**` to approved with the date.

    If any gate came back red, do not tick its box: record the red row, add the finding under
    `## Execution Record`, and leave `**Approval:**` as `pending — <what is red>`.

    Do not edit `docs/`, `README.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` or any
    historical `.planning/` document from this task — the phase-close workflow owns those, and
    planning history stays as written.
  </action>
  <verify>
    <automated>f=.planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md; [ "$(grep -c '⬜ pending' $f | tr -d ' ')" -eq 0 ] && [ "$(grep -c 'nyquist_compliant: true' $f | tr -d ' ')" -eq 1 ] && [ "$(grep -c '## Execution Record' $f | tr -d ' ')" -eq 1 ] && [ "$(grep -c '388 passed, 28 skipped' $f | tr -d ' ')" -ge 1 ] && [ "$(grep -Eo '10-0[1-5]-T[1-3]' $f | sort -u | wc -l | tr -d ' ')" -ge 9 ] && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '⬜ pending' .planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md` returns 0
    - `grep -c 'nyquist_compliant: true' …/10-VALIDATION.md` returns 1
    - `grep -c '## Execution Record' …/10-VALIDATION.md` returns 1
    - `grep -c '388 passed, 28 skipped' …/10-VALIDATION.md` returns ≥ 1
    - `grep -c 're-verified' …/10-VALIDATION.md` returns ≥ 1 and `grep -ci 'redeploy' …/10-VALIDATION.md` returns ≥ 1 in the "not redeployed" sense
    - `grep -Eo '10-0[1-5]-T[1-3]' …/10-VALIDATION.md | sort -u | wc -l` returns ≥ 9 — every mapped Task ID survives the edit (the map uses nine distinct ids across fourteen rows)
    - `git status --porcelain docs README.md src tests evals | wc -l` returns 0 — this task edits only the validation document
  </acceptance_criteria>
  <done>`10-VALIDATION.md` has a status on every row, an Execution Record holding literal gate output and SC-5 evidence marked re-verified rather than redeployed, `nyquist_compliant: true`, and a sign-off that is approved only if every gate came back green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate output → recorded status | A status written from judgement rather than from literal output makes the whole contract decorative |
| docs phase → production code | A docs phase that moves `src/` or the test count has silently done something else |
| deployed release → `main` | SC-5 is a claim about a host this repo does not control; it can drift between the cutover and phase close |
| grep gate → macOS BSD grep | The Phase 10.5 failure mode: a filter that never matches, so the gate certifies nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-18 | Denial of service (vacuous gate) | the whole gate battery | mitigate | Task 1 includes an explicit non-vacuity control — a search that must match and a glob that must not — and its result is recorded in the Execution Record |
| T-10-19 | Tampering | production code drift under a docs phase | mitigate | `git diff --quiet 715e9aa -- src/` plus a widened `--stat` over `src/ tests/ evals/ pyproject.toml`, and an exact-count suite assertion at 388/28 |
| T-10-20 | Repudiation | SC-5 evidence | mitigate | Recorded as literal `fly releases` output plus four HTTP status codes with a check date; a blocking human checkpoint confirms no deploy was run |
| T-10-21 | Elevation of privilege (scope creep) | redeploying inside a docs phase | mitigate | Task 2 forbids `fly deploy`, and drift is routed to a finding rather than to a fix |
| T-10-22 | Tampering | rewriting a gate after it failed | mitigate | Task 3 forbids editing the Criterion and Automated Command columns and requires red rows to be recorded with the approval left pending |
| T-10-SC | Tampering | package installs | accept | This plan installs nothing — verification and record-keeping only |
</threat_model>

<verification>
1. Every row of § Per-Task Verification Map is `✅ green` or `❌ red`; none `⬜ pending`
2. `git diff --quiet 715e9aa -- src/` exits 0
3. `.venv/bin/pytest 2>&1 | tail -1` reports `388 passed, 28 skipped`
4. `ruff check .` exits 0
5. `grep -c 'nyquist_compliant: true' .planning/phases/10-adrs-and-doc-correctness/10-VALIDATION.md` returns 1
6. The Execution Record contains the SC-5 evidence with a check date and the words re-verified, not redeployed
</verification>

<success_criteria>
- All fourteen validation rows carry a real status derived from literal command output
- The non-vacuity control proves the gate shapes can fail
- `src/`, `tests/`, `evals/` and `pyproject.toml` are unchanged since `715e9aa`; suite 388/28; `ruff check .` clean
- SC-5 re-verified against the live host with evidence recorded and nothing redeployed
- `10-VALIDATION.md` is signed off, or approval is explicitly left pending with the red rows named
</success_criteria>

<output>
Create `.planning/phases/10-adrs-and-doc-correctness/10-05-SUMMARY.md` when done.
</output>
