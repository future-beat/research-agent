---
phase: 11-multi-machine-state-and-pooled-postgres
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - tests/test_deploy_config.py
  - fly.toml
  - docs/OPERATIONS.md
  - README.md
autonomous: true
requirements: [REQ-multi-machine-state, REQ-connection-pool]
user_setup: []

must_haves:
  truths:
    - "Removing [[mounts]] can no longer silently disarm a deploy guard — both guards assert in both worlds (SC-5)"
    - "The deploy config guards the stateless topology as an invariant, not as a comment"
    - "Neither fly.toml nor OPERATIONS.md instructs an operator to run the unsupported `fly postgres create`"
    - "PG_CONNECT_TIMEOUT's pooled semantics are documented, and distinguished from PG_POOL_TIMEOUT (SC-6)"
    - "The documented cutover is the external-provider sequence actually being used, in its load-bearing order"
    - "A reader is told what was knowingly given up — the cumulative /metrics history — rather than left to notice"
  artifacts:
    - path: "tests/test_deploy_config.py"
      provides: "Two-armed topology guards plus a runbook-staleness guard"
      contains: "min_machines_running"
    - path: "docs/OPERATIONS.md"
      provides: "Supabase cutover runbook, pooled timeout semantics, pool env vars"
      contains: "PG_POOL_TIMEOUT"
    - path: "fly.toml"
      provides: "Corrected runbook footer and corrected health-budget comment"
  key_links:
    - from: "tests/test_deploy_config.py"
      to: "fly.toml [[mounts]] / min_machines_running"
      via: "two-armed assertion with no skip path"
      pattern: "min_machines_running"
    - from: "docs/OPERATIONS.md"
      to: "PG_POOL_TIMEOUT / PG_CONNECT_TIMEOUT"
      via: "env table rows plus prose distinguishing caller wait from connect attempt"
      pattern: "PG_POOL_TIMEOUT"
---

<objective>
Arm the deploy guards for the topology the phase is about to create, and correct the documentation
that currently instructs an operator down a dead path.

Purpose: SC-5 and SC-6. Two guards in `tests/test_deploy_config.py` **`pytest.skip()` when there is
no mount**, so deleting `[[mounts]]` in plan 11-05 would turn them into no-ops and CI would stay
green over an unguarded topology. Converting both into two-armed assertions is the substance of
SC-5. Separately, `fly.toml` and `docs/OPERATIONS.md` both document `fly postgres create` — the now
**unsupported** unmanaged Fly Postgres path — as the way to go stateless.

Output: guards that cannot be disarmed by deletion, and a runbook that describes the sequence this
phase actually executes.

**This plan does not touch the topology itself.** `[[mounts]]`, the three `*_DB_PATH` env vars and
`min_machines_running` stay exactly as they are — they change in plan 11-05, after the database is
proven, because a machine with neither a volume nor a reachable database is a broken machine. The
two-armed guards written here pass in both worlds, which is the whole point of writing them first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-multi-machine-state-and-pooled-postgres/11-CONTEXT.md
@.planning/phases/11-multi-machine-state-and-pooled-postgres/11-RESEARCH.md
@.planning/phases/11-multi-machine-state-and-pooled-postgres/11-VALIDATION.md
@tests/test_deploy_config.py
@fly.toml
@docs/OPERATIONS.md

<interfaces>
<!-- The exact current state of the two traps, so no exploration is needed. -->

tests/test_deploy_config.py:76  test_local_store_paths_live_under_the_mount
    mounts = fly.get("mounts") or []
    if not mounts:
        pytest.skip("no volume mounted; stores are presumably on Postgres")   # <- the trap

tests/test_deploy_config.py:94  test_a_volume_means_a_single_machine
    if not (fly.get("mounts") or []):
        pytest.skip("no volume; the app is stateless and can scale out")      # <- the trap
    assert fly["http_service"].get("min_machines_running", 1) <= 1

Verified grep baselines in the tree today (each gate below is falsifiable against these):
    grep -c 'fly postgres create' fly.toml            -> 1
    grep -c 'fly postgres create' docs/OPERATIONS.md  -> 2
    grep -c 'PG_POOL_TIMEOUT'     docs/OPERATIONS.md  -> 0
    grep -c 'PG_CONNECT_TIMEOUT'  docs/OPERATIONS.md  -> 1   (already present — see the warning below)
    grep -c 'pytest.skip'         tests/test_deploy_config.py -> 3  (one is the legitimate no-fly.toml fixture)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn the two skipping guards into two-armed assertions</name>
  <read_first>
    - `tests/test_deploy_config.py` in full — both traps, the `fly` fixture, and the module docstring explaining why these tests exist ("two near-misses, not tidiness")
    - `fly.toml:28-58` — the `[env]` block, the `[[mounts]]` block, `min_machines_running`
    - `.planning/phases/11-multi-machine-state-and-pooled-postgres/11-RESEARCH.md` § `tests/test_deploy_config.py` — the per-test change table
  </read_first>
  <action>
    Rewrite `test_local_store_paths_live_under_the_mount` so it asserts in both worlds instead of
    skipping in one. With a mount: keep today's assertion that every present `SESSION_DB_PATH` /
    `METRICS_DB_PATH` / `VECTOR_STORE_PATH` starts with the mount destination. **Without a mount:
    assert all three keys are absent from `[env]`.** Left in place after the volume goes, they point
    into an ephemeral container filesystem, and if `DATABASE_URL` were ever unset the backends
    silently fall back to per-machine SQLite/JSON — the exact split-brain this phase exists to
    remove, but now invisible, because the data no longer even survives a restart. Rename it to
    reflect that it now guards both directions.

    Rewrite `test_a_volume_means_a_single_machine` the same way. With a mount: keep
    `min_machines_running <= 1` — two machines with a volume each hold two independent databases and
    a follow-up routed to the wrong one 404s on a session that demonstrably exists; that invariant is
    still true and still worth guarding if a mount ever returns. **Without a mount: assert
    `min_machines_running >= 2`.** Rename accordingly.

    Neither test may retain a `pytest.skip` for the mount condition. The `fly` fixture's
    `pytest.skip("no fly.toml")` is legitimate and stays.

    Add a new test asserting the deploy runbook is not stale: read `fly.toml` as text and assert
    `fly postgres create` does not appear. That command is the unsupported unmanaged Fly Postgres
    path, and this comment has already gone stale once. Name it so `-k runbook` selects it.

    Add a test asserting `DATABASE_URL` is **not** present in `fly.toml` — it is a secret, the file
    is committed, and it must be set with `fly secrets set`. Assert on the parsed `[env]` table, not
    on the file text, so the explanatory comments may mention the variable name freely.

    Leave `test_fly_config_does_not_target_a_database_app` in place and unmodified. It guards against
    Fly's GitHub integration rewriting `app`, not against the Postgres product, so it stays relevant
    even though there is no longer a Fly Postgres app to point at. Update only its docstring's final
    sentence, which currently describes `fly postgres create` as the thing that makes the app.
  </action>
  <acceptance_criteria>
    - `grep -c 'pytest.skip' tests/test_deploy_config.py` returns `1` (today: `3`) — only the `fly` fixture's no-fly.toml skip survives
    - `grep -c 'min_machines_running' tests/test_deploy_config.py` returns at least `2` (today: `1`) — both arms present
    - `.venv/bin/pytest tests/test_deploy_config.py -v` shows `0` skipped and `0` failed against today's `fly.toml`, which still has the mount
    - Falsification check, run and recorded in the summary: with a scratch copy of `fly.toml` that has `[[mounts]]` deleted and `min_machines_running = 1`, the machine-count guard **fails**. Under the old code it skipped. Restore the real file afterwards
    - Second falsification check: with a scratch copy that has `[[mounts]]` deleted, `min_machines_running = 2` and `SESSION_DB_PATH` still in `[env]`, the store-paths guard **fails**
    - A test selectable by `-k runbook` exists and passes only because `fly postgres create` is absent from `fly.toml` — so it **fails against today's file** until Task 2 lands. Run Task 2 first if you prefer a green intermediate commit; either order is fine so long as both land together
    - A test asserts `DATABASE_URL` is not a key in `fly.toml`'s `[env]`
    - `.venv/bin/ruff check .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_deploy_config.py -v</automated>
  </verify>
  <done>Both topology guards assert in both worlds with no skip path; deleting the mount in plan 11-05 now flips them to their second arm instead of silencing them; the runbook and secret guards exist.</done>
</task>

<task type="auto">
  <name>Task 2: Correct fly.toml's comments — the runbook, the health budget, and the region pin</name>
  <read_first>
    - `fly.toml` in full — the header (lines 1-20), the `[env]` comment, the `[[mounts]]` comment, the `auto_stop_machines` comment, the health-check comment (lines 65-72), the "Going stateless (Phase 8)" footer (lines 87-99)
    - `.planning/phases/11-multi-machine-state-and-pooled-postgres/11-RESEARCH.md` § `fly.toml` Changes and § The `/health` Timing Budget
    - `.planning/phases/11-multi-machine-state-and-pooled-postgres/11-VALIDATION.md` § Cutover Safety
  </read_first>
  <action>
    Comment-only edits. **Change no key and no value in this task** — `[[mounts]]`, the three
    `*_DB_PATH` vars and `min_machines_running = 1` all stay until plan 11-05.

    Replace the "Going stateless (Phase 8)" footer. It documents `fly postgres create` /
    `fly postgres attach`, which is the unsupported unmanaged Fly Postgres path; Fly's own CLI now
    prints that unmanaged Fly Postgres is unsupported and that users are responsible for operations,
    management and disaster recovery. Write the external-provider sequence instead, in its
    load-bearing order: provision Supabase in `ap-southeast-2` with the `vector` extension enabled;
    `fly secrets set DATABASE_URL=...` against the **session-mode Supavisor pooler**
    (`aws-<n>-ap-southeast-2.pooler.supabase.com:5432`, IPv4 — the direct endpoint is IPv6-only on
    the free tier) with `sslmode=require`; deploy; verify `/health`; **only then** remove `[[mounts]]`
    and the three `*_DB_PATH` vars; then `fly scale count 2`. State that there is no attach step —
    `fly secrets set` replaces `fly postgres attach` — and that the volume is kept, not destroyed,
    because detaching is reversible and destroying is not. State that steps up to the mount removal
    are reversible and that removing the mount is the point of no return for per-machine state.

    Correct the health-check comment. It currently claims the 15 s timeout "allows for three store
    probes each bounded by `PG_CONNECT_TIMEOUT` (3s)". That arithmetic is wrong by a factor of two:
    the pre-pool `cursor()` made **two** 3 s connect attempts per probe, so three probes could cost
    up to **18 s against a 15 s timeout**. It has not bitten only because the database was on local
    disk. Write the corrected bound: a caller's wait is now bounded by `PG_POOL_TIMEOUT` (2 s), so
    three sequential probes cost at most ~6 s inside the 15 s window, with `PoolTimeout` deliberately
    excluded from the retry so an outage cannot double it.

    Rewrite the comment above `auto_stop_machines`, which currently explains why a second machine is
    unsafe — the opposite of what becomes true. Keep it honest about the ordering: a second machine
    is safe *once* `DATABASE_URL` is set and the mount is gone, not before.

    Rewrite the header's "primary_region is pinned to the volume" note: once the volume is detached
    the pin becomes a *choice* — proximity to the Supabase region — rather than a constraint.
    Changing region is explicitly out of scope for this phase; say so.

    Add a short comment recording the concurrency link, because it is the justification for the pool
    and it belongs next to the numbers it derives from: `hard_limit = 16` × 2 machines = up to 32
    in-flight requests against one database, which is why `PG_POOL_MAX_SIZE` defaults to 5 (10 of
    Supabase Nano's 60 connections across the fleet).

    Do not touch the "don't merge Fly's Launch PRs" header block; it is unrelated and still true.
  </action>
  <acceptance_criteria>
    - `grep -c 'fly postgres create' fly.toml` returns `0` (today: `1`)
    - `grep -c 'fly postgres attach' fly.toml` returns `0` (today: `2`)
    - `grep -c 'PG_POOL_TIMEOUT' fly.toml` returns at least `1` (today: `0`)
    - `grep -c 'sslmode=require' fly.toml` returns at least `1` (today: `0`)
    - `grep -c 'pooler.supabase.com' fly.toml` returns at least `1` (today: `0`)
    - The values are untouched: `python3 -c "import tomllib;f=tomllib.load(open('fly.toml','rb'));assert f['http_service']['min_machines_running']==1;assert f['mounts'];assert 'SESSION_DB_PATH' in f['env'];print('topology unchanged')"` prints `topology unchanged`
    - `.venv/bin/pytest tests/test_deploy_config.py` reports `0` failed and `0` skipped
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_deploy_config.py -k "runbook or parses or database_app"</automated>
  </verify>
  <done>`fly.toml` documents the external-provider cutover in its correct order, states the corrected health budget, and no longer names an unsupported command — with every key and value unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the OPERATIONS runbook and the README claims the phase falsifies</name>
  <read_first>
    - `docs/OPERATIONS.md` — the "Going stateless" section (~line 75) and the environment-variable table (~line 138)
    - `README.md:187-191` and `:205` — the "SQLite pins you to one machine" claim and the `DATABASE_URL` paragraph; also `:13` (the test count) and `:29` (the phase list)
    - `.planning/phases/11-multi-machine-state-and-pooled-postgres/11-RESEARCH.md` § Provider Decision, § Pool Sizing, § Common Pitfalls
    - `.planning/phases/11-multi-machine-state-and-pooled-postgres/11-CONTEXT.md` § Data: start clean
  </read_first>
  <action>
    Rewrite `docs/OPERATIONS.md`'s "Going stateless" section for the Supabase path, mirroring the
    corrected `fly.toml` footer but with the operator detail: the ordered cutover, which steps are
    reversible, and the explicit instruction **not** to destroy the volume. Record why Supabase and
    not Neon in one paragraph, because it is a defensible engineering story rather than a coin flip:
    `/health` is the liveness probe and deliberately queries all three stores, Fly runs it every 30 s
    per machine, so with two machines the database is queried roughly four times a minute forever.
    Neon's free plan meters compute at **100 CU-hours per project per month** (≈400 h at 0.25 CU) and
    a compute kept permanently awake by those probes bills ~730 h/month, so the project would be
    suspended around day 16 of every month — dropping existing connections and refusing new ones.
    Supabase's free tier has **no compute meter**, and its only idle hazard, a pause after ~7 days of
    low activity, is *prevented* by the same probes. The design that disqualifies one provider is the
    design that keeps the other alive.

    State plainly, in the same section, that this cutover starts against an **empty** database with
    no data migration, that the volume is kept as the backup, and that what is knowingly given up is
    the cumulative `/metrics` history — the two orphaned notes and the old demo sessions are
    disposable. Say `research_agent.migrate` is deliberately **not** exercised, so a later phase
    needing a real migration must not assume that path is proven. Remove the two
    `python -m research_agent.migrate` lines from the runbook block so nobody follows them by
    accident.

    Add the Supabase-specific operational notes: `sslmode=require` is mandatory and libpq's default
    is `prefer`, which silently accepts plaintext — `db.py` passes the DSN through untouched, so
    whatever is in the secret is what is used and nothing in the code will add it for you. Note the
    session-mode pooler host shape and why (IPv4; the direct endpoint is IPv6-only on free). Note
    that a paused free-tier project is restored manually from the dashboard and loses no data.

    Extend the environment-variable table with `PG_POOL_MIN_SIZE` (`1`), `PG_POOL_MAX_SIZE` (`5`) and
    `PG_POOL_TIMEOUT` (`2.0`), and **rewrite the existing `PG_CONNECT_TIMEOUT` row** — this is SC-6.
    The two are now different things and the distinction is the whole point: `PG_CONNECT_TIMEOUT`
    still bounds a single libpq connect attempt, but that attempt is now made by a pool background
    worker, so it no longer bounds how long a *caller* waits; `PG_POOL_TIMEOUT` does. Add a short
    prose paragraph beneath the table saying exactly that, and give the `/health` arithmetic that
    follows from it (three sequential probes × 2 s = 6 s inside Fly's 15 s check timeout).

    Add the spend-cap interaction as a flagged, unfixed note: the daily cap counts only *completed*
    runs, so ~16 concurrent runs can already overshoot `DEMO_DAILY_USD_CAP` by ~3×, and at 32
    concurrent requests across two machines that ceiling roughly doubles. Nothing here makes the race
    more likely per request, but the phase raises how far a burst can exceed the cap. Point at
    Phase 12's `REQ-store-lifecycle-and-ownership`. Silently doubling a money-bounding failure mode
    is the sort of thing a reviewer notices.

    In `README.md`: correct line ~205, which asserts SQLite pins the service to one machine "until
    then"; correct the `DATABASE_URL` paragraph at ~187 to describe the shipped state rather than a
    future one; add Supabase to the stack line as the second vendor; and update the test count on
    line 13 to match `.venv/bin/pytest --collect-only -q | tail -1`. Do not touch the phase checklist
    at line 29 — the phase is not complete until plan 11-05 verifies SC-3 live.
  </action>
  <acceptance_criteria>
    - `grep -c 'fly postgres create' docs/OPERATIONS.md` returns `0` (today: `2`)
    - `grep -c 'research_agent.migrate' docs/OPERATIONS.md` returns `0` in the runbook code block; any remaining mention must be the deliberate "not exercised" note — verify by reading, and record the count in the summary
    - `grep -c 'PG_POOL_TIMEOUT' docs/OPERATIONS.md` returns at least `2` (today: `0`) — one table row plus the prose paragraph
    - `grep -c 'PG_POOL_MIN_SIZE' docs/OPERATIONS.md` and `grep -c 'PG_POOL_MAX_SIZE' docs/OPERATIONS.md` each return at least `1` (today: `0`)
    - **SC-6 gate, strengthened.** `11-VALIDATION.md` proposes `grep -c 'PG_CONNECT_TIMEOUT' docs/OPERATIONS.md ≥ 1`, which **already returns 1 today** and would therefore pass without any work being done. It is replaced by: `grep -v '^|' docs/OPERATIONS.md | grep -c 'PG_POOL_TIMEOUT'` returns at least `1` (prose, not just a table row), **and** the `PG_CONNECT_TIMEOUT` table row text differs from its current text `Seconds before a connection attempt gives up`, **and** a paragraph exists containing both variable names. Record the reasoning in the summary so the substitution reads as deliberate; no gate in the map had been run at the time of this change
    - `grep -c 'Supabase' docs/OPERATIONS.md` returns at least `3` (today: `0`)
    - `grep -c 'sslmode=require' docs/OPERATIONS.md` returns at least `1` (today: `0`)
    - `grep -c 'DEMO_DAILY_USD_CAP' docs/OPERATIONS.md` — a paragraph exists naming both the cap and the 32-concurrent interaction
    - `grep -c 'Supabase' README.md` returns at least `1` (today: `0`)
    - README's stated test count equals the number reported by `.venv/bin/pytest --collect-only -q | tail -1`
    - `.venv/bin/pytest tests/test_deploy_config.py` reports `0` failed
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/pytest tests/test_deploy_config.py && grep -c 'PG_POOL_TIMEOUT' docs/OPERATIONS.md</automated>
  </verify>
  <done>The runbook describes the Supabase cutover in its correct order with the volume kept; SC-6's pooled-timeout semantics are documented in prose as well as in the table; the start-clean tradeoff and the spend-cap interaction are stated rather than left to be noticed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed repo → the public | `fly.toml`, `README.md` and `docs/OPERATIONS.md` are all public; a DSN in any of them is a disclosed credential |
| documentation → future operator | A stale runbook is an instruction to do the wrong thing under time pressure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Information disclosure | `DATABASE_URL` in a committed file | mitigate | Task 1 adds a test asserting `DATABASE_URL` is absent from `fly.toml`'s `[env]`; docs use a placeholder host and never a real password |
| T-11-11 | Information disclosure | plaintext transport to the provider | mitigate | `sslmode=require` documented in both `fly.toml` and OPERATIONS, with the reason (libpq defaults to `prefer`, which silently accepts plaintext, and `db.py` passes the DSN through untouched) |
| T-11-12 | Tampering | a deploy guard that skips instead of asserting | mitigate | Both mount-conditional guards become two-armed; `pytest.skip` count drops to `1`, and the falsification checks in Task 1 prove each arm actually fails when violated |
| T-11-13 | Denial of service | documented pool sizing exceeding the provider tier | accept | 2 machines × `max_size` 5 = 10 of 60 connections; documented so a future raise is a deliberate decision against a recorded ceiling |
</threat_model>

<verification>
- `.venv/bin/pytest tests/test_deploy_config.py -v` — `0` failed, `0` skipped
- Both falsification checks in Task 1 run against scratch copies and recorded in the summary; the real `fly.toml` restored afterwards
- `.venv/bin/pytest` (bare) — no failures; skip count unchanged from plan 11-02's recorded figure, since this plan adds no Postgres-gated tests
- `.venv/bin/ruff check .` — exits 0
- `python3 -c "import tomllib; tomllib.load(open('fly.toml','rb'))"` — parses, and `min_machines_running` is still `1` with `[[mounts]]` still present
</verification>

<success_criteria>
- Neither mount-conditional guard can be disarmed by deleting `[[mounts]]`; both arms proved to fail when violated
- `pytest.skip` appears once in `tests/test_deploy_config.py`, in the `fly` fixture
- `fly postgres create` and `fly postgres attach` appear nowhere in `fly.toml` or `docs/OPERATIONS.md`
- `fly.toml`'s health-check comment states the corrected `PG_POOL_TIMEOUT`-based bound instead of the wrong 3×3 s arithmetic
- The topology values in `fly.toml` are byte-for-byte unchanged — mount, env vars and `min_machines_running = 1` all still present
- `PG_POOL_MIN_SIZE` / `PG_POOL_MAX_SIZE` / `PG_POOL_TIMEOUT` documented, and `PG_CONNECT_TIMEOUT`'s pooled semantics rewritten in both the table and prose (SC-6)
- The start-clean tradeoff, the kept volume, the unexercised `migrate` path and the spend-cap interaction are all stated explicitly
</success_criteria>

<output>
Create `.planning/phases/11-multi-machine-state-and-pooled-postgres/11-03-SUMMARY.md` when done.
Record: the two falsification-check results, the SC-6 gate substitution and its reasoning, and the
`research_agent.migrate` mention count remaining in OPERATIONS with justification for each.
</output>
