---
phase: 18-independent-eval-judge
plan: 04
type: execute
wave: 4
depends_on: [18-03]
files_modified:
  - evals/harness.py
  - evals/graders.py
  - docs/OPERATIONS.md
  - docs/DESIGN.md
  - README.md
  - tests/test_evals.py
autonomous: true
requirements: [REQ-judge-independent-of-critic]

must_haves:
  truths:
    - "At shipped defaults (judge claude-opus-4-8, production critic claude-opus-5), NO collision line fires on a production-shaped record run — proven by a silent-at-new-defaults twin"
    - "An operator-created collision still fires the line once per run, worded as a fact not a fault, pointing at ADR-0012 — a record that exists and whose status agrees (held by 18-03's chain test)"
    - "No doc surface outside README's Limitations bullet asserts judge == critic; the bullet itself is untouched (Phase 22 deletes it) and the transient contradiction is recorded"
    - "README's test count matches the measured post-phase count"
  artifacts:
    - path: "evals/harness.py"
      provides: "_state_judge_critic_relation re-derived: docstring premise and message wording match the new defaults, pointer moved to ADR-0012"
      contains: "ADR-0012"
    - path: "evals/graders.py"
      provides: "module docstring rewritten: judge independent of the critic by model identity, recorded in ADR-0012"
    - path: "docs/OPERATIONS.md"
      provides: "record-mode paragraph re-derived; one line documenting EVAL_JUDGE_MODEL as the evals-CLI override"
    - path: "docs/DESIGN.md"
      provides: "Testing-section trailer extended to ADR-0012"
  key_links:
    - from: "_state_judge_critic_relation stderr line"
      to: "docs/adr/0012-judge-independent-of-the-critic.md"
      via: "the ADR-0012 pointer, resolution guaranteed by the 18-03 chain test (extended, not duplicated)"
      pattern: "ADR-0012"
---

<objective>
Re-derive everything whose premise inverted when the judge left the critic's model: the
operator-facing collision line and its wording tests, the graders docstring, OPERATIONS'
record-mode paragraph, DESIGN's trailer — and close the phase with the full gate.

Purpose: the 16-02 lesson is the spine of this plan — the collision warning fires on
configuration, its wording is the deliverable, and after the flip a collision is
operator-created rather than the deployed arrangement. The line must not misinform the
operator on the one run they read it. Phase 22's README pass must find no contradictions
anywhere else.

Output: re-derived line + tests (including the silent-at-new-defaults twin), clean doc
surfaces with measured grep baselines, corrected README counts, and the phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-independent-eval-judge/18-CONTEXT.md
@.planning/phases/18-independent-eval-judge/18-RESEARCH.md
@.planning/phases/18-independent-eval-judge/18-VALIDATION.md
@evals/harness.py
@evals/graders.py
@docs/OPERATIONS.md
@docs/DESIGN.md

State entering this wave: default flipped (18-01), guard landed (18-02), ADR-0012 exists
with the chain test holding 0005->0010->0012 (18-03). The stderr line still says "This is
the deployed configuration and it is accepted, recorded in ADR-0010" — false on both
counts now: not deployed (defaults differ), and 0010 reads Superseded.

Measured grep baselines (taken 2026-08-13, planner session — re-measure before editing,
the 14-03 rule):
- `judge and the critic share` -> docs/OPERATIONS.md:803 (baseline 1)
- `in production the judge and the critic run` -> evals/graders.py:17 (baseline 1)
- README "740 tests" at :15 and :199 (baseline 2) — will be stale by the phase's added tests
</context>

<interfaces>
From evals/harness.py:632-667 (`_state_judge_critic_relation` — the MECHANISM survives:
None-guarded, fires once per record run on `judge.model == graph.critic_model()`; the
docstring and the printed message are what invert):

```python
if judge is None: return
critic = graph.critic_model()
if judge.model != critic: return
print("note: the judge and the in-graph critic both run on {critic}. ... This is the "
      "deployed configuration and it is accepted, recorded in ADR-0010.", file=sys.stderr)
```

From tests/test_evals.py — the five collision tests (:2516 fires-once, :2528
silent-when-differ, :2540 fact-not-fault, :2560 record-exists [extended by 18-03], :2580
judgeless-refusal-intact). :2516's docstring claims "the DEPLOYED configuration, not a
contrived one" — now false; :2540 REQUIRES the tokens "accepted" and "deployed" — no
longer the honest words.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (18-04.T1): Re-derive the collision line and its wording tests; add the silent-at-new-defaults twin</name>
  <files>evals/harness.py, tests/test_evals.py</files>
  <behavior>
    - Silent-at-new-defaults twin (NEW): `record_with_fakes`-style run with
      `FakeJudge(model=G.JUDGE_MODEL)` and `CRITIC_MODEL=claude-opus-5` (production's
      pin, monkeypatched) -> stderr carries NO collision line. This is the test that
      proves a production-shaped record run is now silent.
    - Fire case (existing :2516, kept): `FakeJudge(model="claude-opus-5")` +
      `CRITIC_MODEL=claude-opus-5` -> exactly one line for two cases, and the line names
      ADR-0012.
    - Wording (existing :2540, re-derived): fault words still forbidden ("misconfig",
      "error", "invalid", "should not", "must not", "fix"); "accepted"/"deployed" no
      longer required — the new required tokens pin the new facts.
  </behavior>
  <action>
    Do NOT touch the mechanism: the equality check, the once-per-run placement, the None
    guard all survive (18-RESEARCH Finding 3 — logic survives, premise inverts).

    Re-derive the docstring: at shipped defaults `JUDGE_MODEL` is `claude-opus-4-8` and
    production pins `CRITIC_MODEL` to `claude-opus-5`, so a production-shaped record run
    never sees this line. A collision is now OPERATOR-CREATED (either knob moved) — still
    legal (both are legitimate knobs), still a statement of fact about what colliding
    verdicts can claim, never a fault. The 16-02 wording rule stands: a line that calls
    the operator's own decision a mistake teaches them to skip the line.

    Re-derive the message. It must: name the shared model; state what colliding verdicts
    can no longer claim (independence of the critic's model — what one waves through the
    other is likelier to wave through); state that the SHIPPED default separates them;
    and point at ADR-0012 (the record that separated them — resolution is guaranteed by
    18-03's chain test; extend nothing there, per the extended-not-duplicated rule).
    Exact phrasing is discretion; whatever ships is what the tests pin. Decide
    deliberately whether the "both run on {model}" sentence shape survives (it is cheap
    continuity for :2516's count assertion) and pin what you decide.

    Update the tests:
    - :2516 — keep the mechanism (once per run); rewrite the docstring honestly (this IS
      now a contrived configuration, driven on purpose); update the pinned strings
      ("ADR-0010" -> "ADR-0012", and the count-substring if the wording moved).
    - :2528 silent-when-differ — should stay green untouched; confirm.
    - :2540 — re-derive required/forbidden token sets against the new message: keep all
      six forbidden fault-words; replace "accepted"/"deployed" with tokens that pin the
      new facts (the shared model name, "adr-0012", and a token from the
      operator-created/shipped-default statement). The wording is the deliverable —
      write the test so the honest sentence passes and a fault-implying or
      stale-premise sentence cannot.
    - ADD the silent-at-new-defaults twin per the behavior block. Name it so its
      docstring states what it guards: production-shaped runs are silent NOW; if a
      future phase re-collides the defaults, this test is the one that asks whether the
      wording is honest again.
    - :2580 judgeless-refusal — untouched, confirm green.

    Mutation probes, each observed then reverted:
    1. Make `_state_judge_critic_relation` print unconditionally (drop the equality
       early-return) -> the silent-at-new-defaults twin AND :2528 red; restore.
    2. Point the message back at ADR-0010 -> :2516/:2540's ADR-0012 assertions red;
       restore. (This is the env-driven wording check of validation row 6 — the fire
       case drives the collision via FakeJudge's model, the honest idiom given pitfall 4:
       JUDGE_MODEL binds at import, so monkeypatching the env var proves nothing here.)

    Commit: `feat(18-04): the collision line survives the flip with its premise re-derived`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_evals.py -k "collision"</automated>
  </verify>
  <done>All collision tests green including the new twin; both mutations observed red and
  reverted; message points at ADR-0012; no fault words; mechanism untouched.</done>
</task>

<task type="auto">
  <name>Task 2 (18-04.T2): Doc surfaces — graders docstring, OPERATIONS, DESIGN, README counts</name>
  <files>evals/graders.py, docs/OPERATIONS.md, docs/DESIGN.md, README.md</files>
  <action>
    Measure the grep baselines BEFORE editing (sentence-shaped, not word-shaped — the
    14-03/13-04 rule; a prose gate's non-vacuity comes from a measured nonzero baseline):
    `judge and the critic share` in docs/ (expect 1, OPERATIONS:803);
    `in production the judge and the critic run` in evals/ (expect 1, graders.py:17).
    Both must read 0 after the edits.

    evals/graders.py module docstring (:13-19): rewrite the final paragraph — the judge
    runs on `claude-opus-4-8`, its own model: independent of the writer it grades AND of
    the critic's model identity (not the critic's model FAMILY — carry the record's own
    honesty), recorded with the re-derived rationale in ADR-0012. Keep the
    judge-never-shares-the-writer's-model reasoning; it is carried forward, not
    superseded.

    docs/OPERATIONS.md record-mode paragraph (:798-806): the premise inverts — at shipped
    defaults no line fires on a production-shaped record run. Rewrite: the staleness-gate
    sentences stay (still true — the gate compares pipeline and critic, never the judge);
    the collision sentence becomes: the recorder prints a collision note only when an
    operator points the judge and critic at the same model, a legal configuration whose
    consequence for what verdicts can claim is recorded in ADR-0012. Add ONE line
    documenting `EVAL_JUDGE_MODEL` as the evals-CLI override defaulting to
    `claude-opus-4-8` (Open Question 3's discretion, taken minimally: it is an evals-CLI
    knob, NOT a service env var — do not add it to the :645 service env table, fly.toml,
    or .env.example).

    docs/DESIGN.md :76: extend the supersession trailer — the paragraph stays "the
    argument as it stood" (historical, correct); the trailer now runs 0005 -> 0010
    (Phase 16) -> 0012 (Phase 18): the judge no longer runs on Opus 5, and the rationale's
    current home is ADR-0012. Read the whole § Testing section, not just line 76.

    Whole-file reads MANDATED (pitfall 7 — three grep audits missed a docstring in 16-03;
    the grep locates neighbourhoods): evals/graders.py header region, evals/harness.py
    :520-700, docs/OPERATIONS.md's evals/record-mode section, docs/DESIGN.md § Testing,
    evals/__main__.py ENTIRE (research found nothing asserting judge == critic, but the
    rule applies — the preview comments reference judge constants), and docs/adr/README.md
    once more post-18-03. Read for premise inversions, not just the grepped phrases.

    README.md — a whole-file pass means COUNTING (sixth phase family running): "740
    tests" at :15 and :199 is stale the moment this phase's tests landed; correct both to
    the MEASURED post-phase count (run the suite, count, then edit — never plan-stated
    arithmetic). Check the stack/evals prose (:199-235 neighbourhood) for judge-model
    assertions. Do NOT touch :285 (the Limitations bullet "The eval judge shares the
    critic's model" — Phase 22 deletes it; between now and then the README bullet
    contradicts the tree DELIBERATELY, recorded in the SUMMARY, not fixed early). Leave
    :32/:47 phase-log bullets (historical, true as written — the 16-03 convention).

    Commit: `docs(18-04): every judge==critic surface re-derived except the bullet Phase 22 owns`.
  </action>
  <verify>
    <automated>sh -c 'test "$(grep -rc "judge and the critic share" docs/ | grep -v ":0" | wc -l | tr -d " ")" = "0" && ! grep -q "in production the judge and the critic run" evals/graders.py && .venv/bin/pytest tests/test_evals.py'</automated>
  </verify>
  <done>Both dead phrasings at measured 0 from measured 1; whole-file passes done with
  findings listed in the SUMMARY (including "nothing found" files, stated); README counts
  measured and corrected; Limitations bullet untouched.</done>
</task>

<task type="auto">
  <name>Task 3 (18-04.T3): Phase gate and the record of what this phase deliberately did not do</name>
  <files>(none — measurement and records only)</files>
  <action>
    Phase gate, keyless throughout:
    - `.venv/bin/pytest` plain — green; quote the final count against the 740/67 baseline
      and reconcile the delta against every test added across waves 1-4 (zero unexplained
      skips — the skip count must still be 67 or each change explained).
    - `.venv/bin/python -m evals` — 41/41, exit 0. The committed fixture survived the
      entire phase unstaled, which is the phase-level proof of validation row 7.
    - `.venv/bin/ruff check .` — clean.

    The SUMMARY must state, explicitly (the recorded-not-folklore rule):
    1. `grade_fixture_current` compares pipeline + critic, never the judge — the judge
       flip does not stale `evals/fixtures/technical-figures.json`, by design, pinned by
       :1896's docstring-boundary test.
    2. The README :285 Limitations bullet still says the judge shares the critic's model
       — a deliberate transient contradiction until Phase 22 deletes it; every OTHER doc
       surface now agrees with the tree.
    3. The live one-verdict Opus 4.8 probe (~$0.06 at the assumed 4K-in/1.5K-out leg) is
       DEFERRED to Phase 21's record run — the next paid judge exercise (the
       validation contract's Manual-Only row; deferral recorded, not silent). The offline
       suite proves the guard and the flip via fakes; only a paid call proves the live
       shape.
    4. No deploy this phase: the evals are operator-invoked CLI, not in the production
       image; fly.toml carries no EVAL_JUDGE_MODEL and nothing live changes.
    5. The consolidated mutation ledger: every gate this phase trusts, with its observed
       red (or honest green with the reason — e.g. prose gates, whose non-vacuity is the
       measured nonzero baseline).

    Reconcile 18-VALIDATION.md: fill the Status column of the Per-Task Verification Map
    with the measured evidence per row, tick the sign-off items, and set
    `nyquist_compliant: true` only if every gate has its recorded red or honest-green
    reason.
  </action>
  <verify>
    <automated>.venv/bin/pytest && .venv/bin/python -m evals && .venv/bin/ruff check .</automated>
  </verify>
  <done>Full suite green with reconciled delta; offline evals 41/41 exit 0; ruff clean;
  all five statements in the SUMMARY; VALIDATION map statuses filled and signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stderr line -> operator | the collision note is read while deciding whether to trust a recording — wording is a trust surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-08 | Repudiation | collision line wording | mitigate | token-pinned tests: fault words forbidden, new facts required, ADR-0012 pointer resolution held by the chain test |
| T-18-09 | Repudiation | doc surfaces | mitigate | measured-baseline greps (1 -> 0) + whole-file passes with findings recorded |
| T-18-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- Silent-at-new-defaults twin green; fire case green with ADR-0012 wording; both mutations observed.
- Dead phrasings measured 1 -> 0; README counts measured, not asserted.
- Full suite + offline evals 41/41 + ruff green keyless; VALIDATION reconciled.
</verification>

<success_criteria>
- A production-shaped record run prints no collision line; an operator-created collision
  prints one honest line pointing at a record that exists.
- No surface outside README :285 asserts judge == critic; the transient is recorded.
- Phase 18's four ROADMAP success criteria all hold, each with a measured gate.
</success_criteria>

<output>
Create `.planning/phases/18-independent-eval-judge/18-04-SUMMARY.md` when done.
</output>
