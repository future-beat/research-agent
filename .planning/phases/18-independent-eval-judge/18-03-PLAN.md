---
phase: 18-independent-eval-judge
plan: 03
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - docs/adr/0012-judge-independent-of-the-critic.md
  - docs/adr/0010-judge-rederived-for-an-independent-critic.md
  - docs/adr/README.md
  - tests/test_evals.py
autonomous: true
requirements: [REQ-judge-independent-of-critic]

must_haves:
  truths:
    - "ADR-0012 exists, supersedes ONLY ADR-0010's judge==critic acceptance, and states plainly that it reopens the reversal register v1.1 closed as spent"
    - "ADR-0010's status line reads Superseded by ADR-0012 (Phase 18); its body is untouched"
    - "The supersession chain 0005 -> 0010 -> 0012 is held by one test, each status line agreeing — extended in the SAME commit as the status flip"
    - "The index's counting prose agrees with the table by derivation, and a checker reds when they diverge"
  artifacts:
    - path: "docs/adr/0012-judge-independent-of-the-critic.md"
      provides: "the new record: independence by model identity, family residual stated, register reopened, Fable declined (attributed), Carried forward section"
      contains: "supersedes ADR-0010"
    - path: "docs/adr/README.md"
      provides: "0012 index row, 0010 status/superseded-by cells, counting prose re-derived from the table"
    - path: "tests/test_evals.py"
      provides: "extended chain test (:2560 family) + derived-counts index checker"
  key_links:
    - from: "docs/adr/0010 status line"
      to: "docs/adr/0012 status line"
      via: "the verbatim three-step convention in docs/adr/README.md"
      pattern: "Superseded by ADR-0012"
    - from: "tests/test_evals.py chain test"
      to: "all three record files"
      via: "read_text on 0005/0010/0012, each status asserted"
---

<objective>
Write ADR-0012, apply the supersession convention to ADR-0010, re-derive the index — and
extend the record-exists chain test in the same commit, because editing 0010's status line
deletes the only occurrence of "supersedes ADR-0005" in that file and reds
tests/test_evals.py:2560 (research trap #2).

Purpose: Phase 18 success criterion 3. This plan runs BEFORE the collision-line
re-derivation (18-04) deliberately: the re-derived operator line points at ADR-0012, and a
pointer must resolve to a record that exists — the whole reason the :2560 family exists.

Output: the record, the flipped status line, the re-derived index with a derived-counts
checker (17-04's unbuilt T3, built here), and the extended chain test — one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-independent-eval-judge/18-CONTEXT.md
@.planning/phases/18-independent-eval-judge/18-RESEARCH.md
@docs/adr/0010-judge-rederived-for-an-independent-critic.md
@docs/adr/README.md

The convention (docs/adr/README.md, verbatim three-step): (1) overturned record's status
line becomes `**Status:** Superseded by ADR-0012 (Phase 18)` — nothing else in that file
changes, ever; (2) new record's status line reads `**Status:** Accepted — supersedes
ADR-0010`; (3) both records' Status and Superseded-by index cells update.

House disciplines that bind this plan: plan-stated arithmetic is a claim to check against
the table (sixth occurrence of this lesson family — 13-05, 14-02, 15-03, 16-02, 16-03,
17-01); a supersession stales COUNTING, FORECASTING and ENUMERATING prose, not just the
status cells (17-04); grep locates neighbourhoods, it does not enumerate sentences (16-03)
— the whole-file pass of docs/adr/README.md is mandatory.
</context>

<interfaces>
From tests/test_evals.py:2560 (the test that REDS on the status flip — its `"supersedes
ADR-0005"` assertion matches text that lives only in 0010's current status line):

```python
def test_judge_critic_collision_warning_points_at_a_record_that_exists():
    adr = pathlib.Path(__file__).resolve().parent.parent / "docs" / "adr"
    record = adr / "0010-judge-rederived-for-an-independent-critic.md"
    assert record.exists(), "record mode names ADR-0010; no such record on disk"
    assert "supersedes ADR-0005" in record.read_text()
    assert "Superseded by ADR-0010" in (adr / "0005-opus-5-eval-judge.md").read_text()
```

From docs/adr/0010:3 (the ONE line that changes in that file):

```
**Status:** Accepted — supersedes ADR-0005
```

From docs/adr/README.md:45-50 (the counting prose that stales — every number re-derived):

```
Eight of the eleven records are `Accepted` today. Three supersessions have actually
happened — ... — and each was forecast by the record it overturned. With Phase 17 the
reversal register in `.planning/ROADMAP.md` is spent: ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1 (18-03.T1): Write ADR-0012</name>
  <files>docs/adr/0012-judge-independent-of-the-critic.md</files>
  <action>
    Create the record at exactly this filename (the chain test and the 18-04 operator
    line will both name it). Match the house ADR shape (read 0010 and 0011 for the form:
    title line, Status, Source, Context, Decision, Consequences).

    Status line: `**Status:** Accepted — supersedes ADR-0010` (convention step 2,
    verbatim). Source: `Phase 18 (2026-08-13), REQ-judge-independent-of-critic`.

    Content requirements, assembled from CONTEXT + REQUIREMENTS (all locked, none
    discretionary):
    1. Supersedes ONLY the "judge == critic is an acceptance" position. State explicitly
       that ADR-0010's other position — the critic stronger than the writer it gates —
       stands untouched: the critic does not change (`CRITIC_MODEL` stays `claude-opus-5`
       in production).
    2. State PLAINLY that this record reopens the reversal register v1.1 closed as spent
       — deliberate and ceremonised, not drift. The register convention is the project's
       mechanism for reversals-with-a-record; reopening it quietly would defeat it.
    3. State the accepted residual in the record's own voice: `claude-opus-4-8` is the
       critic's model FAMILY. A family-correlation skeptic's argument survives.
       Independence here means model IDENTITY — exactly the narrowing ADR-0010 made for
       the critic, stated with the same honesty, not papered over.
    4. Record why Fable 5 was presented and declined: ~2x the judge leg ($10/$50 vs
       $5/$25 — the price table confirms the 2x), a 30-day-retention requirement, hotter
       refusal classifiers. ATTRIBUTE the retention/classifier characterization as the
       user's stated decision context from milestone questioning (2026-08-13), not as
       measured fact (research assumption A2 — the record must not launder an attributed
       reason into an asserted one).
    5. Zero cost change, stated against the verified price identity: Opus 4.8's row is
       identical to Opus 5's in every token class ($5/$25/$6.25/$0.50, verified against
       platform.claude.com 2026-08-13).
    6. `### Carried forward from ADR-0010` section (the 12-06/0011 convention — read
       0011's for the form): the different-job leg (judge audits finished output, critic
       gates drafts), the judge-never-shares-the-WRITER's-model pin, the structured
       verdict + fail-loud parse rationale, `EVAL_JUDGE_MODEL` as the override, and the
       critic's own Phase 16 flip untouched.
    7. Mention the refusal guard as part of the same requirement: `stop_reason` is
       checked before content, so a decline is a graded finding, not a parse error
       (landed in 18-02).

    ADR-0010's body is NEVER edited (records are history — the 16-02/10-01 convention);
    its status line is Task 2's, and only that.

    DO NOT COMMIT — trap #2: the status flip, the chain-test extension, and this record
    must share one commit (the test asserts all three files agree).
  </action>
  <verify>
    <automated>test -f docs/adr/0012-judge-independent-of-the-critic.md && grep -c "supersedes ADR-0010" docs/adr/0012-judge-independent-of-the-critic.md</automated>
  </verify>
  <done>Record exists with all seven content requirements present; status line matches
  the convention verbatim; nothing committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (18-03.T2): Apply the convention, re-derive the index, and build the derived-counts checker</name>
  <files>docs/adr/0010-judge-rederived-for-an-independent-critic.md, docs/adr/README.md, tests/test_evals.py</files>
  <behavior>
    - Index checker (17-04's unbuilt T3, built here): parse docs/adr/README.md's index
      table, DERIVE the record count and the Accepted/Superseded counts from the Status
      cells, and assert the counting prose states those derived numbers — never grep the
      typed string against itself. Mutation: flip one table row's status cell, leave the
      prose alone → checker reds where a literal grep stays green (17-04 probe A5).
  </behavior>
  <action>
    ADR-0010: change line 3 to `**Status:** Superseded by ADR-0012 (Phase 18)`. Nothing
    else in the file. (This is the edit that reds :2560 — Task 3 extends the test in the
    same commit.)

    docs/adr/README.md — whole-file pass, and a whole-file pass means COUNTING:
    - Index table: add the 0012 row (link, title, `Accepted — supersedes
      [ADR-0010](...)`, Superseded-by `—`); flip 0010's Status cell to `Superseded` and
      its Superseded-by cell to `[ADR-0012](0012-judge-independent-of-the-critic.md)
      (Phase 18)` — mirror how 0003/0005/0006's superseded rows are formatted.
    - Counting prose: this plan EXPECTS twelve records, eight Accepted, four
      supersessions — but check every number against the table before typing it (the
      sixth-time lesson; the plan's arithmetic is a claim, not a spec).
    - "each was forecast by the record it overturned" is now FALSE for this supersession:
      ADR-0010 carries no Expected-reversal section (the register was closed as spent).
      Rewrite honestly — three were forecast; the fourth deliberately reopened a spent
      register, and ADR-0012 says so.
    - The "register is spent" paragraph: rewrite — the register was reopened once,
      deliberately, by ADR-0012 in Phase 18, which states the reopening in its own text.
      Do not leave the empty-set claim standing.
    - "Reading a superseded record" prose (~lines 71-102): it ENUMERATES records by name
      (17-04 found it does). Verify each named statement still true; where 0010 is
      described as Accepted or as the judge rationale's current home, re-derive; the
      "what survives is in ADR-0010's Carried forward" fact gains its 0012 sibling.
    - Grep the file for number words and digits (`eight|nine|ten|eleven|twelve|three|
      four` and bare counts) as a LOCATOR, then read every hit's sentence — the grep
      finds neighbourhoods, not sentences.

    New test (beside the :2560 family in tests/test_evals.py): the derived-counts index
    checker. Parse the index table rows (lines matching the `| NNNN |` shape), derive
    total records, Accepted count, Superseded count from the Status cells, and assert
    the counting prose carries the derived numbers in the form the prose uses (spelled
    out or digits — match what ships). Keep it resilient to future rows: derive, compare,
    never hardcode this phase's twelve/eight/four in the assertion itself.

    Mutation probe (before trusting it): flip one row's `Accepted` to `Superseded` in the
    working tree, leave prose untouched → checker reds; restore. Quote the red.

    DO NOT COMMIT — Task 3's commit carries all of it.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_evals.py -k "index or adr or record_that_exists"</automated>
  </verify>
  <done>Status line flipped; index table and every counting/forecasting/enumerating
  sentence re-derived against the table; checker built and observed red under the
  row-flip mutation; nothing committed yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (18-03.T3): Extend the chain test and land everything in one commit</name>
  <files>tests/test_evals.py</files>
  <behavior>
    - `test_judge_critic_collision_warning_points_at_a_record_that_exists` extended to
      hold the FULL chain, each status line agreeing:
      0005 contains `Superseded by ADR-0010`;
      0010 exists and contains `Superseded by ADR-0012`;
      0012 exists and contains `supersedes ADR-0010`.
    - The old `"supersedes ADR-0005" in` 0010 assertion is REPLACED (that text no longer
      exists anywhere in 0010 after the status flip) — the 0005->0010 half is now held
      from 0005's side; state that in the test docstring so the change reads as the
      convention applied, not an assertion dropped.
  </behavior>
  <action>
    Extend the test (extend, not duplicate — the 16-03 lesson's test keeps holding both
    halves of every supersession it names, now three records). Update its docstring: the
    operator-facing line this test serves points at the judge's record; as of 18-04 that
    pointer will be ADR-0012, and this test is what guarantees the pointer resolves.

    Run the full test_evals.py file — with Tasks 1+2 in the working tree the extended
    test is green, and NOTHING else should have moved (the collision wording tests still
    pin "ADR-0010" in the STDERR line, which harness.py still prints — that string is
    18-04's deliverable, untouched here; the record it names still exists, status
    Superseded, which is exactly the state 18-04's re-derivation resolves).

    Mutation probes, each observed then reverted:
    1. Revert 0010's status line to `Accepted — supersedes ADR-0005` → extended test reds
       (chain broken at 0010->0012).
    2. Rename 0012 away (`git mv` in the working tree) → extended test reds on existence.

    Commit Tasks 1+2+3 as ONE commit:
    `docs(18-03): ADR-0012 supersedes ADR-0010's judge==critic acceptance`
    — git show must list 0012, 0010, README.md, and tests/test_evals.py together.

    Wave gate: `.venv/bin/pytest` plain keyless (quote delta), `.venv/bin/python -m
    evals` (41/41 exit 0), `.venv/bin/ruff check .`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_evals.py && .venv/bin/pytest && .venv/bin/python -m evals && .venv/bin/ruff check .</automated>
  </verify>
  <done>Chain test green over three agreeing records; both mutations observed red and
  reverted; ONE commit carrying record + status flip + index + tests; full suite and
  offline evals green with quoted deltas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-facing pointers -> docs/adr | a named record an operator is sent to read must exist and agree (Repudiation surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Repudiation | supersession chain | mitigate | chain test holds all three status lines in one assertion set; a dangling or disagreeing record reds the suite |
| T-18-07 | Repudiation | index counting prose | mitigate | derived-counts checker — counts computed from the table, never the typed string |
| T-18-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- One commit contains 0012 + 0010's status line + README.md + the test extensions.
- Extended chain test observed red under both mutations (status revert, record rename).
- Index checker observed red under the row-flip-without-prose mutation.
- Full suite + offline evals 41/41 + ruff green, keyless, deltas quoted.
</verification>

<success_criteria>
- ROADMAP SC-3 satisfied: ADR-0012 exists, records the supersession, states the register
  reopening plainly, and carries the family residual honestly.
- ADR-0010 differs from its committed state by exactly one line (git diff shows the
  status line and nothing else).
- Counting prose provably agrees with the table, by derivation.
</success_criteria>

<output>
Create `.planning/phases/18-independent-eval-judge/18-03-SUMMARY.md` when done.
</output>
