---
phase: 18-independent-eval-judge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/research_agent/usage.py
  - evals/graders.py
  - tests/test_usage.py
  - tests/test_evals.py
autonomous: true
requirements: [REQ-judge-independent-of-critic]

must_haves:
  truths:
    - "`JUDGE_MODEL` defaults to `claude-opus-4-8` — not the critic's model, stronger than the writer"
    - "A judge run is priced, not `pricing_unknown`: `price_for('claude-opus-4-8')` returns $5/$25 with 1.25x/0.1x cache rates"
    - "The independence pin compares against the DEPLOYED critic (fly.toml's CRITIC_MODEL), not the suite's neutral default"
    - "The committed fixture did not stale: offline evals still 41/41 exit 0 after the flip"
  artifacts:
    - path: "src/research_agent/usage.py"
      provides: "claude-opus-4-8 PRICES row, single undated window"
      contains: "claude-opus-4-8"
    - path: "evals/graders.py"
      provides: "JUDGE_MODEL default claude-opus-4-8 (line 33; nothing else in this file changes this plan)"
    - path: "tests/test_usage.py"
      provides: "direct price pin for the new row"
    - path: "tests/test_evals.py"
      provides: "independence pin: judge != fly.toml's parsed CRITIC_MODEL"
  key_links:
    - from: "evals/graders.py JUDGE_MODEL"
      to: "src/research_agent/usage.py PRICES"
      via: "usage.price_for(G.JUDGE_MODEL, day) in evals/__main__.py preview and tests :2359/:2398"
      pattern: "price_for.*JUDGE_MODEL"
    - from: "tests/test_evals.py independence pin"
      to: "fly.toml [env] CRITIC_MODEL"
      via: "tomllib parse, the tests/test_deploy_config.py:208 idiom"
---

<objective>
Flip the eval judge's default model to `claude-opus-4-8` with its price row landing in the
same commit, and pin the new independence against the deployed critic pin rather than the
suite's neutral default.

Purpose: Phase 18 success criteria 1 and 4 (ROADMAP). This is the wave that makes the
judge independent by model identity; the guard, the record, and the prose follow in later
waves.

Output: PRICES row + default flip in ONE commit (research trap #1), a direct price pin, a
fly.toml-anchored independence pin, and a measured wave gate proving the committed fixture
did not stale.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-independent-eval-judge/18-CONTEXT.md
@.planning/phases/18-independent-eval-judge/18-RESEARCH.md
@.planning/phases/18-independent-eval-judge/18-VALIDATION.md
@src/research_agent/usage.py
@evals/graders.py

Baseline entering this phase (measured 2026-08-13): 740 passed / 67 skipped keyless;
offline evals 41/41 exit 0. Quote deltas against these numbers, keyless throughout
(`ANTHROPIC_API_KEY=""` — the suite must never need a key).

Locked decision (user-ratified at milestone questioning): the judge model is
`claude-opus-4-8` — decided, not open. Fable 5 was presented and declined.
</context>

<interfaces>
From src/research_agent/usage.py (the shape the new row must match — opus-5's row is the
template, byte-identical in structure):

```python
"claude-opus-5": [
    PriceWindow(Price(input=5.0, output=25.0, cache_write_5m=6.25, cache_read=0.50)),
],
```

From evals/graders.py:33 (the one line this plan changes in that file):

```python
JUDGE_MODEL = os.environ.get("EVAL_JUDGE_MODEL", "claude-opus-5")
```

From tests/test_deploy_config.py:198-208 (the deployed-pin idiom the independence pin
borrows — a VALUE read off the parsed [env] table, because presence proves nothing):

```python
CRITIC_MODEL_PIN = "claude-opus-5"
# fly fixture: tomllib-parsed fly.toml; env = fly.get("env", {})
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (18-01.T1): Price the judge's new model — pin RED first, then the row</name>
  <files>src/research_agent/usage.py, tests/test_usage.py</files>
  <behavior>
    - Direct pin: `usage.price_for("claude-opus-4-8")` (undated call) returns a Price with
      input=5.0, output=25.0, cache_write_5m=6.25, cache_read=0.50 — the loops alone would
      pass with the row absent, so the phase owes its own gate (18-RESEARCH Finding 4).
    - RED first: run the pin before the row exists and observe `UnknownModelPricing` —
      quote the red in the SUMMARY.
  </behavior>
  <action>
    Write the direct pin in tests/test_usage.py beside the existing per-model pins. Run it
    against the current table — observe and record the `UnknownModelPricing` red.

    Then add the `claude-opus-4-8` row to PRICES in src/research_agent/usage.py: a single
    undated PriceWindow, byte-identical in shape to the `claude-opus-5` row above it
    (rates verified against platform.claude.com 2026-08-13, per 18-RESEARCH Finding 6 —
    identical to Opus 5's row, which is what makes "zero cost change" exact). A short
    comment noting the row is the eval judge's model since Phase 18 is fine; no dated
    window — do NOT reintroduce one (the Sonnet introductory window is permanently gone).

    Do NOT touch the synthetic `fixture-two-window-model` table in tests/test_usage.py
    (:105-123, installed per-test by the `dated_model` fixture) — it is deliberate test
    scaffolding, untouched by a new real row.

    The table loops (`test_cache_rates_are_multiples_of_the_input_rate`,
    `test_no_model_has_overlapping_or_gapped_windows`,
    `test_window_for_reports_every_real_model_as_open_ended`,
    `test_next_window_is_none_for_every_shipped_model`) pass by construction — run them to
    confirm, change nothing in them.

    DO NOT COMMIT YET. The default flip shares this commit (validation contract row 2:
    "row and default flip share one commit" — research trap #1). Task 2 commits both.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_usage.py</automated>
  </verify>
  <done>Direct pin observed red (`UnknownModelPricing`, quoted) before the row; green
  after; all tests/test_usage.py green; working tree staged, nothing committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (18-01.T2): Flip the default and pin independence against the deployed critic — one commit with Task 1</name>
  <files>evals/graders.py, tests/test_evals.py</files>
  <behavior>
    - New independence pin: `G.JUDGE_MODEL` != fly.toml's parsed `[env]` `CRITIC_MODEL`
      (the deployed pin, `claude-opus-5`). RED first is natural: written before the flip
      it reds, because today both are `claude-opus-5`.
    - Existing pin :606 (`test_the_judge_runs_on_a_different_model_than_the_pipeline`,
      judge != graph.MODEL) stays green untouched — it guards judge != WRITER, a
      different claim.
  </behavior>
  <action>
    Write the new independence pin in tests/test_evals.py near :606. It must compare
    against the DEPLOYED critic, not `graph.critic_model()` — in the keyless suite
    `CRITIC_MODEL` is unset, so `critic_model()` returns `graph.MODEL` and the comparison
    would prove nothing about production (16-02's neutral-default blind spot, 18-RESEARCH
    pitfall 5). Parse fly.toml with tomllib per the tests/test_deploy_config.py:208 idiom
    (`fly.get("env", {}).get("CRITIC_MODEL")`), assert it is present (the pin must fail
    loud if fly.toml regenerates without the key, not silently compare against None), and
    assert `G.JUDGE_MODEL` differs from it. Run it before the flip — observe the red,
    quote it.

    Then flip evals/graders.py:33: the default literal becomes `"claude-opus-4-8"`. Touch
    NOTHING else in graders.py — the module docstring (:13-19) still asserts judge ==
    critic and now reads stale; that is a known intra-phase transient owned by plan 18-04,
    stated in this plan's SUMMARY rather than fixed here (the 16-01/16-03 precedent: code
    cuts over, the record-and-prose plan closes the phase).

    Note on pitfall 4: `Judge.__init__`'s `model: str = JUDGE_MODEL` default binds at
    class-definition time — the pin asserts the CONSTANT (`G.JUDGE_MODEL`), never a
    monkeypatched read, so it pins what an import actually gets.

    Run the quick suite. Expected motion, each verified rather than assumed
    (18-RESEARCH Finding 4): :606 green (opus-4-8 != sonnet-5); the preview pricing tests
    (:2359 requote, :2398 researched-range) green because Task 1's row exists; the
    collision tests (:2516/:2528/:2540/:2560) mechanically green because FakeJudge
    hardcodes `claude-opus-5`; `test_record_writes_a_fixture_per_case_with_fakes` (:2483)
    green because its `"judge": "claude-opus-5"` is FakeJudge's model, not the constant.
    If anything else moves, investigate before committing — the inventory says nothing
    else should.

    Commit Tasks 1+2 as ONE commit (usage.py row + pin + flip + independence pin):
    `feat(18-01): judge defaults to claude-opus-4-8, priced in the same commit`.

    Mutation probes, each observed then reverted:
    1. Flip the default back to `"claude-opus-5"` → the new fly.toml pin reds; :606 stays
       green (proving :606 never guarded critic-independence). Revert.
    2. Delete the PRICES row → the Task 1 direct pin reds, AND :2359/:2398 red with
       `UnknownModelPricing` — the ordering trap demonstrated real, once. Revert.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_evals.py tests/test_usage.py</automated>
  </verify>
  <done>Default is `claude-opus-4-8`; independence pin red-observed pre-flip and green
  post-flip; both mutations observed red and reverted; exactly one commit containing
  usage.py and graders.py together.</done>
</task>

<task type="auto">
  <name>Task 3 (18-01.T3): Wave gate — full suite, offline evals, and the non-staleness fact stated</name>
  <files>(none — measurement and records only)</files>
  <action>
    Run the full suite plain (`.venv/bin/pytest`, no `-q -q`), keyless. Quote the count
    against the 740/67 baseline — the delta must equal the tests this wave added, with
    zero unexplained skips.

    Run `.venv/bin/python -m evals` keyless — expect 41/41, exit 0. This is the MEASURED
    proof of validation row 7: `grade_fixture_current` compares `models["pipeline"]`
    against `graph.MODEL` and `models.get("critic")` against `graph.critic_model()` —
    NEVER the judge — so the flip does not stale `evals/fixtures/technical-figures.json`
    (whose recorded `"judge": "claude-opus-5"` is fixed replay data, not a comparison
    input). `tests/test_evals.py:1896` pins that docstring boundary and must be green in
    the full run. State this fact in the SUMMARY explicitly — the 15-03/16-02 rule:
    recorded, not folklore.

    Run `.venv/bin/ruff check .` — clean.

    Record in the SUMMARY the transients deliberately left open for later waves:
    graders.py module docstring and harness.py collision docstring/message still assert
    judge == critic (18-04); :2516/:2540's "deployed configuration" prose is now contrived
    (18-04); OPERATIONS/DESIGN still state the shared model (18-04); ADR-0012 not yet
    written (18-03).
  </action>
  <verify>
    <automated>.venv/bin/pytest && .venv/bin/python -m evals && .venv/bin/ruff check .</automated>
  </verify>
  <done>Full suite green with quoted delta vs 740/67; offline evals 41/41 exit 0;
  ruff clean; non-staleness fact and open transients stated in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model response -> harness | untrusted model output crosses into scoring (unchanged this plan; guarded in 18-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | usage.PRICES row | mitigate | direct pin + cache-ratio loop: a wrong rate reds a test, and an absent row fails loud via `UnknownModelPricing` (DEC-12), never costs $0 |
| T-18-02 | Repudiation | independence pin | mitigate | compared against fly.toml's parsed value, failing loud if the key vanishes — a regenerated fly.toml cannot silently un-pin it |
| T-18-SC | Tampering | package installs | accept | no packages installed this phase (18-RESEARCH: audit protocol not run — nothing to audit) |
</threat_model>

<verification>
- One commit contains both the PRICES row and the graders.py:33 flip (git show lists both files).
- Full suite green keyless; offline evals 41/41 exit 0; ruff clean.
- Both mutation probes observed red and reverted (quoted in SUMMARY).
</verification>

<success_criteria>
- `evals/graders.py:33` default is `"claude-opus-4-8"`; `EVAL_JUDGE_MODEL` still overrides.
- `price_for("claude-opus-4-8")` returns 5.0/25.0/6.25/0.50; every usage loop green.
- New pin: judge != deployed CRITIC_MODEL (fly.toml-anchored), observed red before the flip.
- Offline evals 41/41 — the committed fixture demonstrably not staled.
</success_criteria>

<output>
Create `.planning/phases/18-independent-eval-judge/18-01-SUMMARY.md` when done.
</output>
