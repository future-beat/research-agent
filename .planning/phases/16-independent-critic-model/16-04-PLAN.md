---
phase: 16-independent-critic-model
plan: 04
type: execute
wave: 4
depends_on: ["16-01", "16-02", "16-03"]
files_modified:
  - fly.toml
  - tests/test_deploy_config.py
autonomous: false
requirements: [REQ-independent-critic-model]

must_haves:
  truths:
    - "CRITIC_MODEL = 'claude-opus-5' is committed in fly.toml [env] — configuration, not a secret — and guarded by a value pin in tests/test_deploy_config.py landing in the SAME commit (USER DECISION 2026-08-10)"
    - "The cutover deploy is the first since v9 and deliberately carries phases 13-15's pending changes; phase 14's booked post-deploy smoke passes (non-zero cost.embedding_usd in /metrics, /pricing windows and multipliers as expected)"
    - "One live demo run on the deployed service shows critic calls priced at Opus rates ($5/$25) and writer calls at Sonnet's window rate, pricing_unknown false, /metrics sane, and the demo still anonymous (no auth header on the run)"
    - "The deploy happens AFTER the phase PR merges, as the final act — never a deploy of unmerged work — and the SUMMARY records it"
    - "The phase gate passes: plain + armed suites, ruff, offline evals 41/41 keyless, zero ci.yml diffs"
  artifacts:
    - path: "fly.toml"
      provides: "the production critic pin, commented like the other [env] vars"
      contains: "CRITIC_MODEL"
    - path: "tests/test_deploy_config.py"
      provides: "value pin guarding the critic stance against Fly tooling rewrites"
      contains: "CRITIC_MODEL"
    - path: ".planning/phases/16-independent-critic-model/16-04-SUMMARY.md"
      provides: "the cutover record: deploy version, booked-smoke outcomes, per-node pricing evidence, total cost"
  key_links:
    - from: "fly.toml [env] CRITIC_MODEL"
      to: "graph.critic_model()"
      via: "Fly env injection at boot — the same seam 16-01 built, now set in production"
      pattern: "CRITIC_MODEL"
    - from: "the live run's usage record"
      to: "PRICES opus row"
      via: "critic calls priced at $5/$25 while writer calls price at Sonnet's window"
      pattern: "pricing_unknown.*[Ff]alse"
---

<objective>
Make the production flip: pin `CRITIC_MODEL = 'claude-opus-5'` in fly.toml's [env]
(guarded in the same commit), run the full phase gate, get sign-off, and then — after the
phase PR merges — perform the one deliberate cutover deploy that carries phases 13-16
together, run the post-deploy smokes OPERATIONS booked, and verify this phase live: one
demo run whose usage record prices critic calls at Opus rates and writer calls at Sonnet
rates.

Purpose: **USER DECISION (2026-08-10) — the critic runs on Opus.** Verbatim rationale:
"Use Opus as the critic's model since it has to be more capable than the writer's model."
This supersedes the earlier deferral: the production flip IS this phase's deliverable.
The code default stays neutral (unset → writer's model) so tests, CI and keyless contexts
are unchanged; production sets the stance visibly in committed configuration.
Output: the fly.toml pin + its guard, a green phase gate, human sign-off, the cutover
deploy, and the SUMMARY's cutover record.

The live demonstration uses **Opus — the real config — not haiku.** Haiku stays in unit
tests only (16-01's undated-row arithmetic, delta $0.0060). Verification-run spend
ceiling: $0.40 (Opus-critic typical ≈ $0.18 per 16-RESEARCH).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-independent-critic-model/16-CONTEXT.md
@.planning/phases/16-independent-critic-model/16-VALIDATION.md
@src/research_agent/graph.py
@fly.toml
@tests/test_deploy_config.py

<interfaces>
Deploy-config guards (tests/test_deploy_config.py — READ AT PLANNING, verified 2026-08-10):
- The guards pin VALUES on the parsed [env] table (`POSTGRES_BACKEND_PINS` at :87-91,
  asserted at :146-153; `DATABASE_URL` absence at :194). NONE assert an exhaustive key
  set — adding a CRITIC_MODEL key breaks nothing. That is the verified baseline, not an
  assumption.
- The pin idiom to follow: `POSTGRES_BACKEND_PINS` — values, not keys, read off the
  parsed table, with an assertion message explaining what silently goes wrong without it.
- fly.toml's own header documents WHY pins exist: Fly's tooling has rewritten this file
  twice ("New files from Fly.io Launch" PRs), and a deleted CRITIC_MODEL key is the
  quiet-misconfiguration class this test file exists for — the critic silently reverts
  to the writer's model, every check stays green, and only the demo's cost profile and
  ADR-0010's stance are betrayed.

The cutover deploy carries (first deploy since v9 — what is pending, read at planning):
- Phase 14 (the ONLY smoke OPERATIONS books, :556-563): "Smoke it whenever the next
  deploy happens: one demo run, then confirm /metrics shows a non-zero
  cost.embedding_usd and that /pricing reports the windows and multipliers you expect."
  14-03's SUMMARY additionally books the first post-deploy GET /pricing as where a human
  sees the current-vs-next windows side by side (the 2026-08-31 Sonnet boundary);
  14-01's SUMMARY books the live confirmation of the neutral geo path (no geo pinning →
  multiplier 1.0, "global" or absent) to the same run.
- Phase 13: NOTHING booked to the deploy — its live leg (real Voyage re-embed) was
  completed 2026-08-09 pre-deploy on scratch tables (13-VALIDATION, done rows); the
  migrate module ships neutrally.
- Phase 15: NOTHING to smoke — evals/ and tests/ are excluded from the image
  (OPERATIONS :16, :620); the phase ships no runtime behaviour.
- Phase 16: CRITIC_MODEL lands set, so the deployed critic runs on claude-opus-5 from
  the first post-deploy request.

Live-leg mechanics:
- `fly deploy -a research-agent` from the MERGED main working tree (deploys are manual;
  enforce_admins is off — which makes deploying unmerged work possible and is exactly
  why the plan forbids it).
- The demo run: one SHORT question through the public endpoint the demo page uses
  (POST /research/stream on research-agent.fly.dev, no auth header — that absence IS the
  anonymity check; read service.py for the request shape if needed).
- Per-node evidence: `fly logs -a research-agent` carries the JSON "model call" lines —
  the critic line must say model=claude-opus-5 with its cost_usd; writer/researcher/
  classifier lines say model=claude-sonnet-5 (graph.py:117 puts cost_usd on each line).
- Cost: Opus-critic typical ≈ $0.18; ceiling $0.40; never a second paid run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The fly.toml pin and its guard — one commit</name>
  <files>fly.toml, tests/test_deploy_config.py</files>
  <read_first>fly.toml [env] block and header comment; tests/test_deploy_config.py :80-153 (STORE_PATH_KEYS, POSTGRES_BACKEND_PINS and their test)</read_first>
  <action>
    SAME-COMMIT RULE (this phase's own checker blocker, applied here by instruction): the
    fly.toml edit and its test pin land in ONE commit — a commit that adds a pinned config
    without its guard, or a guard without its config, is the split this rule forbids.

    1. fly.toml [env]: add `CRITIC_MODEL = 'claude-opus-5'` with a comment in the file's
       voice, explaining it like the other vars: configuration, not a secret — committed
       precisely so the stance is visible; the critic runs on a MORE CAPABLE model than
       the writer (the user's design position, recorded in ADR-0010); code default is
       neutral (unset → the critic runs on MODEL), so CI, tests and keyless contexts
       never see this value — only the deployed service does. Do not touch any other key.
    2. tests/test_deploy_config.py: add a pin test following the POSTGRES_BACKEND_PINS
       idiom — assert `fly.get("env", {}).get("CRITIC_MODEL") == "claude-opus-5"` off the
       parsed table, with a message explaining the failure mode: a deleted or rewritten
       key silently reverts the critic to the writer's model with every check green (the
       exact quiet-misconfiguration class this file exists for, and Fly's tooling has
       rewritten [env] twice). Name the test so `-k critic_model_pin` selects it.
       Baseline stated: `grep -c CRITIC_MODEL fly.toml` is 0 before this task; the
       existing guards were read and pin values, not exhaustive keys, so nothing else
       reds when the key is added.
    3. Gate discipline: `pytest tests/test_deploy_config.py --collect-only -q -k
       critic_model_pin` first (must collect 1); then the discriminating mutations —
       temporarily change the fly.toml value to 'claude-sonnet-5' → ONLY the new test
       reds; temporarily delete the key → ONLY the new test reds; restore both, record
       outcomes for the SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_deploy_config.py --collect-only -q -k critic_model_pin && .venv/bin/pytest tests/test_deploy_config.py && grep -c "CRITIC_MODEL" fly.toml</automated>
  </verify>
  <done>fly.toml [env] carries CRITIC_MODEL = 'claude-opus-5' with an explanatory comment; the pin test exists, collects, and passes; both mutations proved discriminating; one commit holds both files.</done>
  <acceptance_criteria>
    - `grep -c "CRITIC_MODEL" fly.toml` ≥ 1 (baseline 0); the value line reads exactly `CRITIC_MODEL = 'claude-opus-5'`
    - `-k critic_model_pin` collects 1 under --collect-only; full test_deploy_config.py green
    - Mutation evidence recorded: value-change red and key-delete red, each redding ONLY the new pin; restored after
    - `git log --oneline -1 --stat` shows fly.toml and tests/test_deploy_config.py in the SAME commit
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: The full phase gate</name>
  <files>.planning/phases/16-independent-critic-model/16-04-SUMMARY.md</files>
  <read_first>16-VALIDATION.md Validation Sign-Off; .planning/ROADMAP.md § Phase 16 criteria</read_first>
  <action>
    Run the phase gate in a shell with CRITIC_MODEL UNSET (verify: `env | grep -c
    CRITIC_MODEL` → 0 — Pitfall 4: CI must never see it, and neither may the gate; the
    fly.toml pin does NOT leak into a local shell — staleness and critic selection read
    the process env, which is exactly why the fixture stays green here):
    1. `.venv/bin/pytest` — plain suite; expect ≥663 passed / 65 skipped plus this
       phase's new tests (including Task 1's deploy-config pin), zero failures.
    2. Armed leg: with the local PG on :54329 up (restart with LC_ALL=C if down), run the
       armed suite the way prior phases did; expect the armed baseline (was 727/1) plus
       new tests.
    3. `.venv/bin/ruff check .` — clean.
    4. `ANTHROPIC_API_KEY="" .venv/bin/python -m evals` — 41/41 keyless, exit 0.
    5. `git diff --stat main -- .github/workflows/ci.yml` — zero diffs (the keyless
       invariant never moved).
    6. Verify the five ROADMAP criteria against the tree and list each with its evidence
       in the SUMMARY (SC-1: accessor + threading tests; SC-2: discriminator + gate;
       SC-3: limits/OPERATIONS prose; SC-4: ADR-0010 + 0005 status line; SC-5: README
       greps) — plus the cutover pin (Task 1) as the SC-1 production leg.
    Record every count in the SUMMARY. Any red stops the phase close — fix or surface,
    never waive.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q 2>&1 | tail -1 && .venv/bin/ruff check . && ANTHROPIC_API_KEY="" .venv/bin/python -m evals && git diff --quiet main -- .github/workflows/ci.yml</automated>
  </verify>
  <done>All five gate legs green with counts recorded; the five criteria each mapped to concrete evidence in the SUMMARY.</done>
  <acceptance_criteria>
    - Plain suite: 0 failed, ≥663 passed, 65 skipped; armed suite: 0 failed, ≥727 passed
    - ruff clean; offline evals 41/41 exit 0 with ANTHROPIC_API_KEY="" and CRITIC_MODEL unset
    - ci.yml zero diffs against main
    - SUMMARY has a five-row criteria table: SC-1..SC-5, each with the file/test evidence
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase sign-off and merge authorization — the cutover follows the merge</name>
  <files>.planning/phases/16-independent-critic-model/16-04-SUMMARY.md</files>
  <action>Present the phase evidence to the user and pause: the criteria table and gate counts from Task 2, the fly.toml pin and mutation evidence from Task 1, and the spot-check pointers below. State plainly what "approved" authorizes: merging the phase PR, then the cutover deploy (Task 4) — the first since v9, carrying phases 13-16 — and one paid verification run (≈$0.18, ceiling $0.40). Do no further work until the resume signal.</action>
  <what-built>The independent-critic phase, complete and flipped: CRITIC_MODEL seam with four-site threading and attribution proofs (16-01); fixture gate with backfill, collision warning, reservation threshold (16-02); ADR-0010 recording the user's critic-stronger-than-writer position and the judge==critic acceptance (16-03); the fly.toml production pin with its guard, and a green phase gate (16-04). Pending on approval: the cutover deploy and its live verification.</what-built>
  <how-to-verify>
    1. Read .planning/phases/16-independent-critic-model/16-04-SUMMARY.md: the
       five-criteria evidence table, the gate counts, and the fly.toml pin's mutation
       evidence.
    2. Spot-check the reversal: `git diff main -- docs/adr/0005-opus-5-eval-judge.md`
       (exactly the status line) and read docs/adr/0010-judge-rederived-for-an-independent-critic.md
       — your quoted rationale ("more capable than the writer") and the judge==critic
       acceptance should both be recorded positions.
    3. Spot-check the README: the limitation bullet is gone; the residual states the
       critic runs on a stronger model; the status list carries 16.
    4. Spot-check fly.toml: CRITIC_MODEL = 'claude-opus-5' in [env], commented.
    5. Confirm you authorize: PR merge, then the deploy that carries phases 13-16, then
       the one paid verification run.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize merge + cutover deploy + verification run, or describe issues</resume-signal>
  <verify>
    <human-check>User has read the SUMMARY's criteria table and the fly.toml pin and explicitly authorized the merge and the cutover</human-check>
  </verify>
  <done>User typed "approved", or the described issues were resolved and re-presented</done>
</task>

<task type="auto">
  <name>Task 4: The cutover — merge, deploy, booked smokes, live verification, record</name>
  <files>.planning/phases/16-independent-critic-model/16-04-SUMMARY.md</files>
  <read_first>docs/OPERATIONS.md :556-563 (the booked Phase-14 smoke) and :33-69 (deploys are manual); the interfaces block above (what the deploy carries)</read_first>
  <action>
    ORDER IS LOAD-BEARING: deploy AFTER the phase PR merges, as the final act. Deploys
    are manual and enforce_admins is off, which is precisely why the rule needs stating —
    never deploy unmerged work.

    1. Merge: `gh pr merge` the phase PR (or confirm the user already merged it). Then
       `git checkout main && git pull --ff-only` — the working tree that deploys is
       merged main, nothing else.
    2. Deploy: `fly deploy -a research-agent`. Record the version Fly reports (first
       deploy since v9). If fly is unauthenticated, pause with an auth checkpoint rather
       than improvising.
    3. Phase-14's booked smoke, verbatim from OPERATIONS :561-563 — one demo run (this
       run also serves step 4; ONE paid run covers both): a SHORT question through the
       public demo endpoint with NO auth header. Then confirm `/metrics` shows a
       non-zero `cost.embedding_usd`, and `GET /pricing` reports the windows and
       multipliers you expect (the first post-deploy look 14-03 booked: current-vs-next
       Sonnet windows side by side, multipliers at neutral 1.0/none — which also closes
       14-01's booked neutral-geo confirmation). Phases 13 and 15 have nothing booked:
       13's live leg was done 2026-08-09 pre-deploy on scratch tables; 15 ships nothing
       in the image. Say so in the SUMMARY rather than leaving the reader to wonder.
    4. Phase-16 verification, same run: from `fly logs -a research-agent`, capture the
       JSON "model call" lines — the critic line must show model=claude-opus-5 with its
       cost_usd (priced at $5/$25), the writer/researcher/classifier lines
       model=claude-sonnet-5 at Sonnet's window rate; the run's usage shows
       pricing_unknown false and a total cost_usd (record it; expect ≈$0.18). Confirm
       `/health` is ok and the demo stayed anonymous — the run was made with no auth
       header and succeeded. Ceiling: if the run's cost_usd exceeds $0.40, note the
       overrun — never run a second paid time.
    5. Record the cutover in the SUMMARY: USER DECISION quoted, deploy version, what the
       deploy carried (13-16), each booked-smoke outcome, the two pasted log lines, the
       total cost, and the sequence followed (merge → deploy → smoke). Commit the SUMMARY
       update to main as ONE docs commit (`docs(16-04): record the cutover`) — the
       deploy record post-dates its PR by construction, the same shape as phase 13's
       cutover-and-record commit; this is the stated exception, not a throwaway commit.
  </action>
  <verify>
    <automated>grep -q "claude-opus-5" .planning/phases/16-independent-critic-model/16-04-SUMMARY.md && grep -qiE "deploy" .planning/phases/16-independent-critic-model/16-04-SUMMARY.md && curl -sf https://research-agent.fly.dev/health >/dev/null</automated>
  </verify>
  <done>The deploy happened from merged main; phase-14's booked smoke passed; the live run's log lines show the critic on claude-opus-5 priced at Opus rates and the writer on claude-sonnet-5; total cost ≤ $0.40 recorded; the SUMMARY's cutover record is committed. Never silence.</done>
  <acceptance_criteria>
    - SUMMARY contains: a "model call" log line with model claude-opus-5 for the critic AND one with model claude-sonnet-5 for the writer, the run's total cost_usd, pricing_unknown false
    - SUMMARY states the deploy version, that it carried phases 13-16, and the merge-then-deploy sequence; the 13/15 nothing-booked rationale is stated
    - /metrics shows non-zero cost.embedding_usd post-run (phase-14 smoke); /pricing windows and multipliers recorded; /health ok; the run was anonymous (no auth header)
    - Total live spend this phase ≤ $0.40 (target ≈ $0.18)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator wallet → live leg | one paid verification run; ceiling $0.40, typical ≈ $0.18, authorized at the Task 3 checkpoint |
| local env → phase gate | a CRITIC_MODEL leak into the gate shell would stale the fixture and mask the keyless invariant |
| Fly tooling → committed config | "Launch" PRs have rewritten [env] twice; a silently deleted CRITIC_MODEL reverts the critic with every check green |
| branch → production | enforce_admins is off, so deploying unmerged work is possible — and forbidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Denial of service | live-leg spend | mitigate | one run serving both smokes + short question + $0.40 ceiling + no second paid run |
| T-16-11 | Repudiation | cutover record | mitigate | SUMMARY grep gate — silence is not an outcome; deploy version and evidence lines required |
| T-16-12 | Tampering | gate shell env | mitigate | explicit env check before the gate (CRITIC_MODEL unset) |
| T-16-13 | Tampering | fly.toml CRITIC_MODEL pin | mitigate | value pin in test_deploy_config.py, same commit as the config (POSTGRES_BACKEND_PINS idiom), mutation-proven |
| T-16-14 | Elevation of privilege | deploying unmerged work | mitigate | merge → `git checkout main && git pull --ff-only` → deploy; the sequence is a stated acceptance criterion |
| T-16-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
The phase gate IS this plan's pre-merge verification (Task 2); the cutover deploy and its
booked smokes are the post-merge verification (Task 4). One PR for the whole phase per
the standing instruction — all four plans' commits ride gsd/phase-16-independent-critic;
the ONLY branch switch is Task 4's checkout of merged main for the deploy, and the only
post-merge commit is the SUMMARY's cutover record.
</verification>

<success_criteria>
- VALIDATION row 11 (LIVE cutover) satisfied: deploy from merged main, booked smokes
  green, per-node Opus/Sonnet pricing evidence captured
- fly.toml pins CRITIC_MODEL = 'claude-opus-5', guarded in the same commit; all five
  ROADMAP criteria evidenced
- Plain + armed + ruff + keyless evals green; ci.yml zero diffs
- Human sign-off recorded; total live spend ≤ $0.40
</success_criteria>

<output>
`.planning/phases/16-independent-critic-model/16-04-SUMMARY.md` is written incrementally by
Tasks 1-2, presented at Task 3, and finalized by Task 4's cutover record (committed to
main as the one post-merge docs commit).
</output>
