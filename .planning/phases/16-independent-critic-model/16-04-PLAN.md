---
phase: 16-independent-critic-model
plan: 04
type: execute
wave: 4
depends_on: ["16-01", "16-02", "16-03"]
files_modified: []
autonomous: false
requirements: [REQ-independent-critic-model]

must_haves:
  truths:
    - "One real run with CRITIC_MODEL=claude-haiku-4-5 shows per-node attribution in its usage record — or the deferral is stated explicitly (CONTEXT: the plans must state which happened)"
    - "The production flip is NOT made — CRITIC_MODEL stays unset on Fly (deferred by decision)"
    - "The phase gate passes: plain + armed suites, ruff, offline evals 41/41 keyless, zero ci.yml diffs"
  artifacts:
    - path: ".planning/phases/16-independent-critic-model/16-04-SUMMARY.md"
      provides: "the record-or-defer statement with the run's usage evidence (or the deferral rationale)"
  key_links:
    - from: "the live run's usage record"
      to: "PRICES haiku row"
      via: "critic call priced at $1/$5 while writer calls price at Sonnet's window"
      pattern: "pricing_unknown.*[Ff]alse"
---

<objective>
Demonstrate the independent critic live — one run, CRITIC_MODEL=claude-haiku-4-5,
~$0.15-0.25, on the operator's machine with real keys, no Fly deploy and no fly secrets —
then run the full phase gate and close the phase with an explicit record-or-defer
statement.

Purpose: the ROADMAP criteria are code-and-docs complete after 16-03; this plan produces
the live evidence (authorised-if-cheap per CONTEXT) and the phase-close verification.
Output: recorded run evidence in the SUMMARY, green phase gate, human sign-off.

The production flip (setting CRITIC_MODEL on Fly) is explicitly OUT OF SCOPE — an
operator decision deferred by CONTEXT. Unset in production keeps today's behaviour
exactly (the neutral default 16-01 proved byte-identical).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-independent-critic-model/16-CONTEXT.md
@.planning/phases/16-independent-critic-model/16-VALIDATION.md
@src/research_agent/graph.py

<interfaces>
Live-leg mechanics (RESEARCH Environment Availability):
- Requires ANTHROPIC_API_KEY + VOYAGE_API_KEY in the operator's shell. If absent, the
  demonstration is DEFERRED — record that, do not improvise.
- Cheapest honest demonstration: the graph directly (REPL/CLI), not the service — e.g.
  `CRITIC_MODEL=claude-haiku-4-5 .venv/bin/python -m research_agent.graph` runs the
  module's __main__ question, or an inline `app.invoke(initial_state(...))` one-liner
  with a short question. Cost ≈ $0.15-0.25 with a haiku critic (Finding 3); haiku chosen
  over opus because it demonstrates the same attribution for less money.
- Evidence to capture: the run's final usage dict (cost_usd, pricing_unknown False) and
  the per-call "model call" log lines — the critic line must say claude-haiku-4-5 while
  writer/researcher/classifier lines say claude-sonnet-5. The log line carries
  cost_usd per call (graph.py:117), which is the per-node attribution made visible.
- Local PG on :54329 is NOT needed — the graph run uses the in-process store path; if any
  store issue arises, restart PG with LC_ALL=C per the standing note.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The live demonstration (authorised-if-cheap; defer honestly if keys absent)</name>
  <files>.planning/phases/16-independent-critic-model/16-04-SUMMARY.md</files>
  <read_first>16-VALIDATION.md Manual-Only Verifications; graph.py call_model log extra (lines 105-120)</read_first>
  <action>
    Check for keys first: if ANTHROPIC_API_KEY or VOYAGE_API_KEY is missing from the
    environment, SKIP the run, record "deferred — no keys in the execution environment"
    in the SUMMARY, and proceed to Task 2. Spend ceiling: if the run's reported cost_usd
    exceeds $0.40, note it as an overrun — do not run a second time.

    With keys present (spend pre-authorised by CONTEXT at ~$0.15-0.25):
    1. Run ONE research question through the real graph with CRITIC_MODEL exported:
       `CRITIC_MODEL=claude-haiku-4-5` and a SHORT question (cheaper than the module
       default — e.g. invoke app on initial_state with a one-line factual question).
       Capture stdout/stderr including the JSON "model call" log lines.
    2. From the captured logs, extract per-node evidence: the critic call line shows
       model=claude-haiku-4-5 with its cost_usd; writer/researcher/classifier lines show
       model=claude-sonnet-5. The final usage dict shows pricing_unknown False and total
       cost_usd (record it).
    3. Confirm the return path: run nothing further with the var unset — the byte-identical
       neutral default was already proven by 16-01's payload-equality test; cite that test
       instead of paying for a second run.
    4. Write the record-or-defer statement into the SUMMARY: which happened, the question
       asked, total cost, and the critic-vs-writer model evidence (paste the two log
       lines). The production flip: explicitly NOT made; CRITIC_MODEL remains unset on Fly.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-independent-critic-model/16-04-SUMMARY.md && grep -qE "deferred|claude-haiku-4-5" .planning/phases/16-independent-critic-model/16-04-SUMMARY.md</automated>
  </verify>
  <done>Either the run happened and the SUMMARY carries the per-node log evidence + total cost, or it is recorded as deferred with the reason. Never silence.</done>
  <acceptance_criteria>
    - SUMMARY contains one of: (a) a "model call" log line with model claude-haiku-4-5 for node critic AND one with model claude-sonnet-5 for node writer, plus the run's total cost_usd; or (b) the explicit deferral statement with reason
    - SUMMARY states verbatim that the production flip was not made
    - Total live spend this phase ≤ $0.40 (target $0.15-0.25)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: The full phase gate</name>
  <files>.planning/phases/16-independent-critic-model/16-04-SUMMARY.md</files>
  <read_first>16-VALIDATION.md Validation Sign-Off; .planning/ROADMAP.md § Phase 16 criteria</read_first>
  <action>
    Run the phase gate in a shell with CRITIC_MODEL UNSET (verify: `env | grep -c
    CRITIC_MODEL` → 0 — Pitfall 4: CI must never see it, and neither may the gate):
    1. `.venv/bin/pytest` — plain suite; expect ≥663 passed / 65 skipped plus this
       phase's new tests, zero failures.
    2. Armed leg: with the local PG on :54329 up (restart with LC_ALL=C if down), run the
       armed suite the way prior phases did; expect the armed baseline (was 727/1) plus
       new tests.
    3. `.venv/bin/ruff check .` — clean.
    4. `ANTHROPIC_API_KEY="" .venv/bin/python -m evals` — 41/41 keyless, exit 0.
    5. `git diff --stat main -- .github/workflows/ci.yml` — zero diffs (the keyless
       invariant never moved).
    6. Verify the five ROADMAP criteria against the tree and list each with its evidence
       in the SUMMARY (SC-1: accessor + threading tests; SC-2: discriminator + gate;
       SC-3: limits/OPERATIONS prose; SC-4: ADR-0010 + 0005 status line; SC-5: README
       greps).
    Record every count in the SUMMARY. Any red stops the phase close — fix or surface,
    never waive.
  </action>
  <verify>
    <automated>.venv/bin/pytest -q 2>&1 | tail -1 && .venv/bin/ruff check . && ANTHROPIC_API_KEY="" .venv/bin/python -m evals && git diff --quiet main -- .github/workflows/ci.yml</automated>
  </verify>
  <done>All five gate legs green with counts recorded; the five criteria each mapped to concrete evidence in the SUMMARY.</done>
  <acceptance_criteria>
    - Plain suite: 0 failed, ≥663 passed, 65 skipped; armed suite: 0 failed, ≥727 passed
    - ruff clean; offline evals 41/41 exit 0 with ANTHROPIC_API_KEY="" and CRITIC_MODEL unset
    - ci.yml zero diffs against main
    - SUMMARY has a five-row criteria table: SC-1..SC-5, each with the file/test evidence
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase close sign-off — the spend record and the criteria table</name>
  <files>.planning/phases/16-independent-critic-model/16-04-SUMMARY.md</files>
  <action>Present the phase-close evidence to the user and pause: the record-or-defer statement from Task 1, the five-criteria evidence table and gate counts from Task 2, and the spot-check pointers below. Do no further work until the resume signal.</action>
  <what-built>The independent-critic phase, complete: CRITIC_MODEL seam with four-site threading and attribution proofs (16-01); fixture gate with backfill, collision warning, reservation threshold (16-02); ADR-0010 supersession and the README/prose honesty pass (16-03); the live demonstration (or its recorded deferral) and a green phase gate (16-04).</what-built>
  <how-to-verify>
    1. Read .planning/phases/16-independent-critic-model/16-04-SUMMARY.md: the
       record-or-defer statement (was the ~$0.15-0.25 haiku-critic run made? at what
       cost?), and the five-criteria evidence table.
    2. Spot-check the reversal: `git diff main -- docs/adr/0005-opus-5-eval-judge.md`
       (exactly the status line) and read docs/adr/0010-judge-rederived-for-an-independent-critic.md
       — does the re-derived argument hold water for you?
    3. Spot-check the README: the limitation bullet is gone, the status list carries 16.
    4. Confirm you accept the live-leg outcome (run or deferral) and that the production
       flip stays deferred.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe issues (e.g. the ADR argument, the README residual, or an unacceptable deferral)</resume-signal>
  <verify>
    <human-check>User has read the SUMMARY's record-or-defer statement and criteria table and responded</human-check>
  </verify>
  <done>User typed "approved", or the described issues were resolved and re-presented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator wallet → live leg | one paid run; ceiling $0.40, target $0.15-0.25, pre-authorised by CONTEXT |
| local env → phase gate | a CRITIC_MODEL leak into the gate shell would stale the fixture and mask the keyless invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Denial of service | live-leg spend | mitigate | keys-check + short question + $0.40 ceiling + no second run |
| T-16-11 | Repudiation | record-or-defer statement | mitigate | SUMMARY grep gate — silence is not an outcome |
| T-16-12 | Tampering | gate shell env | mitigate | explicit env check before the gate (CRITIC_MODEL unset) |
| T-16-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
The phase gate IS this plan's verification (Task 2). Additionally: one PR for the whole
phase per the standing instruction — all four plans' commits ride
gsd/phase-16-independent-critic; no branch creation or switching.
</verification>

<success_criteria>
- VALIDATION row 11 (LIVE) satisfied: run-with-evidence or explicit deferral
- All five ROADMAP criteria evidenced; production flip untouched
- Plain + armed + ruff + keyless evals green; ci.yml zero diffs
- Human sign-off recorded
</success_criteria>

<output>
`.planning/phases/16-independent-critic-model/16-04-SUMMARY.md` is written incrementally by
Tasks 1-2 and finalized after the Task 3 sign-off.
</output>
