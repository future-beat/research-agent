---
phase: 16-independent-critic-model
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - evals/harness.py
  - evals/__main__.py
  - tests/test_evals.py
  - src/research_agent/limits.py
  - docs/OPERATIONS.md
autonomous: true
requirements: [REQ-independent-critic-model]

must_haves:
  truths:
    - "The one recorded fixture stays green with CRITIC_MODEL unset (offline 41/41 keyless preserved) and goes stale the moment an operator sets it (SC-2 fixture leg)"
    - "New recordings always carry a critic entry in the models map"
    - "The $0.20 reservation stays flat with a documented threshold naming the Opus-critic typical, the 3-call worst case, and the Sept-1 Sonnet boundary (SC-3)"
    - "Record mode warns on stderr when the judge and critic share a model"
  artifacts:
    - path: "evals/harness.py"
      provides: "grade_fixture_current with backfill semantics + recorder writing the critic entry + collision warning"
      contains: "critic_model"
    - path: "src/research_agent/limits.py"
      provides: "reserved_run_usd docstring extended with the critic threshold"
      contains: "CRITIC_MODEL"
    - path: "docs/OPERATIONS.md"
      provides: "CRITIC_MODEL / DEMO_RESERVED_RUN_USD pairing in the flip guidance"
      contains: "CRITIC_MODEL"
  key_links:
    - from: "evals/harness.py grade_fixture_current"
      to: "graph.critic_model()"
      via: "backfill comparison: models.get('critic') or models['pipeline']"
      pattern: "get\\(.critic.\\)"
    - from: "evals/harness.py record_case_to_fixture"
      to: "fixture models map"
      via: "models={'pipeline': ..., 'judge': ..., 'critic': graph.critic_model()}"
      pattern: "critic.*critic_model\\(\\)"
---

<objective>
Extend Phase 15's fixture staleness gate for the independent critic (backfill semantics),
make the recorder write the critic entry from now on, add the record-mode judge/critic
collision warning, and document the reservation threshold in limits.py + OPERATIONS.

Purpose: SC-2's fixture leg (a critic-model change must be able to stale recordings),
SC-3 (the spend cap accounts for a costlier critic path — as a documented threshold, not a
model-aware coupling), and ADR-0010's known-limit warning.
Output: extended gate + recorder + warning, updated designed-red pins in test_evals.py,
reservation prose in two places. Offline evals stay 41/41 keyless.

Implements CONTEXT decisions: backfill semantics (`models.get("critic") or
models["pipeline"]`); the fixture stays green while CRITIC_MODEL is unset; re-recording
DEFERRED to the full record run; flat $0.20 + documented threshold; stderr collision
warning. Discretion exercised and recorded: no `/pricing` or `/health` surfacing this
phase (CONTEXT left it open "if at all"; the fixture map, logs, and pricing_unknown
already carry the critic model — an additive `/pricing` block is deferred, noted in the
SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-independent-critic-model/16-CONTEXT.md
@.planning/phases/16-independent-critic-model/16-RESEARCH.md
@evals/harness.py
@evals/fixtures.py
@tests/test_evals.py
@src/research_agent/limits.py
@docs/OPERATIONS.md

<interfaces>
From evals/harness.py (today):
- `grade_fixture_current(fixture) -> G.Grade` at line 331: compares ONLY
  `fixture["models"]["pipeline"]` against `graph.MODEL`. Its docstring's "Cannot catch:"
  paragraph describes exactly this phase's gap and is CONTENT-PINNED by
  tests/test_evals.py:1648-1656 (`assert "Cannot catch:" in doc`, `"graph.MODEL" in doc`,
  `"CRITIC" in doc and "Phase 16" in doc`) — docstring and pin MUST move together.
- `record_case_to_fixture` at ~line 470: builds `models={"pipeline": graph.MODEL,
  "judge": judge.model}` at line 512; the comment above it says a map "Phase 16 moves one
  of these models without moving the other".
- `record_suite` at line 562: receives `judge`, loops cases; the natural once-per-run
  warning site. `judge.model` exists (graders.py Judge class, lines 658-660).
- Report dicts carry `"model": graph.MODEL` at lines 604 and 670 — ADDITIVE ONLY; never
  rename (`/pricing`'s "model" key and evals __main__ read them).

From evals/fixtures.py:
- `REQUIRED_MODEL_ROLES = ("pipeline", "judge")` — DO NOT add "critic" (hard-stale
  rejected in RESEARCH Finding 5: it converts a graded red into a load error and destroys
  a $0.24 fixture). `build_fixture` keeps extra roles verbatim already — zero fixtures.py
  changes needed.
- The one fixture evals/fixtures/technical-figures.json:
  `models == {"pipeline": "claude-sonnet-5", "judge": "claude-opus-5"}` — no critic key;
  recorded 2026-08-10 at 225b06b when the code HAD no critic seam, so its critic ran on
  graph.MODEL by construction. That is what makes backfill honest.

From graph (built in 16-01): `graph.critic_model()` — env-read per call; unset → graph.MODEL.

From tests/test_evals.py:
- :2048-2068 `test_record_writes_a_fixture_per_case_with_fakes` pins at :2062
  `fixture["models"] == {"pipeline": graph.MODEL, "judge": "claude-opus-5"}` — a DESIGNED
  RED once the recorder writes the critic entry; update the expected dict deliberately.
- `record_with_fakes` (~:2030) uses `FakeJudge`; `replayable()`/`committed()` build
  fixtures for replay tests; every replay test monkeypatches FIXTURES_DIR (load-bearing
  since 15-06 committed the first recording).

From src/research_agent/limits.py:
- `reserved_run_usd()` lines 114-130: default 0.20; docstring carries Phase 14's cap-note
  idiom ("Sized on observation, not on the per-run hard cap"); limits.py deliberately
  imports neither usage nor graph — keep it that way.

From docs/OPERATIONS.md:
- Config table row `DEMO_RESERVED_RUN_USD` at :467; the Phase 14 cap-note block at
  :541-551 ends "Revisit the default if a future pricing dimension pushes a typical run
  above $0.20 — raise DEMO_RESERVED_RUN_USD proportionally" — the exact idiom to extend.
- Baseline: `grep -c CRITIC_MODEL docs/OPERATIONS.md` is 0 today.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate backfill + recorder critic entry + docstring rewrite + preview note</name>
  <files>evals/harness.py, evals/__main__.py</files>
  <read_first>evals/harness.py lines 320-365 (gate + docstring), 470-518 (recorder); evals/__main__.py lines 195-280 (_assumed_pipeline_cost and its constants comment)</read_first>
  <action>
    1. `grade_fixture_current`: keep the pipeline comparison; add the critic comparison with
       BACKFILL semantics — `recorded_critic = fixture.get("models", {}).get("critic") or
       recorded_pipeline`; green only when `recorded_pipeline == graph.MODEL` AND
       `recorded_critic == graph.critic_model()`. The stale detail message must NAME the
       critic role and models when the critic comparison is what failed (the operator's
       first question is "which model moved"). Behaviour by construction: CRITIC_MODEL
       unset → critic_model() == MODEL → technical-figures stays green, offline stays
       41/41 keyless; the moment the suite's environment sets CRITIC_MODEL, the fixture
       goes stale — the DESIGNED staleness, not a bug.
    2. Rewrite the gate's docstring honestly: it now compares pipeline AND critic; the
       backfill is a fact about pre-16 recordings, not a guess (at record time the code had
       no critic seam, so the critic ran on graph.MODEL — absence of the key means pre-16;
       from this phase on the recorder always writes it, and the pre-16 population is
       exactly one file, replaced when the deferred full record run happens). State the NEW
       claim boundary ("Cannot catch:" a change to any model the map still does not
       compare — e.g. the judge's, whose recorded verdicts are fixed data). This rewrite
       lands TOGETHER with the pin updates IN THIS TASK'S COMMIT — checker blocker fix:
       a Task-1 commit that breaks the pins at tests/test_evals.py:2062 (models-map) and
       :1648-1656 (docstring content) while deferring their updates to Task 2 is a
       knowingly-red commit, violating the same-task designed-red criterion and the
       clean-history standing instruction. Therefore: update BOTH pins in this task, in the
       same commit as the recorder and docstring changes. Task 2 keeps the NEW gate/recorder
       tests only. This task's verify must run
       `pytest tests/test_evals.py -k "models_map or docstring or fixture" --collect-only`
       then the selected tests, not merely `python -m evals` — a knowingly-red suite must be
       impossible at this commit.
    3. `record_case_to_fixture`: extend the map to
       `models={"pipeline": graph.MODEL, "judge": judge.model, "critic": graph.critic_model()}`.
       Update the adjacent comment: the map now says which model did which job, per role.
    4. `evals/__main__.py`: add one comment at `_assumed_pipeline_cost`'s constants noting
       the preview lumps the whole pipeline turn at graph.MODEL and therefore under-quotes
       when CRITIC_MODEL points at a pricier model — the honest-note follow-up to Phase
       15's 35% under-quote correction. Comment only; no behaviour change this phase.
  </action>
  <verify>
    <automated>ANTHROPIC_API_KEY="" .venv/bin/python -m evals</automated>
  </verify>
  <done>Gate compares both roles with backfill; recorder writes the critic entry; docstring states the new boundary; offline evals still 41/41 keyless with CRITIC_MODEL unset in this shell.</done>
  <acceptance_criteria>
    - `ANTHROPIC_API_KEY="" .venv/bin/python -m evals` exits 0 reporting 41/41, CRITIC_MODEL unset
    - `grep -n "critic" evals/harness.py` shows the backfill expression in grade_fixture_current and the critic entry in record_case_to_fixture's models map
    - `git diff --stat evals/fixtures.py evals/fixtures/` is empty — REQUIRED_MODEL_ROLES unchanged, no fixture re-record (deferred per CONTEXT)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate + recorder tests (pin updates moved to Task 1 — checker blocker fix)</name>
  <files>tests/test_evals.py</files>
  <read_first>tests/test_evals.py :1648-1656 (docstring pin), :2048-2068 (models-map pin), the replayable()/committed() helpers, record_with_fakes (~:2030); 16-VALIDATION.md row 6</read_first>
  <behavior>
    - fixture_critic_gate family: (a) CRITIC_MODEL unset → a pre-16-shaped fixture (no critic key) grades green via backfill; (b) CRITIC_MODEL set to another name → stale, detail names the critic; (c) a fixture WITH a critic key is compared directly — backfill not consulted
    - designed reds resolved: the :2062 map pin expects the three-role map; the :1648-1656 docstring pin expects the rewritten boundary
  </behavior>
  <action>
    Test names must satisfy the 16-VALIDATION selector `-k fixture_critic_gate`
    (`--collect-only` first — SIXTEEN vacuous gates across seven phases). Use
    monkeypatch.setenv/delenv for CRITIC_MODEL; build fixtures through the existing
    replayable()/committed() helpers and monkeypatch FIXTURES_DIR as every replay test does.

    Cases:
    a. delenv CRITIC_MODEL → grade_fixture_current on a fixture whose models map has no
       critic key (the pre-16 shape) passes — backfill compares pipeline against both.
    b. setenv CRITIC_MODEL="claude-haiku-4-5" → the same fixture is stale; assert the
       detail string contains "claude-haiku-4-5" (the operator learns which model moved).
    c. A fixture whose map carries "critic": "claude-haiku-4-5" while CRITIC_MODEL is
       unset (critic_model() == graph.MODEL) → stale: direct comparison beats backfill
       when the key is present.
    d. Recorder: update the :2062 pin — with fakes, a written fixture's models map is
       exactly {"pipeline": graph.MODEL, "judge": "claude-opus-5",
       "critic": graph.critic_model()} — this resolves Task 1's designed red; say so in
       the commit message.
    e. Docstring pin: update :1648-1656 to the rewritten docstring's claim boundary —
       keep asserting "Cannot catch:" is present, and pin at least one string proving the
       critic comparison is now claimed (e.g. "critic_model" in doc). Never weaken the pin
       to vacuity: it must red if someone reverts the docstring to the pre-16 text
       (prove by mutation: restore the old docstring, watch it red, restore).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_evals.py --collect-only -q -k fixture_critic_gate && .venv/bin/pytest tests/test_evals.py -k "fixture_critic_gate or fixture_current or (record and models)" && .venv/bin/pytest tests/test_evals.py</automated>
  </verify>
  <done>Selector collects ≥3; the two designed reds are resolved deliberately (updated, not deleted); all of test_evals.py green.</done>
  <acceptance_criteria>
    - `-k fixture_critic_gate` collects ≥3 tests under `--collect-only`
    - The tests formerly pinning at :2062 and :1648-1656 still exist and pass with updated expectations
    - Mutation probes recorded in SUMMARY: reverting the gate to pipeline-only comparison reds ONLY case (b)/(c) (VALIDATION mutation 3); reverting the recorder's critic entry reds ONLY the map pin (mutation 5); both restored after
    - `.venv/bin/pytest tests/test_evals.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Judge/critic collision warning + reservation threshold prose</name>
  <files>evals/harness.py, tests/test_evals.py, src/research_agent/limits.py, docs/OPERATIONS.md</files>
  <read_first>evals/harness.py record_suite (:562+); src/research_agent/limits.py :114-130; docs/OPERATIONS.md :460-475 and :541-551; 16-RESEARCH.md Finding 3</read_first>
  <action>
    1. Collision warning (CONTEXT decision): at the top of `record_suite`, once per record
       run, when `judge is not None and judge.model == graph.critic_model()`, print one
       line to sys.stderr stating that the judge and the in-graph critic share a model, so
       recorded verdicts correlate with the gate they audit (ADR-0010's known limit —
       name the ADR). A warning, never an error: both are legitimate operator knobs;
       policy belongs in the record, not a crash (RESEARCH Don't-Hand-Roll). Silent when
       they differ or judge is None.
    2. Test, name matching `-k judge_critic_collision_warning` (--collect-only first):
       drive record_with_fakes with CRITIC_MODEL monkeypatched to the FakeJudge's model
       (read the helper first — its model surfaces as "claude-opus-5" in the :2062 pin) →
       capsys err contains the warning exactly once; silent twin: differing models → no
       warning on stderr.
    3. `reserved_run_usd` docstring (limits.py): extend in Phase 14's cap-note idiom,
       stating: an Opus-class critic adds ~$0.03 to a typical run (≈$0.18 total — the
       $0.20 estimate STAYS honest); a fully-revised 3-critic-call worst case adds
       ~$0.08-0.13 (≈$0.28 — by design OUTSIDE the estimate: sized on the typical run,
       the $1.00 per-run cap bounds the tail, settle() replaces the estimate with real
       cost); and from 2026-09-01 the Sonnet standard rate ALONE lifts a typical run to
       ~$0.21-0.22 — the estimate breaks with no critic change at all. Advice: raise
       DEMO_RESERVED_RUN_USD to ~$0.30 when setting CRITIC_MODEL to a model pricier than
       the writer's. Do NOT make reserved_run_usd() model-aware — limits.py imports
       neither usage nor graph, and coupling the money store to the price table to correct
       a ≤$0.13 admission estimate buys nothing when settlement is already real-cost;
       record the rejected alternative in the SUMMARY for ADR-0010's consequences.
    4. docs/OPERATIONS.md: pair the operator steps — a short block near the Phase 14
       cap-note (:541-551, same idiom): when setting CRITIC_MODEL, review
       DEMO_RESERVED_RUN_USD (thresholds condensed from the docstring, including the
       2026-09-01 boundary sentence); note the service validates pricing (pricing_unknown)
       but NOT API compatibility of an arbitrary CRITIC_MODEL — supported:
       current-generation models (RESEARCH Pitfall 5/A1). Add CRITIC_MODEL to the
       configuration table: default "unset — the critic runs on MODEL".
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_evals.py --collect-only -q -k judge_critic_collision_warning && .venv/bin/pytest tests/test_evals.py -k judge_critic_collision_warning && grep -q "CRITIC_MODEL" src/research_agent/limits.py && grep -q "CRITIC_MODEL" docs/OPERATIONS.md && grep -q "2026-09-01" src/research_agent/limits.py && .venv/bin/pytest tests/test_limits.py</automated>
  </verify>
  <done>Warning fires on collision and only then, proven with capsys; reservation threshold documented in both places (baseline: zero CRITIC_MODEL mentions in either); reserved_run_usd() behaviour unchanged (test_limits.py green, no code change to the function body).</done>
  <acceptance_criteria>
    - `-k judge_critic_collision_warning` collects ≥1 and passes (collision fires once, non-collision silent)
    - `grep -c "CRITIC_MODEL" docs/OPERATIONS.md` ≥ 2 (config table + flip guidance); baseline was 0
    - `grep -q "2026-09-01" src/research_agent/limits.py` succeeds (the Sept-boundary sentence)
    - `.venv/bin/pytest tests/test_limits.py` exits 0 — reserved_run_usd() stays flat, docstring-only change
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture → CI verdict | a gate reading the wrong map key would grade stale recordings green forever |
| operator env → record spend | judge/critic collision degrades what recorded verdicts can claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | grade_fixture_current backfill | mitigate | direct-comparison-beats-backfill test (case c) — a present critic key is never ignored |
| T-16-05 | Repudiation | recorder models map | mitigate | :2062 pin updated to require the critic entry — absence means pre-16 by construction |
| T-16-06 | Information disclosure | judge==critic recordings | mitigate | stderr warning + ADR-0010 known limit (16-03); never a crash |
| T-16-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- CI-shape check: `ANTHROPIC_API_KEY="" .venv/bin/python -m evals` → 41/41 keyless, with
  CRITIC_MODEL confirmed unset in the shell (`env | grep -c CRITIC_MODEL` → 0).
- Full plain suite green: `.venv/bin/pytest` (baseline 663/65 + plan-01 and plan-02 tests).
- Zero diffs in evals/fixtures.py, evals/fixtures/, and .github/workflows/ci.yml.
- Mutation probes per task recorded in the SUMMARY; where a probe reds by an unrelated
  route, record which and why.
</verification>

<success_criteria>
- SC-2 fixture leg: gate goes stale on a critic-model change and only then; 41/41 keyless preserved
- SC-3: flat $0.20 + documented threshold (typical ~$0.18, worst ~$0.28 outside by design, Sept-1 boundary alone breaks the estimate) in limits.py docstring AND OPERATIONS
- VALIDATION rows 5 (reservation), 6 (fixture gate), 7 (collision warning) satisfied; row 9's harness-docstring half done here TOGETHER with its pin (the graders/DESIGN half is 16-03)
- Recorder writes the critic entry going forward; re-record stays deferred
</success_criteria>

<output>
Create `.planning/phases/16-independent-critic-model/16-02-SUMMARY.md` when done. Record:
collect-only counts, mutation-probe outcomes, the /pricing-surfacing deferral (discretion),
the rejected model-aware-reservation alternative (feeds ADR-0010), and any line drift.
</output>
