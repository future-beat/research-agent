---
phase: 16-independent-critic-model
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - docs/adr/0010-judge-rederived-for-an-independent-critic.md
  - docs/adr/0005-opus-5-eval-judge.md
  - docs/adr/README.md
  - evals/graders.py
  - docs/DESIGN.md
  - README.md
autonomous: true
requirements: [REQ-independent-critic-model]

must_haves:
  truths:
    - "The eval-judge choice is stated as a fresh decision with its own reasoning, superseding ADR-0005 (SC-4)"
    - "ADR-0010 records TWO positions from the USER DECISION: critic-stronger-than-writer as Hesam's quoted rationale, and the judge==critic acceptance — the honest narrowing of ADR-0005's independence claim"
    - "README no longer says 'The eval judge runs on a stronger model precisely because of this' — the limitation bullet is deleted, not rewritten (SC-5); the residual states reality: the critic runs on a STRONGER model than the writer, and the judge shares the critic's model"
    - "No document in the repo states the dead premise ('the critic shares the writer's model') as current fact"
  artifacts:
    - path: "docs/adr/0010-judge-rederived-for-an-independent-critic.md"
      provides: "the re-derived judge decision + the two recorded positions"
      contains: "supersedes ADR-0005"
    - path: "docs/adr/0005-opus-5-eval-judge.md"
      provides: "status-line-only supersession edit"
      contains: "Superseded by ADR-0010 (Phase 16)"
    - path: "README.md"
      provides: "whole-file pass; limitation deleted; v1.1 status entry for 16 stating what shipped"
  key_links:
    - from: "docs/adr/README.md index"
      to: "ADR-0010"
      via: "0005 and 0010 rows updated per the verbatim 3-step convention"
      pattern: "0010"
    - from: "docs/DESIGN.md:74"
      to: "ADR-0010"
      via: "one-line forward reference"
      pattern: "ADR-0010|0010-"
---

<objective>
Write ADR-0010 (the judge re-derived from scratch, plus the two positions the USER
DECISION created), perform the supersession mechanics on ADR-0005 and the index, and fix
every location where the dead premise lives: graders.py's module docstring, DESIGN.md:74's
forward reference, and the README whole-file pass with the limitation bullet DELETED and
a residual that states the new reality.

Purpose: SC-4 and SC-5 — the second-strongest reversal in the milestone, recorded rather
than quietly contradicted, now including the production flip's rationale in the user's
own words.
Output: docs/adr/ has 10 records; README's Limitations list no longer contains the critic
bullet; the Status list carries the phase-16 entry saying what actually shipped.

Implements CONTEXT decisions + USER DECISION (2026-08-10): ADR-0010 supersedes ADR-0005
(status-line edit ONLY on 0005; ADR-0002 untouched); "different job" stands alone;
independence = judge ≠ writer's model, with judge==critic recorded as an ACCEPTED
consequence of the chosen config (not merely a possible operator state); the
critic-stronger-than-writer stance recorded as Hesam's rationale, quoted verbatim;
structured-verdict half carried forward; DESIGN.md:74 one-line forward reference; README
limitation DELETED per the standing instruction (corrected twice) — grep first for facts
that live only in the deleted prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-independent-critic-model/16-CONTEXT.md
@.planning/phases/16-independent-critic-model/16-RESEARCH.md
@docs/adr/README.md
@docs/adr/0005-opus-5-eval-judge.md
@docs/adr/0007-anonymous-identity-fairness-global-cap.md
@evals/graders.py
@docs/DESIGN.md
@README.md

<interfaces>
Supersession convention (docs/adr/README.md, verbatim 3-step):
1. Overturned record: status line ONLY → `**Status:** Superseded by ADR-0010 (Phase 16)`.
   Context/Decision/Consequences are NEVER edited.
2. New record: `**Status:** Accepted — supersedes ADR-0005`; carries `**Source:**` (not
   `**Promoted from:**` — it originates in this phase; the 0006-0009 precedent).
3. Index: update Status and Superseded-by cells for BOTH rows; the pre-named forecast on
   the 0005 row lands.
Precedent for the carry-forward shape: ADR-0007's "Carried forward from ADR-0006" section.

Index prose that ALSO goes stale with a tenth record (docs/adr/README.md):
- Line ~46: "Eight of the nine records are `Accepted` today. One supersession has
  actually happened" — becomes nine of ten / two supersessions.
- Line ~63: "ADR-0006 through ADR-0009 are the odd ones out" (the Source-vs-Promoted
  paragraph) — 0010 joins the Source: group.

The USER DECISION this plan must record (CONTEXT § "USER DECISION (2026-08-10)"):
- Verbatim rationale to quote in ADR-0010: "Use Opus as the critic's model since it has
  to be more capable than the writer's model." — Hesam's call, a NEW design position,
  not an inference from research.
- Production config: CRITIC_MODEL = 'claude-opus-5' in fly.toml [env] (16-04's cutover);
  code default stays neutral (unset → writer's model).
- Consequence: judge (claude-opus-5) and critic (claude-opus-5) share a model in
  production — the collision warning (16-02) fires on the chosen config as a fact.

Stale-prose inventory (grep-verified, RESEARCH Finding 6):
- evals/graders.py:13-17 module docstring: "The critic inside the graph shares the
  writer's model, which makes it a decent proofreader..." — states the dead premise as
  fact; REWRITE (this is code prose, not the README limitation).
- docs/DESIGN.md:74: "**The judge runs on a different, stronger model than the
  pipeline.** The in-graph critic shares the writer's model ... Recorded as ADR-0005."
  — one-line forward-reference update only; the narrative stays (the ADR README says
  DESIGN "stays as it is"; Phase 10's precedent allows fixing what is verifiably false).
- README.md:252 (Limitations): "**The critic shares the writer's model.** ... The eval
  judge runs on a stronger model precisely because of this." — DELETE the bullet.
- README.md:32 (Status, phase 6): "...LLM judge on a stronger model." — SURVIVES:
  factually true (Opus default vs Sonnet); the reason is not stated there.
- README v1.1 status list ends at line 46 with "15 — Answer-quality evals"; the new
  phase-16 entry goes after it.
- docs/adr/0002: NOT edited (its "Known limit" prose becomes configuration-dependent, but
  the convention forbids editing content; ADR-0010's prose carries the one-line residual).

Pinning tests relevant here:
- tests/test_evals.py:464 `assert G.JUDGE_MODEL != graph.MODEL` — SURVIVES UNCHANGED (it
  pins exactly the independence ADR-0010 re-targets: judge ≠ WRITER; the judge deliberately
  EQUALS the critic's production model, which no test forbids). Do not touch it.
- Before editing graders.py prose, grep tests/ for pins on the graders module docstring
  (the harness docstring pin was :1648-1656, handled in 16-02; verify none exists for
  graders.py's module docstring and record the check).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ADR-0010 + supersession mechanics on 0005 and the index</name>
  <files>docs/adr/0010-judge-rederived-for-an-independent-critic.md, docs/adr/0005-opus-5-eval-judge.md, docs/adr/README.md</files>
  <read_first>docs/adr/README.md (whole file); docs/adr/0005-opus-5-eval-judge.md; docs/adr/0007-anonymous-identity-fairness-global-cap.md (the Carried-forward section shape); 16-RESEARCH.md Finding 4; 16-CONTEXT.md § USER DECISION</read_first>
  <action>
    Create docs/adr/0010-judge-rederived-for-an-independent-critic.md following the record
    shape (title line `# ADR-0010 — ...`, Status, Source, Context/Decision/Consequences).
    Status: `**Status:** Accepted — supersedes ADR-0005`. Provenance: `**Source:**` naming
    Phase 16 / REQ-independent-critic-model (not Promoted from).

    The argument (own it in the record — RESEARCH Finding 4 is the draft; the USER
    DECISION adds two positions it must state):
    - Context: ADR-0005's premise ("the critic shares the writer's model") is removed by
      this phase — CRITIC_MODEL makes the critic's model independent, defaulting to
      shared in code and SET TO claude-opus-5 in production (fly.toml [env], the 16-04
      cutover). The question answered from scratch: with an independent critic, what is
      the judge FOR?
    - Decision, four legs:
      (a) DIFFERENT JOB — stands alone: the critic gates drafts against notes, inline,
          shaping what ships; the judge grades final answers against question + rubric,
          retrospectively, across the golden set — its verdicts are the recording refusal
          gate and the replayed assertions of every keyless CI run. True whatever models
          either runs on: removing the shared-model premise removed a reason the judge had
          to be STRONGER, not a reason the judge EXISTS.
      (b) CRITIC STRONGER THAN WRITER — a NEW design position, the USER'S, quoted
          verbatim: "Use Opus as the critic's model since it has to be more capable than
          the writer's model." This is Hesam's rationale, not an inference; the record
          states it as the reason production pins CRITIC_MODEL to claude-opus-5. Where
          ADR-0005 justified a strong JUDGE by a weak critic, this position wants the
          CRITIC itself stronger than what it gates.
      (c) INDEPENDENCE — re-targeted, and honestly narrowed: the hard requirement is
          judge ≠ WRITER's model (JUDGE_MODEL != graph.MODEL, already pinned at
          tests/test_evals.py:464, which survives unchanged). Judge-vs-CRITIC
          independence is ACCEPTED AS LOST under the chosen config: the judge is
          independent of the writer, and deliberately shares the critic's model
          (both claude-opus-5) — so its verdicts are not independent of the critic's
          family. That is the honest narrowing of ADR-0005's independence claim, recorded
          as an acceptance rather than discovered later; record mode states the fact on
          stderr (16-02) rather than forbidding. Honest note: Opus 5 and Sonnet 5 share
          a vendor and family anyway; cross-family judging would be stronger independence
          but the harness is Anthropic-SDK-only and out of scope — ADR-0005 never claimed
          it either.
      (d) "STRONGER" FOR THE JUDGE — demoted to preference: a defensible default for a
          discrimination task across arbitrary domains, at eval-time-only cost — but
          "stronger because the critic is weak" is dead. (Strength survives as a REASON
          only in leg (b), where it is the user's position about the critic.)
    - Conclusion: JUDGE_MODEL stays claude-opus-5 (no judge flip); the CRITIC flips to
      claude-opus-5 in production via fly.toml [env] — the flip is this phase's
      deliverable, executed in 16-04 after the phase PR merges. Had the re-derivation
      concluded the JUDGE should change, that flip would be a recorded consequence and
      deferred (CONTEXT).
    - Carried forward from ADR-0005 (the ADR-0007 section shape): the structured-verdict
      half, verbatim in substance — a mis-parsed verdict is a confident wrong number.
    - Consequences also note: ADR-0002 is deliberately not edited — its "Known limit"
      becomes configuration-dependent, and the one-line residual is stated here:
      independence is configuration; production configures the critic ABOVE the writer.
      Record the rejected model-aware reservation (from 16-02's SUMMARY) as a considered
      alternative, and the demo's per-run cost rising deliberately (typical ≈ $0.18
      against the $0.20 reservation, thresholds in limits.py/OPERATIONS).

    Then the mechanics, verbatim per the convention:
    1. 0005 status line → `**Status:** Superseded by ADR-0010 (Phase 16)`. NOTHING else
       in 0005 changes — the diff must be exactly one line.
    2. Index: 0005 row Status → Superseded, Superseded-by → ADR-0010 link (the pre-named
       forecast lands); add the 0010 row (Accepted — supersedes ADR-0005).
    3. Update the index's counting prose: "Eight of the nine records" → nine of ten, "One
       supersession has actually happened" → two (0007→0006 in Phase 12, 0010→0005 in
       Phase 16); fold 0010 into the Source:-carrying group paragraph ("0006 through
       0009" → through 0010, adjusting the sentence that enumerates origins).
  </action>
  <verify>
    <automated>ls docs/adr/00*.md | wc -l | grep -q 10 && grep -q "Superseded by ADR-0010 (Phase 16)" docs/adr/0005-opus-5-eval-judge.md && grep -qc "supersedes ADR-0005" docs/adr/0010-judge-rederived-for-an-independent-critic.md && grep -q "more capable than the writer" docs/adr/0010-judge-rederived-for-an-independent-critic.md && git diff main --numstat -- docs/adr/0005-opus-5-eval-judge.md | awk 'END{exit !(NR==1 && $1==1 && $2==1)}' && git diff --quiet main -- docs/adr/0002-separate-critic-node.md && .venv/bin/pytest tests/test_evals.py -k judge</automated>
  </verify>
  <done>Ten records; 0005's diff is exactly one changed line (the status line); 0002 has zero diffs; the index rows and counting prose are consistent; ADR-0010 carries the verbatim user quote and the judge==critic acceptance; the :464 pin still passes untouched.</done>
  <acceptance_criteria>
    - `git diff main --numstat -- docs/adr/0005-opus-5-eval-judge.md` shows exactly 1 added / 1 removed line — against MAIN, not the working tree, which is empty after the commit and would pass vacuously (the checker caught the awk exiting 0 on empty input; the `END{exit !(NR==1 …)}` form also fails on zero lines)
    - `git diff main -- docs/adr/0002-separate-critic-node.md` is empty (against MAIN)
    - ADR-0010 contains: the different-job leg; the VERBATIM quote "more capable than the writer's model" attributed as the user's rationale (grep-gated above); the judge==critic ACCEPTANCE stating verdicts are not independent of the critic's family; "preference" for judge-stronger; a "Carried forward from ADR-0005" section; and the judge-stays / critic-flips-in-production conclusion naming fly.toml and 16-04
    - docs/adr/README.md index has a 0010 row and the 0005 row reads Superseded; the "Eight of the nine" and "0006 through 0009" prose is updated
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: graders.py docstring rewrite + DESIGN.md:74 forward reference</name>
  <files>evals/graders.py, docs/DESIGN.md</files>
  <read_first>evals/graders.py lines 1-31; docs/DESIGN.md line 74 and its section; grep output for docstring pins (see action)</read_first>
  <action>
    Grep FIRST for content pins: `grep -rn "graders" tests/test_evals.py | grep -i "doc\|__doc__"`
    and `grep -rn "decent proofreader\|shares the writer" tests/ evals/` — record the
    result; if a pin exists, update it together with the prose (the 16-02 harness
    precedent).

    1. evals/graders.py module docstring (lines 13-17): rewrite the third paragraph. It
       currently states the dead premise as fact ("The critic inside the graph shares the
       writer's model, which makes it a decent proofreader and a poor independent
       evaluator..."). New text states what is true now: the judge runs on a different
       model than the pipeline's writer (JUDGE_MODEL vs graph.MODEL) because a judge
       sharing the writer's model would inherit the blind spots it exists to find; the
       in-graph critic's model is configurable (CRITIC_MODEL, default: the writer's;
       production: claude-opus-5) — and in production the judge and critic share a model,
       an acceptance ADR-0010 records along with the judge's re-derived rationale. Keep
       it to roughly the same length — module prose, not an essay.
    2. docs/DESIGN.md:74: ONE-LINE forward-reference update only, the narrative stays
       (the ADR README says DESIGN stays as it is; Phase 10's precedent permits fixing
       what is verifiably false). Minimum change that stops the passage asserting a dead
       rationale as current: amend the closing sentence "Recorded as ADR-0005." to note
       supersession — e.g. "Recorded as [ADR-0005](adr/0005-opus-5-eval-judge.md),
       superseded in Phase 16 by [ADR-0010](adr/0010-judge-rederived-for-an-independent-critic.md)
       — the critic's model is configurable now (production runs it on Opus), and the
       judge's rationale is re-derived there." One sentence of edit; do not rewrite the
       paragraph.
  </action>
  <verify>
    <automated>! grep -q "decent proofreader" evals/graders.py && grep -q "ADR-0010\|0010-judge" evals/graders.py && grep -q "0010-judge" docs/DESIGN.md && .venv/bin/pytest tests/test_evals.py && git diff main --numstat -- docs/DESIGN.md | awk 'END{exit !(NR==1 && $1<=2 && $2<=1)}'</automated>
  </verify>
  <done>graders.py no longer states the shared-model premise as fact and points at ADR-0010; DESIGN.md:74's forward link resolves to 0010; DESIGN diff is ≤2 lines changed; the full evals test file stays green.</done>
  <acceptance_criteria>
    - `grep -c "shares the writer's model" evals/graders.py` prints 0
    - `grep -q "0010" docs/DESIGN.md` succeeds and the edit touches only the line-74 paragraph (git diff shows a single hunk)
    - Docstring-pin grep results recorded in the SUMMARY (found/none; if found, updated together)
    - `.venv/bin/pytest tests/test_evals.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: README whole-file pass — delete the limitation, state the new reality, add the status entry</name>
  <files>README.md</files>
  <read_first>README.md IN FULL (whole-file pass is the standing instruction); RESEARCH Finding 6's grep table (the facts-in-deleted-prose audit); 16-CONTEXT.md § USER DECISION</read_first>
  <action>
    Standing user instruction, corrected twice — follow exactly:
    1. GREP FIRST for facts that live only in the deleted prose:
       `grep -n "critic" README.md` and `grep -n "stronger model" README.md`. RESEARCH
       Finding 6 already audited: the only load-bearing fact in the bullet ("the judge
       runs on a stronger model") survives independently at README.md:32. Re-verify
       against the tree and record the grep output in the SUMMARY.
    2. DELETE the Limitations bullet at README.md:252 ("**The critic shares the writer's
       model.** ... precisely because of this.") — deleted, NOT rewritten. The residual
       (allowed as ONE short sentence, either a slim new bullet or attached where it
       fits) states REALITY, not the old hypothetical: the critic runs on a STRONGER
       model than the writer (CRITIC_MODEL pins claude-opus-5 in production; unset, it
       falls back to the writer's model), and the judge shares the critic's model — an
       acceptance recorded in ADR-0010. Nothing longer than a sentence (a link to the
       ADR is fine).
    3. Status list: add the v1.1 phase-16 entry after the 15 entry (line ~46), matching
       the established voice and length, and saying what SHIPPED — the capability AND the
       flip, e.g.: "**16 — Independent critic.** `CRITIC_MODEL` makes the critic
       separately configurable, and production pins it to Opus — the critic runs on a
       more capable model than the writer it gates; per-node cost attribution prices each
       call's model, and the eval-judge rationale is re-derived rather than inherited
       ([ADR-0010](docs/adr/0010-judge-rederived-for-an-independent-critic.md))."
       Checkbox state: match how earlier in-flight v1.1 entries are marked (13-15 are
       [x]; mark 16 [x] — it ships with this PR).
    4. WHOLE-FILE PASS: read every section for sentences the phase falsified — check at
       least: the Status list (32 survives as-is), the configuration/operations pointers
       (OPERATIONS gained CRITIC_MODEL in 16-02 — if README carries a config summary,
       keep it consistent, including that production sets the var in fly.toml [env]), the
       evals section (fixture staleness now also watches the critic model — one clause if
       the README describes the gate), and the remaining Limitations bullets (the "Only
       one of forty answers is recorded" bullet stays true — re-recording was deferred).
       Fix only what is false; this is a pass, not a rewrite.
  </action>
  <verify>
    <automated>! grep -q "precisely because of this" README.md && ! grep -q "The critic shares the writer's model" README.md && grep -q "CRITIC_MODEL" README.md && grep -qE "16 — |16 —" README.md</automated>
  </verify>
  <done>The limitation bullet is gone; the stronger-model fact survives at :32; the status list carries 16 and says the flip shipped; the residual is a single sentence stating the critic-stronger reality and the judge==critic acceptance; whole-file pass performed with findings recorded.</done>
  <acceptance_criteria>
    - `grep -c "precisely because of this" README.md` prints 0 (baseline: 1 at :252)
    - `grep -c "The critic shares the writer's model" README.md` prints 0 (baseline: 1)
    - Line 32's "LLM judge on a stronger model" phrase is still present (`grep -q "judge on a stronger model" README.md`)
    - The residual sentence exists and states the critic runs on a stronger model than the writer (not the old "configuration, not default" hypothetical); the v1.1 status list contains a 16 entry naming the flip; whole-file-pass findings (what was checked, what changed, what was verified still true) recorded in the SUMMARY
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs → reader trust | prose stating a dead premise as fact misleads every future reader and reviewer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Tampering | ADR-0005 content | mitigate | one-line-diff gate (git diff --numstat == 1/1); convention forbids content edits |
| T-16-08 | Repudiation | README facts lost with deleted prose | mitigate | grep-first audit; the surviving fact pinned by acceptance grep on :32's phrase |
| T-16-09 | Tampering | ADR-0002 accidental edit | mitigate | zero-diff gate on 0002 |
| T-16-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
- `.venv/bin/pytest` full plain suite green (no code changes in this plan besides a
  docstring; graders.py import must stay clean — ruff check).
- `.venv/bin/ruff check .` clean.
- `ANTHROPIC_API_KEY="" .venv/bin/python -m evals` → 41/41 keyless.
- `git diff docs/adr/0002-separate-critic-node.md` empty; 0005 diff exactly the status line.
- tests/test_evals.py:464 (`JUDGE_MODEL != graph.MODEL`) untouched and green.
</verification>

<success_criteria>
- SC-4: ADR-0010 exists with the four-leg argument — including the USER'S verbatim
  critic-stronger rationale and the judge==critic acceptance (the honest narrowing of
  ADR-0005's independence claim) — the carried-forward structured-verdict half, and the
  judge-stays / critic-flips-in-production conclusion; supersession mechanics exact
- SC-5: the README sentence and its bullet are gone; the deleted prose's only load-bearing fact survives at :32; the residual states the new reality
- VALIDATION rows 8 (ADR), 9's graders/DESIGN half, and 10 (README) satisfied
- No document states the dead premise AS CURRENT FACT — DESIGN.md:74's narrative may retain the phrase with the supersession note appended as current fact (grep-clean across README, DESIGN, graders)
</success_criteria>

<output>
Create `.planning/phases/16-independent-critic-model/16-03-SUMMARY.md` when done. Record:
the whole-file-pass findings, the pin-grep results for graders.py, and the residual
sentence chosen for the README.
</output>
