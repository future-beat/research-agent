---
phase: 16-independent-critic-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/research_agent/graph.py
  - tests/test_graph_smoke.py
autonomous: true
requirements: [REQ-independent-critic-model]

must_haves:
  truths:
    - "An operator can set CRITIC_MODEL and the critic node alone runs on that model (SC-1)"
    - "With CRITIC_MODEL unset, the deployed behaviour is byte-identical to today — every node sends graph.MODEL and the usage record is unchanged (SC-1 neutral default)"
    - "A critic call is priced at the critic's model rate, and an unpriced critic model fires pricing_unknown (SC-2)"
  artifacts:
    - path: "src/research_agent/graph.py"
      provides: "critic_model() accessor + model threaded through all four sites of call_model"
      contains: "def critic_model"
    - path: "tests/test_graph_smoke.py"
      provides: "accessor, four-site threading, misbilling discriminator, per-node attribution tests"
      contains: "critic_misbilling_discriminator"
  key_links:
    - from: "src/research_agent/graph.py critic_node"
      to: "critic_model()"
      via: "call_model(state, 'critic', model=critic_model(), ...)"
      pattern: "model=critic_model\\(\\)"
    - from: "src/research_agent/graph.py call_model"
      to: "usage_accounting.record"
      via: "the threaded model name, NOT the module constant"
      pattern: "record\\(state\\[.usage.\\], call, model\\)"
---

<objective>
Create `graph.critic_model()` and thread a per-call `model` through `call_model`'s four
naming sites (span :96, API call :99, `record()` :103, log :111), so the critic — and only
the critic — can run on `CRITIC_MODEL` while every other node stays byte-identical. Prove
attribution with the misbilling discriminator: an unpriced `CRITIC_MODEL` fires
`pricing_unknown` only if the threaded name reached `record()`.

Purpose: SC-1 (independent critic model, neutral default) and SC-2 (per-node cost
attribution). This is the phase's entire implementation seam; everything in later plans
consumes `critic_model()`.
Output: modified `graph.py`, new tests in `tests/test_graph_smoke.py`, whole suite green.

Implements CONTEXT decisions: CRITIC_MODEL env with neutral default; four-site threading
with attribution as a passed constant (the CORRECTED premise — `CallUsage.from_response`
never reads `response.model`; only `inference_geo` is response-observed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-independent-critic-model/16-CONTEXT.md
@.planning/phases/16-independent-critic-model/16-RESEARCH.md
@src/research_agent/graph.py
@src/research_agent/usage.py
@tests/test_graph_smoke.py

<interfaces>
From src/research_agent/graph.py (today — the seam to change):
- `MODEL = "claude-sonnet-5"` (module constant, line 38); `graph.py` does NOT import `os` today
- `call_model(state, node, **kwargs)` names the model at exactly four sites, all reading `MODEL`:
  line 96 `span(f"node.{node}", ..., model=MODEL, ...)`; line 99 `client().messages.create(model=MODEL, **kwargs)`;
  line 103 `usage_accounting.record(state["usage"], call, MODEL)`; line 111 `"model": MODEL` in the log extra
- `critic_node` (line 409) calls `call_model(state, "critic", max_tokens=2000, thinking={"type": "adaptive"}, output_config={"effort": "medium"}, messages=[...])`

From src/research_agent/usage.py:
- `record(totals, call, model, on=None) -> float` — already takes the model name; an
  `UnknownModelPricing` inside sets `totals["pricing_unknown"] = True` and returns 0.0
- PRICES rows: `claude-sonnet-5` (TWO dated windows, boundary 2026-08-31 — NEVER use in
  exact-cost assertions); `claude-opus-5` $5/$25 and `claude-haiku-4-5` $1/$5 (single
  UNDATED windows — safe for exact arithmetic)

From tests/test_graph_smoke.py:
- `FakeClient` (line 50): `create(self, **kwargs)` receives and currently IGNORES
  `kwargs["model"]`; records `(node, prompt)` tuples in `self.calls`; `Usage` fakes fixed
  1000 input / 100 output tokens per call, researcher bills 2 web searches
- `fake_client` fixture (line 89) monkeypatches `graph.client` and installs an InMemoryStore
- Env-read-per-call idiom: `sessions_token()` in limits.py — "Read per call, not cached in
  a module constant: tests flip it with monkeypatch.setenv"
- caplog idiom precedent: tests/test_identity.py:117-151
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: critic_model() accessor and four-site threading in call_model</name>
  <files>src/research_agent/graph.py</files>
  <read_first>src/research_agent/graph.py lines 24-47 (imports, MODEL), 84-121 (call_model), 409-436 (critic_node)</read_first>
  <action>
    Per the CONTEXT decisions (CRITIC_MODEL env, four-site threading):

    1. Add `import os` to graph.py's imports (it is absent today).
    2. Add `critic_model() -> str` near MODEL: returns `os.environ.get("CRITIC_MODEL", "").strip() or MODEL`.
       Read per call, never cached at module scope — the sessions_token idiom; a module-level
       read freezes the value at import and breaks the monkeypatch.setenv test idiom.
       Docstring states the neutral default: unset/blank means the writer's model, so
       deploying this phase is a no-op until an operator sets it. No validation beyond
       strip-or-default: an unknown-but-real model must be allowed (that is the feature);
       an unpriced one is pricing_unknown's job (DEC-12), not the accessor's.
    3. Change `call_model(state, node, **kwargs)` to `call_model(state, node, *, model=None, **kwargs)`
       (keyword-only, so every existing call site stays byte-identical). First line resolves
       `model = model or MODEL`. Then use the local `model` at ALL FOUR sites: the span kwarg
       at line 96, `client().messages.create(model=model, **kwargs)` at line 99,
       `usage_accounting.record(state["usage"], call, model)` at line 103, and the log extra
       `"model": model` at line 111. Missing any one site is the silent-misbilling failure
       this plan's tests exist to catch — an Opus critic recorded as Sonnet under-reports
       every critic call 2.5x with no error anywhere.
    4. In `critic_node`, pass `model=critic_model()` to its `call_model` call. No other
       node's call site changes. Model SELECTION happens at the call site; cost COMPUTATION
       stays in CallUsage.cost_usd (Phase 14's single multiplication point) — do not add a
       second resolution or multiplication site, and do not add a usage→graph import.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import os; os.environ['CRITIC_MODEL']='x-test'; from research_agent import graph; assert graph.critic_model()=='x-test'; os.environ['CRITIC_MODEL']='  '; assert graph.critic_model()==graph.MODEL; del os.environ['CRITIC_MODEL']; assert graph.critic_model()==graph.MODEL" && .venv/bin/pytest tests/test_graph_smoke.py tests/test_usage.py</automated>
  </verify>
  <done>graph.py has critic_model() and a keyword-only model parameter used at all four sites; critic_node passes model=critic_model(); existing smoke + usage tests all green with zero test edits (the neutral default holds).</done>
  <acceptance_criteria>
    - `grep -c "def critic_model" src/research_agent/graph.py` prints 1
    - `grep -c "model=critic_model()" src/research_agent/graph.py` prints 1 (critic_node only)
    - `grep -n "model=MODEL" src/research_agent/graph.py` prints nothing (all four sites read the local)
    - `.venv/bin/pytest tests/test_graph_smoke.py tests/test_usage.py` exits 0 with no test file changes in this task
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FakeClient kwargs capture + the four discriminating test families</name>
  <files>tests/test_graph_smoke.py</files>
  <read_first>tests/test_graph_smoke.py lines 30-110 (fakes + fixture); 16-RESEARCH.md "Code Examples" and "Discriminating mutations"; 16-VALIDATION.md rows 1-4</read_first>
  <behavior>
    - test_critic_model_accessor_*: unset → MODEL; set → env value; blank → MODEL; monkeypatch.setenv flip observed per call (kills the cached-at-import mutation)
    - test_critic_threading_four_sites_*: with CRITIC_MODEL=claude-haiku-4-5, the name reaches (a) the API payload, (b) the span, (c) record()/usage, (d) the log line — each asserted separately
    - test_critic_misbilling_discriminator_*: CRITIC_MODEL=some-unpriced-name → pricing_unknown True; unset → False; non-critic cost still accrues
    - test_per_node_attribution_*: exact arithmetic on UNDATED rows only (haiku $1/$5, opus $5/$25); Sonnet never appears in an exact assertion
  </behavior>
  <action>
    Extend FakeClient minimally: alongside `self.calls`, record the full create() kwargs per
    call (e.g. `self.calls_with_kwargs.append((node, dict(kwargs)))`) — two lines, no
    behaviour change; existing tests keep using `self.calls` untouched.

    Add four test families, names chosen so the 16-VALIDATION.md selectors match (run
    `--collect-only` on each selector before trusting it — SIXTEEN vacuous gates across
    seven phases is the standing warning):

    A. `-k critic_model_accessor` — the accessor behaviours in <behavior>, plus the
       NEUTRAL-DEFAULT BYTE-IDENTITY assertion (not a prose claim): with
       monkeypatch.delenv("CRITIC_MODEL", raising=False), invoke the full graph on
       FakeClient and assert (1) every captured call's kwargs["model"] == graph.MODEL,
       (2) the full per-call kwargs dicts compare equal (==) to those of a second run with
       CRITIC_MODEL explicitly set to graph.MODEL — set-to-same-value and unset produce
       identical payloads, so the default path adds nothing, and (3) the final usage dict
       has pricing_unknown False. This is also Pitfall 4's guard: CI must never see
       CRITIC_MODEL, and this test makes a future workflow export a red, not a
       mystery-stale fixture.

    B. `-k critic_threading_four_sites` — with monkeypatch.setenv("CRITIC_MODEL",
       "claude-haiku-4-5"), one graph run, then four SEPARATE assertions:
       (a) API site: from calls_with_kwargs, the critic call's kwargs["model"] ==
           "claude-haiku-4-5" AND every non-critic call's == graph.MODEL;
       (b) span site: monkeypatch graph.span with a recording context manager capturing
           (name, attributes); assert the "node.critic" span got model="claude-haiku-4-5"
           and "node.writer" got graph.MODEL;
       (c) record site: covered by the exact-arithmetic assertion in family D (state it in
           this test's docstring, or assert here that the run's cost_usd includes the
           haiku-rate critic call — see D);
       (d) log site: caplog at INFO (the tests/test_identity.py idiom) — find the
           "model call" records and assert the critic call's record has attribute
           model == "claude-haiku-4-5" and a writer call's has graph.MODEL. If the logger
           does not propagate to caplog, monkeypatch graph.log with a recorder capturing
           the extra dicts instead — assert the same two facts; note which route was used.

    C. `-k critic_misbilling_discriminator` — THE phase-central test.
       monkeypatch.setenv("CRITIC_MODEL", "some-unpriced-model"); full FakeClient run;
       assert result["usage"]["pricing_unknown"] is True AND result["usage"]["cost_usd"] > 0
       (writer/researcher still priced). Twin: delenv → pricing_unknown is False.
       Docstring must state the discrimination: pricing_unknown can only flip if the
       threaded name reached record() — threading messages.create alone changes nothing here.

    D. `-k per_node_attribution` — exact, date-safe arithmetic. FakeClient bills exactly
       1000 in / 100 out per call and 2 web searches on the researcher only. With
       CRITIC_MODEL=claude-haiku-4-5, the critic call's contribution is exactly
       (1000*1 + 100*5)/1e6 = $0.0015. Compute the expected total as: run-with-critic-on-
       haiku total minus run-with-CRITIC_MODEL-unset total == $0.0015 minus the same call's
       Sonnet-priced amount — OR, simpler and Sonnet-free: run once with
       CRITIC_MODEL=claude-haiku-4-5 and once with CRITIC_MODEL=claude-opus-5 and assert
       the totals differ by exactly ((1000*5+100*25) - (1000*1+100*5))/1e6 = $0.0060
       (both undated rows; the Sonnet-priced remainder cancels in the subtraction, and
       Phase 14 multipliers are at their 1.0 defaults under the fakes). Never write an
       exact assertion against claude-sonnet-5: its intro window closes 2026-08-31 and
       call_model prices at wall-clock today (Pitfall 1).

    Use monkeypatch.setenv/delenv exclusively for env manipulation so nothing leaks
    between tests.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_graph_smoke.py --collect-only -q -k "critic_model_accessor or critic_threading_four_sites or critic_misbilling_discriminator or per_node_attribution" && .venv/bin/pytest tests/test_graph_smoke.py -k "critic_model_accessor or critic_threading_four_sites or critic_misbilling_discriminator or per_node_attribution" && .venv/bin/pytest</automated>
  </verify>
  <done>All four selectors collect ≥1 test each and pass; full plain suite green at ≥663 passed (baseline 663/65 plus the new tests) with zero existing-test edits.</done>
  <acceptance_criteria>
    - Each of the four `-k` selectors collects at least 1 test under `--collect-only` (record the counts)
    - `.venv/bin/pytest` exits 0; passed count ≥ 663 + number of new tests; skipped stays 65
    - Mutation probes run and recorded in the SUMMARY (see verification block) — each probe reds ONLY its designated test family, then the tree is restored clean
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → billing | CRITIC_MODEL is operator input that changes what the invoice says; a mis-threaded name silently misbills |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Repudiation | call_model record() site | mitigate | misbilling discriminator: unpriced CRITIC_MODEL must flip pricing_unknown (Task 2C) |
| T-16-02 | Tampering | call_model API/span/log sites | mitigate | per-site assertions with a kwargs-capturing fake and span/log capture (Task 2B) |
| T-16-03 | Elevation | neutral default drift with CRITIC_MODEL unset | mitigate | full-payload dict-equality assertion (Task 2A) + untouched 663-test baseline |
| T-16-SC | Tampering | package installs | accept | no packages installed this phase (RESEARCH: audit not applicable) |
</threat_model>

<verification>
Mutation probes (gate discipline — perform each, record the result, revert):
1. Temporarily change record() back to `record(state["usage"], call, MODEL)` → ONLY the
   misbilling-discriminator family reds (and possibly D's arithmetic). Revert.
2. Temporarily change messages.create back to `model=MODEL` → ONLY the four-site API
   assertion (and span/log stay green — note it). Revert.
3. Temporarily cache the accessor (`_CRITIC = os.environ.get(...)` at module scope) → ONLY
   the accessor monkeypatch-flip test reds. Revert.
`git status` clean of probe leftovers before commit. Where a probe reds an extra test by an
unrelated route, record which and why (the 16-VALIDATION warning).

Phase-line check: `.venv/bin/pytest` (baseline 663/65 + new) and
`ANTHROPIC_API_KEY="" .venv/bin/python -m evals` still 41/41 keyless — CRITIC_MODEL is
unset in CI and nothing in this plan changes eval behaviour.
</verification>

<success_criteria>
- SC-1: critic model configurable via CRITIC_MODEL; unset is provably byte-identical (payload dict equality, not prose)
- SC-2: threaded model reaches record(); unpriced critic fires pricing_unknown; exact per-node arithmetic on undated rows only
- VALIDATION rows 1-4 (critic_model_accessor, critic_threading_four_sites, critic_misbilling_discriminator, per_node_attribution) each have a collecting, passing selector
- Full suite ≥663 passed / 65 skipped; offline evals 41/41 keyless
</success_criteria>

<output>
Create `.planning/phases/16-independent-critic-model/16-01-SUMMARY.md` when done. Record:
collect-only counts per selector, mutation-probe results (which test redded per probe), and
any line-number drift from the plan (the tree wins; record the deviation).
</output>
