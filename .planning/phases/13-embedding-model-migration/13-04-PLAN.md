---
phase: 13-embedding-model-migration
plan: 04
type: execute
wave: 4
depends_on: ["13-03"]
files_modified:
  - tests/test_migrate.py
  - docs/adr/0008-embedding-migration-two-commands.md
  - docs/adr/README.md
  - docs/OPERATIONS.md
  - README.md
  - .planning/intel/decisions.md
autonomous: true
requirements: [REQ-embedding-model-migration]

must_haves:
  truths:
    - "Cutover is explicit and reversible: the PGVECTOR_TABLE flip is the whole cutover, proven both directions; the old table survives (SC-3)"
    - "ADR-0008 records what survives of DEC-10 (copy-only IS its operation) and what is new (a measured re-embed path)"
    - "The README limitation about changing embedding models is rewritten honestly — the path exists, the check is still loud, the corpus-scale caveat stated"
  artifacts:
    - path: "tests/test_migrate.py"
      provides: "cutover_reversible gate — both directions, old table intact throughout"
    - path: "docs/adr/0008-embedding-migration-two-commands.md"
      provides: "Status: Accepted, Source: line (DEC-10 was never promoted — nothing to supersede), Context/Decision/Consequences"
      contains: "Source:"
    - path: "docs/OPERATIONS.md"
      provides: "Operator procedure: quiesce–migrate–flip, rollback = pointing back, manual old-table drop, first-count_tokens HF fetch note"
    - path: "README.md"
      provides: "Honest rewrite of the embedding-model limitation"
  key_links:
    - from: "docs/adr/README.md index table"
      to: "docs/adr/0008-embedding-migration-two-commands.md"
      via: "new index row, Status Accepted, Superseded by —"
      pattern: "0008"
    - from: ".planning/intel/decisions.md DEC-10"
      to: "ADR-0008"
      via: "one-line annotation pointing at the ADR"
      pattern: "ADR-0008"
---

<objective>
Prove the cutover both directions, then write the record: ADR-0008 (the DEC-10 reversal,
done honestly), the operator procedure in OPERATIONS.md, and the README limitation rewrite.

Purpose: SC-3's reversibility claim needs a test, not a paragraph; and this phase is a
design reversal, so the ADR is a deliverable, not paperwork.

Output: cutover gate + ADR-0008 + index/decisions annotations + OPERATIONS/README rewrites,
all with baselined grep gates and mutations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-embedding-model-migration/13-CONTEXT.md
@.planning/phases/13-embedding-model-migration/13-RESEARCH.md
@docs/adr/README.md
@.planning/intel/decisions.md
</context>

<interfaces>
From src/research_agent/memory.py:
  PGVECTOR_TABLE = os.environ.get("PGVECTOR_TABLE", "research_notes")   # line 58, read at import
  VECTOR_DIMENSIONS = int(os.environ.get("VECTOR_DIMENSIONS", "1024"))  # line 59
  PgVectorMemoryStore(table=..., dimensions=..., embedder=..., database=...)  # honors explicit table

From docs/adr/README.md:
  Record shape: title `# ADR-0008 — <Title>`, `**Status:**` line, provenance line (`**Source:**` for
  non-DESIGN.md records — the ADR-0006/0007 precedent), then ## Context / ## Decision / ## Consequences.
  Supersession convention applies only between numbered records — DEC-10 has no record, so the
  `Accepted — supersedes ADR-000M` form does NOT apply here.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → operator behaviour | OPERATIONS.md is what a future operator (or Hesam in three months) will actually follow during a real cutover |
| ADR prose → decision history | Overclaiming ("byte-identical recall through the index") would poison the record this phase exists to keep honest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-14 | Tampering (irreversible cutover) | rename/drop-based cutover docs | mitigate | Procedure is env-flip only; test proves both directions; docs state no command ever drops the old table |
| T-13-15 | Repudiation (dishonest docs) | README/ADR overclaiming | mitigate | ADR states the exact decomposition (byte join + exact scan, index excluded); README keeps the honest-scale caveat |
| T-13-16 | DoS (write loss during migration) | mid-migration writes | accept | Quiesce–migrate–flip stated plainly: the corpus is ≤7 days by TTL; any missed mid-migration note self-expires within a week — dual-write is theatre at this scale (RESEARCH A5, CONTEXT honest-scale note) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 13-04.1: The cutover gate — reversible both directions, old table intact throughout</name>
  <files>tests/test_migrate.py</files>
  <read_first>src/research_agent/memory.py lines 54–59 and PgVectorMemoryStore.__init__, tests/test_migrate.py copy tests (13-02.3), src/research_agent/recall_golden.py</read_first>
  <action>
    Add `test_cutover_reversible` (matches `-k cutover_reversible`), Postgres-gated:

    1. Seed `migration_test_notes_old` with the golden set; `embeddings copy` to
       `migration_test_notes_cutover`.
    2. "Flip forward": construct `PgVectorMemoryStore(table="migration_test_notes_cutover",
       dimensions=5, embedder=FakeEmbedder(), database=...)` — the explicit `table=` parameter is
       the same seam `PGVECTOR_TABLE` feeds at import (memory.py line 58 flows into the
       constructor default); assert `store.query(...)` for each golden query returns exactly the
       old table's exact-scan golden results.
    3. "Flip back": construct a store pointed at the OLD table; assert identical golden results
       again — rollback is pointing back, proven.
    4. Throughout: old-table row count taken before step 1 is unchanged after every step, and no
       step dropped or altered the old table.
    5. Include a comment stating the production act this test models: `fly secrets set
       PGVECTOR_TABLE=...` (+ `VECTOR_DIMENSIONS=N` after a re-embed) + restart; the env var is
       read at memory.py import, so the constructor's `table=` parameter is the testable core of
       the same seam (reimporting the module in-test is forbidden — Pitfall 5).

    Mutation (observe red, revert, record): point the "flip back" store at the cutover table
    instead of the old one after DELETEing a row from the cutover table → equality assertion red
    (proves the test distinguishes the two tables and is not vacuously comparing a table to itself).
  </action>
  <verify>
    <automated>DATABASE_URL=postgresql://postgres@localhost:54329/postgres .venv/bin/pytest tests/test_migrate.py -k cutover_reversible -x</automated>
  </verify>
  <acceptance_criteria>
    - Test green; the mutation observed red and recorded.
    - The test never sets PGVECTOR_TABLE via env + reimport; it exercises the constructor seam the env feeds.
    - Old-table integrity asserted at every step, not just at the end.
  </acceptance_criteria>
  <done>SC-3 proven both directions; VALIDATION row cutover_reversible satisfied.</done>
</task>

<task type="auto">
  <name>Task 13-04.2: ADR-0008 + index row + DEC-10 annotation</name>
  <files>docs/adr/0008-embedding-migration-two-commands.md, docs/adr/README.md, .planning/intel/decisions.md</files>
  <read_first>docs/adr/README.md (record shape + index + the "odd ones out" note), docs/adr/0007-anonymous-identity-fairness-global-cap.md (the Source:-line precedent and a Carried-forward section to imitate), .planning/intel/decisions.md DEC-10 entry, 13-RESEARCH.md §Pattern 6</read_first>
  <action>
    Create `docs/adr/0008-embedding-migration-two-commands.md`:
    - Title: `# ADR-0008 — Embedding migration is two commands: copy-only preserved, re-embed measured`
      (or tighter; keep the two-commands framing in the title).
    - `**Status:** Accepted` — NOT "supersedes ADR-000M": DEC-10 was never promoted to a numbered
      record, so there is nothing to supersede by the README's convention. Do not edit any
      existing ADR's status line.
    - `**Source:**` line naming `.planning/intel/decisions.md` DEC-10 and Phase 13 (the
      ADR-0006/0007 precedent for records with no DESIGN.md passage behind them). NOT
      `**Promoted from:**`.
    - `## Context`: DEC-10's rule and its real reason ("two suspects and no way to separate
      them"); why a re-embed path re-opens exactly that ambiguity; the honest-scale note (7-day
      TTL corpus — the value is the demonstrable path, not corpus rescue).
    - `## Decision`: two commands, never one. What survives of DEC-10: the copy-only command IS
      DEC-10's operation verbatim (same vectors, free, exact), and its rationale survives as the
      design rule that the two variables are never changed in one command — now MEASURED by the
      golden recall set rather than enforced by prohibition. What is new: the re-embed command,
      made safe by construction — golden set before/after, copy-only proving the infrastructure
      half, delta attributable to the model. Record the honest decomposition: byte-identical is
      asserted on vectors (SQL join) and on exact-scan query results, never through the HNSW
      index; index agreement is a separate scale-bounded set-equality check. Record
      quiesce–migrate–flip and why dual-write is rejected at this scale. Record that cutover is
      the pre-existing `PGVECTOR_TABLE`/`VECTOR_DIMENSIONS` env flip and rollback is pointing
      back; no command drops the old table.
    - `## Consequences`: `### Accepted` — Voyage spend now has a priced preview but still no
      `/metrics` accounting (named gap, Phase 14); the production model flip remains an operator
      decision the tooling now makes possible; a >2000-dimension target is refused (halfvec
      unbuilt). `### Rejected alternative` — one do-everything migrate command (re-confounds the
      variables DEC-10 separated).

    Update `docs/adr/README.md`:
    - Add the 0008 index row (Status `Accepted`, Superseded by `—`).
    - Update the index prose ("Six of the seven records..." and the odd-ones-out paragraph) to
      count eight records and include 0008 among the `Source:` records.

    Update `.planning/intel/decisions.md` DEC-10:
    - Append one line to the entry noting Phase 13's disposition: copy-only preserved, re-embed
      added under measurement — see ADR-0008. Do not rewrite the entry's original text.
  </action>
  <verify>
    <automated>test -f docs/adr/0008-embedding-migration-two-commands.md && grep -q "^\*\*Status:\*\* Accepted" docs/adr/0008-embedding-migration-two-commands.md && [ "$(grep -c "Source:" docs/adr/0008-embedding-migration-two-commands.md)" -ge 1 ] && [ "$(grep -c "Promoted from" docs/adr/0008-embedding-migration-two-commands.md)" -eq 0 ] && grep -q "0008" docs/adr/README.md && grep -q "ADR-0008" .planning/intel/decisions.md && ls docs/adr/0*.md | wc -l | grep -q 8 && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Measured baselines stated: 7 ADR record files today → 8 after; `Source:` occurrences in
      0008 == 1 (file did not exist); "ADR-0008" in decisions.md baseline 0 → >= 1.
    - Gate mutation (observe red, revert, record): temporarily change `**Source:**` to
      `**Promoted from:**` in 0008 → the verify command's Promoted-from clause fails.
    - The three `##` headings appear in README-mandated order; no existing ADR file modified
      except docs/adr/README.md's index/prose.
    - The ADR nowhere claims byte-identical recall through the index.
  </acceptance_criteria>
  <done>The reversal has an honest, convention-correct record; the index and DEC-10 point at it.</done>
</task>

<task type="auto">
  <name>Task 13-04.3: OPERATIONS.md procedure + README limitation rewrite</name>
  <files>docs/OPERATIONS.md, README.md</files>
  <read_first>README.md line ~209 (the limitation bullet), docs/OPERATIONS.md §"Going stateless"/migrate section and the env-var table row for PGVECTOR_TABLE/VECTOR_DIMENSIONS, 13-RESEARCH.md §Pattern 5</read_first>
  <action>
    docs/OPERATIONS.md — add an "Changing the embedding model or dimension" operator procedure
    (near the existing migrate section, which must also drop its "deliberately not exercised"
    claim now that migrate.py has real coverage — update that sentence honestly):
    1. Quiesce (stop or idle the service — stated plainly with the TTL justification: the corpus
       is ≤7 days of notes; a note written mid-migration and missed self-expires within a week, so
       dual-write machinery is deliberately not built).
    2. `python -m research_agent.migrate embeddings copy --from research_notes --to <new>` (same
       model: infrastructure-only move) OR `... embeddings re-embed --from research_notes --to
       <new> --model <m> [--dimensions N]` — preview prints, `--yes` required to spend; first
       `count_tokens` call fetches the HF tokenizer over the network once, then caches.
    3. Verify: the command's printed fidelity/recall numbers.
    4. Flip: `fly secrets set PGVECTOR_TABLE=<new>` (+ `VECTOR_DIMENSIONS=<N>` after a re-embed),
       restart. Rollback: point the env back — the old table survives; no command ever drops it.
    5. Dropping the old table is a manual operator act, after confirmation, never automated.
    6. Dimension ceiling note: >2000 refused (pgvector HNSW limit; voyage-3.5's 2048 unsupported
       until a halfvec path exists).

    README.md — rewrite the limitation bullet "Changing embedding model means a new pgvector
    table. The column width is fixed at creation; the dimension check fails loudly but can't
    migrate for you." The rewrite must be honest, not promotional: a new model still means a new
    table, but `python -m research_agent.migrate embeddings re-embed` now builds it — cost
    previewed before any spend, tenancy and TTL carried, cutover a reversible env flip — and a
    copy-only sibling moves a corpus without touching the model, so a recall change is
    attributable to the model, not the move. Keep the loud-check sentence (it still fails loudly;
    it still refuses to coerce). Add the scale caveat: with the 7-day note TTL the live corpus is
    tiny — the point is the path, not corpus rescue.
  </action>
  <verify>
    <automated>[ "$(grep -c "Changing embedding model means a new pgvector table" README.md)" -eq 0 ] && grep -q "embeddings re-embed" README.md && grep -q "embeddings re-embed" docs/OPERATIONS.md && grep -qi "rollback" docs/OPERATIONS.md && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - Measured baseline stated: the old phrase occurs exactly 1 time in README.md today → 0 after;
      "embeddings re-embed" README baseline 0 → >= 1. (memory.py's _check_dimensions message is
      code, not docs — it is deliberately untouched; the grep targets README.md only.)
    - Gate mutation (observe red, revert, record): restore the old phrase temporarily → the verify
      command's first clause fails.
    - OPERATIONS.md names all six steps including the manual drop and the quiesce justification;
      the "deliberately not exercised" claim about migrate.py is gone (baseline: present at
      OPERATIONS.md line ~200 today).
    - No doc claims byte-identical recall through the index or implies large-corpus rescue.
  </acceptance_criteria>
  <done>Operator procedure exists and is followable; the README limitation is honestly rewritten (per-phase README deliverable satisfied).</done>
</task>

</tasks>

<verification>
- `DATABASE_URL=postgresql://postgres@localhost:54329/postgres .venv/bin/pytest tests/test_migrate.py -x` green.
- Full suite armed: `DATABASE_URL=... REQUIRE_POSTGRES=1 .venv/bin/pytest` — 573 passed / 1 skipped maintained or improved.
- All grep gates pass with their baselines recorded and their mutations observed red.
</verification>

<success_criteria>
- SC-3 fully TRUE and tested; the reversal recorded per convention; docs honest.
- VALIDATION rows cutover_reversible / ADR grep gate / README grep gate satisfied by 13-04.1–13-04.3.
</success_criteria>

<output>
Create `.planning/phases/13-embedding-model-migration/13-04-SUMMARY.md` when done, including the mutation-red evidence table.
</output>
