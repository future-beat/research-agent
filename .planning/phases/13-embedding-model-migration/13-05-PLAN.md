---
phase: 13-embedding-model-migration
plan: 05
type: execute
wave: 5
depends_on: ["13-04"]
files_modified: []
autonomous: false
requirements: [REQ-embedding-model-migration]

must_haves:
  truths:
    - "The real Voyage re-embed path was demonstrated ONCE against Supabase scratch tables — seeded, migrated, verified, dropped — never the production notes table"
    - "The cost preview was shown before the spend and the actual billed tokens recorded against it"
    - "The copy-only path showed zero delta live; the re-embed delta was recorded as the model's, by construction"
    - "No scratch table remains in Supabase afterwards; no production config was flipped"
  artifacts:
    - path: ".planning/phases/13-embedding-model-migration/13-05-SUMMARY.md"
      provides: "Recorded demo transcript: preview, predicted vs actual tokens, copy zero-delta, re-embed delta, cleanup proof"
  key_links:
    - from: "the demo"
      to: "Supabase (session pooler)"
      via: "DATABASE_URL — the prod-shaped path (pooler, prepare_threshold=None, search_path with extensions)"
      pattern: "migration_demo_"
---

<objective>
Demonstrate the path live, once: seed Supabase scratch tables with real voyage-3.5
embeddings, run the copy (zero delta, proven live), run the re-embed to voyage-3.5-lite
(preview shown, ~cents spent with --yes, delta recorded), verify tenancy, drop everything,
and record the whole transcript. This is the portfolio artifact — demonstrable beats working.

Purpose: CONTEXT settled decision — the re-embed path is proven live once, against the
prod-shaped Supabase (pooler, search_path), never against `research_notes`, with no
production model flip.

Output: recorded demonstration in the SUMMARY; zero residue in Supabase.

**Branch note (do NOT perform git surgery in-plan):** this branch is stacked on
`gsd/phase-12-caller-identity`. If PR #6 has merged by execution time, the operator rebases
`gsd/phase-13-embedding-migration` onto `main` BEFORE executing this phase's waves. One PR
for the whole phase after execution. Nothing is pushed by any task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-embedding-model-migration/13-VALIDATION.md
@src/research_agent/migrate.py
@src/research_agent/recall_golden.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo → production Supabase | The scratch tables live in the SAME database as `research_notes`; a wrong --to/--from touches production data |
| Demo → Voyage billing | Real spend (~cents) behind the --yes gate; preview must be recorded before it |
| Cleanup → prod hygiene | Scratch-table cleanup failure leaves junk in prod Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-17 | Tampering (prod data) | table name argv | mitigate | Every command uses the `migration_demo_` prefix; `research_notes` appears in NO command of the runbook; operator confirms each command before it runs |
| T-13-18 | Repudiation (unrecorded spend) | Voyage billing | mitigate | Preview transcript captured before --yes; post-run predicted-vs-actual tokens recorded |
| T-13-19 | DoS (junk in prod) | scratch cleanup | mitigate | Cleanup is a gated step: after DROPs, a catalog query proves zero `migration_demo_%` tables remain; the checkpoint is not approvable until that query's output is recorded |
| T-13-20 | Elevation (accidental cutover) | fly config | mitigate | The runbook contains no `fly secrets` command; production model flip is explicitly out of scope |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 13-05.1: Live demonstration — seed, copy (zero delta), re-embed (measured delta), clean up</name>
  <files>none (SUMMARY records the transcript)</files>
  <read_first>docs/OPERATIONS.md embedding-migration procedure (13-04.3), src/research_agent/recall_golden.py, 13-VALIDATION.md §Manual-Only Verifications</read_first>
  <action>
    Requires the operator-adjacent secrets (`DATABASE_URL` for Supabase, `VOYAGE_API_KEY`) and
    ~cents of real Voyage spend. The spend is small enough that the executor MAY run the steps
    itself, recording all output — but this task is a blocking checkpoint so the operator chooses
    who drives and approves the result either way.

    Runbook (every table carries the `migration_demo_` prefix; `research_notes` appears nowhere):
    1. Seed: create `migration_demo_notes_src` (vector(1024)) via a small script/REPL using
       `recall_golden.GOLDEN_NOTES` texts embedded with the real `VoyageEmbedder()` (voyage-3.5),
       inserting `(text, embedding, owner, created_at)` with the golden owners — a handful of
       notes, owner-scoped. (This seeding embed also costs a fraction of a cent; note it.)
    2. Copy live: `python -m research_agent.migrate embeddings copy --from migration_demo_notes_src
       --to migration_demo_notes_copy` — record the printed fidelity numbers (counts equal,
       unmatched 0, byte-diff 0). Run the golden delta with `recall_golden` against src vs copy
       using the real embedder for queries: MUST be zero delta. First HF-tokenizer fetch, if it
       happens here, gets noted (subsequent calls offline).
    3. Re-embed live: `python -m research_agent.migrate embeddings re-embed
       --from migration_demo_notes_src --to migration_demo_notes_reembed --model voyage-3.5-lite`
       FIRST WITHOUT --yes: record the preview and the refusal (exit code). Then re-run with
       `--yes`: record the preview again, the run, and predicted-vs-actual tokens.
    4. Verify: owner/created_at carried on the re-embed target (spot query); run the golden
       queries against src (voyage-3.5) vs reembed (voyage-3.5-lite) and record the DELTA — it may
       be nonzero; that is the point; it is the model's delta by construction, because step 2
       proved the infrastructure half at zero.
    5. Clean up (gated): DROP all three `migration_demo_*` tables, then run
       `SELECT tablename FROM pg_tables WHERE tablename LIKE 'migration_demo_%'` and record the
       empty result. The checkpoint is NOT approvable without this recorded empty result.
    6. Record everything in 13-05-SUMMARY.md: full transcript, preview vs actual cost, both
       deltas, cleanup proof. No `fly secrets` command runs; no production flip.
  </action>
  <what-built>Waves 1–4 built and mutation-proved the entire path locally; this run exercises the identical commands once against the prod-shaped Supabase with real Voyage embeddings.</what-built>
  <how-to-verify>
    1. Confirm the transcript shows the preview BEFORE the --yes run, and a refusal without --yes.
    2. Confirm copy delta == zero and the re-embed delta is recorded (whatever it is).
    3. Confirm the pg_tables query returned zero migration_demo_% rows.
    4. Confirm no command referenced research_notes and no fly config changed.
  </how-to-verify>
  <verify>
    <automated>MISSING — live-only by design (real spend + secrets); the local equivalents are the 13-01..13-04 gates, all green: DATABASE_URL=postgresql://postgres@localhost:54329/postgres .venv/bin/pytest tests/test_migrate.py -x</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contains: preview transcript, refusal-without---yes evidence, predicted vs actual
      token counts, copy zero-delta numbers, re-embed delta, and the empty pg_tables cleanup query.
    - Actual spend recorded in USD (expected: well under $0.01).
    - Zero references to research_notes in any executed command; zero fly config changes.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the transcript and cleanup proof are recorded, or describe issues</resume-signal>
  <done>The demonstrable path is demonstrated and recorded; Supabase is clean; production untouched.</done>
</task>

<task type="auto">
  <name>Task 13-05.2: Phase gate battery — suite green, gates reconciled, VALIDATION signed off</name>
  <files>.planning/phases/13-embedding-model-migration/13-VALIDATION.md</files>
  <read_first>.planning/phases/13-embedding-model-migration/13-VALIDATION.md, the four wave SUMMARYs</read_first>
  <action>
    Close the phase's gate battery:
    1. Run the full suite plain and armed; record final counts against the entering baselines
       (plain 527/47, armed 573/1) — maintained or improved, every new skip justified.
    2. Reconcile the Per-Task Verification Map: flip each row's Status to done with its task ID,
       confirming every mutation-red record exists across the four SUMMARYs (the
       thirteen-vacuous-gates discipline: no gate is trusted without its recorded falsification).
    3. Complete the Validation Sign-Off checklist and set `nyquist_compliant: true` and
       `wave_0_complete: true` in the VALIDATION frontmatter, status `complete`.
    4. Do not push; the phase's single PR is raised after `/gsd:verify-work`.
  </action>
  <verify>
    <automated>DATABASE_URL=postgresql://postgres@localhost:54329/postgres REQUIRE_POSTGRES=1 .venv/bin/pytest 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Armed run reports >= 573 passed / <= 1 skipped (or improvements explained line by line).
    - 13-VALIDATION.md: zero remaining `TBD` task IDs, zero pending rows, sign-off checked,
      `nyquist_compliant: true`.
  </acceptance_criteria>
  <done>Phase gates closed with evidence; ready for /gsd:verify-work and the single phase PR.</done>
</task>

</tasks>

<verification>
- Live demo transcript recorded; Supabase clean; production untouched.
- Full armed suite >= 573/1; VALIDATION map fully reconciled with mutation evidence.
</verification>

<success_criteria>
- The one manual-only VALIDATION row (LIVE) satisfied by 13-05.1; sign-off by 13-05.2.
- All five ROADMAP success criteria now hold: SC-1/SC-2/SC-4 (plan 03), SC-3 (plans 02+04), SC-5 (plans 02+03+05 — copy half zero-delta, re-embed delta isolated by construction and measured live).
</success_criteria>

<output>
Create `.planning/phases/13-embedding-model-migration/13-05-SUMMARY.md` when done, containing the full demo transcript and cost record.
</output>
