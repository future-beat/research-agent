---
phase: 13-embedding-model-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/research_agent/migrate.py
  - tests/test_migrate.py
autonomous: true
requirements: [REQ-embedding-model-migration]

must_haves:
  truths:
    - "The legacy SQLite/JSON-to-Postgres migration carries owner and created_at on every note — nothing is orphaned to owner='' and no TTL restarts"
    - "The legacy migration carries owner on every session"
    - "A SQLite-to-Postgres round trip is proven by a test against a real Postgres, not assumed"
  artifacts:
    - path: "src/research_agent/migrate.py"
      provides: "Repaired migrate_notes (text, embedding, owner, created_at) and migrate_sessions (owner passed through); owner-aware dedup; expired-session skip stated in output"
    - path: "tests/test_migrate.py"
      provides: "First-ever test coverage for migrate.py: owner/created_at preservation gate + full round-trip gate, both mutation-falsified"
  key_links:
    - from: "src/research_agent/migrate.py"
      to: "research_agent.sessions.PostgresSessionStore.create"
      via: "owner= keyword argument (exists since Phase 12)"
      pattern: "owner=session\\.owner"
    - from: "src/research_agent/migrate.py"
      to: "pgvector notes table"
      via: "INSERT carrying all four note columns"
      pattern: "text, embedding, owner, created_at"
---

<objective>
Repair `migrate.py`'s live data-loss bug, then prove the legacy path with its first-ever
tests against a real Postgres.

Purpose: `migrate_notes` today inserts only `(text, embedding)` — under the Phase 12 schema
every migrated note lands on `owner=''` (belongs to nobody) with a fresh `created_at` (TTL
restarted), and `migrate_sessions` drops `owner` too. The repair must land BEFORE the new
embedding subcommands (waves 2–3) extend this file, so the new commands inherit a proven
base, not a broken idiom.

Output: repaired `migrate.py` + new `tests/test_migrate.py` with two Postgres-gated tests,
each with a measured baseline and a recorded mutation observed red.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-embedding-model-migration/13-CONTEXT.md
@.planning/phases/13-embedding-model-migration/13-RESEARCH.md
@src/research_agent/migrate.py
@src/research_agent/memory.py
@src/research_agent/sessions.py
</context>

<interfaces>
From src/research_agent/sessions.py (all three store classes):
  def create(self, task, state, session_id=None, owner="") -> str  # owner exists since Phase 12
  Session dataclass carries .owner, .created_at, .updated_at, .turns

From src/research_agent/memory.py:
  PgVectorMemoryStore(embedder=None, dsn=None, table=PGVECTOR_TABLE, dimensions=VECTOR_DIMENSIONS, database=None)
  PgVectorMemoryStore._literal(vector) -> str          # pgvector text format
  Notes schema: id BIGSERIAL, text TEXT, embedding vector(N), created_at TIMESTAMPTZ DEFAULT now(), owner TEXT DEFAULT ''
  JSON store entries: {"text", "embedding", "owner", "created_at"}  # created_at is an epoch float; pre-Phase-12 entries may lack both keys
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON/SQLite source files → Postgres | Migration reads operator-owned files; malformed entries must fail loud, not insert garbage |
| Test suite → shared local Postgres | Tests write to shared `sessions` table on :54329; must use unique ids and clean up |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering (data loss) | migrate_notes | mitigate | Carry owner + created_at explicitly in the INSERT column list; test gate with mutation proof — this bug is live TODAY |
| T-13-02 | Repudiation | migrate_sessions expired-skip | mitigate | Print the skipped-expired count so silent omission becomes stated behaviour |
| T-13-03 | Elevation (cross-tenant) | owner-blind dedup | mitigate | Dedup keys on (text, owner, created_at), never text alone — same text under two owners is two legitimate rows |
| T-13-SC | Tampering | package installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 13-01.1: Repair migrate.py — carry owner and created_at, owner-aware dedup, state the expired-session skip</name>
  <files>src/research_agent/migrate.py</files>
  <read_first>src/research_agent/migrate.py (whole file, 177 lines), src/research_agent/memory.py lines 454–512 (schema + _literal), src/research_agent/sessions.py create() signatures and the Session dataclass</read_first>
  <action>
    Four repairs, per RESEARCH §Migrate.py Forensics items 1–4 and Pitfall 5 (do NOT restructure
    the CLI into subcommands yet — that is plan 13-02; keep `main(argv)` behaviour byte-compatible
    for the documented bare invocation):

    1. `migrate_notes`: insert `(text, embedding, owner, created_at)` instead of `(text, embedding)`.
       Read `entry.get("owner", "")` and `entry.get("created_at", 0.0)` from each JSON entry;
       convert the epoch float to a timezone-aware datetime via `datetime.fromtimestamp(ts, tz=timezone.utc)`
       before binding — the target column is TIMESTAMPTZ. A missing/zero created_at migrates as
       epoch 0 (already expired under the 7-day TTL), which honestly mirrors store semantics for
       pre-Phase-12 orphans; add a one-line comment saying so.
    2. Dedup: replace the text-only `existing` set with the key `(text, owner, created_at-epoch)` —
       fetch `text, owner, extract(epoch from created_at) AS created_at` from the target and
       compare against the entry's fields, so the same text under two owners migrates as two rows.
       Round the epoch comparison to microseconds (Postgres timestamptz precision) so a
       re-run matches what was inserted.
    3. `migrate_sessions`: pass `owner=session.owner` to `target.create(...)`, and add `owner = %s`
       to the restoring UPDATE (belt and braces — create() sets it, the UPDATE proves it survives).
    4. `migrate_sessions`: `source.list()` TTL-filters, so expired sessions are silently skipped
       today. Keep the behaviour (expired data should not be resurrected) but STATE it: count rows
       in the raw sqlite table (`SELECT count(*) FROM sessions` via the source's connection) versus
       rows listed, and print e.g. "N expired session(s) not migrated" when the difference is
       positive.

    Testability (Pitfall 5): give `migrate_notes` explicit keyword parameters
    `table: str = PGVECTOR_TABLE` and `dimensions: int = VECTOR_DIMENSIONS`, used everywhere the
    module constants were used inside the function, so tests pass values instead of monkeypatching
    imported constants. `main()` continues to pass nothing (defaults preserve current behaviour).
    Preserve the loud width-vs-dimensions SystemExit verbatim in wording, now comparing against the
    `dimensions` parameter.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from research_agent.migrate import migrate_notes, migrate_sessions, main; import inspect; sig=inspect.signature(migrate_notes); assert 'table' in sig.parameters and 'dimensions' in sig.parameters; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The notes INSERT column list is exactly `(text, embedding, owner, created_at)`; grep gate:
      `grep -c "owner, created_at" src/research_agent/migrate.py` >= 1 (baseline today: 0).
    - `target.create` in migrate_sessions is called with `owner=session.owner` (baseline today: no owner kwarg).
    - Dedup key includes owner: no remaining `SELECT text FROM` text-only existing-set query.
    - Bare `python -m research_agent.migrate --dry-run` still parses and prints the DRY RUN banner
      (legacy surface untouched: `--sessions-db/--metrics-db/--notes/--dry-run` flags unchanged).
    - The expired-session skip is printed, not silent.
  </acceptance_criteria>
  <done>migrate.py carries owner/created_at on every insert path; dedup is owner-aware; explicit table/dimensions parameters exist; legacy CLI surface unchanged.</done>
</task>

<task type="auto">
  <name>Task 13-01.2: Prove it — test_migrate.py with preservation and round-trip gates, mutation-falsified</name>
  <files>tests/test_migrate.py</files>
  <read_first>tests/test_store_contract.py lines 1–130 (HAS_POSTGRES skip idiom, table naming, FakeEmbedder import path), tests/test_memory_stores.py lines 21–46 (FakeEmbedder), src/research_agent/migrate.py (as repaired in 13-01.1)</read_first>
  <action>
    Create `tests/test_migrate.py` — the first test file for this module. Follow the suite's
    conventions: `HAS_POSTGRES` guard from `os.environ.get("DATABASE_URL")`,
    `@pytest.mark.skipif(not HAS_POSTGRES, ...)` on every Postgres test, no conftest.

    Fixtures build real sources in tmp_path:
    - A JSON notes file with >= 4 entries using 5-dim FakeEmbedder-shaped vectors, spanning owners
      `alice`, `bob`, and `""`, including the SAME text under two different owners, each with a
      distinct nonzero epoch `created_at`.
    - A SQLite sessions DB built through `SQLiteSessionStore` with >= 2 sessions under distinct
      owners (use uuid4-hex session ids so parallel CI runs cannot collide in the shared Postgres
      `sessions` table); plus one expired session (created via the store then aged by direct
      UPDATE on the sqlite file) to assert the skip line prints.

    Tests (names are load-bearing — the VALIDATION contract greps for them):
    1. `test_migrate_preserves_owner_and_created_at` (matches `-k migrate_preserves_owner`):
       call `migrate_notes(path, dry_run=False, table="migration_test_notes_legacy", dimensions=5)`
       against the local Postgres, then assert per-row: every migrated note's `owner` equals the
       source entry's owner (zero rows with owner='' beyond those that started as ''), and
       `extract(epoch from created_at)` equals the source epoch within 1e-3 — i.e. NOT now().
       Also assert the same-text/two-owners pair produced two rows (owner-aware dedup).
    2. `test_migrate_legacy_roundtrip` (matches `-k migrate_legacy_roundtrip`): drive
       `main(["--sessions-db", ..., "--metrics-db", ..., "--notes", ...])` end to end with the env
       pointing at the local PG; assert exit 0, migrated session row in Postgres carries the source
       owner and original created_at/updated_at/turns, notes row-count matches source entry count,
       re-running `main` a second time copies 0 (idempotent), and the "expired session(s) not
       migrated" line appears in captured stdout. For the notes table pass VECTOR_DIMENSIONS=5 via
       monkeypatch BEFORE building the fixture path — but since migrate_notes takes explicit
       params, prefer calling functions directly where the round trip allows; where `main` is
       required, monkeypatch.setenv then reimport is forbidden (Pitfall 5) — instead point `main`
       at a 1024-dim fixture? No: keep it honest and simple — have the round-trip test call the
       three migrate_* functions in sequence with explicit args (the same order main uses) and
       reserve one thin assertion driving `main(["--dry-run", ...])` for the banner + exit code,
       which touches no dimension-sensitive path.
       Teardown: DROP TABLE the legacy test notes table; DELETE the uuid-keyed session rows.

    Gate discipline (record in SUMMARY, revert after observing red):
    - Mutation A: temporarily remove `owner` from the notes INSERT column list → test 1 must fail
      on the orphaned-owner assertion. Revert.
    - Mutation B: temporarily drop the created_at binding (let the column default to now()) →
      test 1 must fail on the epoch-equality assertion. Revert.
    - Mutation C: temporarily drop `owner=session.owner` from create() → test 2 must fail. Revert.
    Record each observed red (test name + one-line failure) in the task commit's SUMMARY notes.
  </action>
  <verify>
    <automated>DATABASE_URL=postgresql://postgres@localhost:54329/postgres .venv/bin/pytest tests/test_migrate.py -k "migrate_preserves_owner or migrate_legacy_roundtrip" -x</automated>
  </verify>
  <acceptance_criteria>
    - Both tests pass against the local :54329 Postgres; both were observed RED under mutations A–C
      before the final green, and the falsifications are recorded.
    - Baseline stated: migrate.py test coverage was ZERO entering this phase.
    - Tests use dedicated table name `migration_test_notes_legacy` (never `contract_test_notes`,
      Pitfall 7) and uuid session ids; teardown removes everything they created.
    - Full suite still green: plain run matches or improves 527 passed / 47 skipped.
  </acceptance_criteria>
  <done>migrate.py has real, mutation-falsified coverage; SQLite→Postgres round trip proven with owner and TTL fidelity.</done>
</task>

</tasks>

<verification>
- `DATABASE_URL=postgresql://postgres@localhost:54329/postgres .venv/bin/pytest tests/test_migrate.py -x` green.
- `.venv/bin/pytest` (plain) — 527/47 baseline maintained or improved; any new skip justified.
- Mutations A–C each observed red and reverted; recorded in SUMMARY.
</verification>

<success_criteria>
- The live owner-orphaning / TTL-restart bug is fixed and can no longer regress silently.
- The legacy migration path is proven (VALIDATION rows 1–2 satisfied by tasks 13-01.2).
</success_criteria>

<output>
Create `.planning/phases/13-embedding-model-migration/13-01-SUMMARY.md` when done, including the mutation-red evidence table.
</output>
