---
phase: 22.5
slug: demo-shows-progress
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-16
---

# Phase 22.5 — Validation Strategy

> A hotfix whose defect the whole test suite missed. The suite proved the stream's
> contract and never asked whether a human would wait — so the gates here have to cover
> the *perceived* behaviour, not only the wire format, and the one thing that cannot be
> tested keylessly (a real 120-second gap) is named rather than hidden.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest; `pythonpath = [".", "src", "tests"]` |
| **Quick run command** | `.venv/bin/pytest tests/test_service.py tests/test_csp.py 2>/dev/null \|\| .venv/bin/pytest tests/test_service.py` |
| **Full suite command** | `.venv/bin/pytest -p no:warnings 2>&1 \| tail -1` (a second `-q` suppresses the count line) |
| **Evals command** | `ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals --quiet; echo $?` |

**Measured baseline (2026-08-16, on `main` — this branch does NOT contain Phase 22's
work, which is held on PR #33):** 827 passed / 72 skipped keyless; evals 65/65 exit 0;
ruff clean. Confirm at execution — the plan's numbers are claims.

**Production evidence this phase answers to** (`repro-2026-08-16.log`, one $0.2235 run
against v21): classifier +2s, researcher **+122s**, terminal `result` +183s, run recorded
and billed, connection healthy throughout.

---

## Sampling Rate

- **After every task commit:** quick command + keyless evals
- **Before verification:** full suite, evals, ruff; and the CSP derivation gates green
- **After deploy:** the one measurement only production can give (below)

---

## Per-Task Verification Map

*(Planner assigns Task IDs/Plan/Wave; executor fills Status with measured evidence.)*

| Task ID | Plan | Wave | Requirement | Criterion | Test Type | Automated Command / Mutation | Status |
|---------|------|------|-------------|-----------|-----------|------------------------------|--------|
| TBD | TBD | TBD | REQ-demo-shows-progress | A `node` event with `status: "started"` is emitted when the supervisor routes to a worker, BEFORE that worker's completion event — asserted on ordering, not just presence | unit | mutation: restore the `continue` that discards the supervisor chunk → the ordering test reds. This is the headline gate: it must fail on today's code | pending |
| TBD | TBD | TBD | REQ-demo-shows-progress | `routed_to: "done"` draws no stage; the starting event carries only the node name (per research: `_node_detail`'s fields are post-execution and would misreport read early) | unit | mutation: emit a started event for `done` → red; mutation: attach `_node_detail` to a started event → the payload test reds | pending |
| TBD | TBD | TBD | REQ-demo-shows-progress | **Phase 19's contract survives literally:** exactly one terminal event (`result` XOR `error`) per stream, counted with started events present; `test_stream_does_not_emit_supervisor_hops` needs ZERO changes (research: reuse of `event: node` keeps it intact — if that test needs editing, the design drifted) | unit | mutation: count a started event as terminal → red; the untouched-test claim is evidence, verified by `git diff` showing no edit to it | pending |
| TBD | TBD | TBD | REQ-demo-shows-progress | Revision loops do not collide: a scripted multi-revision run (writer/critic twice, as the real repro did) produces a readable transcript with no orphaned or duplicated row — the single in-flight pointer design, justified by the graph having no fan-out | unit + reasoning | scripted 2-revision stream asserted event-by-event; the no-fan-out property stated with its graph.py evidence, since it is what makes the simple design safe | pending |
| TBD | TBD | TBD | REQ-demo-shows-progress | `/ask/stream` inherits the fix (it shares `_stream`) — follow-up turns announce their stages too, including the researcher a reaching follow-up triggers | unit | a follow-up stream test asserts a started event; mutation: gate the emit to the research route only → red | pending |
| TBD | TBD | TBD | REQ-demo-shows-progress | The demo page renders the starting stage immediately and resolves it on completion; the new JS lands INSIDE the existing single `<script>` block (research: keeps every CSP test code-change-free) | structural | CSP derivation gates green with the edited page; mutation: hand-maintain a hash → red; block-count assertion still 1 | pending |
| TBD | TBD | TBD | REQ-demo-shows-progress | The blocking `/research` route and the follow-up blocking route are unchanged in shape | structural | their response-model tests stay green untouched | pending |
| TBD | TBD | TBD | REQ-demo-shows-progress | Docs: README's documented SSE example (~:107-119) shows the new event — the one public contract the research found; README whole-file pass; counts re-measured | grep + prose | the example matches what the code emits, checked against a real stream capture at deploy | pending |

---

## Manual-Only Verifications

| Behavior | Why Manual | Instructions |
|----------|------------|--------------|
| **The gap a visitor actually sees** (success criterion 4) | The defect is 120 seconds of real web search; no keyless test can produce it, and the suite's blindness to exactly this is why the bug reached a user | After deploy, re-run the committed reproduction (`repro-2026-08-16.log`'s method) and record the timestamp of the FIRST `researcher` signal. Expected: ~+2s (routing) instead of +122s (completion). Same cost as the original, ~$0.22 — **user-approved before spending**, and compared against the archived log rather than a remembered number |
| The page as a human sees it | DOM behaviour; no JS harness exists in this repo | The owner watches one live run and confirms the label changes within seconds and the transcript reads correctly through a revision loop |

---

## Validation Sign-Off

- [ ] The headline mutation (restore the `continue`) observed red — it must fail on today's code
- [ ] `test_stream_does_not_emit_supervisor_hops` verified UNEDITED via `git diff`
- [ ] Terminal-event-count property re-proven with started events present
- [ ] Revision-loop transcript asserted event-by-event
- [ ] CSP gates green on the edited page, hashes derived not written
- [ ] Suite + evals green at measured counts; ruff clean
- [ ] Post-deploy: first researcher signal timed against the archived repro
- [ ] `nyquist_compliant: true` at reconciliation

**Approval:** pending execution.
