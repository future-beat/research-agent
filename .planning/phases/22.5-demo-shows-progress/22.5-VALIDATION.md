---
phase: 22.5
slug: demo-shows-progress
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-08-16
reconciled: 2026-08-16
---

# Phase 22.5 — Validation Strategy

> A hotfix whose defect the whole test suite missed. The suite proved the stream's
> contract and never asked whether a human would wait — so the gates here have to cover
> the *perceived* behaviour, not only the wire format, and the one thing that cannot be
> tested keylessly (a real 120-second gap) is named rather than hidden.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest; `pythonpath = [".", "src", "tests"]` |
| **Quick run command** | `.venv/bin/pytest tests/test_service.py tests/test_csp.py 2>/dev/null \|\| .venv/bin/pytest tests/test_service.py` |
| **Full suite command** | `.venv/bin/pytest -p no:warnings 2>&1 \| tail -1` (a second `-q` suppresses the count line) |
| **Evals command** | `ANTHROPIC_API_KEY="" VOYAGE_API_KEY="" .venv/bin/python -m evals --quiet; echo $?` |

**Measured baseline (2026-08-16, on `main` — this branch does NOT contain Phase 22's
work, which is held on PR #33):** 827 passed / 72 skipped keyless; evals 65/65 exit 0;
ruff clean. Confirm at execution — the plan's numbers are claims.

**Measured at reconciliation (2026-08-16, after all three tasks):** 832 passed /
72 skipped keyless — +5 passed, zero new skips, matching the plan's claim exactly;
`-k stream` 23 → 28 selected, also as claimed; evals 65/65 at a real `$?` of 0;
`ruff check .` clean. The five added tests, by name:
`test_stream_announces_a_stage_before_it_completes`,
`test_the_demo_page_handles_the_started_status_the_stream_emits` (T1),
`test_stream_never_announces_the_terminal_routing`,
`test_stream_pairs_started_and_completed_across_a_revision_loop`,
`test_ask_stream_announces_its_stages_too` (T2). All five match `-k stream`, which
is why the selection grew by exactly five. Two existing assertions were updated
(`test_stream_emits_progress_then_exactly_one_result`'s fourth,
`test_ask_stream_persists_the_follow_up`'s name list) and no other test in the
suite reacted.

**Production evidence this phase answers to** (`repro-2026-08-16.log`, one $0.2235 run
against v21): classifier +2s, researcher **+122s**, terminal `result` +183s, run recorded
and billed, connection healthy throughout.

---

## Sampling Rate

- **After every task commit:** quick command + keyless evals
- **Before verification:** full suite, evals, ruff; and the CSP derivation gates green
- **After deploy:** the one measurement only production can give (below)

---

## Per-Task Verification Map

*(Task IDs/Plan/Wave assigned at planning, 2026-08-16 — all eight rows land in the single
wave-1 plan `22.5-01`, whose three tasks are T1 tracer / T2 expansion gates / T3 docs and
reconciliation. Executor fills Status with measured evidence.)*

| Task ID | Plan | Wave | Requirement | Criterion | Test Type | Automated Command / Mutation | Status |
|---------|------|------|-------------|-----------|-----------|------------------------------|--------|
| T1 | 22.5-01 | 1 | REQ-demo-shows-progress | A `node` event with `status: "started"` is emitted when the supervisor routes to a worker, BEFORE that worker's completion event — asserted on ordering, not just presence | unit | `test_stream_announces_a_stage_before_it_completes`. Mutation: restore the `continue` that discards the supervisor chunk → the ordering test reds. This is the headline gate: it must fail on today's code, and the plan observes it red BEFORE the fix exists (test-first) as well as by re-applying the mutation after | **PASS — the headline gate was red first.** Written and run against wholly unmodified source: `AssertionError: expected one started and one completed researcher event / assert 1 == 2 / where 1 = len([(1, {'node': 'researcher', 'recalled_from_memory': 0})])`. Green after the four-line `_stream` branch. Re-applying the mutation (bare `continue` restored) reds it again on the same assertion with the same `1 == 2`. Commit `38a4dfc` |
| T2 (`done` filter) + T1 (payload) | 22.5-01 | 1 | REQ-demo-shows-progress | `routed_to: "done"` draws no stage; the starting event carries only the node name (per research: `_node_detail`'s fields are post-execution and would misreport read early) | unit | `test_stream_never_announces_the_terminal_routing` (with a non-vacuity assertion that the terminal hop IS in the trace) + the key-set assertion in `test_stream_announces_a_stage_before_it_completes`. Mutation: drop the `!= "done"` filter → red; mutation: spread `_node_detail` into the started payload → the key-set assertion reds, recording the fabricated field that leaks | **PASS.** `test_stream_never_announces_the_terminal_routing` green, with the non-vacuity half asserting the `done` hop IS in the terminal trace. Dropping the `!= "done"` filter reds it (`Extra items in the left set: 'done'`) and the revision-loop gate too (`Left contains one more item: ('done', 'started')`). Spreading `_node_detail(routed_to, node_state)` into the started payload reds the key-set assertion, leaking exactly the predicted field: `Extra items in the left set: 'recalled_from_memory'` — fabricated, because at routing time `trace[-1]` is the supervisor's own decision dict and `.get(..., 0)` returns the default |
| T1 (contract) + T2 (unedited proof) | 22.5-01 | 1 | REQ-demo-shows-progress | **Phase 19's contract survives literally:** exactly one terminal event (`result` XOR `error`) per stream, counted with started events present; `test_stream_does_not_emit_supervisor_hops` needs ZERO changes (research: reuse of `event: node` keeps it intact — if that test needs editing, the design drifted) | unit | `test_stream_emits_progress_then_exactly_one_result`'s first three assertions survive VERBATIM (only its fourth, the payload sequence, is updated). The untouched-test claim is proven by extracting both protected tests' source from the merge base and comparing byte-for-byte — deliberately not `git diff \| grep '^@@'`, whose function-context header would false-flag them | **PASS, and the design did not drift.** `test_stream_emits_progress_then_exactly_one_result`'s first three assertions survive verbatim with started events present; only its fourth changed, and it now asserts MORE (the full eight-element interleaving, not four completion names). Both protected tests proven UNEDITED by extracting each function's source from `git merge-base HEAD main` (`b3c3fce`) and comparing byte-for-byte: `test_stream_does_not_emit_supervisor_hops` 524 bytes equal, `test_stream_carries_node_detail` 360 bytes equal. Neither was opened |
| T2 | 22.5-01 | 1 | REQ-demo-shows-progress | Revision loops do not collide: a scripted multi-revision run (writer/critic twice, as the real repro did) produces a readable transcript with no orphaned or duplicated row — the single in-flight pointer design, justified by the graph having no fan-out | unit + reasoning | `test_stream_pairs_started_and_completed_across_a_revision_loop` asserts the exact 16-element alternation; its docstring states the no-fan-out property with graph.py:730-744 evidence. Mutation: emit a started event only on each node's FIRST occurrence → this test reds while T1's stays green, showing what a single-pass fixture cannot see | **PASS at the plan's exact arithmetic** — the scripted two-rejection run produced the sixteen-element alternation as written, with no reshaping of fixture or graph. The `seen`-set mutation (started emitted only on each node's FIRST occurrence) reds it precisely at the second writer pair — `At index 8 diff: ('writer', None) != ('writer', 'started')` — while Task 1's ordering gate, the terminal-routing gate, the follow-up gate and `test_stream_emits_progress_then_exactly_one_result` all stayed GREEN. That green half is the point: it is a live demonstration that a single-pass fixture cannot see this |
| T2 (gate) + T1 (count) | 22.5-01 | 1 | REQ-demo-shows-progress | `/ask/stream` inherits the fix (it shares `_stream`) — follow-up turns announce their stages too. **Scoped at planning (checker W4):** the gate covers the common responder+critic shape; the *reaching* follow-up (which adds a researcher hop) is argued rather than gated, because the emit sits in shared code with no route-specific branch. The record says which is which rather than implying both were measured | unit | `test_ask_stream_announces_its_stages_too`, plus the updated event count in `test_ask_stream_persists_the_follow_up`. Mutation: gate the emit on the run not being a follow-up → the follow-up gate reds while the research-route gates stay green | **PASS on both halves.** `test_ask_stream_announces_its_stages_too` asserts responder started/completed then critic started/completed; `test_ask_stream_persists_the_follow_up`'s list grew 3 → 5 with its turn-count assertion untouched. Gating the emit on `state.get("mode") != "followup"` reds BOTH follow-up gates (`At index 0 diff: ('responder', None) != ('responder', 'started')`) while all four research-route gates stayed green — so the follow-up gate earns its place rather than restating Task 1's. Scope honoured: the reaching follow-up (researcher hop) is argued from the shared `_stream`, not gated |
| T1 | 22.5-01 | 1 | REQ-demo-shows-progress | The demo page renders the starting stage immediately and resolves it on completion; the new JS lands INSIDE the existing single `<script>` block (research: keeps every CSP test code-change-free) | structural | The five CSP gates green over the edited bytes; `<style>` block proven byte-identical against the merge base; `test_the_demo_page_handles_the_started_status_the_stream_emits` ties the page's vocabulary to a real capture. Mutations: rename the emitted status without touching the page → red; split the new JS into a second `<script>` block → block-count gate red and `GET /` 500s | **PASS.** All nine `-k "csp or demo_page"` gates green over the edited bytes, hashes still derived. `<style>` block proven byte-identical against merge base `b3c3fce` (zero CSS edits — the muted dot was already a pending state). `test_the_demo_page_handles_the_started_status_the_stream_emits` reads the literal off a real stream, strips comments, and asserts non-vacuously (`sawTerminal` still present). Renaming the emitted status to `beginning` without touching the page reds it (`assert {'beginning'} == {'started'}`); the page contains `"started"` once and `"beginning"` zero times, so the page-source half would red too. Moving the new JS into a second `<script>` block reds the count gate (`assert 2 == 1`) AND makes `csp.policy()` raise `ValueError: ... has 2 inline script block(s) and 1 inline style block(s)`, i.e. `GET /` 500s rather than serving a half-hashed policy |
| T3 | 22.5-01 | 1 | REQ-demo-shows-progress | The blocking `/research` route and the follow-up blocking route are unchanged in shape | structural | `_execute` is untouched; their response-model tests stay green UNEDITED, confirmed in the full-suite reconciliation where the only changed test assertions are the two the plan names | **PASS.** `_execute` was never opened; `git diff` on `service.py` touches only `_stream`'s docstring and its supervisor branch. The full keyless suite is 832 passed / 72 skipped with exactly two updated assertions — the two the plan names — and no blocking-route test edited or reacting |
| T3 | 22.5-01 | 1 | REQ-demo-shows-progress | Docs: README's documented SSE example (~:107-119) shows the new event — the one public contract the research found; README whole-file pass; counts re-measured | grep + prose | The example is compared against a real keyless capture taken from the revision-loop test (temporary print, reverted and the revert confirmed), not transcribed from the plan; whole-file pass recorded with its greps; counts re-measured against 827/72 | **PASS.** A real keyless stream was captured from the revision-loop test via a temporary print run with `-s`; the print was reverted and the revert confirmed by `git status --porcelain tests/test_service.py` returning empty (the gate FAILS rather than merely printing if it survives). README's example gained the classifier's started line, field names compared character-by-character against the capture (`{"node": "classifier", "status": "started"}`), plus one sentence distinguishing the two forms. Whole-file pass recorded: the only other streaming mentions are the phase-4/9 checklist lines and the `/research/stream` API row, all still true; the trace-endpoint row is unrelated. Limitations section untouched (`git diff` shows +7 lines, all in the SSE example region). Counts re-measured: 832/72, `-k stream` 28, evals 65/65 exit 0, ruff clean |

---

## Manual-Only Verifications

| Behavior | Why Manual | Instructions |
|----------|------------|--------------|
| **The gap a visitor actually sees** (success criterion 4) | The defect is 120 seconds of real web search; no keyless test can produce it, and the suite's blindness to exactly this is why the bug reached a user | After deploy, re-run the committed reproduction (`repro-2026-08-16.log`'s method) and record the timestamp of the FIRST `researcher` signal. Expected: ~+2s (routing) instead of +122s (completion). Same cost as the original, ~$0.22 — **user-approved before spending**, and compared against the archived log rather than a remembered number |
| The page as a human sees it | DOM behaviour; no JS harness exists in this repo | The owner watches one live run and confirms the label changes within seconds and the transcript reads correctly through a revision loop |

---

## Validation Sign-Off

- [x] The headline mutation (restore the `continue`) observed red — it must fail on today's code.
      **Observed twice.** First test-first, against wholly unmodified source: `assert 1 == 2`,
      today's stream carrying one researcher event where the gate demands two. Then again by
      re-applying the mutation after the fix, red on the same assertion. The gate is not vacuous.
- [x] `test_stream_does_not_emit_supervisor_hops` verified UNEDITED by extracting its source
      from `git merge-base HEAD main` and comparing byte-for-byte — NOT via `git diff | grep`,
      which reports the enclosing function rather than which bytes changed (checker W3).
      **524 bytes equal at `b3c3fce`; `test_stream_carries_node_detail` 360 bytes equal.**
      Neither file position was opened. This is the evidence the change is additive to Phase 19's
      contract rather than a renegotiation of it — the started payload's `node` is the TARGET's
      name, so "no supervisor hops on the wire" stays literally true.
- [x] Terminal-event-count property re-proven with started events present —
      `test_stream_emits_progress_then_exactly_one_result`'s first three assertions survive
      verbatim; its fourth now asserts the full eight-element interleaving.
- [x] Revision-loop transcript asserted event-by-event — the exact sixteen-element alternation
      across writer x3 and critic x3, matching the production repro's shape.
- [x] CSP gates green on the edited page, hashes derived not written — nine `-k "csp or demo_page"`
      gates green, `<style>` block byte-identical, one script block, zero handlers, zero `style=`.
- [x] Suite + evals green at measured counts; ruff clean — 832 passed / 72 skipped (from 827/72,
      +5 with zero new skips), `-k stream` 28 (from 23), evals 65/65 at a real `$?` of 0,
      `ruff check .` clean.
- [ ] Post-deploy: first researcher signal timed against the archived repro.
      **Open, and deliberately so.** It waits on a deploy plus one owner-approved paid run of
      roughly $0.22. The number that matters is the timestamp of the FIRST `researcher` signal —
      expected ~+2s (the routing hop), compared against `repro-2026-08-16.log`'s archived +122s
      rather than a remembered figure. Ticking this before the measurement exists would be exactly
      the vacuous green this phase was created to answer for.
- [x] `nyquist_compliant: true` at reconciliation — every keylessly automatable behaviour now
      carries a gate, and the two that genuinely cannot be automated (the wall-clock gap, the DOM
      as a human sees it) are named in the Manual-Only table rather than hidden behind a passing
      suite.

**Approval:** executed 2026-08-16, plan `22.5-01`, commits `38a4dfc` (T1), `c6da690` (T2), T3 below.
Keyless validation complete; the single post-deploy measurement remains open by design.
