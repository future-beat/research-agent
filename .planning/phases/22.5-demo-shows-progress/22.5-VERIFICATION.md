---
phase: 22.5-demo-shows-progress
verified: 2026-08-17T02:23:39Z
status: human_needed
score: 3/5 roadmap success criteria verified
behavior_unverified: 2
retrospective: true  # the phase shipped (PR #34, Fly v22) and was never verified; the gap was found during /gsd:complete-milestone
method: >-
  goal-backward, adversarial. SUMMARY and VALIDATION were read as claims and never as evidence.
  Three mutations applied and observed red in an ISOLATED copy of the tree (never in the working
  tree — a second verifier is live in .planning/phases/21/*); the suite re-run at four separate
  commits to reproduce the recorded arithmetic from scratch; the CSP re-derived both locally and
  against the bytes the deployed service actually serves; the real SSE wire captured and diffed
  against README's published example.
verifier_mutations: 3   # all applied in a scratchpad copy; working tree never modified
verifier_recomputations: 7
overrides_applied: 0
re_verification: false

# Gates re-run by the verifier, not read from a SUMMARY
gates:
  full_suite: "834 passed, 72 skipped in 30.01s, exit 0 — `ANTHROPIC_API_KEY='' VOYAGE_API_KEY='' .venv/bin/pytest -p no:warnings`, single -q-free invocation so the count line is visible"
  stream_selected: "`-k stream` 28 selected — matches the record exactly"
  offline_evals: "PASS 65/65 cases (100% vs 90% required), real $? = 0 — `ANTHROPIC_API_KEY='' VOYAGE_API_KEY='' .venv/bin/python -m evals --quiet`"
  ruff: "All checks passed! — `.venv/bin/ruff check .`, exit 0"
  suite_at_merge_base_b3c3fce: "827 passed / 72 skipped, `-k stream` 23 — reproduces the record's stated baseline"
  suite_at_branch_tip_a0f5a08: "828 passed / 72 skipped, `-k stream` 23 — the TRUE pre-code baseline; one higher than the record states (see warning 1)"
  suite_at_phase_end_ad12208: "833 passed / 72 skipped, `-k stream` 28 — one higher than the record's 832 (see warning 1)"
  headline_mutation: "restore the bare `continue` that discards the supervisor chunk → 7 gates RED, headline verbatim `assert 1 == 2 where 1 = len([(1, {'node': 'researcher', 'recalled_from_memory': 0})])`. Reproduces the SUMMARY's quoted failure character-for-character"
  done_filter_mutation: "drop the `!= \"done\"` filter → RED on two gates: `Left contains one more item: ('done', 'started')` and `'done'` in the announced set"
  vocabulary_mutation: "rename the emitted status to `beginning`, page untouched → RED `assert {'beginning'} == {'started'}` — lands on the vocabulary assertion first, exactly as the SUMMARY recorded honestly"
  protected_tests_byte_identical: "`test_stream_does_not_emit_supervisor_hops` and `test_stream_carries_node_detail` extracted from b3c3fce and from HEAD: sha equal (db9ca37a, f4e76812). Still identical TODAY, two merges after the phase"
  terminal_contract: "`test_stream_emits_progress_then_exactly_one_result`'s first four assertions byte-verbatim against b3c3fce; only the payload-sequence assertion changed, and it now asserts MORE. Failure path asserts `[name for name, _ in events] == ['error']` — a full list equality, so no result can accompany an error"
  csp_local: "both hashes recomputed from today's index.html bytes are present in `csp.policy(DEMO_PAGE)`; 1 script block, 1 style block, 0 `on*` handlers, 0 `style=` attributes"
  csp_live: "GET https://research-agent.fly.dev/ with `Accept: text/html` → 200; served page is BYTE-IDENTICAL to the local file (23,901 bytes); both sha256 hashes derived from the live bytes are present in the served Content-Security-Policy header"
  live_page_carries_the_fix: "the deployed page contains `pendingRow`, `pendingNode` and the `\"started\"` literal exactly once — the fix is live, and survived the later RTL edit (70a0528)"
  wire_vs_readme: "real SSE bytes captured keylessly from the isolated copy; the four data lines README publishes are byte-identical to what the server emits"
  blocking_routes_untouched: "`git show b05bb89 -- src/research_agent/service.py` touches exactly two hunks, both inside `_sse`/`_stream`. `_execute` was never opened"
  working_tree: "unmodified by this verification — every mutation ran against a scratchpad copy shadowing the editable install via PYTHONPATH"

# Not gaps. Recorded so /gsd:complete-milestone inherits the fact rather than the silence.
warnings:
  - finding: "The recorded suite counts are each ONE LOW. The record says 827 → 832; the tree says 828 → 833. The +5 delta, the zero new skips, the `-k stream` 23 → 28 and the five named tests are all exactly right — only the two absolute numbers are stale."
    evidence: >-
      Measured by re-running the suite at four commits in isolated archives: b3c3fce (the merge
      base the record used) = 827/72; a0f5a08 (22.5's last commit before any code change, i.e. the
      TRUE baseline) = 828/72; b05bb89 = 830/72; 9e9a680 and ad12208 = 833/72. Per-file collection
      diff between b3c3fce and ad12208 shows test_service.py 151 → 156 (the five named tests,
      exactly as claimed) AND test_evals.py 193 → 194 — a sixth test the phase never wrote.
      `git diff --stat a0f5a08 ad12208 -- tests/` shows 22.5 touched test_service.py and nothing
      else, and `git log b3c3fce..a0f5a08 -- tests/test_evals.py` attributes the sixth to Phase 22's
      commit 1937c8a ("gated by a derived-counts test").
    root_cause: >-
      22.5-VALIDATION:29-31 states the premise "on `main` — this branch does NOT contain Phase 22's
      work, which is held on PR #33". Git contradicts it: `git merge-base --is-ancestor f6eec95
      b05bb89` succeeds, so the PR #33 merge IS an ancestor of 22.5's first code commit, and
      Phase 22's 1937c8a was already in the baseline tree. The likely mechanism is a stale local
      `main` ref — `git merge-base HEAD main` returned b3c3fce, a commit predating the phase-22
      merge — so the baseline was measured one commit family too early and every downstream figure
      inherited the offset.
    why_it_counts: >-
      The SUMMARY's boldest process claim is that these numbers were "measured rather than trusted:
      832 passed / 72 skipped (claimed 832/72)" and that "the plan's arithmetic was correct in every
      particular". That specific claim does not reproduce — 832 is the plan's PREDICTION (827 + 5),
      and the tree at that commit says 833. In a project whose whole method is measure-don't-trust,
      a prediction reported as a measurement is the failure mode worth naming, even when the
      substance behind it is sound. Nothing about the fix, the gates or the goal depends on it.
    scope: "Three files carry the stale pair: .planning/ROADMAP.md:440,442; 22.5-01-SUMMARY.md:46,152,231; 22.5-VALIDATION.md:30,33,109."
    fix: >-
      A four-line record correction before archival: 827 → 828 and 832 → 833 at the seven sites,
      with one clause noting the branch DID contain Phase 22's work. Cheap, and it keeps the
      milestone's arithmetic honest where /gsd:complete-milestone will read it. Alternatively,
      annotate rather than rewrite — the numbers are reproducible AT b3c3fce, so they are stale
      rather than fabricated.
  - finding: "The byte figures for the two protected tests are 524/360 in the record; the verifier extracted 522/358."
    evidence: "Same two-byte offset on both, consistent with a different trailing-newline convention in the extraction script. The load-bearing claim — sha equality between b3c3fce and HEAD — reproduces exactly (db9ca37a, f4e76812)."
    why_it_counts: "It does not. Recorded only so a future reader who re-extracts and gets 522 does not think the proof failed."
    fix: "None needed."

# Verifier nits. None changes a verdict.
notes:
  - "The headline mutation is the strongest evidence in this phase and it reproduces perfectly. Restoring the bare `continue` reds SEVEN gates, and the headline one fails on the exact assertion the SUMMARY quotes, down to the repr: `assert 1 == 2 where 1 = len([(1, {'node': 'researcher', 'recalled_from_memory': 0})])`. That single line IS the production defect in test form — one researcher event, carrying only post-hoc detail, arriving 120 seconds late. Meanwhile both protected Phase 19 tests stay GREEN under that same mutation, which is what proves the new gate is not a restatement of the old contract."
  - "The `done` filter and the vocabulary coupling both bite. Dropping the filter reds the terminal-routing gate with `Extra items in the left set: 'done'` — i.e. the page would have drawn an unresolvable `done` row, precisely the orphan criterion 2 forbids. Renaming the status reds `assert {'beginning'} == {'started'}`. The SUMMARY's key-decision 6 already recorded that this red lands on the vocabulary assertion rather than the page-source one, and that the page half was confirmed separately; the verifier reproduced both — index.html contains `\"started\"` once and `beginning` zero times."
  - "The CSP property is stronger today than the phase could have claimed. index.html has been edited AGAIN since (70a0528, the RTL alignment fix, +36 lines and one new test), and the derivation still holds: both hashes recomputed from today's bytes appear in the locally derived policy AND in the header the deployed service actually returns, with the served page byte-identical to the repo file. Derivation-not-hand-maintenance is doing exactly the work Phase 19 designed it for — two page edits later, nobody has had to touch a hash."
  - "The new `node_state[\"next_step\"]` read cannot raise. `next_step` is a declared field of the graph state TypedDict (graph.py:224), initialised to `\"\"` at graph.py:259, and every branch of the supervisor assigns it before graph.py:678 builds the decision from it. A path that failed to assign would break `route()` (graph.py:717) and the run itself, not just the wire — so the phase added no new failure mode to a stream whose headers are already sent."
  - "Criterion 2's client half is honestly un-gated rather than quietly assumed. There is no JS harness in this repo — no package.json, no jsdom, no Playwright — so nothing can assert the DOM transition. What the phase did instead is the strongest available substitute: it gated the SERVER-side property the single-pointer design depends on (strict started/completed alternation across a scripted writer x3 / critic x3 revision loop, sixteen elements asserted element-by-element) and coupled the page's status literal to a value read off a real stream. The verifier confirms the pointer is per-run — declared at index.html:394-395 inside `async function run()` beside the `stages` container it points into — so an abandoned run cannot leak a row into the next. The remaining risk is DOM-only and is named in the phase's own Manual-Only table."
  - "Criterion 4 is the one the phase deliberately did NOT close, and it was right not to. Its sign-off box at 22.5-VALIDATION:112 is unticked with an explicit rationale: it needs a deploy plus one owner-approved ~$0.22 paid run, and the number that matters is the timestamp of the FIRST researcher signal compared against repro-2026-08-16.log's archived +122s. Ticking it would have been the vacuous green this phase exists to answer for. Per instruction the verifier did NOT spend to close it — the only live calls made were free GETs of the static page."
  - "All three commit hashes the SUMMARY's self-check names (38a4dfc, c6da690, 0b73267) still resolve as objects, though the commits that landed in main are b05bb89 / 9e9a680 / ad12208 — the branch was rebased before merge. The self-check's claim is true; the reader should just not expect those hashes in `git log main`."
  - "No debt markers. `TODO|FIXME|XXX|HACK|PLACEHOLDER|not yet implemented|coming soon` over all four phase-modified files returns only `FOLLOWUP_PLACEHOLDER`, a UI placeholder-text constant at index.html:171, which predates this phase."
  - "The verification was run without touching the working tree, deliberately: a second verifier is live in .planning/phases/21/*. All three mutations were applied to a scratchpad copy of src/ + tests/, executed with `PYTHONPATH=<copy>/src` so it shadows the `__editable__.research_agent-0.9.0.pth` path entry. Verified: `research_agent.__file__` resolved into the copy, the unmutated copy was green at 28/28, and `git status --porcelain` shows zero source changes attributable to this verification."

human_verification:
  - test: "After the next deploy, re-run repro-2026-08-16.log's method against production and record the timestamp of the FIRST `researcher` signal (the started event), not the researcher's completion."
    expected: "~+2s, at the supervisor's routing hop, against the archived +122s. Closes ROADMAP criterion 4 and 22.5-VALIDATION's open sign-off box."
    why_human: "Roadmap criterion 4 says 'measured, not asserted'. The 120 seconds are real web search; no keyless test can produce them. Costs ~$0.22 of real spend and requires the owner's approval at deploy time — the verifier deliberately did not spend to close it."
  - test: "Watch one live run in a browser through a revision loop (a question the critic rejects at least once) and confirm the muted row for the running stage appears immediately, turns green in place on completion, and the finished transcript has no duplicated or permanently-muted row."
    expected: "Row count after the run equals the number of completions; the label changes within seconds of submitting, not after two minutes."
    why_human: "DOM behaviour, and this repo has no JS harness. The server-side alternation the design depends on IS gated; what no test here can see is the page acting on it. Already named Manual-Only in 22.5-VALIDATION."
  - test: "Decide whether to correct the stale suite counts (827→828, 832→833) across ROADMAP, SUMMARY and VALIDATION before /gsd:complete-milestone archives the milestone."
    expected: "Either a seven-site correction with a clause noting the branch did contain Phase 22's work, or an explicit accepted-as-is decision recorded where it outlives the milestone."
    why_human: "Record-accuracy judgement, not a correctness one. Nothing about the shipped fix depends on it — but the milestone close-out cites these numbers, and the SUMMARY presents a prediction as a measurement. See warning 1."
---

# Phase 22.5: The demo shows progress — Verification Report

**Phase Goal:** A visitor watching the demo sees the stage that is running, not the last one
that finished, so the two minutes the researcher spends searching read as progress rather than
a hang.

**Verified:** 2026-08-17T02:23:39Z
**Status:** human_needed — 3/5 roadmap criteria verified, 2 present-but-behaviour-unverified,
0 failed. Both open items were already named as open by the phase itself.
**Retrospective:** Yes. This phase shipped as PR #34 and deployed as Fly v22 without a
verification report; the gap was found while running `/gsd:complete-milestone`.
**Method:** goal-backward and adversarial. The starting hypothesis was that the tasks completed
and the goal was missed. SUMMARY and VALIDATION were treated as claims throughout. Three
mutations were applied and observed red, the suite was re-run at four separate commits to
rebuild the recorded arithmetic from scratch, the CSP was re-derived against the bytes the
deployed service serves, and the real SSE wire was captured and diffed against README.

---

## Goal Achievement

### Success Criteria (the ROADMAP contract)

| # | Criterion | Status | Evidence the verifier observed |
|---|-----------|--------|-------------------------------|
| 1 | The stream emits an event when a stage STARTS, derived from the supervisor's existing `routed_to`, in addition to the completions it already sends | ✓ VERIFIED | `service.py:400-411` reads `node_state["next_step"]` off the supervisor chunk and yields `{"node": routed_to, "status": "started"}`. Real wire captured keylessly: `classifier/started` → `classifier/topic_type` → `researcher/started` → `researcher/recalled_from_memory`. Restoring the pre-fix `continue` reds 7 gates, headline verbatim `assert 1 == 2` — the defect in test form |
| 2 | The demo page renders the starting stage immediately and resolves it when the matching completion arrives — no stage row is ever orphaned or duplicated | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | **Server half VERIFIED:** strict alternation gated element-by-element across a scripted writer×3/critic×3 loop (16 elements); the `done` routing announces nothing (dropping the filter reds with `Left contains one more item: ('done', 'started')`). **Client half present, wired, deployed** — `pendingRow`/`pendingNode` at index.html:394-395, per-run scope inside `run()`, in-place upgrade at :425-441, fallback arm at :431-437 — and live on production. **But no JS harness exists in this repo**, so the DOM transition itself is exercised by no test. Named Manual-Only by the phase |
| 3 | Phase 19's streaming guarantees survive: exactly one terminal event, the `run_finished`/`run_failed` pair unchanged, the derived CSP still matches the page after the edit | ✓ VERIFIED | Terminal contract: `names.count("result") == 1` and `names[-1] == "result"` survive verbatim from b3c3fce; the failure path asserts `== ["error"]` as a full list equality. Both protected tests byte-identical at HEAD (sha db9ca37a, f4e76812). CSP: hashes recomputed from today's bytes match the local policy **and the header served by research-agent.fly.dev**, with the served page byte-identical to the repo file — proven *after* the later RTL edit |
| 4 | The gap a visitor sees between the first and second visible stage is bounded by the supervisor's routing, not the researcher's runtime — **measured, not asserted** | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED (open by design) | Structurally established and gated (`started_index < completed_index`, the started payload an exact `{node, status}` key set). The wall-clock measurement is **deliberately open**: 22.5-VALIDATION:112's sign-off box is unticked with an explicit rationale, and it needs a deploy plus ~$0.22 of owner-approved spend. The verifier did not spend to close it |
| 5 | The blocking `/research` route and the follow-up routes are unaffected in shape | ✓ VERIFIED | `git show b05bb89 -- src/research_agent/service.py` touches exactly two hunks, both inside `_sse`/`_stream`. `_execute` was never opened. `git diff --stat a0f5a08 ad12208 -- tests/` shows one test file changed; no blocking-route test was edited, and all pass |

**Score: 3/5 verified. 0 failed. 2 present-behaviour-unverified — both already declared open by
the phase's own record.**

---

## Independent Measurements (nothing below is read from a SUMMARY)

### Gates the record claims, reproduced

| Gate | Record's claim | Verifier measured | Match |
|---|---|---|---|
| Full keyless suite (today, HEAD) | — | **834 passed, 72 skipped**, exit 0 | n/a — two merges after the phase |
| Full suite at `ad12208` (phase end) | 832 / 72 | **833 passed / 72 skipped** | ✗ one low |
| Full suite at `a0f5a08` (true baseline) | 827 / 72 | **828 passed / 72 skipped** | ✗ one low |
| Full suite at `b3c3fce` (the base used) | 827 / 72 | **827 passed / 72 skipped** | ✓ |
| `-k stream` selected | 23 → 28 | **23 → 28** | ✓ |
| Offline evals, real `$?` | 65/65, exit 0 | **PASS 65/65 (100% vs 90%)**, `$?` = **0** | ✓ |
| `ruff check .` | clean | **All checks passed!** | ✓ |
| Protected tests byte-identical | 524 B / 360 B, sha equal | **sha equal** (522 B / 358 B by this extraction) | ✓ on the load-bearing half |
| Page shape | 1 script, 1 style, 0 `on*`, 0 `style=` | **1, 1, 0, 0** | ✓ |

The suite was invoked without a second `-q`, so the count line is visible rather than suppressed.

### The mutations, re-applied by the verifier

All three ran against an isolated scratchpad copy shadowing the editable install, so the working
tree was never modified — a second verifier is live in this repository.

| Mutation | Must red | Observed |
|---|---|---|
| Restore the bare `continue` that discards the supervisor chunk | the headline gate | **RED, 7 gates.** Verbatim: `assert 1 == 2 where 1 = len([(1, {'node': 'researcher', 'recalled_from_memory': 0})])`. Both protected Phase 19 tests stayed **GREEN** — the new gate is not a restatement of the old contract |
| Drop the `!= "done"` filter | terminal-routing + revision-loop gates | **RED both**: `Extra items in the left set: 'done'`; `Left contains one more item: ('done', 'started')` |
| Rename the emitted status to `beginning`, page untouched | the wire↔page coupling | **RED**: `assert {'beginning'} == {'started'}` — on the vocabulary assertion first, exactly as the SUMMARY recorded. Page half confirmed separately: `"started"` ×1, `beginning` ×0 in index.html |

The first is the phase's whole claim, and it is evidence rather than ceremony: it fails on
today's pre-fix code, on the exact assertion the record names.

### The deployed service (free GETs only — no run was started, nothing was spent)

```
GET https://research-agent.fly.dev/  (Accept: text/html)  → 200, 23,901 bytes
served page == repo src/research_agent/static/index.html   → True (byte-identical)
contains pendingRow / pendingNode / "started" ×1           → True
script blocks 1, style blocks 1
sha256 of the live <script> block  → present in the served Content-Security-Policy
sha256 of the live <style>  block  → present in the served Content-Security-Policy
```

The fix is live, and the CSP derivation still holds two page edits later.

### README's published SSE example, against real bytes

Captured from the isolated copy and compared line by line. The four data lines README publishes
are byte-identical to what the server emits:

```
event: node
data: {"node": "classifier", "status": "started"}

event: node
data: {"node": "classifier", "topic_type": "technical"}

event: node
data: {"node": "researcher", "status": "started"}

event: node
data: {"node": "researcher", "recalled_from_memory": 0}
```

README:136-139's sentence distinguishing the two forms of a `node` event is present and accurate.

### The arithmetic, rebuilt from scratch

The one place the record does not reproduce. Measured by re-running the suite in isolated
archives at four commits:

| Commit | What it is | Measured |
|---|---|---|
| `b3c3fce` | the merge base the record used | 827 / 72, stream 23 |
| `a0f5a08` | 22.5's last commit before any code — the **true** baseline | **828 / 72**, stream 23 |
| `b05bb89` | the fix + its first gates | 830 / 72 |
| `9e9a680` / `ad12208` | phase end | **833 / 72**, stream 28 |

The sixth test the phase did not write is in `test_evals.py` (193 → 194), attributable to
Phase 22's commit `1937c8a`. `git merge-base --is-ancestor f6eec95 b05bb89` succeeds: the PR #33
merge **is** an ancestor of 22.5's first code commit, contradicting 22.5-VALIDATION:29-31's
premise that "this branch does NOT contain Phase 22's work". The delta (+5), the zero new skips,
the `-k stream` 23 → 28 and the five named tests are all exactly right; the two absolute numbers
are stale by one. See warning 1.

---

## Anti-Patterns

| File | Pattern | Severity | Finding |
|---|---|---|---|
| all four phase-modified files | `TODO\|FIXME\|XXX\|HACK\|PLACEHOLDER` | — | None. The single hit is `FOLLOWUP_PLACEHOLDER`, a UI placeholder-text constant predating this phase |
| `service.py` `_stream` | stub / empty implementation | — | None. The branch reads real state and yields real bytes; proven by capture and by three mutations |
| `index.html` | orphaned code | — | None. `pendingRow`/`pendingNode` are read, written and reset on the same code path, and the deployed page carries them |

---

## Requirements Coverage

| Requirement | Status | Evidence |
|---|---|---|
| REQ-demo-shows-progress | ✓ Satisfied, with one measurement open by design | The started event exists, flows, is gated by five keyless tests that bite under mutation, and is deployed. What remains open is the wall-clock confirmation criterion 4 demands and the DOM behaviour no harness here can see — both named by the phase itself |

---

## Verdict

**The phase goal is achieved.** The started event exists, is derived from the supervisor's
`routed_to` exactly as designed, carries the routed-to TARGET's name (which is why Phase 19's
"no supervisor hops on the wire" test survives byte-identical), is filtered on the terminal
routing, alternates strictly with completions across a revision loop, reaches the follow-up
route through the shared `_stream`, and is live on production behind a CSP still derived from
the page's own bytes. Every one of those is something the verifier measured, not something the
SUMMARY said.

Nothing in this phase is weaker than claimed in substance. The record is unusually honest about
its own limits: criterion 4 is left explicitly open rather than ticked, the client-side DOM is
routed to a Manual-Only row rather than hidden behind a passing suite, and mutation 1(c)'s
imprecise red is recorded rather than smoothed over. All three reproduce.

**One claim does not reproduce:** the suite counts. The record says 827 → 832 and asserts they
were "measured rather than trusted"; the tree says 828 → 833, because the branch did contain
Phase 22's work despite the record's premise that it did not. The delta and every named test are
correct — this is a stale absolute, not a fabricated one, and it changes no verdict. But in a
project whose method is measure-don't-trust, a prediction reported as a measurement is worth
naming.

**Can this phase be archived honestly?** Yes — with one small correction first. The two open
items are legitimately open and already documented as such; they do not block archival, and the
post-deploy measurement should be carried forward as a live obligation rather than quietly
dropped. The stale counts should be corrected (or explicitly accepted) before
`/gsd:complete-milestone` cites them, since the milestone close-out inherits those numbers.

---

_Verified: 2026-08-17T02:23:39Z (retrospectively — the phase shipped as PR #34 / Fly v22)_
_Verifier: Claude (gsd-verifier), goal-backward, adversarial stance_
