---
phase: 14-real-cost-accounting
plan: 03
type: execute
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - src/research_agent/usage.py
  - src/research_agent/service.py
  - src/research_agent/metrics.py
  - src/research_agent/limits.py
  - README.md
  - docs/OPERATIONS.md
  - tests/test_usage.py
  - tests/test_service.py
  - tests/test_metrics.py
autonomous: true
requirements: [REQ-real-cost-accounting]

must_haves:
  truths:
    - "/pricing reports the multipliers in effect, the current AND next price window (next nullable from day one), and the Voyage embedding row with the telemetry caveat — every pre-phase key byte-identical (SC-2, additive-only rollout constraint)"
    - "/pricing returns 501 when either the LLM or the embedding model is unpriced — DEC-12 fail-loud, both providers"
    - "/metrics cost section carries embedding_tokens, embedding_requests, embedding_usd; both backends produce identical shape; every pre-phase key unchanged (SC-5)"
    - "No test asserts a PARTICULAR window is current or calls today() on an assertion path — the suite is green on 2026-09-02 without edits"
    - "README's 'Cost is computed from list prices' limitation is rewritten honestly: what the number now approximates, the telemetry caveat, and what is still NOT included (SC-5 docs half)"
    - "OPERATIONS documents both env vars, the hybrid geo semantics, and the ~1.33 under-reservation threshold as a docs note — no guardrail change (settled decision)"
    - "This phase ships with the next deploy — no live-deploy checkpoint invented; recorded explicitly"
  artifacts:
    - path: "src/research_agent/usage.py"
      provides: "window_for(model, on) and next_window(model, on) date-parameterised helpers returning PriceWindows"
      contains: "def next_window"
    - path: "src/research_agent/service.py"
      provides: "/pricing additive keys: multipliers, windows (current+next), embedding; 501 on unpriced embedding model"
      contains: "windows"
    - path: "src/research_agent/metrics.py"
      provides: "_SUM_COLUMNS + _summarise + PG coercion carry the three embedding aggregates"
      contains: "embedding_usd"
    - path: "README.md"
      provides: "honest cost-limitation rewrite (per-phase deliverable)"
    - path: "docs/OPERATIONS.md"
      provides: "COST_DISCOUNT_FACTOR + INFERENCE_GEO_MULTIPLIER rows; threshold note"
      contains: "COST_DISCOUNT_FACTOR"
  key_links:
    - from: "src/research_agent/service.py pricing()"
      to: "usage.window_for / usage.next_window / usage.cost_discount_factor / usage.inference_geo_multiplier"
      via: "endpoint stays thin, date logic in unit-testable helpers; factors DISPLAYED here, never multiplied"
      pattern: "next_window"
    - from: "src/research_agent/service.py pricing()"
      to: "memory.EMBEDDING_MODEL / usage.voyage_price_for"
      via: "attribute read at request time so tests can monkeypatch.setattr"
      pattern: "EMBEDDING_MODEL"
    - from: "src/research_agent/metrics.py _summarise"
      to: "/metrics cost payload"
      via: "shared shape, contract-tested identical across backends"
      pattern: "embedding_usd"
---

<objective>
Surface what plans 01–02 built: `/pricing` gains the multipliers in effect, current+next
windows across the 25-days-out Sonnet boundary, and the Voyage row; `/metrics` aggregates
embedding spend; README and OPERATIONS say plainly what the number is and is not.

Purpose: SC-2 whole, SC-5's surface and docs halves. The September boundary is the operator
decision this surface exists to expose (caps bind ~1/3 sooner from 09-01); the README
rewrite is this phase's per-phase deliverable.

Output: extended /pricing and /metrics payloads (additive only — deployed demo page must
not break), window helpers, rewritten docs, and the explicit ships-with-next-deploy note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-real-cost-accounting/14-CONTEXT.md
@.planning/phases/14-real-cost-accounting/14-RESEARCH.md
@src/research_agent/usage.py
@src/research_agent/service.py
@src/research_agent/metrics.py
@README.md
@docs/OPERATIONS.md
</context>

<interfaces>
From src/research_agent/service.py (lines 864-887 — current /pricing, keep byte-identical keys):
  payload: model, usd_per_mtok{input,output,cache_write_5m,cache_read},
           web_search_usd_per_request, max_run_cost_usd; 501 via HTTPException on UnknownModelPricing
  Existing test: test_pricing_endpoint_reports_todays_rates asserts input in (2.0, 3.0) — the
  boundary-proof idiom to copy for any value assertion.

From src/research_agent/usage.py (after 14-01/14-02):
  PriceWindow(price: Price | float, since: date | None, until: date | None), .covers(day)
  PRICES: claude-sonnet-5 has two windows (until 2026-08-31 / since 2026-09-01);
          claude-opus-5 and claude-haiku-4-5 each have ONE undated window
  cost_discount_factor() / inference_geo_multiplier()  # display these; NEVER multiply here
  voyage_price_for(model, on) raises UnknownModelPricing

From src/research_agent/memory.py: EMBEDDING_MODEL (module attr, env-read at import — reference
  as memory.EMBEDDING_MODEL at request time so monkeypatch.setattr works in tests)

From src/research_agent/metrics.py (after 14-02): _SUM_COLUMNS string, _summarise cost dict,
  PG summary() int/float coercion loop — the three places aggregates are declared.

Demo page: static/index.html only LINKS /pricing (line ~142), parses nothing — additive safe.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /pricing payload → deployed demo page + external readers | Phase 12 rollout constraint: additive only; removing/renaming a key breaks a consumer that cannot be redeployed atomically |
| /pricing content → operators | Exposes only published rates and operator-chosen env factors — no secrets, no per-caller data (ASVS V4 posture unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | DoS (payload breakage) | /pricing and /metrics consumers | mitigate | Additive-only pinned by payload_additive test asserting every pre-phase key present and unchanged in type |
| T-14-10 | Tampering | service.py multiplying displayed factors into a price | mitigate | Factors appear in the payload dict only; structural grep re-gate + 14-02's e2e value gate already proves the pipeline |
| T-14-11 | DoS (time bomb) | windows.next non-null assumption dying 2026-09-01 | mitigate | next nullable from day one; helper tests pin next-is-None after the boundary and for undated models |
| T-14-12 | Repudiation | Docs implying invoice parity | mitigate | README/OPERATIONS/payload notes use the CONTEXT-mandated telemetry phrasing verbatim in spirit: approximation, not the invoice |
| T-14-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 14-03.1: Window helpers + the additive /pricing payload with dual-provider 501</name>
  <files>src/research_agent/usage.py, src/research_agent/service.py, tests/test_usage.py, tests/test_service.py</files>
  <read_first>src/research_agent/service.py lines 864-887 (pricing endpoint) and the imports block, src/research_agent/usage.py PriceWindow/PRICES, tests/test_service.py test_pricing_endpoint_reports_todays_rates (line ~1271) and the make_client fixture, 14-RESEARCH.md §"The /pricing extension"</read_first>
  <action>
    1. usage.py helpers (date logic lives here so it is unit-testable at fixed dates;
       the endpoint keeps using today):
       - `window_for(model: str, on: date | None = None) -> PriceWindow`: the covering
         window; raises UnknownModelPricing when none covers (same message discipline as
         price_for).
       - `next_window(model: str, on: date | None = None) -> PriceWindow | None`: among
         the model's windows whose `since` is strictly after `on`, the earliest; None when
         there is none — None for undated models (opus/haiku) and None for sonnet-5 from
         2026-09-01 onward. Both work for models in PRICES only (Voyage windows are flat
         floats; not needed here).

    2. service.py pricing(): keep every existing key and value expression untouched, then
       add three keys (names from RESEARCH — Claude's-discretion names, now fixed):
       - `"multipliers"`: {"cost_discount_factor": usage_accounting.cost_discount_factor(),
         "inference_geo_multiplier": usage_accounting.inference_geo_multiplier(),
         "inference_geo_note": "applied per response usage.inference_geo; this service
         sends global routing"}. Display only — no arithmetic in service.py.
       - `"windows"`: for graph.MODEL, {"current": {...}, "next": {...} | None} where each
         window serialises as {"since": ISO-string-or-null, "until": ISO-string-or-null,
         "usd_per_mtok": the four-field dict in the existing key order}. Build via
         window_for/next_window; a small local serialiser keeps the endpoint thin.
       - `"embedding"`: {"model": memory.EMBEDDING_MODEL (attribute read at request time),
         "usd_per_mtok": usage_accounting.voyage_price_for(memory.EMBEDDING_MODEL),
         "note": "computed from Voyage-reported token counts — telemetry, not billing
         truth; an approximation of the invoice, not the invoice"}.
       - 501: extend the existing try/except so UnknownModelPricing from EITHER price_for
         OR voyage_price_for raises HTTPException(501, str(exc)) — the settled call from
         plan review (Open Question 2 → 501; the misconfiguration must be as loud as the
         LLM equivalent). Import memory at module top alongside graph.

    3. Helper tests in tests/test_usage.py, all fixed dates:
       - window_for("claude-sonnet-5", date(2026, 8, 31)).price.input == 2.0 and
         window_for(..., date(2026, 9, 1)).price.input == 3.0.
       - `test_next_window_is_the_september_window_before_the_boundary` (contributes to
         `-k boundary` coverage): next_window("claude-sonnet-5", date(2026, 8, 9)).since
         == date(2026, 9, 1).
       - next_window("claude-sonnet-5", date(2026, 9, 1)) is None (the time-bomb pin);
         next_window("claude-opus-5", date(2026, 8, 9)) is None (undated models).

    4. Endpoint tests in tests/test_service.py — SHAPE only, never values derived from
       today's date (copy the `input in (2.0, 3.0)` idiom if a value must be touched):
       - `test_pricing_payload_carries_multipliers_windows_and_embedding` (matches
         `-k pricing_payload`): all pre-phase keys still present with unchanged values;
         "multipliers" has the three subkeys; "windows"."current"."usd_per_mtok" has the
         four rate keys; "windows" contains the "next" key and it is either None or a
         dict with since/until/usd_per_mtok; "embedding"."model" and "usd_per_mtok"
         present, "note" mentions approximation. With env vars monkeypatched
         (COST_DISCOUNT_FACTOR=0.9), the payload reports 0.9 — /pricing surfaces what is
         in effect (SC-2), not hardcoded defaults.
       - `test_pricing_501_when_the_embedding_model_is_unpriced`:
         monkeypatch.setattr(memory, "EMBEDDING_MODEL", "voyage-4") → GET /pricing → 501,
         body mentions voyage-4. (voyage-4 is deliberately absent from VOYAGE_PRICES.)
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_usage.py tests/test_service.py -q -k "window or pricing_payload or pricing_501" -x</automated>
  </verify>
  <acceptance_criteria>
    - GET /pricing (TestClient) returns 200 with all pre-phase keys AND the three new keys;
      the 501 test returns exactly status 501 for the unpriced embedding model.
    - `grep -rn "date.today()\|datetime.now()" tests/test_service.py tests/test_usage.py`
      shows no new occurrence on an assertion path (Pitfall 6).
    - Structural re-gate: `grep -rln "cost_discount_factor()\|inference_geo_multiplier()" src/research_agent/`
      outputs exactly usage.py and service.py, and in service.py the call results appear
      ONLY inside the returned payload dict — never in an arithmetic expression (confirm
      by reading the diff; the behavioural guarantee is 14-02's e2e ratio gate).
    - Mutation K: hardcode "next": {} (non-null) unconditionally → the next-is-None helper
      test at date(2026, 9, 1) stays green BUT the endpoint shape test must still accept
      null — so instead falsify the helper: make next_window return the CURRENT window →
      the since == date(2026, 9, 1) assertion red; revert, record.
  </acceptance_criteria>
  <done>SC-2 complete: /pricing exposes multipliers in effect, both windows with a nullable next, and the embedding row; unpriced models 501 on both providers; nothing a deployed consumer reads has changed.</done>
</task>

<task type="auto">
  <name>Task 14-03.2: /metrics embedding aggregates, both backends, additive shape pinned</name>
  <files>src/research_agent/metrics.py, tests/test_metrics.py, tests/test_service.py</files>
  <read_first>src/research_agent/metrics.py _SUM_COLUMNS (line ~218), _summarise cost section (line ~193), PostgresMetricsStore.summary coercion loop (line ~370), tests/test_metrics.py test_cost_and_tokens_are_summed and the backend fixture/contract idiom, 14-RESEARCH.md §"Voyage spend into the run record" (metrics bullet)</read_first>
  <action>
    1. metrics.py — three declaration points, no other logic:
       - `_SUM_COLUMNS`: add COALESCE sums for embedding_tokens, embedding_requests
         (AS themselves) and embedding_cost_usd (AS embedding_cost_usd).
       - `_summarise` "cost" dict: add "embedding_tokens", "embedding_requests" (ints) and
         "embedding_usd": round(totals["embedding_cost_usd"] or 0.0, 6). Append after the
         existing keys — additive, nothing reordered or renamed.
       - PostgresMetricsStore.summary(): add embedding_tokens/embedding_requests to the
         int-coercion tuple and coerce embedding_cost_usd to float beside cost_usd
         (SUM(BIGINT) arrives as Decimal).

    2. Tests:
       - tests/test_metrics.py `test_metrics_includes_embedding_spend` (matches
         `-k metrics_includes_embedding_spend`): record two RunRecords with embedding
         fields (e.g. 25/1/$0.0000015 and 50/2/$0.000003) plus one pre-phase-shape record
         (zeros) → summary()["cost"] sums to 75 tokens, 3 requests,
         embedding_usd == round(0.0000045, 6); run through the file's existing
         both-backends idiom so SQLite and Postgres (HAS_POSTGRES-gated) produce
         identical shape.
       - tests/test_service.py `test_payloads_stay_additive_for_deployed_consumers`
         (matches `-k payload_additive`): GET /metrics → the cost section contains EVERY
         pre-phase key (total_usd, avg_usd_per_run, model_calls, input_tokens,
         output_tokens, cache_read_tokens, cache_creation_tokens, web_searches) AND the
         three new ones; a RunResponse from a fake run contains every pre-phase top-level
         field (session_id, mode, task, topic_type, answer, approved, forced_stop_reason,
         revision_count, max_revisions, iterations, max_iterations, retries, usage,
         cost_usd, trace) — the usage dict may gain keys (additive) but no field is
         renamed or removed. This is the Phase-12-rollout-constraint gate for every
         deployed consumer including the demo badge.
  </action>
  <verify>
    <automated>DATABASE_URL=postgresql://postgres@localhost:54329/postgres .venv/bin/pytest tests/test_metrics.py tests/test_service.py -q -k "metrics_includes_embedding_spend or payload_additive" -x</automated>
  </verify>
  <acceptance_criteria>
    - summary()["cost"]["embedding_usd"] sums recorded embedding_cost_usd across rows in
      BOTH backends with identical shape; pre-phase cost keys unchanged (existing
      test_cost_and_tokens_are_summed stays green unmodified — that is the baseline gate).
    - Mutation L: remove embedding_cost_usd from _SUM_COLUMNS →
      `test_metrics_includes_embedding_spend` red with a KeyError/missing-key on
      embedding_usd (the intended route); revert, record.
    - Mutation M: rename "total_usd" to "total" in _summarise →
      `test_payloads_stay_additive_for_deployed_consumers` red; revert, record — proving
      the additive gate actually bites.
  </acceptance_criteria>
  <done>SC-5 surface half: /metrics carries embedding spend, contract-identical across backends, with the additive constraint enforced by a test that has been observed to bite.</done>
</task>

<task type="auto">
  <name>Task 14-03.3: Say what the number is — README rewrite, OPERATIONS env rows, threshold note, ships-with-next-deploy</name>
  <files>README.md, docs/OPERATIONS.md, src/research_agent/limits.py</files>
  <read_first>README.md line 204 (the limitation bullet to rewrite) and the surrounding Limitations section, docs/OPERATIONS.md lines 440-510 (env table + reservation explanation), src/research_agent/limits.py lines 30-45 and 115-125 (DEMO_RESERVED_RUN_USD docs), 14-RESEARCH.md §"The cap interaction" and §"Voyage spend into the run record" (magnitude honesty)</read_first>
  <action>
    Documentation only — no logic changes anywhere in this task (limits.py gets a
    docstring/comment edit exclusively; the scope fence forbids guardrail changes).

    1. README.md: replace the "Cost is computed from list prices" bullet (baseline: exactly
       1 occurrence) with an honest rewrite covering, in this order:
       - What changed: reported cost now approximates the invoice — a configurable
         discount factor (COST_DISCOUNT_FACTOR) and the inference_geo multiplier
         (INFERENCE_GEO_MULTIPLIER sets the rate; the RESPONSE's usage.inference_geo
         decides per call whether it applies — an env-only flag could disagree with the
         bill when a workspace pins geo) feed cost computation at one choke point.
       - Voyage embedding spend is now counted (~$0.0002 of a ~$0.15 run — the point is
         the accounting surface is complete, not that the number moved), computed from
         Voyage-reported token counts, which are telemetry, not billing truth — Phase 13
         observed a one-word document report 0 tokens while returning a valid embedding.
       - What is still NOT included: invoice reconciliation (the number approximates the
         bill, it is not the bill), the geo multiplier on the web-search fee (docs scope
         1.1x to token categories), Voyage's free-allowance tiers, 1h-cache write rates
         (unused by this service).
       - Keep the effective-dating sentence and the "read /pricing, not a document"
         pointer — both still true and now stronger (/pricing shows current AND next
         windows).
    2. docs/OPERATIONS.md:
       - Env table (near AGENT_MAX_RUN_COST_USD, line ~460): add rows for
         `COST_DISCOUNT_FACTOR` (default 1.0 — "negotiated discount applied to computed
         Anthropic cost; ≤0 or unparseable falls back to 1.0 so a typo cannot disarm the
         spend caps") and `INFERENCE_GEO_MULTIPLIER` (default 1.1 — "the RATE applied
         only when a response reports usage.inference_geo == 'us'; applicability is
         observed from the API, not declared").
       - Near the DEMO_RESERVED_RUN_USD reservation explanation (line ~500): the
         quantified note — runs land ≈ $0.15 against a $0.20 reservation, so
         under-reservation needs a combined multiplier > ~1.33, unreachable at the
         published 1.1 with discount ≤ 1.0; if a future pricing dimension pushes typical
         run cost above $0.20, raise DEMO_RESERVED_RUN_USD proportionally. A discount
         < 1.0 makes the estimate MORE conservative (cap binds sooner during bursts,
         never overshoots) — safe, so it is a note, not a resize. Also state:
         AGENT_MAX_RUN_COST_USD now bounds multiplied cost — a discounted deployment gets
         more work per capped dollar, which is the correct semantics (the cap bounds
         spend, not calls).
       - Deployment note: this phase ships with the NEXT deploy; no dedicated cutover.
         At neutral defaults ("" geo responses, factors unset) the deployed service's
         numbers are unchanged; the post-deploy smoke in 14-VALIDATION's manual row
         happens then — record "ships with next deploy" as the chosen option.
    3. limits.py: extend the `reserved_run_usd` (line ~123) docstring/comment with one
       sentence: settled cost is now multiplied (discount × geo) + embedding spend;
       under-reservation needs combined multiplier > ~1.33 — see OPERATIONS. No code.
    4. No new ADR: not a reversal — a pure extension of DEC-12 (CONTEXT). State that
       decision explicitly in the SUMMARY rather than leaving it implicit.
  </action>
  <verify>
    <automated>bash -c 'set -e; test "$(grep -c "Cost is computed from list prices" README.md)" = 0; test "$(grep -c "COST_DISCOUNT_FACTOR" docs/OPERATIONS.md)" -ge 1; test "$(grep -c "INFERENCE_GEO_MULTIPLIER" docs/OPERATIONS.md)" -ge 1; test "$(grep -ci "1.33" docs/OPERATIONS.md)" -ge 1; grep -qi "telemetry" README.md; echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - Grep gates with measured baselines (from 14-VALIDATION): "Cost is computed from list
      prices" in README 1 → 0; COST_DISCOUNT_FACTOR / INFERENCE_GEO_MULTIPLIER /
      "1.33" in OPERATIONS 0 → ≥1 each. These are content-presence gates on prose — the
      honest-green reason a mutation run is skipped for them is that the gate IS the
      content change itself; record that reason, per the gate-discipline directive.
    - README states all three: approximation (not the invoice), the telemetry caveat with
      the 0-token example, and the still-not-included list. Prose review against CONTEXT's
      honesty constraint recorded in the SUMMARY.
    - limits.py diff is comment/docstring only: `git diff --stat src/research_agent/limits.py`
      shows changes and `.venv/bin/pytest tests/test_limits.py -q` is green with zero code
      paths altered.
    - The ships-with-next-deploy choice is written in OPERATIONS and echoed in the SUMMARY
      (VALIDATION manual row: "record which" — this records it).
    - Full suite final: plain ≥ 529 passed / 63 skipped; armed ≥ 591 passed / 1 skipped.
  </acceptance_criteria>
  <done>SC-5 docs half: the docs say which number this is and is not; operators can find both env vars, the hybrid geo semantics, and the cap threshold; the deploy posture is recorded, not implied.</done>
</task>

</tasks>

<verification>
- Plain suite ≥ 529 passed / 63 skipped; armed suite ≥ 591 passed / 1 skipped — final phase gate before /gsd:verify-work.
- Mutations K–M observed red by the intended route, reverted, recorded; prose gates recorded
  as honest-green with reason.
- `grep -rln "cost_discount_factor()\|inference_geo_multiplier()" src/research_agent/` →
  exactly usage.py + service.py (display-only in service.py, confirmed on the diff).
- No live-deploy checkpoint: the phase ships with the next deploy (recorded in OPERATIONS
  and the SUMMARY) — deliberately not invented here.
</verification>

<success_criteria>
- SC-2: /pricing exposes multipliers in effect, current+next windows, embedding row; 501 on
  unpriced models from either provider.
- SC-5: /metrics carries embedding spend; README/OPERATIONS say what the figure is and is
  not, with the telemetry caveat.
- The 2026-09-01 boundary cannot silently break the payload or the suite (nullable next,
  fixed-date tests, shape-only endpoint assertions).
- VALIDATION rows 9–13 satisfied (T1 row 9, T2 rows 10–11, T3 rows 12–13).
</success_criteria>

<output>
Create `.planning/phases/14-real-cost-accounting/14-03-SUMMARY.md` when done, including the
mutation table (K–M), the prose-gate honest-green notes, and the no-ADR + ships-with-next-deploy
decisions stated explicitly.
</output>
