---
phase: 21.5-classifier-on-opus-5
plan: 01
subsystem: graph + evals
tags: [per-node-model, classifier, fixture-provenance, dataset-taxonomy, adr, keyless, zero-spend]

# Dependency graph
requires:
  - phase: 21.5-classifier-on-opus-5
    plan: "research"
    provides: "Q1 — the trap and its resolution (default claude-opus-5 DIRECTLY, never MODEL); Q2 — the staleness-gate precondition that does NOT transfer from the critic; Q4 — the three-way label split with chatty-label-falls-back's structural conflict; Q5 — the refusal list re-derived as eight cases, not the believed six; Q6 — the three smoke assertions that would go red"
  - phase: 16-independent-critic-model
    plan: "*"
    provides: "call_model(state, node, *, model=None) — the four-site per-node model seam this phase extends with zero new mechanism, and critic_model()'s shape, mirrored here with deliberately inverted polarity"
  - phase: 18-independent-eval-judge
    plan: "*"
    provides: "ADR-0012 and the judge on claude-opus-4-8 — the reason a judge move was never in scope here (the critic already IS Opus 5)"
provides:
  - "src/research_agent/graph.py::classifier_model() — CLASSIFIER_MODEL, stripped, or 'claude-opus-5' DIRECTLY; read per call; the trap closed and gated keylessly"
  - "src/research_agent/graph.py::CLASSIFIER_PROMPT_TEMPLATE — the classifier prompt as a module constant, verified byte-identical (477 chars, exact string equality against the old f-string), with its three consumers named"
  - "classifier_node threading model=classifier_model() to all four call_model naming sites — payload, span, cost record, log line"
  - "evals/harness.py — record_case_to_fixture writes models.classifier; grade_fixture_current's BODY unchanged, its docstring separating the two uncompared roles by reason"
  - "evals/classifier_probe.py — the committed, spend-guarded two-model probe; prompt by IDENTITY; keyless invocation prints the quote and exits 2 with no client constructed"
  - "fly.toml CLASSIFIER_MODEL = 'claude-opus-5' + tests/test_deploy_config.py::CLASSIFIER_MODEL_PIN — the value pinned with the fail-direction inverted (absence legal, drift red)"
  - "evals/dataset.py — three cases relabelled in lockstep, two why-fields annotated; general stratum 11 -> 8 against a floor of 7"
  - "docs/adr/0013-classifier-on-its-own-model.md + the re-derived index — the record of the two deliberate asymmetries with the ADR-0010 precedent"
  - "+21 keyless tests: 13 smoke (accessor / four-site threading / misbilling / attribution / dispatch pin), 4 provenance, 3 probe, 1 deploy pin"
  - "the wave-2 handoff: the exact probe invocation, the EIGHT-case refusal list re-derived from REFUSALS.json with Phase 21's per-case actuals summing to $1.913055, and the corrected P-06 env assertion for CLASSIFIER_MODEL"
affects: [21.5-02, 22-01, 22-02]

# Tech tracking
tech-stack:
  added: []  # zero packages. 21.5-RESEARCH's Package Legitimacy Audit is N/A and stays N/A.
  patterns:
    - "Do not extend a proven pattern past the PRECONDITION that made it safe. The critic's staleness gate is inert in CI only because critic_model() == MODEL there; under a non-neutral default the identical code grades 19 recordings stale on every push. Measured, not argued."
    - "A neutral default is correct when the capability is opt-in and wrong when the requirement IS the opt-in. The same code shape, the same author, opposite right answers one function apart."
    - "Pin a shared string by IDENTITY (`is`), not equality, when the failure mode is a copy. Measured here: `==` stayed True against a byte-identical copy while `is` went False — equality would have been green while the probe measured a prompt that no longer shipped."
    - "An exact-equality assertion on a metadata map is an ANNOUNCEMENT gate. tests/test_evals.py:2984's own comment said 'exact-equality so a fourth cannot arrive unannounced'; the fourth arrived and it fired. That red is the design working, not blast radius."
    - "Assert against the file's own constant, never the accessor the code under test called. A test comparing to `classifier_model()` grades the shell it runs in and stays green through the exact mutation it exists to catch."

key-files:
  created:
    - evals/classifier_probe.py
    - docs/adr/0013-classifier-on-its-own-model.md
  modified:
    - src/research_agent/graph.py
    - tests/test_graph_smoke.py
    - evals/harness.py
    - tests/test_evals.py
    - fly.toml
    - tests/test_deploy_config.py
    - evals/dataset.py
    - docs/adr/README.md
    - README.md
    - docs/OPERATIONS.md
    - evals/__main__.py

key-decisions:
  - "The default is `\"claude-opus-5\"` written as a literal in classifier_model(), never MODEL. Mutation-verified: reverting it to MODEL reds SEVEN keyless tests, including the $0.0060 attribution delta collapsing to $0.0015. The trap 21.5-CONTEXT named is now caught by CI rather than by a failed paid run."
  - "grade_fixture_current's BODY is byte-identical. The whole Task 2 change to that function is docstring, and the docstring now separates the judge's uncompared role from the classifier's BY REASON — the judge's recording stays true, the classifier's is uncompared because of the default's polarity. Conflating them is how a future reader 'restores consistency' and stales 19 fixtures."
  - "One UNPLANNED red, reported rather than silenced: tests/test_evals.py:2984 (test_record_writes_a_fixture_per_case_with_fakes) asserts exact equality on the models map, and its own comment reads 'exact-equality so a fourth cannot arrive unannounced'. The fourth role arrived and it fired exactly as designed. Updated deliberately with a re-derived comment; the guard now stands at the fifth."
  - "general-defines-a-term RELABELLED, resolving 21.5-RESEARCH's Open Question 2 toward correction rather than leave-and-document. Measured cost: the general stratum lands at 8 against its floor of 7, one spare. The alternative (leave-and-document, margin 2) was available and defensible; correction was chosen because unlike chatty-label-falls-back there is no structural reason its label must stay wrong — it simply was wrong."
  - "chatty-label-falls-back untouched in every label field, and the floor's bite OBSERVED rather than asserted: flipping its expect_topic_type to technical dropped the off-menu-general list from 3 to 2 and red test_dataset_taxonomy_per_stratum_minimums with `assert 2 >= 3` naming the two survivors."
  - "README's classifier-role paragraph (:184-196) judged by reading and left untouched — it describes what the label DOES and names no model, and still reads complete now that the Stack line and config prose name one. The Phase 20-02 precedent."
  - "Every measured COUNT in README and OPERATIONS is untouched. Counts move once, at 21.5-02's close-out, after the re-record settles them. Updating them twice is how two surfaces drift apart."
  - "The probe's two arms are LITERALS (claude-sonnet-5 / claude-opus-5), not accessors. Reading classifier_model() there would mean the probe measured whatever the operator's shell said, producing a report that cannot be compared against the one before it."

# Metrics
duration: 95min
completed: 2026-08-16
status: complete

actuals:
  tokens: 19736    # chars/4 over the realized diff (78,945 chars across 13 files)
  tasks: 4
  commits: 4
---

# Phase 21.5 Plan 01: The classifier's own model, keyless end to end Summary

**One-liner:** `classifier_model()` defaults to `claude-opus-5` DIRECTLY — never `MODEL`, the trap closed and caught by seven keyless reds — threaded through `call_model`'s existing four-site seam; `models.classifier` recorded as provenance with the symmetric staleness gate demonstrated redding all 19 committed fixtures and then rejected; the probe committed and spend-guarded with the shipped prompt by identity; three disputed labels corrected inside the stratum floors and the fourth left with its structural conflict documented; ADR-0013 recording both deliberate asymmetries with the Phase 16 precedent.

**Zero spend. Nothing in this wave passed `--yes`.** Every evals invocation ran with `ANTHROPIC_API_KEY="" VOYAGE_API_KEY=""` explicitly prefixed. The probe was BUILT and unit-tested; it was never run against a real API. Running it is 21.5-02's checkpoint 1.

## Measured baselines and deltas

| Gate | Before | After | Delta |
|------|--------|-------|-------|
| Full suite, keyless (`.venv/bin/pytest -p no:warnings`) | 806 passed / 72 skipped | **827 passed / 72 skipped** | **+21 passed**, +0 skipped |
| Collected items, keyless | 878 | **899** | +21 added, **0 removed**, 0 renamed |
| `tests/test_graph_smoke.py` | 44 | **57** | +13 |
| `tests/test_evals.py` | 186 | **193** | +7 |
| `tests/test_deploy_config.py` | 13 | **14** | +1 |
| Offline evals (`python -m evals --quiet`), real `$?` | 59/59, exit 0 | **59/59 (100% vs 90% required), exit 0** | unchanged — labels moved, the denominator did not |
| `git diff --stat --exit-code evals/fixtures/` | clean | **clean** | **zero fixture edits** — P-02 working |
| Fixtures on disk / refusals | 19 / 21 | 19 / 21 | unchanged — the re-record is 21.5-02's |
| `.venv/bin/ruff check src tests evals` | clean | **clean** | — |

The addopts caveat from Phase 21 still bites: `pyproject.toml`'s addopts already carries `-q`, so a second `-q` suppresses the count line. Every count above was read with `-p no:warnings` and no extra `-q`.

### The +21, reconciled by name

| # | Test | Task |
|---|------|------|
| 1 | `test_classifier_model_accessor_defaults_to_opus_not_the_writers_model` | 1 |
| 2 | `test_classifier_model_accessor_returns_the_configured_model` | 1 |
| 3 | `test_classifier_model_accessor_treats_blank_as_unset` | 1 |
| 4 | `test_classifier_model_accessor_reads_the_environment_on_every_call` | 1 |
| 5 | `test_classifier_threading_four_sites_a_reaches_the_api_payload` | 1 |
| 6 | `test_classifier_threading_four_sites_b_reaches_the_span` | 1 |
| 7 | `test_classifier_threading_four_sites_c_reaches_the_cost_record` | 1 |
| 8 | `test_classifier_threading_four_sites_d_reaches_the_log_line` | 1 |
| 9 | `test_classifier_misbilling_discriminator_unpriced_classifier_is_reported` | 1 |
| 10 | `test_classifier_misbilling_discriminator_stays_silent_when_unset` | 1 |
| 11 | `test_per_node_attribution_prices_the_classifier_at_its_own_rate` | 1 |
| 12 | `test_per_node_attribution_two_independent_nodes_move_separately` | 1 |
| 13 | `test_the_classifier_prompt_keeps_the_substring_the_fakes_dispatch_on` | 1 |
| 14 | `test_record_writes_the_classifier_role_at_its_shipped_default` | 2 |
| 15 | `test_record_writes_the_classifier_role_from_the_environment` | 2 |
| 16 | `test_the_classifier_role_is_provenance_and_never_grades` | 2 |
| 17 | `test_the_classifier_role_never_becomes_required` | 2 |
| 18 | `test_the_probe_refuses_to_spend_without_yes` | 3 |
| 19 | `test_the_probe_sends_the_shipped_prompt_by_identity` | 3 |
| 20 | `test_the_probes_case_list_is_derived_from_the_dataset` | 3 |
| 21 | `test_the_classifier_model_pin_is_intact` | 3 |

878 + 21 = 899 collected; 899 − 72 = 827. Both sides close exactly. Task 4 adds no test: it edits data and prose, and the EXISTING taxonomy and ADR-index gates are its referee — both green at zero edits to themselves.

## The three planned smoke reds, observed before any test was touched

Run immediately after the graph change and before a single test edit. **Exactly three failures, exactly the three 21.5-RESEARCH Q6 predicted, and no fourth:**

```
FAILED tests/test_graph_smoke.py::test_critic_model_accessor_unset_is_byte_identical_to_setting_it
FAILED tests/test_graph_smoke.py::test_critic_threading_four_sites_a_reaches_the_api_payload
FAILED tests/test_graph_smoke.py::test_per_node_attribution_leaves_every_other_node_on_the_writers_model
3 failed, 41 passed in 1.69s
```

with the verdicts, verbatim:

| Line | Test | Failure |
|---|---|---|
| :799 | `..._unset_is_byte_identical_to_setting_it` | `assert False` — the blanket `all(m == graph.MODEL for m in ...)` line |
| :817 | `..._four_sites_a_reaches_the_api_payload` | `AssertionError: assert 'claude-opus-5' == 'claude-sonnet-5'` |
| :977 | `..._leaves_every_other_node_on_the_writers_model` | `At index 0 diff: ('classifier', 'claude-opus-5') != ('classifier', 'claude-sonnet-5')` |

*(The plan cited :978 for the third; pytest reports :977, the first line of the multi-line `assert per_call == [...]` statement. Same assertion, the tuple `("classifier", graph.MODEL)` sits on :978 inside it.)*

**How each was resolved — deliberately, with re-derived docstrings, and `CLASSIFIER_MODEL` delenv'd in all three so a dev shell's export cannot flip them:**

- **:799** — the payload-equality comparison KEPT (unset vs `CRITIC_MODEL=MODEL` still produces byte-identical requests; both runs classify on Opus 5). The blanket equality replaced by four per-node assertions. Docstring re-derived: the CI-guard story now names TWO variables, because keyless replay depends on `critic_model() == MODEL` **and** on the classifier being deliberately uncompared.
- **:817** — `models["classifier"] == OPUS`, compared against the file's own constant rather than the accessor, with a docstring saying why: a test comparing to `classifier_model()` would grade whatever this shell resolved and stay green through the exact mutation it exists to catch.
- **:977** — the tuple becomes `("classifier", OPUS)`. Docstring re-derived: "Independence is the critic's alone" is now false, and the honest statement is that what stays on the writer's model is the writer and the researcher.

## Every mutation, observed

Six mutations across four tasks. Every one applied, run, its actual output recorded, reverted, and re-run green. **Not one stayed green.**

### Task 1 (a) — the trap: `classifier_model()`'s fallback reverted to `MODEL`

Seven keyless reds, in a shell with nothing exported:

```
FAILED test_classifier_model_accessor_defaults_to_opus_not_the_writers_model
FAILED test_classifier_model_accessor_treats_blank_as_unset
FAILED test_classifier_model_accessor_reads_the_environment_on_every_call
FAILED test_per_node_attribution_prices_the_classifier_at_its_own_rate
FAILED test_critic_model_accessor_unset_is_byte_identical_to_setting_it
FAILED test_critic_threading_four_sites_a_reaches_the_api_payload
FAILED test_per_node_attribution_leaves_every_other_node_on_the_writers_model
```

with `tests/test_graph_smoke.py:1238: assert 0.0015000000000000013 == 0.006 ± 1.0e-09` — the per-call attribution delta collapsing from Opus−Haiku to Haiku−Haiku, which is the arithmetic proof that the name reached `record()`. `test_classifier_model_accessor_returns_the_configured_model` correctly stayed green (its env is set, so the fallback is not exercised). **This is the researched trap caught as a keyless red rather than as a repeat of Phase 21's refusals.**

### Task 1 (b) — the dispatch literal reworded ("one word" → "one term")

9 smoke reds, and the offline evals fell **59/59 → 32/59**. The blast radius is the point: both scripted clients dispatch on that substring, so rewording it silently routes the classifier call to their *writer* branch — no exception, just an eval grading a research report as a topic label. Reds included `test_rejected_draft_is_rewritten_then_approved` and `test_a_critic_that_never_approves_stops_at_the_revision_cap`, which have nothing to do with models.

### Task 2 — the staleness cascade, the load-bearing demonstration

A classifier comparison added to `grade_fixture_current` in the obvious symmetric shape (`models.get("classifier") or pipeline`, against `graph.classifier_model()`), measured in a keyless shell with nothing exported:

```
fixtures on disk: 19
fixture_current RED: 19
carrying a 'classifier' key: 0
budget-cap-is-labelled.json: the CLASSIFIER was recorded on 'claude-sonnet-5' but this
  tree runs it on 'claude-opus-5' (CLASSIFIER_MODEL) -- the recording describes a
  pipeline that no longer exists; re-record
```

Offline evals **59/59 → 40/59** — exactly the nineteen replay legs, and `pytest tests/test_evals.py` went 10 failed / 180 passed.

**This is the argument for P-02 and for ADR-0013's departure from the Phase 16 precedent, stated in exactly those terms:** the precondition that makes the critic's staleness gate harmless in CI is `critic_model() == graph.MODEL` wherever nobody set the variable. `classifier_model()` returns `claude-opus-5` in EVERY environment from the moment this plan merges, so the identical code grades all 19 committed recordings stale on every push — nineteen cases this phase has no budget to re-record and no reason to distrust. Mutation reverted; the observation is the evidence, and it is quoted in the ADR.

### Task 3 (a) — `fly.toml`'s CLASSIFIER_MODEL flipped to the writer's model

```
FAILED tests/test_deploy_config.py::test_the_classifier_model_pin_is_intact
AssertionError: fly.toml's [env] sets CLASSIFIER_MODEL to 'claude-sonnet-5', which is
neither absent nor 'claude-opus-5'. Absence is fine -- the code default in
graph.classifier_model() is that same model -- but a DIFFERENT value is honoured live
and moves the deployed classifier off the choice docs/adr/0013-... measured and recorded,
with nothing else in this repository going red.
```

### Task 3 (b) — the probe's template replaced by a byte-identical local copy

The measurement that justifies `is` over `==`, rather than an assertion that it does:

```
equality (==) would have stayed GREEN: True
identity (is): False
FAILED tests/test_evals.py::test_the_probe_sends_the_shipped_prompt_by_identity
```

A copy passes every equality check on the day it is written, and measures a prompt that no longer ships the day after — with a report that looks perfectly reasonable.

### Task 4 — the stratum floor's bite

`chatty-label-falls-back`'s `expect_topic_type` temporarily flipped to `technical`:

```
counts under mutation: contested 6, general 7, sparse 8, technical 17
off-menu general under mutation: ['unknown-label-falls-back', 'empty-label-falls-back'] 2

E   AssertionError: ['unknown-label-falls-back', 'empty-label-falls-back']
E   assert 2 >= 3
FAILED tests/test_evals.py::test_dataset_taxonomy_per_stratum_minimums
```

The floor bites on the off-menu clause with zero spare, which is exactly why P-05 forbids touching this case. Reverted; the taxonomy tests were green throughout and were never weakened.

## The one unplanned red — a FINDING, reported not silenced

`tests/test_evals.py::test_record_writes_a_fixture_per_case_with_fakes` at **:2984** went red on Task 2's provenance field:

```
AssertionError: assert {'pipeline': ...laude-opus-5'} == {'pipeline': ...ude-sonnet-5'}
```

Its own comment, written before this phase existed, read: *"Three roles since Phase 16, and exact-equality so a fourth cannot arrive unannounced."* **It was doing precisely its job.** The fourth role arrived and the pin forced the addition to be deliberate instead of incidental — an announcement gate, not unmeasured blast radius. Updated with a re-derived comment; the guard now stands at the fifth role. No other test outside the three named smoke assertions went red at any point in this plan.

## The labels, resolved — measured before and after

```
topic_counts BEFORE: {None: 2, contested: 6, general: 11, sparse: 8, technical: 13}
topic_counts AFTER:  {None: 2, contested: 6, general:  8, sparse: 8, technical: 16}
off-menu general: ['unknown-label-falls-back', 'empty-label-falls-back',
                   'chatty-label-falls-back']  — 3 before, 3 after
labelled cases (expect_topic_type is not None): 38 of 40, before and after
```

**The plan's claim (11 → 8, off-menu unchanged at 3) checked and confirmed.** The `general` floor is 7, so the stratum lands with exactly one spare.

| Case | Action | Reasoning recorded in | Notes/report/pins |
|---|---|---|---|
| `general-defines-a-term` | relabelled `general → technical`, both fields in lockstep | appended to `why` | untouched |
| `followup-with-no-prior-research` | relabelled `general → technical`, both fields in lockstep | a `#` comment above the fields | untouched |
| `injection-tries-to-force-approval` | relabelled `general → technical`, both fields in lockstep | a `#` comment above the fields | untouched |
| `chatty-label-falls-back` | **no label field touched** | appended to `why` | untouched |

**Why lockstep is not optional:** `ScriptedClient` replays `topic_label` offline, and a stale `"general"` there is a valid `RESEARCH_STRATEGY` key that never falls back — so moving `expect_topic_type` alone would leave the offline leg computing `general` against a `technical` expectation. Both fields, always.

**Why `followup-with-no-prior-research`'s reasoning went in a comment rather than its `why`:** that `why` carries the "reach" tokens `test_dataset_taxonomy_reaching_cases_say_what_they_measure` pins across exactly four cases. It is byte-identical.

**Why `chatty-label-falls-back` is the odd one out, in the case's own words:** its two legs want different answers and no model choice reconciles them. The offline leg needs `general` unconditionally — its scripted chatty label is off-menu and firing that fallback is the case's entire job. The live leg reads a Kalman filter as technical on any competent model, which is what both models said. Its `expect_topic_type` is therefore unreachable via live classification BY CONSTRUCTION: a designed, permanent, documented divergence, not a defect to chase. It is also one of exactly three cases holding the off-menu floor at zero spare, demonstrated above.

**Open Question 2, resolved toward correction.** 21.5-RESEARCH flagged `general-defines-a-term` as the one call it did not find a structurally-forced answer for. It was relabelled, per the research's primary recommendation and P-05: unlike `chatty-label-falls-back` there is no offline-vs-live reason its label must stay wrong — it simply was wrong, and two models reading PUT/DELETE idempotency as technical is evidence about the label. The cost is one general-stratum case (margin 1 rather than 2), stated rather than hidden, and the default path stays covered by `general-summary`, `general-explains-a-concept` and `general-how-a-mechanism-works`.

## The offline suite cannot see this change, and says so

The honesty note 21.5-CONTEXT asks for, stated rather than left for a green suite to imply: **`ScriptedClient` returns each case's authored `topic_label` without ever calling a model**, so the classifier's model is invisible to every offline eval by design. A green keyless suite says nothing about classification quality.

What the 21 keyless tests DO prove is structural: the accessor's default and its per-call read, the resolved name reaching all four `call_model` naming sites, the misbilling discriminator, the provenance field, the probe's refusal and prompt identity, and the deploy value. The quality claim rests entirely on the probe, which is paid and is 21.5-02's.

## What shipped

### Task 1 — the accessor, the constant and the threading: `20bbe8c`

`classifier_model()` directly after `critic_model()`, with a docstring carrying the full inversion argument. `classifier_node` passes `model=classifier_model()`; nothing else about the call changed. The prompt moved to `CLASSIFIER_PROMPT_TEMPLATE`, verified **byte-identical** by direct comparison against a reconstruction of the old f-string:

```
byte-identical: True | len: 477
```

One variable, kept. Pricing needed zero `usage.py` changes — `claude-opus-5` has had a PRICES row since the critic moved there, and `test_classifier_misbilling_discriminator_stays_silent_when_unset` is that claim tested rather than assumed.

### Task 2 — provenance recorded, comparison deliberately absent: `3bd8224`

`record_case_to_fixture` writes `"classifier": graph.classifier_model()`, read at record time. `grade_fixture_current`'s **body is byte-identical**; the change is entirely docstring, and it now separates the judge's uncompared role from the classifier's by reason. `REQUIRED_MODEL_ROLES` stays `("pipeline", "judge")` and is pinned — promoting the role would stop 19 fixtures *loading*, a harder failure than staleness since the loader raises before any grader runs.

### Task 3 — the probe, its gates, and the deploy stance: `300bdcb`

Keyless invocation, measured:

```
classifier probe: 38 labelled case(s) x 2 models = 76 calls
  assuming ~140 input / ~5 output tokens per call (measured 2026-08-15)
  claude-sonnet-5: 38 calls, $0.0125
  claude-opus-5: 38 calls, $0.0314
  estimated total: $0.0439
error: --yes is required to spend. 76 call(s) would be made at the estimate above
($0.0439); nothing was run, and no API client was built.
exit=2
```

$0.0439 against the 2026-08-15 run's actual $0.053 — same order, as expected, and it will be re-quoted at run time regardless. The refusal test monkeypatches `anthropic.Anthropic` to a bomb, so "refused" is proven to mean *no client constructed*, not *refused after reading your key*.

`fly.toml` carries the line with its inverted direction spelled out; the deploy pin asserts the VALUE while treating absence as legal.

### Task 4 — the labels, ADR-0013, and the prose: `4dd7875`

ADR-0013 in the house Nygard shape, `**Status:** Accepted` — it supersedes nothing, being a second per-node model rather than a reversal of the first. Its load-bearing content is the two deliberate asymmetries, argued rather than asserted, with the measured 19-red cascade quoted as evidence for the second.

The index's counting prose **re-derived by counting the table's own cells**, not by incrementing from memory: nine of the thirteen records Accepted, four supersessions, eight `**Source:**` records. `test_the_adr_index_counting_prose_is_derived_from_the_table` green at **zero edits to itself**.

Prose surfaces: the README Stack line (`Opus 5 critic and classifier`), the README config prose (the mirror-image `CLASSIFIER_MODEL` sentence), the OPERATIONS env-table row, one sentence in `### CRITIC_MODEL and the reservation, together` (+$0.0005/run, no reservation impact), the extended "Recorded evals see this too" paragraph, and one honest sentence in `evals/__main__.py`'s preview comment. **No count moved on any surface, and the Limitations section is byte-identical.**

## Deviations from plan

**1. [Rule 3 — blocking] The Task 2 mutation revert was performed with `git checkout -- evals/harness.py`, which also discarded Task 2's legitimate edits.** Caught immediately, both edits re-applied verbatim, and the file verified clean of mutation residue (`grep -c "CLASSIFIER_MODEL)" evals/harness.py` → 0; `grep -n "recorded_classifier"` → no output) before the diff was staged. No mutated state was ever committed. Recorded because the surgical form (`git checkout -- <file>`) is only surgical when the file contains nothing else worth keeping — reverting a mutation by hand, or by inverse patch, is the safer idiom when a task's own work sits in the same file.

**2. Plan line reference off by one.** The plan cites `tests/test_graph_smoke.py:978` for the third planned red; pytest reports `:977`, the first line of the multi-line `assert per_call == [...]` statement. Same assertion. Noted for accuracy, not as a problem.

Everything else executed as written.

## Wave 2 handoff

### The probe invocation, exact

```bash
cd "/Users/hesamabbaszadi/Documents/Projects/Research agent"
.venv/bin/python -m evals.classifier_probe --yes \
  --report .planning/phases/21.5-classifier-on-opus-5/classifier-probe-report.json
```

`--report` already defaults to that path, so the flag is belt-and-braces. Quote to approve against: **~$0.0439** (76 calls), re-printed by the run itself before anything is spent. Exit 0 means the shape held (Opus ≥ Sonnet AND zero regressions); exit 1 means it did not, which is a **checkpoint back to the user with the new numbers** — not a silent proceed and not a silent abort. The report's `cost_usd` is measured from the responses' own usage, so actual-vs-quote is available directly.

Note the probe now runs against the **corrected** dataset: three cases carry `technical` where the 2026-08-15 run scored them against `general`, so both arms' absolute scores should rise. The trust criterion is the shape, not the 34/38.

### The re-record list — EIGHT cases, derived from `REFUSALS.json` this session

`kind == "grader"` AND `"topic_type"` in `detail`, re-derived at execution as the runbook requires. **The research's correction of CONTEXT's believed-six is confirmed exactly**, including the sum:

| Case | `detail` (verbatim) | Phase 21 actual |
|---|---|---|
| `empty-label-falls-back` | `topic_type, approval, forced_stop` | $0.174771 |
| `followup-with-no-prior-research` | `topic_type, judge_followup_honesty` | $0.016931 |
| `followups-chain-of-three` | `topic_type, followup_research_bounded, followup_research_bounded` | $0.885972 |
| `general-defines-a-term` | `topic_type` | $0.110746 |
| `general-explains-a-concept` | `topic_type` | $0.230427 |
| `general-how-a-mechanism-works` | `topic_type` | $0.163975 |
| `general-summary` | `topic_type` | $0.179913 |
| `injection-tries-to-force-approval` | `topic_type` | $0.150320 |
| **sum** | | **$1.913055** |

$1.913055 is a **baseline, not a number to approve against** — CONTEXT's "~$2.40" is a rounder earlier estimate, and the runbook's re-quote-at-run-time discipline applies unchanged. Three of the eight carry a second, unrelated grader failure and are predicted to refuse again for that reason, which is the machinery working and stays documented.

`chatty-label-falls-back` is correctly **absent** from this list — its `kind` is `judge_truncated` (judge verdict truncated at max_tokens=1500), an unrelated finding, and its `topic_type` divergence is the structural one documented in the case.

Union gate baseline, measured now: **19 fixtures + 21 refusals = 40, overlap `[]`.** A successful re-record MUST remove the `REFUSALS.json` entry in the same commit that adds the fixture, or the overlap arm reds.

### The corrected P-06 env assertion

`CLASSIFIER_MODEL` **cannot** join the runbook's "must be unset = poison" list. Unset is now the *correct* state (it resolves to `claude-opus-5`). The assertion before `--yes` must be:

```bash
[ -z "${CLASSIFIER_MODEL:-}" ] || [ "$CLASSIFIER_MODEL" = "claude-opus-5" ] \
  || { echo "REFUSING: CLASSIFIER_MODEL=$CLASSIFIER_MODEL"; exit 1; }
```

i.e. **unset, or explicitly `claude-opus-5`; anything else halts.** `CRITIC_MODEL`, `EVAL_JUDGE_MODEL`, `VECTOR_STORE` and `DATABASE_URL` keep their existing must-be-unset assertions unchanged.

### Still owed by 21.5-02

The counts. README's test totals, the recorded/refused split, and the evals denominator all move once the re-record settles, and are re-measured **once**, at close-out. Phase 22's preconditions (SUMMARY exists, ROADMAP checkbox flipped, `REQ-classifier-model` flipped with traceability) are also 21.5-02's, along with ADR-0013's number being taken here — Phase 22's plans derive their number from `ls docs/adr/` and will land on 0014, which is the path they were built for.

## Self-Check: PASSED

Files claimed as created, verified on disk:

```
FOUND: evals/classifier_probe.py
FOUND: docs/adr/0013-classifier-on-its-own-model.md
FOUND: .planning/phases/21.5-classifier-on-opus-5/21.5-01-SUMMARY.md
```

Commits claimed, verified in `git log`:

```
FOUND: 20bbe8c  feat(21.5-01): the classifier gets its own model, defaulting to Opus 5
FOUND: 3bd8224  feat(21.5-01): record models.classifier as provenance, compare it nowhere
FOUND: 300bdcb  feat(21.5-01): the committed probe, its keyless gates, and the fly.toml stance
FOUND: 4dd7875  docs(21.5-01): the disputed labels resolved, ADR-0013, and the model story in prose
```

Final state: 827 passed / 72 skipped keyless · evals 59/59 exit 0 · `git diff --stat --exit-code evals/fixtures/` clean · ruff clean · zero spend.
