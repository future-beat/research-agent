---
phase: 9.1
slug: package-reorganisation
status: complete
retroactive: true
remastered: 2026-08-12
nyquist_compliant: true # measured against the 2026-08-12 tree; no contract existed at execution time
---

# Phase 9.1 — Validation (retroactive)

> **Remastered record.** Housekeeping phase; its contract was "everything still works
> from the new layout," and CI was the gate that enforced it — including catching the
> two bugs the reorganisation introduced (`5c01b3e`, same day).

## Criteria → evidence, measured 2026-08-12

| Criterion (reconstructed) | Where enforced today | Measured |
|---|---|---|
| The suite passes against the installed package, not the checkout | `src/` layout + CI | **737 passed / 65 skipped** keyless, measured this date |
| The image installs `[service]` only; tests and evals stay out | Dockerfile + `pyproject.toml` extras | Structural; unchanged since |
| The service boots and serves from the reorganised tree | CI container smoke test; the v1.0-era deploy | Standing |

## Honest gaps

- The reorganisation merged to `main` with its two bugs and was fixed in a follow-up
  commit rather than amended — v1.0's workflow committed straight to `main`;
  branch-and-PR discipline is a v1.1-era practice.
- The closing README's "364 tests" was **verified at remaster**: the suite at `5c01b3e`
  was re-run keylessly on 2026-08-12 and reports exactly **364 passed / 28 skipped**
  (392 collected). One discrepancy surfaced by that measurement: MILESTONES.md's v1.1
  stats line opens its test range at **436**, a figure that does not reproduce from
  this tree keyless or armed (armed ceiling: 392). The 436 was presumably measured
  against a later tree; the v1.0 side of that range should read 364.
