---
phase: 14-real-cost-accounting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/research_agent/usage.py
  - tests/test_usage.py
  - tests/test_migrate.py
autonomous: true
requirements: [REQ-real-cost-accounting]

must_haves:
  truths:
    - "With env unset and a response reporting no geo, every cost equals today's list-price cost — the phase changes nothing for the deployed service until an operator opts in"
    - "COST_DISCOUNT_FACTOR=0.9 and INFERENCE_GEO_MULTIPLIER=1.1 compose: a response reporting inference_geo 'us' costs list × 1.1 (token portion only) × 0.9 (whole call)"
    - "A response reporting '' or 'global' is never geo-multiplied, regardless of the env — applicability is observed, only the rate is configured (D: hybrid geo)"
    - "An unpriced model, or an unrecognized inference_geo value, flips pricing_unknown; cost stays a floor and never a guessed zero (DEC-12, SC-4)"
    - "Multiplied cost still ratios exactly 1.5 across 2026-08-31 → 2026-09-01 at fixed dates — multipliers compose with effective-dating, they do not replace it (SC-3)"
    - "COST_DISCOUNT_FACTOR=0 or garbage falls back to neutral — a typo cannot zero every cost and silently disarm the budget guardrail"
    - "migrate.py's cost preview still quotes list price under a non-neutral env (Phase 13 printed promise)"
  artifacts:
    - path: "src/research_agent/usage.py"
      provides: "cost_discount_factor() and inference_geo_multiplier() env readers; CallUsage.inference_geo field captured in from_response; multiplied cost_usd arithmetic — the ONE choke point"
      contains: "cost_discount_factor"
    - path: "tests/test_usage.py"
      provides: "multiplier_choke_point, geo_applies_by_response, pricing_unknown_survives_multipliers, boundary_with_multipliers, fallback tests — all fixed-date"
    - path: "tests/test_migrate.py"
      provides: "preview-stays-list-price pin under non-neutral env"
  key_links:
    - from: "src/research_agent/usage.py CallUsage.from_response"
      to: "the SDK response usage.inference_geo field"
      via: "defensive getattr, same idiom as every other usage field"
      pattern: "inference_geo"
    - from: "src/research_agent/usage.py CallUsage.cost_usd"
      to: "cost_discount_factor() / inference_geo_multiplier()"
      via: "per-call env read inside the arithmetic — nowhere else in src multiplies cost"
      pattern: "cost_discount_factor\\(\\)"
---

<objective>
Apply `discount × geo` at the one place Claude tokens become USD — `CallUsage.cost_usd` —
with geo applicability decided by the response's `usage.inference_geo` field and only the
rate coming from env. Prove composition, fail-loud, and the September boundary with
fixed-date tests, and pin that the Phase 13 migration preview still quotes list price.

Purpose: SC-1 (multipliers feed cost computation), SC-3 (boundary survives), SC-4
(pricing_unknown unchanged). Every downstream consumer — per-run cap, RunRecord, /metrics,
spend_since, reserve/settle, RunResponse, evals, demo badge — inherits the multiplied
number with zero further call sites (the Phase 12 lesson, applied to arithmetic).

Output: multiplied `cost_usd` in usage.py; ~10 new fixed-date tests; a structural
no-second-site gate with measured baseline and observed-red mutations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-real-cost-accounting/14-CONTEXT.md
@.planning/phases/14-real-cost-accounting/14-RESEARCH.md
@src/research_agent/usage.py
@tests/test_usage.py
</context>

<interfaces>
From src/research_agent/usage.py (all current — this plan extends, never renames):
  CallUsage(frozen dataclass): input_tokens, output_tokens, cache_read_input_tokens,
    cache_creation_input_tokens, web_search_requests, web_fetch_requests
  CallUsage.from_response(response) -> CallUsage      # field(obj, name) = int(getattr(...) or 0)
  CallUsage.cost_usd(model, on=None) -> float          # the choke point
  record(totals, call, model, on=None) -> float        # catches UnknownModelPricing -> pricing_unknown
  max_run_cost_usd() -> float                          # the env-parse fallback idiom to copy
  price_for / voyage_price_for / preview_cost_usd      # preview stays LIST PRICE (untouched)

From tests/test_usage.py: FakeUsage / FakeResponse / FakeServerToolUse fakes;
  monkeypatch.setenv idiom for env-read functions (test_budget_reads_the_environment).
anthropic 0.120.0 Usage declares `inference_geo` [VERIFIED in RESEARCH]; fakes lacking
  the attribute must read as "".
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → cost arithmetic | Operator-set factors feed the number every guardrail reads; misparse must fail toward MORE cost, never less |
| API response → billing math | usage.inference_geo comes from Anthropic's response, not caller input — callers cannot influence it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering/DoS | COST_DISCOUNT_FACTOR=0 zeroes cost, disarms budget + daily cap | mitigate | ≤0 or unparseable → fall back to default (1.0 / 1.1); pinned by test (Pitfall 3) |
| T-14-02 | Repudiation | Blind env geo disagreeing with the invoice (workspace default_inference_geo) | mitigate | Applicability from the response field, never the env (Pitfall 4); mutation-proven |
| T-14-03 | Tampering | A second multiplication site now or later | mitigate | Structural gate: factor functions referenced only in usage.py (baseline 0 anywhere in src); behavioural single-application e2e lands in 14-02.T3 |
| T-14-04 | Tampering | Multipliers leaking into the migration preview (Phase 13 "list price" promise) | mitigate | preview stays wired to voyage_price_for only; pinned by test under non-neutral env (Pitfall 5) |
| T-14-SC | Tampering | package installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 14-01.1: Multiply at the choke point — geo field, env readers, cost_usd arithmetic, and the core composition tests</name>
  <files>src/research_agent/usage.py, tests/test_usage.py</files>
  <read_first>src/research_agent/usage.py (whole file, 265 lines — especially cost_usd:193-200, from_response:173-191, max_run_cost_usd:259-264), tests/test_usage.py (whole file — FakeUsage/FakeResponse and the monkeypatch env idiom), 14-RESEARCH.md §"The inference_geo reality" and §"The one choke point"</read_first>
  <action>
    Implement the settled hybrid design (CONTEXT §Post-research calls — do not re-decide):

    1. `CallUsage` gains `inference_geo: str = ""`. `from_response` captures it with the
       file's defensive idiom: `str(getattr(usage, "inference_geo", None) or "")` — absent
       on fakes/old SDKs reads as "". The frozen-dataclass equality test
       (`from_response(FakeResponse(None)) == CallUsage()`) must keep passing unmodified.

    2. Two module-level env readers, copying the `max_run_cost_usd` shape exactly (read
       per call, never cached at module scope, so monkeypatch.setenv works):
       - `cost_discount_factor() -> float`: env `COST_DISCOUNT_FACTOR`, default 1.0.
       - `inference_geo_multiplier() -> float`: env `INFERENCE_GEO_MULTIPLIER`, default 1.1
         (the published rate — the default is the RATE, not blanket applicability).
       Both: unparseable OR ≤ 0 → return the default. Docstrings must state why the ≤ 0
       clamp exists (a zero factor is DEC-12's fail-open wearing a new hat — the budget
       guardrails read this number).

    3. `cost_usd(model, on)` arithmetic, one commented expression:
       - Resolve geo factor from `self.inference_geo`: "" or "global" → 1.0; "us" →
         `inference_geo_multiplier()`; ANY other value → raise `UnknownModelPricing`
         naming the unrecognized geo value (record() already converts that raise into
         `pricing_unknown = True` with tokens counted — DEC-12's shape extended to a
         pricing dimension; a future "eu" must not be silently billed at 1.0).
       - The geo factor multiplies the TOKEN portion only (the four token classes), NOT
         the web-search fee — docs scope the 1.1x to "token pricing categories"
         (RESEARCH assumption A1; say so in the comment).
       - `cost_discount_factor()` multiplies the WHOLE call cost including the web-search
         fee (RESEARCH assumption A2; say so in the comment).
       - `price_for`, `voyage_price_for`, `preview_cost_usd`, PRICES, VOYAGE_PRICES:
         untouched. List prices never mutated; effective-dating resolves exactly as today.

    4. Core tests in tests/test_usage.py, new section "Multipliers" — names are
       load-bearing, the VALIDATION contract greps `-k` for them; every date fixed,
       never today():
       - `test_multiplier_choke_point_composes_discount_and_geo` (matches
         `-k multiplier_choke_point`): monkeypatch COST_DISCOUNT_FACTOR=0.9 and
         INFERENCE_GEO_MULTIPLIER=1.1; CallUsage(input_tokens=1_000_000,
         inference_geo="us").cost_usd("claude-sonnet-5", date(2026, 8, 1)) ==
         pytest.approx(2.0 * 1.1 * 0.9). Also assert neutral case in the same test
         module: env deleted + inference_geo="" → exactly 2.0 (the deployed service is
         unchanged until opt-in).
       - `test_geo_applies_by_response_not_by_env` (matches `-k geo_applies_by_response`):
         with INFERENCE_GEO_MULTIPLIER=1.1 set, inference_geo="" and "global" both cost
         exactly list; "us" costs list × 1.1; and from_response on a FakeUsage WITH
         inference_geo="us" captures it while a FakeUsage without the attribute reads "".
       - `test_pricing_unknown_survives_multipliers` (matches
         `-k pricing_unknown_survives_multipliers`): with both env vars set non-neutral,
         record() against "some-new-model" → cost 0.0, tokens counted,
         pricing_unknown True (existing semantics, now under multipliers); AND record()
         of a call with inference_geo="eu" against claude-sonnet-5 → pricing_unknown True,
         cost contribution 0.0, tokens still accumulated.
       - `test_the_web_search_fee_is_discounted_but_not_geo_multiplied`:
         CallUsage(web_search_requests=1000, inference_geo="us") with discount 0.9, geo
         1.1 → pytest.approx(10.0 * 0.9) — fee × discount, fee NOT × geo.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_usage.py -q -k "multiplier_choke_point or geo_applies_by_response or pricing_unknown_survives_multipliers or web_search_fee" -x</automated>
  </verify>
  <acceptance_criteria>
    - All four new tests pass; the whole of tests/test_usage.py stays green (no existing
      assertion edited — additive only).
    - `CallUsage(input_tokens=1_000_000, inference_geo="us").cost_usd("claude-sonnet-5", date(2026,8,1))`
      returns exactly 1.98 under COST_DISCOUNT_FACTOR=0.9 + INFERENCE_GEO_MULTIPLIER=1.1,
      and exactly 2.0 with env unset and inference_geo="".
    - `grep -rln "cost_discount_factor\|inference_geo_multiplier" src/research_agent/`
      outputs exactly `src/research_agent/usage.py` (measured baseline entering the phase:
      0 occurrences anywhere in src, per 14-VALIDATION). The structural no-second-site gate.
    - `record()`'s except clause is unchanged — unknown geo reaches pricing_unknown through
      the SAME UnknownModelPricing path as an unpriced model, not a parallel mechanism.
  </acceptance_criteria>
  <done>cost_usd is the one multiplication site; hybrid geo implemented per D: response decides applicability, env sets the rate; core composition and fail-loud tests green.</done>
</task>

<task type="auto">
  <name>Task 14-01.2: Boundary, fallback, and preview-list-price pins — then falsify the gates by mutation</name>
  <files>tests/test_usage.py, tests/test_migrate.py</files>
  <read_first>tests/test_usage.py §Budget configuration (the fallback-test idiom, lines 206-219), tests/test_migrate.py (existing structure and any preview/cost tests — find where preview_cost_usd is exercised), 14-RESEARCH.md §"SC-3 already half-exists" and §Gate discipline</read_first>
  <action>
    1. Boundary test in tests/test_usage.py:
       - `test_boundary_with_multipliers` (matches `-k boundary_with_multipliers`): with
         COST_DISCOUNT_FACTOR=0.5 monkeypatched, the same
         CallUsage(input_tokens=1_000_000, output_tokens=1_000_000) costs on
         date(2026, 8, 31) vs date(2026, 9, 1) still ratio exactly 1.5
         (after == pytest.approx(before * 1.5)) AND before == pytest.approx(12.0 * 0.5) —
         multipliers compose with effective-dating rather than replacing it. Fixed dates
         only; no datetime.now()/date.today() on any assertion path (Pitfall 6 — this
         suite must be green on 2026-09-02 without edits).

    2. Fallback tests, copying `test_an_unparseable_budget_falls_back_to_the_default`:
       - `test_a_zero_discount_factor_falls_back_to_neutral`: COST_DISCOUNT_FACTOR="0" →
         cost_discount_factor() == 1.0 (and a priced call still costs list, not $0).
       - `test_an_unparseable_discount_factor_falls_back_to_neutral`: "ten percent" → 1.0.
       - `test_a_nonpositive_geo_multiplier_falls_back_to_the_published_rate`:
         INFERENCE_GEO_MULTIPLIER="-1" → inference_geo_multiplier() == 1.1; unset → 1.1.

    3. Preview pin in tests/test_migrate.py:
       - `test_preview_cost_stays_list_price_under_non_neutral_env`: monkeypatch
         COST_DISCOUNT_FACTOR=0.5 and INFERENCE_GEO_MULTIPLIER=2.0, then
         preview_cost_usd(1_000_000, "voyage-3.5", date(2026, 8, 9)) == pytest.approx(0.06)
         — the Phase 13 printed "quotes list price" promise holds (Pitfall 5).

    4. Gate discipline — baselines and mutations (record every observation in the SUMMARY,
       revert each mutation immediately after observing red):
       - Baselines (measured, from 14-VALIDATION): suite plain 529 passed / 63 skipped;
         COST_DISCOUNT_FACTOR / INFERENCE_GEO_MULTIPLIER occurrences in src: 0 entering
         the phase.
       - Mutation A: delete the `* cost_discount_factor()` term from cost_usd →
         `test_multiplier_choke_point_composes_discount_and_geo` AND
         `test_boundary_with_multipliers` must go red on their cost assertions (not an
         import error). Revert.
       - Mutation B: hard-code the geo factor to apply unconditionally (ignore
         self.inference_geo) → `test_geo_applies_by_response_not_by_env` must go red on
         the ""/"global"-unmultiplied assertion. Revert.
       - Mutation C: replace the unknown-geo raise with a silent factor 1.0 →
         `test_pricing_unknown_survives_multipliers` must go red on the "eu" assertion.
         Revert.
       - Mutation D: make preview_cost_usd multiply by cost_discount_factor() →
         `test_preview_cost_stays_list_price_under_non_neutral_env` must go red. Revert.
       Each mutation must go red BY THE INTENDED ROUTE — a red from an unrelated failure
       is a false positive for the gate it was meant to test.

    5. Full plain suite run: everything green, count ≥ 529 passed (new tests raise it),
       63 skipped unchanged, zero new skips.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_usage.py tests/test_migrate.py -q -k "boundary_with_multipliers or falls_back or preview_cost_stays_list_price" -x</automated>
  </verify>
  <acceptance_criteria>
    - All five new tests pass at fixed dates; `grep -rn "date.today()\|datetime.now()" tests/test_usage.py`
      shows no new occurrence on an assertion path (baseline: 0 in the file today).
    - Mutations A–D each observed red by the intended assertion, reverted, and recorded in
      the SUMMARY as a table: mutation → test name → one-line failure text.
    - Full plain suite: ≥ 529 passed / 63 skipped, no new skips, no existing test edited.
    - preview_cost_usd(1_000_000, "voyage-3.5", date(2026,8,9)) == 0.06 under
      COST_DISCOUNT_FACTOR=0.5 — the preview quotes list price, stated by a test, not prose.
  </acceptance_criteria>
  <done>SC-3's multiplied-boundary composition, the fail-toward-more-cost clamps, and the list-price preview promise are all pinned and mutation-falsified.</done>
</task>

</tasks>

<verification>
- `.venv/bin/pytest tests/test_usage.py tests/test_migrate.py` fully green.
- `.venv/bin/pytest` (plain) ≥ 529 passed / 63 skipped; no new skips.
- `grep -rln "cost_discount_factor\|inference_geo_multiplier" src/research_agent/` → exactly
  `src/research_agent/usage.py` (this changes to usage.py + service.py in plan 14-03, where
  service.py only DISPLAYS the values — re-gated there).
- Mutations A–D recorded red-then-reverted in the SUMMARY.
</verification>

<success_criteria>
- SC-1 arithmetic half: discount and geo feed cost computation at exactly one site, hybrid
  geo per the settled decision.
- SC-3: multiplied cost resolves across 2026-08-31 → 09-01 with fixed-date tests.
- SC-4: pricing_unknown fails loud under multipliers, including for unknown geo values.
- VALIDATION rows 1–4 satisfied (14-01.T1 rows 1–3, 14-01.T2 row 4).
</success_criteria>

<output>
Create `.planning/phases/14-real-cost-accounting/14-01-SUMMARY.md` when done, including the
mutation-red evidence table and the measured grep baselines.
</output>
